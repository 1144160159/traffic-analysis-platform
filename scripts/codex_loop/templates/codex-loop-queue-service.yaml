apiVersion: v1
kind: Service
metadata:
  name: codex-loop-queue-service
  namespace: traffic-analysis
  labels:
    app.kubernetes.io/name: codex-loop
    app.kubernetes.io/component: queue-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: codex-loop
    app.kubernetes.io/component: queue-service
  ports:
    - name: http
      port: {queue_service_port}
      targetPort: http
