{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://traffic-analysis-platform.local/contracts/alignment/python-locator-resolution-receipt.schema.json",
  "title": "Candidate-bound Python AST locator resolution receipt",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version", "artifact_kind", "status", "proof_level", "candidate",
    "resolver", "locator", "ambiguity_count", "resolved_at", "proof_ceiling"
  ],
  "properties": {
    "schema_version": {"const": "1.0.0"},
    "artifact_kind": {"const": "PYTHON_LOCATOR_RESOLUTION_RECEIPT"},
    "status": {"const": "RESOLVED"},
    "proof_level": {"const": "LANGUAGE_AST"},
    "candidate": {"$ref": "#/$defs/candidate"},
    "resolver": {"$ref": "#/$defs/resolver"},
    "locator": {"$ref": "#/$defs/locator"},
    "ambiguity_count": {"const": 1},
    "resolved_at": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(?:\\.[0-9]+)?Z$"
    },
    "proof_ceiling": {"const": "EXACT_LOCATOR_ONLY_NOT_FUNCTION_DESIGN_OR_EXECUTION_AUTHORIZATION"}
  },
  "$defs": {
    "sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "candidate": {
      "type": "object", "additionalProperties": false,
      "required": ["commit", "manifest_path", "manifest_sha256"],
      "properties": {
        "commit": {"type": "string", "pattern": "^[0-9a-f]{40}$"},
        "manifest_path": {"type": "string", "minLength": 1},
        "manifest_sha256": {"$ref": "#/$defs/sha256"}
      }
    },
    "resolver": {
      "type": "object", "additionalProperties": false,
      "required": ["resolver_id", "engine", "engine_version", "source_path", "source_sha256"],
      "properties": {
        "resolver_id": {"const": "traffic-python-ast-locator@1"},
        "engine": {"const": "python/ast"},
        "engine_version": {"type": "string", "minLength": 3},
        "source_path": {"const": "scripts/alignment/python_ast_locator.py"},
        "source_sha256": {"$ref": "#/$defs/sha256"}
      }
    },
    "position": {
      "type": "object", "additionalProperties": false,
      "required": ["byte_offset", "line", "column"],
      "properties": {
        "byte_offset": {"type": "integer", "minimum": 0},
        "line": {"type": "integer", "minimum": 1},
        "column": {"type": "integer", "minimum": 1}
      }
    },
    "call": {
      "type": "object", "additionalProperties": false,
      "required": ["expression", "line"],
      "properties": {
        "expression": {"type": "string", "minLength": 1},
        "line": {"type": "integer", "minimum": 1}
      }
    },
    "locator": {
      "type": "object", "additionalProperties": false,
      "required": [
        "locator_id", "language", "path", "module", "qualified_symbol",
        "declaration_kind", "query", "match_strategy", "signature",
        "candidate_blob_sha256", "source_span_sha256", "normalized_ast_sha256",
        "start", "end", "calls"
      ],
      "properties": {
        "locator_id": {"type": "string", "pattern": "^LOC-[A-Za-z0-9._-]+$"},
        "language": {"const": "python"},
        "path": {"type": "string", "pattern": "^(scripts|tests|ml|mlops)/.+\\.py$"},
        "module": {"type": "string", "minLength": 1},
        "qualified_symbol": {"type": "string", "minLength": 1},
        "declaration_kind": {"enum": ["FUNCTION", "ASYNC_FUNCTION", "METHOD", "ASYNC_METHOD", "CLASS", "MODULE_CONSTANT", "MODULE_VARIABLE"]},
        "query": {"type": "string", "minLength": 1},
        "match_strategy": {"const": "EXACT_PYTHON_MODULE_OR_CLASS_DECLARATION"},
        "signature": {"type": "string", "minLength": 1},
        "candidate_blob_sha256": {"$ref": "#/$defs/sha256"},
        "source_span_sha256": {"$ref": "#/$defs/sha256"},
        "normalized_ast_sha256": {"$ref": "#/$defs/sha256"},
        "start": {"$ref": "#/$defs/position"},
        "end": {"$ref": "#/$defs/position"},
        "calls": {"type": "array", "items": {"$ref": "#/$defs/call"}}
      }
    }
  }
}
