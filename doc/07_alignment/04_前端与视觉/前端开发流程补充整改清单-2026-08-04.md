# 前端开发流程补充整改清单

## 1. 来源、范围与结论

- 来源：`/root/前端开发流程.pdf`，9 页，SHA256 `36b37347946537ea865859532ab326e7b493136bbc02a5650aae53fbdf4011b1`。
- 提取时间：2026-08-04；提取工具：PyMuPDF，只读提取。
- 对齐范围：现有 28 条视觉验收路由、102 个 canonical ID、Feature Contract、真实 API/权限/状态机及 Windows Chrome G5。
- 登记原则：本清单是现有 canonical ID 的前端子任务和验收补充，不新增、删除或重编号 102 项基线；未完成项继续保持原状态。
- 结论：覆盖 28 页的版本化页面设计契约、3 条兼容 alias、102 个 canonical ID、29 个 API plan、Design Token、capture plan 和不可变候选绑定已经形成并通过代码侧门禁。当前 v4 绑定的是已部署且逐文件对账通过的干净候选，合同仍为 bundle 外部版本化绑定；Windows Chrome 已覆盖 6/28 个业务页面，逐页 Design QA、其余写操作、全状态、无障碍和多视口仍未完成，因此不得据此关闭整体前端整改。

## 2. PDF 要求映射到现有整改项

| 补充项 | 优先级 | 绑定 canonical ID | 要求 | 关闭证据 |
|---|---|---|---|---|
| UI-ADD-001 页面设计契约注册表 | P0 | F-COMMON-001、F-ADAPTER-001 | 28 条路由逐条固定 route、目标任务、基准视口、视觉真值、信息架构、API/TS 类型、Token、必须保留项、禁止项和 owner/reviewer | 版本化注册表；路由覆盖 28/28；未知、重复、孤儿条目为 0；候选 hash 绑定 |
| UI-ADD-002 路由与状态模型保护 | P0 | F-ADAPTER-001、F-COMMON-002、各领域 Feature Contract | 禁止擅改 Tab、默认对象、detail/query 状态、权限入口和 AppShell；六类 `removed_*` 继续为 0 | 路由/Tab/控件/scope/API/审计差异；深链、刷新、前进后退和直接 URL 回放 |
| UI-ADD-003 真实业务事实门禁 | P0 | F-ADAPTER-002、F-DASHBOARD-001、F-MODEL-001、F-MLOPS-001、F-NOTIFICATION-001、F-AUTH-001 | 禁止固定业务数字、随机趋势、前端派生实体/拓扑、默认租户和纯前端伪状态；缺失必须是 unavailable/partial | 静态风险目录为 0；zero/empty/missing/partial 样本；浏览器显示值到源事实逐字段对账 |
| UI-ADD-004 同视口 Design QA | P0 | F-COMMON-001、各页面领域 ID | 源图和实现必须同一 route、viewport、DPR、浏览器和页面状态；P0/P1/P2 逐轮回修后才能通过 | 每目标保存 source、actual、diff、metrics、capture-meta、迭代记录；最终 result=pass |
| UI-ADD-005 全状态矩阵 | P1 | F-COMMON-002、F-COMMON-004、各异步命令领域 ID | 覆盖 hover、selected、disabled、loading、empty、partial、stale、forbidden、error、accepted、running、success、failed、cancelled、compensated | 状态到合同枚举映射；DOM、截图、HAR、trace、审计和终态效果同一 action/job ID |
| UI-ADD-006 图标与视觉约束 | P1 | F-COMMON-001 | 复用现有组件、Token 和图标库；禁止 Emoji、字符图标、临时 CSS 图形、无依据渐变/辉光/玻璃拟态和营销式大数字 | lint/静态扫描；设计契约例外清单；同视口 QA |
| UI-ADD-007 可访问性与响应式 | P1 | F-COMMON-004、各页面领域 ID | 检查语义标题、阅读顺序、键盘焦点、标签、错误恢复、目标尺寸、对比度、动效、缩放和重排 | Windows Chrome 键盘旅程、axe/DOM 语义检查、1366×768/1600×900/1920×1080 和缩放证据 |
| UI-ADD-008 工程验收与提交护栏 | P0 | F-COMMON-001 | Design QA 后执行 typecheck、lint、test、build；P0/P1/P2 未关闭不得进入候选 | 候选 manifest、测试结果、diff、reviewer 和独立验收签字 |
| UI-ADD-009 权威来源与导出语义 | P0 | F-ADAPTER-002、F-GRAPH-001、各报告/导出领域 ID | 浏览器本地历史、缓存、下载和派生统计必须显式标注本地范围；客户端 JSON 下载不得命名为服务端证据或审计导出 | UI 文案与 Feature Contract 对账；服务端 job/manifest/audit 未形成前不得出现“证据导出成功” |
| UI-ADD-010 空态层级与缺失聚合 | P1 | F-COMMON-004、F-TOPIC-001、各页面领域 ID | empty/missing/partial 提示不得遮挡图例或控件；同屏多分区缺失应说明统一原因、可恢复动作和仍可用数据 | 目标视口截图；DOM 阅读顺序；缺失分区与 `meta.missing_sections` 对账；重试/降级行为 |

## 3. 2026-08-04 Windows Chrome 抽查

### 3.1 审计边界

- 浏览器：Windows Chrome 151，经 Xshell `127.0.0.1:9224`。
- 页面：态势大屏、资产台账、模型管理、MLOps、通知配置、系统设置。
- 视口：1920×1080；mock 关闭；6/6 页面无应用侧 4xx/5xx、requestfailed、console error、pageerror 或整页水平溢出。
- 通用捕获脚本最初将 Chrome 151 的第三方 `Permissions policy: unload` 事件误记为应用错误，现已按消息来源分栏保留；该分类修正不豁免真正的应用错误。
- 这些截图证明当前部署页面的可见状态，不证明当前隔离工作树候选已部署，也不替代交互、权限、数据对账和 Design QA diff。

### 3.2 页面发现

| 页面 | 健康度 | 新增/确认问题 | 绑定项 |
|---|---|---|---|
| 态势大屏 | 高风险 | 部署截图显示大量精确覆盖率、地理风险点、外联流量、证据环和拓扑实体。隔离工作树已移除静态拓扑、固定外联地区、证据环、异常链路、响应统计和视觉拆解 fixture；缺源时显示空集合或暂不可用，但真实 topology/evidence/response 字段尚待服务端补齐并随候选复验。 | UI-ADD-003；F-DASHBOARD-001；F-ADAPTER-002 |
| 资产台账 | 中风险 | 五类 Tab 与 AppShell 保持；但表格多列和右侧摘要在 1920×1080 仍大量截断，详情入口存在禁用态但缺少就地原因，底部上下文需滚动后才能发现。需要把列宽、换行、disabled/empty/error 状态纳入契约。 | UI-ADD-001/004/005/007；F-ASSET-005/006 |
| 模型管理 | 高风险 | 顶部平均 F1 与选中模型指标存在不同口径但未清楚标注；Champion/Challenger 名称、版本和图表文字严重拥挤/截断。隔离工作树已删除固定 F1/AUC/PSI/误报率及按 model ID 生成趋势的逻辑，只接受显式指标与 trend/values/series；真实时间序列合同和候选页面复验仍未完成。 | UI-ADD-003/004/007；F-MODEL-001；F-ADAPTER-002 |
| MLOps | 高风险 | 当前部署页任务表有水平滚动和操作图标拥挤；“评估任务 0”与“评估门禁 3”口径容易混淆。旧适配器会生成固定六任务；隔离工作树已改为只接收真实 workflow，但尚未部署和执行同 bundle G5。 | UI-ADD-003/004/005/007；F-MLOPS-001；F-ADAPTER-002 |
| 通知配置 | 中风险 | 页面信息架构完整，但订阅表和历史表字号/列宽偏紧，状态与操作密集。部署页面仍显示过去固定兜底特征（订阅规则 28、静默窗口 4）；隔离工作树已改为缺失显示暂不可用，尚待候选验证。 | UI-ADD-003/004/005/007；F-NOTIFICATION-001；F-ADAPTER-002 |
| 系统设置 | 中风险 | RBAC、Token、留存和集成在一屏可见，但表格密度高，禁用令牌操作缺少原因提示；部署页面的 21 类 scope、5/7 集成、3 项待审计需逐字段证明来源。隔离工作树已移除 7/3 固定兜底，尚未部署。 | UI-ADD-003/004/005/007；F-AUTH-001；F-ADAPTER-002 |

### 3.3 本轮截图校验

| Step | 页面 | 运行时 | Screenshot SHA256 |
|---:|---|---|---|
| 1 | 态势大屏 | PASS，截图已人工检查 | `3b9bf7fc867902a27f272bed3c5a45bfe9f3cdaff5bfb8a2855dc4e5f07e785a` |
| 2 | 资产台账 | PASS，截图已人工检查 | `4f85bde4a781e95c828a6b9f80c405d1c5f225bb3abc0d089b649e5d3da7d067` |
| 3 | 模型管理 | PASS，截图已人工检查 | `77f4280b11168cc2c6cb3d36e3a0d77ee21bf5831166281d07eec5b8e67eb05a` |
| 4 | MLOps | PASS，截图已人工检查 | `d2b302b70376ef1f6528130a8e90d7d2ef949188518ecf2acd5b9464e8dafdde` |
| 5 | 通知配置 | PASS，截图已人工检查 | `d6e14714d0dedc001b44ec549fd427da03b5a0ee9d3c0450668aa4da47e52d1f` |
| 6 | 系统设置 | PASS，截图已人工检查 | `4654b865a51dee783015ff1a91d7d4c64ac69f8333d8b54285d58bf4558b052e` |

截图当前保存在 `/tmp/20260804-frontend-process-audit-v1/`，仅作为本轮工作证据；正式关闭必须重新捕获到不可变候选 evidence run，并绑定 candidate source、bundle 与镜像 digest。

### 3.4 本轮已落地的代码护栏

- F-ADAPTER-002 静态目录已扩展到 19 条规则；新增 `runtime_static_business_fallback`，阻断“真实响应缺字段时回落到 `*Fallback*`、固定时间窗或固定质量分”的运行时路径。
- 19 条规则扫描 68 个生产 TypeScript 文件，`open_findings=0`；目录 SHA256 为 `53ce715471f434e8515d32af5e721951fa320afc75b8ccc2c6fbf46f2d183ed2`。
- 审计、数据质量、部署、加密流量、模型、探针、规则、设置和专题页的缺失 KPI 不再显示固定 0、92%、98.2% 等业务值，统一显示 `- / 暂不可用`。
- 态势大屏不再通过 `visual_breakdown` 查询参数关闭真实 API 并进入静态业务 fixture；模型趋势仅消费 API 显式数组。
- `DataQualityPage` 已删除生产运行路径和源码中失去引用的静态 KPI、趋势、DLQ 样本、设置、侧栏告警及对账数据；缺失分区显示 unavailable，权威空数组保持 empty，Topic 迟到/丢弃比例按服务端计数计算。通用动作的 `202` 只提示“已受理”，不再显示为最终成功。
- Campaign 动作已统一识别 accepted/running/succeeded/partial/failed/cancelled/compensated，partial、失败、取消和补偿不再被合并成成功；视觉拆解查询参数在 production bundle 中无法激活。
- 验证：前端完整单测 50 文件/269 用例全部通过、lint 0 error（18 个既有 warning）、production build PASS；生产包对已知数据质量静态业务哨兵扫描为 0。
- 上述代码已进入候选 Web UI 镜像 `docker.io/traffic/web-ui:remediation-ui-50af49cf5f33`（source SHA256 `50af49cf5f3328474235612a1ec4156819a77e357aaf455176cb07270134d123`，image ID `sha256:ca1dd833d515c77c786f56fea5bec2bf861dcd87a44d0d4e99453ca038333cc9`），并完成四条主样板的 Windows Chrome G5 只读抽查；该抽查仍不覆盖全部 28 路由、写动作、键盘和多视口，因此 UI-ADD-003、004、005、007 仍不得整体关闭。

### 3.5 Product Design 页面审计与问题关闭规则

- 前端页面审计统一采用 Product Design combined audit，不再把设计协作白板作为审计入口或交付门禁。
- 每次审计只使用本轮实际捕获、保存并人工检查通过的生产候选截图；旧截图、缓存截图、代码推断和 HTTP 2xx 不能替代本轮页面证据。
- 每个步骤必须绑定截图、页面状态、用户任务、UX 发现和可访问性风险；截图能看到的风险与仍需键盘、DOM、缩放或辅助技术验证的边界必须分开记录。
- 问题状态只允许 `OPEN → IN_PROGRESS → VERIFYING → CLOSED`。执行解决即关闭：代码、定向测试、同 bundle 重拍、交互/API/权限/错误状态和不可变证据全部通过时，当轮直接写为 `CLOSED`。
- `CLOSED` 是终态，不再进入后续开放问题清单、进度播报、难度排序或阻塞项。历史关闭证据仍按 hash 保留；同类问题若回归，必须登记新的 occurrence ID，不复活并重复播报旧问题。
- 默认进度只列出 `OPEN / IN_PROGRESS / VERIFYING`。完整约束由 `contracts/ui/product-design-audit-policy.v1.json` 和自动校验门禁固定。

### 3.6 候选部署与 Windows Chrome G5 证据

- 浏览器链路：Windows Chrome 151，经 Xshell `127.0.0.1:9224`；生产候选关闭 mock，视口为 1920×1080。
- 首轮 `20260804-frontend-remediation-ui-50af49cf5f33-g5` 保留为失败证据：资产列偏好接口被旧 `asset-service` 当作资产 ID，返回 404。
- 部署候选 `asset-service` 后，偏好接口已返回 200 且包含 `data + meta`；对应镜像为 `docker.io/traffic/asset-service:remediation-asset-pref-9b4123ba911d`，image ID 为 `sha256:dafc5ea5cfd9787b90452aabd0275fb5e44b03f5b4865c52d207d88771a93820`。
- 第二轮 `20260804-frontend-remediation-ui-50af49cf5f33-g5-r2` 保留为失败证据：资产列表和邻居查询因线上 `assets`、`asset_topology_links` 缺少 `revision` 返回 500。已按只增不删的 expand 方式为两表增加 `BIGINT NOT NULL DEFAULT 1 CHECK (revision > 0)`，未重写已有数据。
- 第三轮不可变证据目录为 `doc/02_acceptance/runs/20260804-frontend-remediation-ui-50af49cf5f33-g5-r3/`：assets、alerts、topics、graph 四条旅程全部 PASS，真实 API 数分别为 6、4、1、1；`bad_responses`、request failure、console error、page error 均为 0，四张截图均绑定 SHA256。
- 候选启动同时发现线上 `assets.asset_id` 仍为 `TEXT`，而 canonical DDL 和 outbox/projection 表要求 `UUID`。102 条现有 ID 全部具有 UUID 可转换形状，但本轮没有直接原位修改主键；asset event/discovery outbox 与 OS/Nebula 资产投影显式保持关闭，待按 expand→backfill/shadow→切换执行独立迁移。因此本次 G5 PASS 只证明四条只读页面链路，不关闭资产原子写入、outbox 和跨存储投影门禁。

### 3.7 四样板截图复检后的增量整改

- 图谱页原“查询治理/查询历史/最近查询/通过”实际来自浏览器 `localStorage` 中成功返回的查询，容易被理解为服务端全局治理数据。现已改为“查询治理（当前浏览器）”“本浏览器查询历史”“最近查询（本地）”“已返回”，并增加“不得代表服务端全局指标”的范围说明。
- 图谱页原“导出证据”只在浏览器生成当前响应 JSON，没有服务端 job、MinIO manifest 或审计终态。现已改为“导出视图 JSON”，下载文件和反馈明确声明不是服务端证据对象、不替代审计导出；真正服务端对象化导出仍为 OPEN。
- 专题页在无拓扑关系时，空态文字原与绝对定位图例重叠。现将空态限定在图例下方区域并水平、垂直居中，保留图例和空态的独立阅读层级。
- 新增 `graphEntityLogic.test.ts` 两组回归检查；定向测试 2 文件/5 用例、eslint quiet 和 production build 全部通过。
- 新候选镜像 `docker.io/traffic/web-ui:remediation-ui-60871b6a7a18`，dist hash `60871b6a7a184830368222d2957b87ebce3cb2f7b1da2eb69d4a598cb3150d49`，Docker image ID `sha256:f6d0e4b9dd3e6897909a78978426f1426646be7bf96f11a726b49d9d8a8b2c4b`。
- 不可变 Windows Chrome 证据为 `doc/02_acceptance/runs/20260804-frontend-remediation-ui-60871b6a7a18-g5/`：四条旅程全部 PASS，API 数为 6/4/1/1，mock=false，应用侧 4xx/5xx、request failure、console error 和 page error 均为 0；专题空态和图谱权威范围已人工复检。

### 3.8 UI-ADD-001 页面设计契约与工程护栏

- 新增 `web/ui/src/routes/pageDesignContracts.v1.json` 和 JSON Schema，逐页固定28条验收页面的 route、组件、目标任务、基准状态、1920×1080视觉真值、信息架构、API plan、生产 TypeScript 类型源、canonical ID、scope、保留项、owner 和独立 reviewer。三条旧专题 URL 只作为兼容 alias，不重复计页。
- `scripts/alignment/verify_ui_page_design_contracts.py` 复算页面合同、route manifest、29个 API plan、Design Token、capture plan、候选镜像、镜像 manifest、运行时观测和 Windows Chrome 样本证据；结果为28/28、102个 canonical ID，重复、未知和孤儿均为0。
- 不可变小型证据包为 `doc/02_acceptance/runs/20260804-ui-page-design-contract-v1/`。其 `pass` 只代表 UI-ADD-001 代码侧合同门禁，不代表28页 G5/G7。绑定候选仍为 `docker.io/traffic/web-ui:remediation-ui-60871b6a7a18`，合同未嵌入该 bundle，Windows Chrome 样本仍为4/28。
- 修复 Campaign、加密流量、通知、剧本和规则页面的 Hook 依赖问题；把告警详情、告警列表、部署和规则页面的非组件导出迁到独立逻辑模块，Fast Refresh 边界不再告警；生产与测试 Router 同时启用相同的 React Router v7 future 行为。
- 最终代码侧验证：52个 Vitest 文件、277个用例全部通过；eslint 0 error/0 warning；TypeScript 与 Vite production build PASS；`git diff --check` PASS。Vite 仍提示 ECharts/Ant Design 原始 vendor chunk 超过500 kB，先保留为性能跟踪项，必须取得生产传输和运行 profile、确定 gzip/load 预算后再调整 chunk 拓扑。
- 仓库级 `make alignment-validate` 最终 PASS。新增生产逻辑模块触发的 adapter-risk、configuration、service-identity、PKI 和 PostgreSQL mutation 目录漂移均已重新生成并复验；静态业务风险 `open_findings=0`，620条 mutation 中 `unclassified=0`，Kafka 18个 workload 身份同步测试通过。

### 3.9 UI-ADD-002 路由状态首个整改切片

- 审计日志选中记录和详情 Tab 已从组件局部状态迁移为 `log_id + detail` URL 权威状态，支持直接链接、刷新、前进和后退；无 `audit:write` 权限时不能通过 `detail=review` 打开复核态。
- 专题切换及数据质量进入重放对账改为只更新自身 query 键，不再清空跨页对象、trace 或其他深链上下文；公共合并函数对空值执行删除并保持输入对象不可变。
- 资产分类、对象、详情和搜索状态改为只替换自身 query 键，保留 campaign、trace 等来源上下文；资产变更审计以及行为基线审计不再跳转不存在的 `/audit` 或使用接收端不识别的 camelCase 参数，统一进入 `/audit-log?object_type=...&object_id=...`。
- 告警、战役、基线进入取证统一使用 `alert_id/campaign_id/baseline_id`；取证页同时兼容旧别名。来源引用不再仅显示：GET 任务列表按租户及来源字段过滤，POST 新任务把 `alert_id/campaign_id/baseline_id/evidence_id/evidence_type` 写入 PostgreSQL `tasks.params`，`create=1` 只打开确认抽屉。历史任务缺少这些字段时保持真实空结果，不由前端推断关联。
- 清除四类导出语义风险：加密流量不再本地拼装“外联报告”；战役列表在服务端仅受理时不再下载浏览器 JSON；战役详情改走报告 worker、终态轮询及大小/SHA256 校验下载；攻击链提交真实报告任务后只提示受理。模型页仍允许下载当前浏览器快照，但文件内容和按钮均明确为本地且 `audit_evidence=false`。
- 当前完整验证为55个 Vitest 文件/288个用例、eslint 0 error/0 warning、production build PASS；取证服务及 `internal/forensics/...` Go 测试通过。28页 query/path 所有权已登记，新候选已部署；取证来源引用完成 9/9 真实写入、幂等、过滤、跨租户和 PostgreSQL history/outbox/request/audit 对账，并由 Windows Chrome 深链回放命中同一任务。其余页面深链、写动作和浏览器历史回放尚未完成，UI-ADD-002 保持 `IN_PROGRESS`。

### 3.10 v4 干净候选与六页证据绑定

- 当前 Web UI 候选为 `docker.io/traffic/web-ui:remediation-ui-8a1d66200751`，source SHA256 为 `8a1d66200751158f82f482ae009895e82d6c0956f431c6f3af8e33760e813793`，image ID 为 `sha256:17c39716d29bb0087c55935a5c3f705391bebc19f062f464992bc9ecf2aad1b4`，OCI digest 为 `sha256:30823cf9895a8bb0b293562b8552db56427f689d0bf090eb002b7cdf6e1ce2b7`。
- v2 候选因旧 hashed chunks 残留被拒绝；中间候选 `remediation-ui-2f3dee291b56` 又因截图复检发现模型指标第二行裁切而未绑定为当前候选。现行 Dockerfile会清空精确静态目录，模型指标 CSS 只约束直接卡片子节点；G6 对账证明本地与线上83个非运行时文件路径及逐文件 SHA256 完全一致，仅允许 `config.js` 和 `config.js.template` 两个运行时文件。
- 页面设计契约 v4 证据包为 `doc/02_acceptance/runs/20260804-ui-page-design-contract-v4/`。校验器强制复算合同、route manifest、API plan、Token、capture plan、Dockerfile、dist/source 组合哈希、镜像身份、运行时观测、静态 bundle、Forensics、Windows Chrome 以及模型指标裁切测量，结果为28/28路由、102个 canonical ID、29个 API plan，重复、未知和孤儿项均为0。
- Windows Chrome v12 对资产、告警、专题、图谱、模型和取证六页执行真实只读旅程，`journey_count=6`，mock=false，应用侧错误为0。取证页请求携带 `alert_id/campaign_id/baseline_id/evidence_id/evidence_type`，并显示 Forensics v4 证据中的同一 `job_id`。
- Forensics v4 通过9/9：202稳定回执、trace、幂等重放、冲突、组合过滤、权威空结果、跨租户隔离、PostgreSQL来源引用和 history/outbox/request/audit 同 trace 对账。服务 `/ready` 现在同时检查事务迁移所需列、表和 migration version，Schema 不完整时不再伪健康。
- 最新工程门禁：`make alignment-validate` PASS；前端55个测试文件/290个用例、lint和production build PASS；Forensics Go 测试 PASS；`git diff --check` PASS。Vite 的 Ant Design/ECharts 大 chunk 提示仍作为性能整改项保留，必须先取得实际加载 profile 和批准预算。
- 模型页缺失 delta 不再伪装成0，长版本号提供分层换行和完整 title，选中版本上下文与全局平均 F1 口径分离；Windows Chrome 实测7张指标卡 `client_height=scroll_height=269`、`clipped=false`。本次裁切缺陷已关闭，但28页完整 Design QA、键盘和多视口仍保持 OPEN。

## 4. 实施顺序

1. W1：将 UI-ADD-001、002、003、008 纳入 F-COMMON-001/F-ADAPTER-001/002 的候选阻断门禁。
2. W1—W2：先清零态势大屏、模型、MLOps、通知、设置的固定事实和派生实体，再补 zero/empty/missing/partial 单元测试。
3. 各领域波次：在领域 Feature Contract 中补 route/query/detail/Tab 和全状态矩阵，不改变既有兼容表面。
4. 候选部署后：资产、告警、专题、图谱、模型和取证六页只读 G5 已通过；继续用同一 Windows Chrome、同一 bundle 补齐其余 22 路由、写动作、键盘、多视口门禁和有源图页面的同视口 Design QA，失败即回修并重拍。
5. G7：只有 P0/P1/P2 视觉和交互问题、真实数据对账、工程测试及独立 reviewer 全部通过，相关页面才能关闭；G8 继续独立判定。

## 5. 明确非结论

- 本清单不宣称 28 路由已完成 Design QA；当前不可变候选 G5 只证明6个业务页面的只读样本。
- 四条主样板以及模型、取证页面的当前候选已部署并通过只读运行时抽查，但不代表全部 28 路由或写链路已经发布验收。
- 截图无法证明键盘、屏幕阅读器、权限、幂等、审计、回滚和最终业务效果；这些仍需专项证据。
- PDF 中建议的 Skill 链被转化为工程门禁，但 Skills 名称本身不是验收结果。
