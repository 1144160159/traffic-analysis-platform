# 权限体系问题清单(深度梳理,2026-08-18)

三轮全面复查的完整问题账本。每项标注状态:✅已修复(live 证据)/ ⏳登记待办 / 🚫BLOCKED(缺环境)。
详细证据链见 `统一权限体系设计方案-2026-08-18.md` §8 与 `三份设计文档与当前实现差异清单-2026-08-17.md` §5。

## 1. 网关层(APISIX)

| # | 问题 | 影响 | 状态 |
|---|---|---|---|
| G1 | /api/v1/* 全部挂 OIDC bearer_only,机器凭证(API token)无法通过 | ADR-6 外部化受阻 | ✅ analysis/assets 两路由移除 OIDC(上游=权威判定层),其余保持纵深防御;live:API token 经网关 200 |
| G2 | 客户端自供 X-User-ID/X-Username/X-Roles/X-Permissions 头未剥离(SUB-FS-08) | 依赖可信身份头的服务(rule-manager 无 JWT 时的回退模式)存在伪造面 | ⏳ 已接共享中间件/自有认证的服务全部从 token 取身份,不受头影响;网关侧显式剥离待 APISIX proxy-rewrite 配置评审 |
| G3 | /api/v1/auth/login 匿名 + limit-req(100/s) | 密码爆破面 | ⏳ 有验证码前置;账号锁定策略待确认(未在本次范围验证) |
| G4 | 网关 OIDC discovery 走 https://keycloak.iam.svc:8443(集群内),服务侧走 http://10.0.5.8:30180 | 双入口一致性 | ✅ live 两条路径均验签通过;证书轮换需同时维护两入口(登记) |
| G5 | /api/internal/v1/audit/batches 网关带 OIDC,服务侧 Authenticate | 双层认证 | ✅ 行为一致;Go 侧无调用方(外部审计注入预留) |

## 2. 共享鉴权中间件(internal/common/authz)

| # | 问题 | 影响 | 状态 |
|---|---|---|---|
| M1 | issuer 未强制(Config.Issuer 空则跳过) | 同密钥异 issuer 的 token 可能通过 | ✅ analysis/asset 设 AUTHZ_ISSUER 严格校验 |
| M2 | 缺失 tenant_id 声明回落 DefaultTenant(ADR-2 要求缺失→403) | 租户属性未配置用户静默落入 default | ✅ RequireTenantClaim=true(analysis/asset live:无声明 token 403) |
| M3 | 未拒绝 typ=ID/Refresh 的 JWT(ID token 可作访问凭证) | 令牌形态混淆 | ✅ verifyClaims 拒绝非 Bearer typ;live:refresh token 作 Bearer 401 |
| M4 | 契约路径 {param} 模板不匹配(仅精确匹配) | 全部含参数段契约操作从未被强制执行 | ✅ 模板段匹配(段数一致+逐段相等或 {x}+最长静态前缀);live:POST /alerts/{id}/feedback viewer 403 AUTHZ_403 |
| M5 | API token 回退不执行 ip_whitelist | IP 白名单形同虚设 | ✅ TokenValidator 携带 *http.Request,校验 CanAccessFromIP;live:白名单外来源 401 "api token rejected by ip whitelist" |
| M6 | 契约拒绝(AUTHZ_401/403)只记 zap 日志,不落审计 | ADR-5 拒绝留痕缺契约层 | ✅ EnforceContract 增 ContractDenyAuditor,alert/graph/forensics/rule-manager 接入;live:CONTRACT_ACCESS_DENIED 经 materializer 落库可查 |
| M7 | JWKS 获取失败即 401(fail-closed) | 可用性 | ✅ 语义正确(零信任);密钥轮换期短暂 401 可接受(登记) |
| M8 | 无 aud/azp 校验 | 任意客户端 token 均可用于业务面 | ✅ 双层闭合:共享中间件 AllowedAZP(analysis/asset)+ OIDC 回退路径 OIDC_ALLOWED_AZP(6 服务);live:admin-cli token 全服务 401 "azp not in allowed client whitelist" |
| M9 | shadow 模式不注入 principal | 影子观察期业务层身份缺失 | ⏳ 现状全 enforce;若再启用 shadow 需注入占位主体(登记) |

## 3. 认证服务(auth-service)

| # | 问题 | 影响 | 状态 |
|---|---|---|---|
| A1 | OIDC 回退 tenant 缺失回落 default | 同 M2 | ⏳ auth-service 承担登录链,保留宽松语义;业务面由中间件强制(登记) |
| A2 | 密码登录仍签发 HMAC 双 token(兼容窗口) | P1 双 token 未彻底下线 | ✅ 前端侧已下线:httpClient 401 重试桥与 app token 回退移除(全站单 KC 令牌),web 345 绿+镜像部署;auth-service HMAC 签发保留为兼容窗口(服务端下线待外部客户端迁移) |
| A3 | KC 用户 Logout 不终止 KC SSO 会话 | 登出后 30 天内 token 链仍可用 | ✅ Provider.Logout(调 KC logout 端点)+ Logout 携带 refresh_token;live:登出后同 refresh 兑换 **400 Session not active**;前端 logout 携带存储的刷新令牌 |
| A4 | KC 用户 PATCH /me、settings 按 subject 分片 | 数据分片/写失败 | ✅ resolveLocalUser(主键→external_id)+ user_settings_history 表补建;live:PATCH /me 200、PUT settings 200 |
| A5 | 角色映射双份硬编码,无契约 | ADR-3 漂移 | ✅ oidc-role-map.v1.json 契约+共享解释器+奇偶校验测试 |
| A6 | 权限变更事件:本地无角色 API,KC 角色变更无事件 | ADR-5 user.events.v1 联动缺 | ⏳ 角色权威=Keycloak,变更审计需 KC Admin Events 接入(登记) |
| A7 | admin-cli 令牌无角色→viewer(设计) | 运维令牌无法业务放行 | ✅ 语义正确;业务面已拒绝其缺 tenant 声明 token |

## 4. 契约解释器与逐服务接入

| # | 问题 | 影响 | 状态 |
|---|---|---|---|
| C1 | auth-service 已挂(3 操作) | — | ✅ live 403/200 |
| C2 | alert-service(29+) | — | ✅ live(含模板路径) |
| C3 | graph-service(4) | — | ✅ live(viewer 契约授予 graph:read) |
| C4 | asset-service(25) | HTTP 面此前零校验 | ✅ 共享中间件+契约 live |
| C5 | forensics-service(pcap 8) | — | ✅ live viewer 403 AUTHZ_403 |
| C6 | rule-manager(models 3) | — | ✅ live viewer 403 AUTHZ_403 model:read |
| C7 | 契约外操作(analysis /runs 等)无逐操作判定 | 契约未覆盖面 | ⏳ 契约扩充或服务级 scope 门(登记) |

## 5. 审计三联

| # | 问题 | 影响 | 状态 |
|---|---|---|---|
| U1 | 高风险操作拒绝留痕 | SUB-FS-12 | ✅ 白名单/pcap/规则/模型+契约层 CONTRACT_ACCESS_DENIED 全落库可查 |
| U2 | materializer 物化行 resource_id 为空(事件字段映射不全) | 审计页对象列空 | ✅ JSON 兼容映射(resource_type/resource_id/source_ip/timestamp);live:CONTRACT_ACCESS_DENIED 行携带 resource_type=contract_operation、resource_id=saveAlertView;有界回放仍登记 |
| U3 | audit-materializer 新部署后历史 audit.logs 未回放 | 上线前事件缺失 | ⏳ 回放需新消费组+ACL 放行(现仅 audit-consumer 组授权)或 Kafka admin CLI 作业;已登记操作程序,待评审后执行 |

## 6. API token(ADR-6)

| # | 问题 | 影响 | 状态 |
|---|---|---|---|
| T1 | 统一校验器+租户绑定+契约判定 | — | ✅ 全生命周期 live |
| T2 | IP 白名单未执行 | 见 M5 | ✅ live |
| T3 | 创建 API 不接受 ip_whitelist(仅 PUT) | 创建即锁 IP 不便 | ⏳ CreateTokenRequest 增字段(登记) |
| T4 | 存量 2 行 bcrypt token | 校验兼容 | ✅ 双方案校验器 |

## 7. 前端

| # | 问题 | 影响 | 状态 |
|---|---|---|---|
| W1 | 单 token(KC)+401 桥+refresh 单飞 | — | ✅ web 345 测试绿 |
| W2 | 路由/按钮由 permissions+allowed-actions 驱动 | — | ✅ |
| W3 | 登出后 KC SSO 未终止(同 A3) | — | ⏳ 登记 |

## 8. 探针/内部面

| # | 问题 | 影响 | 状态 |
|---|---|---|---|
| P1 | PROBE_AUTH_TOKEN(probe-token-default-001)无 Go 消费点 | 探针自检链 | ⏳ 核对:消费方为仓库外 probe-agent 二进制(k8s manifest 注入+对齐测试守护),Go 侧无消费点;凭证轮换需探针侧协同——如实登记 |
| P2 | asset gRPC 面仅 logging/recovery 拦截器 | gRPC 无鉴权 | ⏳ 核查:default-deny 网络策略已应用,50053 仅 apisix+同命名空间可达;本仓无 gRPC 客户端(外部 probe-agent 依赖未知),加固需探针协同——如实登记 |
| P3 | nebula-http-gateway /api/v1/ngql 无鉴权(未部署) | 任意 nGQL 执行 | ✅ 代码已接共享中间件(JWKS+issuer+租户声明强制+azp 白名单+契约,shadow 默认/上线前 enforce);部署门禁仍登记 |
| P4 | /internal/v1 路由 Authenticate 对齐 | — | ✅ 无 Go 调用方,外部注入预留 |

## 9. Keycloak 侧

| # | 问题 | 影响 | 状态 |
|---|---|---|---|
| K1 | 用户属性(tenant=default)写入 204 但不落库(KC 24.0.5 环境异常) | 属性驱动租户声明失效 | ✅ 改为 traffic-ui 硬编码 claim 映射器 tenant_id=default(单租户正确);多租户须恢复属性驱动(登记环境异常) |
| K2 | publicClient 切换自动轮换 client secret(4 次) | 配置漂移 | ✅ 同步流程固化(ESO 源→k8s→重启);续借 token 不再翻转(SSO 30 天) |
| K3 | SSO idle 1800s→30 天、access token 1800s、refresh 链 30 天 | — | ✅ |
| K4 | 无 analyst/operator/viewer 真实用户 | 角色矩阵未 live | ⏳ G06 浏览器矩阵缺环境 BLOCKED |

## 10. 仓储层

| # | 问题 | 影响 | 状态 |
|---|---|---|---|
| R1 | 租户过滤在 whitelist/forensics/rules 原子命令中强制 | — | ✅ tenant_id WHERE + 集成测试 |
| R2 | threat_intel_feeds.revision 代码/DDL 漂移 | 调度错误循环 | ✅ 列补建 |
| R3 | model_action/deployment/feedback/whitelist 投影表缺失(历史) | 服务启动失败 | ✅ 全部补建 |

## 汇总

- 本轮(第三次梳理)新修复:M1-M6、C6、K1、R2(9 项,live 证据齐)
- 登记待办:G2 网关头剥离、M8 aud 校验、M9 shadow 主体、A2 双 token 下线、A3/A6 KC 会话终止与角色事件、C7 契约外操作、U2/U3 materializer 修饰与回放、T3 创建带 IP 白名单、P1-P3 探针/gRPC/nebula、K1 多租户属性恢复、K4 角色矩阵(G06)
- BLOCKED:G06-G09/P5(缺浏览器矩阵/故障演练/性能/发布环境)
