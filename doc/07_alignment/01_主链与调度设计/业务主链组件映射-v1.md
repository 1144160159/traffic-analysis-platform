# 业务主链 → 组件映射 v1

日期:2026-08-17
依据:`统一调度中心架构模式映射与设计原则审计-v1.md`(同目录)、卷A/卷B、代码级设计卷
状态:主链全部环节已具名归属;表中"状态"如实标注(上线/代码层/未接线)

## 0. 组件清单(职责单一)

| 组件 | 进程/形态 | 单一职责 | 边界(禁止) |
|---|---|---|---|
| **analysis-service** | Go 服务(PG 权威) | 调度中心:命令入口、计划冻结、物化、闸门、回执权威、终局、报告请求、outbox 中继 | 禁止 pcap 解析/流聚合/特征/检测推理 |
| **probe-agent** | Rust daemonset | 采集执行器:在线采集 + 对象回放(发生地在探针) | 禁止裁决 Run 终态 |
| **ingest-gateway** | Go 服务 | 传输路由:探针 mTLS/命令路由、流量入 Kafka、ACK→回执转发 | 禁止业务裁决 |
| **flink-session-job** | Flink 常驻 | 会话化(共享分支 + run-scoped 键) | — |
| **flink-feature-job** | Flink 常驻 | 特征计算(exact-set)、S2 回执作业 | — |
| **flink-behavior-job** | Flink 常驻 | 加密识别(S3)+ 威胁检测/聚合(S4) | — |
| **flink-alert-generator-job** | Flink 常驻 | RunScopeRouter(信封派生)、告警生成 | — |
| **flink-rule-job** | Flink 常驻 | RULE_DETECTION(S4 规则分支) | — |
| **PG / CH / Kafka / MinIO** | 基础设施 | 权威事实 / 分析结果 / 消息通道 / 对象存储 | — |
| **web-ui** | React | 七页面 + 向导(仅表现) | 禁止绕过服务端鉴权 |
| **mlops** | Python 流程 | 评估闭环(盲评包/质量门禁/95%·5%) | — |

## 1. 主链环节 → 组件映射

### 1.1 前置选择(两个执行环节,同链归一)

| 环节 | 归属组件 | 代码 | 状态 |
|---|---|---|---|
| AUTO_DEFAULT 解析(默认模板) | analysis-service(装配层) | `adapters/plan_catalog.go` + `service/plan_resolver.go` | 已上线(live 验证) |
| MANUAL_CUSTOM 解析(覆盖项+探针选择) | analysis-service(P2) | `service/plan_author.go`(保存/审批)+ CustomPlanResolver | 已上线(live 验证) |
| 计划冻结(双哈希) | analysis-service | `service/plan_compiler.go` | 已上线 |

### 1.2 S0 触发→物化

| 环节 | 归属组件 | 代码/通道 | 状态 |
|---|---|---|---|
| 四种触发归一 | analysis-service | `service/trigger.go`(幂等键+台账) | 已上线 |
| 物化事务(Task/Run/attempt/预留/outbox 同事务) | analysis-service → PG | `repository/repository.go` MaterializeAnalysisTaskAtomic | 已上线(live 集成 PASS) |

### 1.3 S1 采集(两条源,同一出口)

| 环节 | 归属组件 | 代码/通道 | 状态 |
|---|---|---|---|
| 源命令构造+manifest 校验 | analysis-service(adapter 协议转换) | `service/stage_dispatcher.go` + `adapters/pcap_replay.go` | 已上线(core-v18,命令经 bridge 校验) |
| 命令投递 | analysis-service → **probe.control.v2** → ingest-gateway bridge | `adapters/probe_command_publisher.go` | 已上线 |
| **回放执行(发生地在探针)** | **probe-agent(Rust)** | `control.rs::ReplayWindowCommand` + `capture/pcap_replay_op.rs`(取对象→hash→有界回放→feed) | 代码层+测试;**执行链接线未完成**(BuiltinProbeExecutor 未挂) |
| 在线采集执行 | probe-agent(Rust) | CaptureWindowCommand + capture 管线 | 代码层(同前) |
| 共享分支喂入 | probe-agent → ingest-gateway(UploadFlows)→ **flow.events.v1** | Rust sender + gateway handler | 在线采集已上线;回放喂入随执行链 |
| S1 回执回流 | probe ACK → gateway bridge → **analysis.receipts.v1** → analysis-service ReceiptApplier | `service/receipt_applier.go` | **gateway ACK→回执发布未接线**(回执消费者已上线) |

### 1.4 订阅广播与信封派生(调度→数据面上下文通道)

| 环节 | 归属组件 | 代码/通道 | 状态 |
|---|---|---|---|
| RunSubscription 发布 | analysis-service(outbox 中继) | `service/run_event_relayer.go` → **analysis.run.events.v1** | 已上线(16 条 live 消费验证) |
| 订阅 fencing 更新(开闸时) | analysis-service(闸门循环) | `service/stage_gate.go` | 已上线(batch token 修复 live 验证) |
| **信封派生(0..N)** | **flink-alert-generator-job** | `RunRouterJob/RunRouterProcessFunction` → **analysis.envelopes.v1** | 已上线(7,359 条 live 验证) |

### 1.5 S2 会话化+特征

| 环节 | 归属组件 | 代码/通道 | 状态 |
|---|---|---|---|
| run-scoped 会话键 | flink-session-job | `RunScopedFlowKey` | 代码层(**live 作业清单无 Session Job**;共享分支会话化亦未上线,与 CH flows_raw 停摆一致) |
| 特征 exact-set 计算 | flink-feature-job | `SelectedFeatureCalculator` | 代码层(Feature Extraction Job v3 live 运行,但其输入 session.events.v1 上游会话化未上线;envelopes 消费未接) |
| S2 回执(过渡:按 run 聚合真实计数) | flink-feature-job | `RunReceiptJob/RunReceiptProcessor` → analysis.receipts.v1 | 已上线(Run Receipt Job RUNNING) |

### 1.6 S3 加密识别 + S4 检测

| 环节 | 归属组件 | 代码 | 状态 |
|---|---|---|---|
| 加密识别 typed outcome | flink-behavior-job | `EncryptedTrafficRecognizer` | 代码层(未接 envelopes/回执) |
| 威胁检测 typed outcome | flink-behavior-job | `SelectedThreatDetector` | 代码层(同前) |
| 检测聚合(守恒+聚合 hash) | flink-behavior-job | `DetectionAggregate` | 代码层(同前) |
| 规则检测分支 | flink-rule-job | 规则引擎 | 代码层(同前) |
| 结果写 CH(run_id) | flink-* → CH | `analysis_run_results_local`(P0 已建表) | 代码层(同前) |
| S3/S4 回执 | flink-* → analysis.receipts.v1 | (同 RunReceiptJob 模式扩展) | **未接线** |

### 1.7 S5 终局 + 报告

| 环节 | 归属组件 | 代码 | 状态 |
|---|---|---|---|
| 对账(Reconcile) | analysis-service | `service/orchestrator.go`(闸门谓词) | 代码层 |
| 机器摘要+三件套+终态 | analysis-service | `service/finalizer.go` FinalizeRunAtomic + 真值表 | 代码层(live PG 集成 PASS);**终局循环未上线** |
| 人读报告请求/状态机 | analysis-service | `service/human_report.go` | 代码层(live PG 报告链 PASS) |
| 报告对象生成(worker) | 独立报告 worker(设计)/analysis-service(当前同进程服务) | — | **未部署** |

### 1.8 评估挂接(P5,Run 形态)

| 环节 | 归属组件 | 代码 | 状态 |
|---|---|---|---|
| 评估 Run(PCAP_REPLAY + 评估阶段) | analysis-service 编排 + mlops 执行 | `mlops/scripts/evaluate_blind_package.py --emit-run-contract` | 代码层 |
| 盲评包校验/质量门禁/95%·5% | mlops | evaluate_* 脚本 | 代码层;**无冻结带标签样本,BLOCKED** |

## 2. 缺口清单(环节已有归属、链路未闭合)

| # | 缺口 | 归属组件(明确) | 依赖 |
|---|---|---|---|
| G1 | probe `pcap_replay` 执行链接线(命令→取对象→回放→共享分支→ACK 计数) | probe-agent | BuiltinProbeExecutor 挂载 pcap_replay_op + sender 复用 |
| G2 | gateway bridge ACK→analysis.receipts.v1 回执发布 | ingest-gateway | G1 |
| G3 | S2/S3/S4 run-scoped 接线(envelopes 消费 → 各计算器 → CH + 回执) | flink-session/feature/behavior/rule | G1+G2 后逐阶段 |
| G4 | 终局循环(全阶段终态 → finalizer → 三件套+终态) | analysis-service | G3 |
| G5 | 报告 worker 部署 | 独立 worker 或 analysis-service | G4 |

## 3. 结论

主链每一环节都有唯一归属组件,组件边界与七大原则审计文档一致;缺口均为"归属已定、链路未闭",不是归属不清。
