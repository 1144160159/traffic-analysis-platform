# M01 早期受信验证四目录原子切换阻断预检

> 决策：`BLOCKED_PRECONDITIONS`。这是只读集合预检，不生成未来目录，也不改动四个现役 registry。

## 集合结果

- 现役四目录：各 1289 个唯一 atomic ID，exact-set 相同；M01 为 56 个。
- 候选 M01：保留 47、移除 9、新增 37，合计 84。
- 候选全局：`1289 - 9 + 37 = 1317`。
- 任务边界：N015 承担受信验证基础列车；N010 仅保留 P038-P044 调用方迁移并依赖 N015；N003 同样消费 N015。

## 前置条件

| 检查 | 状态 | 说明 | 阻断原因 |
|---|---|---|---|
| `M01-SW-C01` | `PASS_STATIC` | four active registry hashes and exact atomic ID sets remain frozen and identical | - |
| `M01-SW-C02` | `PASS_STATIC` | v2 allocation catalog design and set arithmetic are deterministic and internally exact | - |
| `M01-SW-C03` | `BLOCKED` | one immutable design candidate and one same-commit implementation candidate are frozen in two acyclic stages | candidate freeze work order is BLOCKED_IMPLEMENTATION_SOURCES_AND_CLEAN_WORKTREE and both candidate manifests are absent |
| `M01-SW-C04` | `BLOCKED` | all 36 candidate-bound design reviews are independently completed and signed | 36 review work orders remain BLOCKED_EXTERNAL_INPUTS |
| `M01-SW-C05` | `BLOCKED` | external verifier image digest SBOM provenance and signature verification receipt are bound | external image work order is BLOCKED_SOURCE_AND_EXTERNAL_AUTHORITY_INPUTS and receipt is absent |
| `M01-SW-C06` | `BLOCKED` | future task claim PR-design and execution-overlay catalogs are generated from one candidate exact set | all four future candidate catalog paths are intentionally absent |
| `M01-SW-C07` | `BLOCKED` | named owners reviewers approvers and trusted signatures authorize the exact atomic switch | named identities approvals signed overlay and switch authorization are absent |
| `M01-SW-C08` | `BLOCKED` | authorized atomic commit can validate and replace all four catalogs without partial visibility | no authorized atomic switch candidate or protected merge receipt exists |

## 原子协议

1. freeze one immutable 81-source design candidate then one same-commit implementation candidate after review image and evidence closure
2. complete validate and independently sign all 36 candidate-bound design review receipts
3. bind the external verifier image digest SBOM provenance and signature verification receipt
4. generate four future catalogs from one reviewed candidate and exact 1317-ID set
5. validate four schemas hashes ID sets N010 N015 boundary dependencies and completion contracts
6. obtain named switch authorization and protected merge receipt for the exact candidate hashes
7. replace task claim PR-design and overlay catalogs in one atomic commit then rerun all gates

任一步失败都必须在替换任何现役文件前终止；不存在先切 task、稍后补 claim/design/overlay 的合法中间态。即使将来完成切换，执行仍保持 DRAFT/BLOCKED，直到 signed overlay 与真实 gate evidence 到齐。

Proof ceiling: `READ_ONLY_SWITCH_SET_MATH_AND_BLOCKER_PREFLIGHT_ONLY_NOT_FUTURE_REGISTRY_MATERIALIZATION_ATOMIC_SWITCH_CANDIDATE_IDENTITY_REVIEW_SIGNATURE_IMPLEMENTATION_TEST_EXECUTION_AUTHORIZATION_OR_ACCEPTANCE`
