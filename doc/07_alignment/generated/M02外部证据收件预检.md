# M02外部证据收件预检

状态：`BLOCKED_EXTERNAL_INPUTS_INCOMPLETE / NO-GO`

该预检只建立C03-C07主输入的exact-set收件协议；不创建人员、候选、签名或评审结论，也不把结构校验等同于受信验签。

## 当前缺口

- Candidate：设计manifest `MISSING`，implementation manifest `MISSING`，同commit=false；需覆盖163个唯一源文件。
- Responsibility：实名完整任务 0/16；trusted signature verifier=false。
- Locator receipts：validated=0/269，missing=269，invalid=0，unexpected=0。
- Compatibility/default-off reviews：validated=0/213，missing=213，invalid=0，unexpected=0。
- Review receipts：validated=0/425，missing=425，invalid=0，unexpected=0。

## Gate输入状态

| Gate | 输入状态 | 阻断 |
|---|---|---|
| `REG03-C03` | `BLOCKED` | clean same-candidate locator exact-set, compatibility/default-off review exact-set, or trusted signature verification is incomplete |
| `REG03-C04` | `BLOCKED` | review exact-set or trusted signature verification is incomplete |
| `REG03-C06` | `BLOCKED` | signed named responsibility intake cannot be trusted until the independent verifier is installed |
| `REG03-C07` | `BLOCKED` | design and clean implementation candidates are missing, invalid, or cross-commit |

即使四项均为`READY`，仍只表示主输入exact-set可交给switch gate复核，不表示registry可切换。

## 证明上限

`EXTERNAL_INPUT_EXACT_SET_PREFLIGHT_ONLY_NOT_IDENTITY_INVENTION_SIGNATURE_VERIFICATION_LOCATOR_RESOLUTION_REVIEW_APPROVAL_IMPLEMENTATION_SWITCH_EXECUTION_OR_ACCEPTANCE`
