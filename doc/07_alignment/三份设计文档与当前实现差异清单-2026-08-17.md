# 三份设计文档与当前实现差异清单(2026-08-17)

对照输入:
1. `doc/07_alignment/课题一PR里程碑代码级详细设计.md`(9724 行;重点第 76.43—76.48 节及 v1 Topic / RunScope / Flink 连续性条款)
2. `doc/07_alignment/课题一主营业务流程闭环与中期详细设计对齐方案.md`(804 行)
3. `doc/07_alignment/统一分析任务调度中心菜单与UI详细设计.md`(848 行)

对照实现面:Go `internal/analysis/*`(v32 线上镜像)、Rust probe-agent(wire-replay 线上镜像)、Flink run 系列作业(线上 RUNNING)、Web `web/ui/src/*`(本地源码)、live PG `traffic_platform`(2026-08-17 实测)。

状态词与文档自认一致:设计文档均为 `DRAFT_*/NOT_EXECUTED/NOT_IMPLEMENTED`,本清单不改变任何执行授权;实现侧状态如实登记为 IMPLEMENTING/PARTIAL。

---

## 5. 补齐进度(2026-08-17 晚,按 §4 顺序实施;实施中条目标 IMPLEMENTED,其余仍为缺口)

### 5.1 已闭环(测试+live 证据)

| 条目 | 实现 | 证据 |
|---|---|---|
| Outbox broker ACK 台账 | MarkOutboxPublished 记录 broker_ack=true+published_at(RequiredAcks=all 的 nil 返回=全 ISR ACK 事实);outbox 台账聚合查询(按 topic/state + 旧 PENDING 年龄);迁移 `27-analysis-scheduler-v3.sql` | live:71 行 PUBLISHED、台账列已 apply;partition/offset 级台账需 sarama/读侧对账(如实登记 §5.3) |
| Stage retry 合同 | `RetryService.RetryStage`+`RetryStageAtomic`(run 非终态+latest FAILED+DEDICATED_OPERATION→新 attempt;SHARED_STREAM→STAGE_RETRY_UNSUPPORTED;attempt 预算 3)+ POST `/runs/{id}/retry-stage` | 单测全绿;live PG 集成测试 `TestLivePGStageRetryContract`(shared 拒绝/dedicated 重试 attempt2/终态拒绝)PASS |
| cron 解析 | 5 字段 cron 解析器(列表/范围/步进/日周并集)+ NextFire/PrevFireOrNow;Scheduler CRON_WINDOW 触发(windowID=c-{fire}-{window_ms},非法 cron fail-closed 不触发) | 单测 4 项;live 调度链未启用 cron 样本(窗口样本已闭环,cron 样本登记待办) |
| 窗口前置准备 | `ComputePrepareAt`/`EvaluateLateActivation`(FAIL_WINDOW/DELAY_WINDOW/BOUNDED_REPLAY_IF_PROVEN 三策略,未证明 replay 输入 fail-closed);Scheduler 未到 prepare_at 不触发 | 单测 2 项;PREPARE 订阅两段已 live(PREPARE rev1→ACTIVE rev2) |
| DRR 台账 | `analysis_drr_state` 表+`RecordDrrServe`(按 effective_class deficit/epoch 累积,S1 领取时记录) | 迁移已 apply;向量折算与"选中+扣配额+lease+outbox 同事务"仍为缺口(§5.3) |
| RunSubscription 字段+PREPARE 两段 | 契约 12 字段补全(task_id/plan_revision/source_kind/prepare_at/lease_epoch/effective_policy/expires_at);物化发 PREPARE(rev1),S1 领取发 ACTIVE(rev+1,带预分配 fence);路由器 PREPARE 忽略、仅 ACTIVE 匹配;relayer 覆盖 plan/run/report 三 topic | live:两段订阅同一 run 双记录(PREPARE/ACTIVE)均 PUBLISHED;v40 新 run 全链闭环 VERIFIED;镜像 analysis-service core-v40/v42/v43、router run-router-v4 |
| G05 人读报告 worker 全链 | `cmd/report-worker`(consume report.requests→GET 冻结摘要(零 PG 凭证)→确定性 HTML→MinIO analysis-reports→ApplyWorkerReceipt(VERIFYING)→VerifyAndConfirm(AVAILABLE));新端点 GET `/runs/{id}/summary`、GET `/reports`、POST `/reports/{id}/worker-receipt|verify`;Kafka SCRAM 主体 traffic-report-worker+ACL+Secret;遗留请求确定性隔离 | live:report 4ace1868 状态 QUEUED→AVAILABLE,MinIO 对象 1.9KiB(sha 999ab7b7…)内容与冻结摘要一致;worker 镜像 20260817v3 RUNNING |
| UI 六维+调度页+报告中心+向导 | 前端:AnalysisRunView 增 report_state(后端 RunView/ListRuns 六轴投影,core-v47);运行列表/详情展示报告状态;调度管理页(列表+新建简表+激活/暂停 If-Match);报告中心页(人读报告列表+状态+对象 hash);向导改批准任务定义目录下拉+会话级稳定幂等键(crypto.randomUUID,重试复用);analysisApi 增 schedules/reports/retry-stage | web 单测 345 全绿+build 通过;镜像 web-ui:analysis-six-axis-20260818 上线,网关 /analysis/runs 200;后端六轴 live(报告状态随行) |
| per-input AnalysisResult 物化 | RunBehaviorReceiptProcessor 侧输出每输入×required detector typed disposition 行(NOT_RUN 兜底),经 `RunDetectionResultClickHouseSinkFactory` 落库 CH `traffic.analysis_detections`(批写+重试,fail-closed);CH 凭证走 Secret `ch-flink-analysis-credentials`(零明文) | live:run dc5a0a3d 落 18,868 行(147 POSITIVE/14,891 NEGATIVE/3,830 INCOMPATIBLE/0 NOT_RUN);镜像 flink-behavior-job:run-receipt-v2 RUNNING |
| 全 disposition 计数 | 回执 fence(DETECTION_AGGREGATE/两条 detector_fence)增加 `not_run` 六类计数;Go 对账/闭包按六类覆盖判定(EvidenceSufficient=六类之和=total 且 error=0;AllInputsExplicitNegative=全阴性且其余五类为 0);机器摘要 key_findings 携带六类 | live:total=9398=147+7338+1913+0+0+0 精确闭合;machine summary 六类齐;analysis-service core-v38 |
| Schedule 权威链 | `ScheduleService`(Save:修订自动分配+spec sha 不含 revision 保幂等;Activate DRAFT\|PAUSED→ACTIVE;Pause ACTIVE→PAUSED;List/Head)+ 端点 POST/GET `/api/v1/analysis/schedules` + `/activate` `/pause`(If-Match expected revision)+ 激活头 CAS+审计 | 单测 4 项;live PG 集成测试 `TestLivePGScheduleAuthorityAndStateWalk`(save→幂等回放→activate→陈旧 revision CAS 拒绝→list→pause→tick 视图清空)PASS;live API:保存 `eeef51d3`/`de0e3243`、激活、暂停、恢复(PAUSED→ACTIVE)全链 200 |
| Schedule 触发 worker | 修正真实断点:`repoAdapter.EnqueueMaterialize` 此前未接线且 MaterializeWorker 未启动(即"Schedule 0 实现"的根因);现 `MATERIALIZE_ENABLED` 启动 worker 直接领取 PENDING_MATERIALIZATION(SKIP LOCKED)+ 每轮 AdmissionReservation 过期扫描 | live:CONTINUOUS_WINDOW 60s 滚动窗自动触发,4 个连续 run 闭环 SUCCEEDED/THREAT_FOUND/COMPLETE/VERIFIED(42822dab/09592836/dc726806/017edbf8),trigger_kind=CONTINUOUS_WINDOW、effective_class=BASELINE、schedule_revision=1、window_id 编码正确 |
| 正交 TriggerKind | TriggerKind 枚举校验(CRON/CONTINUOUS/EVENT)+ Tick 跳过 ON_DEMAND/EVENT_DRIVEN;窗口对齐+misfire 三策略(既有)+ FORBID_OVERLAP 实现 | 单测 5 项;live:重叠窗口触发转 SUPPRESSED(2 条,FORBID_OVERLAP_CONFLICT 原因入审计) |
| EffectiveSchedulingPolicy | `ResolveEffectiveSchedulingPolicy`(override??schedule??default 类解析、逐维 min cap、plan.min≤requested≤cap、canonical sha);on-demand 与 schedule 两条物化路径均解析并冻结;触发表三列加法迁移 `common/sql/pg/26-analysis-scheduler-v2.sql`(live 已 apply) | 单测 4 项;live:调度 run 冻结 BASELINE+policy sha;按需路径改 definition.default_class 解析 |
| AdmissionReservation 生命周期 | Consume(RESERVED→CONSUMED,S1 领取)/Expire(过期扫描→EXPIRED,过期不可消费)/Release(终态,既有) | live PG 集成测试(consume/expire/过期拒消费/活动 run 检测);live:运行中 run 预留 CONSUMED、终态 run RELEASED |
| Run 状态机行走 | 事实驱动幂等 `RunStateWalker.Sync`(订阅广播=PlanReady/准入 CONSUMED/业务节点领取/全业务终态 四事实→CanAdvance+ValidateRunTransition 循环 CAS);调用点:订阅发布后、S1 领取后(先 consume)、终局循环(FINALIZING 既有迁移) | live:日志连续 PREPARING→QUEUED(PLAN_READY)→RUNNING(LEASE_ACQUIRED)(62987f14 等 4 run);集成测试 ACCEPTED→PREPARING→QUEUED→RUNNING→FINALIZING |
| StageAttempt DISPATCHED | DEDICATED_OPERATION 节点 PENDING→DISPATCHED(领取)→RUNNING(命令接受 CAS,不写双回执)→终态;对账不变式"每终态 attempt 恰一条 fence 匹配回执"保持 | 单测全绿;live 三次迭代修复见 5.2 |
| 契约裁决登记 | 节点命名保持 76.43.2 词形(SOURCE_ACTIVATE/ENCRYPTED_RECOGNIZER,与代码一致;对齐方案 §11 措辞为文档内部漂移);Topic 目录保持现役 5 topic,76.34.3 十二 topic 为后续双写迁移;StageAttempt 字段差量待补 | 本清单 §1.1/§1.5 |
| §20 页面到 API 唯一映射(全 API 面) | `repository/api_surface.go`(任务定义权威 Create/Activate/Suspend CAS+审计、详情/方案/报告策略投影、报告策略修订(修订号自动分配+sha 回放)、报告重试(原地 FAILED/CANCELLED→QUEUED,uq 单行语义+独立 outbox 事件)、下载票(仅 AVAILABLE+短期有效)、调度触发历史投影、资源视图(Reservations/DRR/OutboxLedger/Queue)、阶段回执投影、GetTaskByRunID)+ `service/api_surface.go`(TaskDefinitionService/PreflightService(不物化)/RetryTaskService(同 task 新 run)/HumanReportService.RetryReport+IssueDownloadTicket/ResourceService)+ handler 端点(POST/GET `/task-definitions`+`/{id}/activate\|suspend\|plans\|report-policies`、POST `/triggers/preflight`、POST `/runs/{id}/retry-task`、GET `/runs/{id}/receipts`、GET `/runs/{id}/results`(CH 只读,env 门控 fail-closed 503)、POST `/reports/{id}/retry\|download-ticket`、GET `/schedules/{id}/triggers`、GET `/resources`)+ 迁移 `28-analysis-api-surface-v4.sql`(download_tickets+human_report_policies) | 单测(枚举/幂等键/plan source 校验);live PG 集成测试 `TestLivePGAPISurfaceAuthority`(create→replay→异载荷 409→activate/suspend CAS→详情→plan 投影→policy save/replay/frozen rev→触发历史→retry-task 新 run+回放→stage receipts→report retry 原地重排+回放+非 FAILED 拒绝→ticket 仅 AVAILABLE→资源视图→DRR 累积)PASS;live API 全端点 curl 200/201/409:`d2d479ee` 定义 create/replay/异载荷 409/activate/suspend、报告策略 201、preflight 200(冻结 sha 不物化)、retry-task 201、receipts 200、results 200(CH detection_rows 六类)、download-ticket 201、trigger history 200、resources 200 |
| retry-task 整 Run 重试全链 | POST `/runs/{id}/retry-task`(同 task 新 run:冻结原 plan revision/spec/effective policy,窗口 now+10min;ON_DEMAND 身份,不携带 schedule_revision;判别联合幂等) | live:run `8fa80b85` 由 retry-task 创建并全链闭环 SUCCEEDED/THREAT_FOUND/COMPLETE/VERIFIED(9 阶段全 SUCCEEDED);同键回放返回同 task/run |
| DRR 向量折算+单事务选中(§76.45.3) | 冻结权重 v1(cpu=1000/core、memory=500/GiB、gpu=16000/device、io=100 per MB/s,milli;空向量=1 quantum;负值/非数 fail-closed)+ `DRRVectorCost` 单测 11 例;迁移 `29-analysis-scheduler-queue-v1.sql`(analysis_stage_queue 稳定排序 deadline NULLS LAST, ready_at, run_id, execution_node_id, attempt + DRR quantum 升 1000 milli);物化事务种子每节点队列行(cost 随行);`ClaimStageLeaseAtomic` 单事务:DRR 排序选中(FOR UPDATE SKIP LOCKED)+ queue CAS READY→CLAIMED + attempt CAS PENDING→DISPATCHED(新 fencing token+lease)+ DRR 更新(deficit+=cost-quantum)+ 预留 RESERVED→CONSUMED + ACTIVE 订阅 dispatch outbox + 审计同事务提交;派发循环改走 claim(移除散落的 Mark/Consume/RecordDrr 调用) | live PG 集成测试 `TestLivePGClaimStageLeaseSingleTransaction`(9 行 queue 种子、稳定排序先先取、deficit=5500=2500-1000+5000-1000、attempt/queue/预留/outbox/审计同事务、空候选 nil)PASS;live:run `140aba85` S1 领取后 queue=CLAIMED(cost 2000)/attempt=RUNNING+lease/预留=CONSUMED/DRR deficit=1000 quantum=1000;镜像 analysis-service core-v52/v53 |
| 运行详情 retry-stage 按钮 + 任务编排页真实化 | 运行详情失败节点行增"重试"按钮(仅 actions.retry_stage 服务端判定 + 该节点 FAILED 时暴露;SHARED_STREAM 由服务端 STAGE_RETRY_UNSUPPORTED 引导整 Run 重试);`AnalysisOrchestrationPage` 由静态五段占位改为真实编排(任务定义下拉+预检(不物化,展示冻结 sha/源类型/兼容性)+保存默认/定制计划(MANUAL 需覆盖项 JSON)+计划修订表+草稿审批激活)+ `saveAnalysisPlan`/`preflightAnalysisPlan` 客户端 | web 单测 345 全绿+tsc+vite build;镜像 web-ui:analysis-orchestration-20260818 上线,网关 /analysis/orchestration 200 |
| 任务定义详情五 Tab 前端 | `AnalysisTaskDefinitionDetailPage`(基本信息/方案版本/调度计划/报告策略/审计记录;激活/挂起按钮由 GET allowed-actions 服务端驱动,If-Match expected revision;报告策略保存 Modal)+ 路由 `/analysis/task-definitions/:taskDefinitionId`(routeManifest+pageApiPlans+pageDesignContracts 所有权登记)+ 任务管理页行链接详情;后端 detail API 增审计记录投影(core-v54) | web 单测 345 全绿(routeManifest/pageApiPlans/pageDesignContracts 三测试更新)+tsc+vite build;镜像 web-ui:analysis-task-def-detail-20260818 上线,网关 /analysis/task-definitions/{id} 200;live:detail API 返回五 Tab 数据(plans 0/schedules 0/policies 1/audit 3),allowed-actions SUSPENDED→{activate:false,suspend:false} |
| lease 过期回收(§76.45.3) | `ExpireStageLeasesAtomic`(DISPATCHED 且 lease 过期的 attempt 回 PENDING+清 token/lease,队列 CLAIMED→READY 重入队,审计 LEASE_EXPIRED;RUNNING 不回退,终态由回执流驱动;单批单事务 FOR UPDATE SKIP LOCKED)+ 派发循环每 30s 扫描 | live PG 集成测试 `TestLivePGLeaseExpiryAndAutoReport`(领取→1ms 过期→回收 1 条→attempt PENDING+token 清空+queue READY→再领取成功)PASS;live 循环部署(core-v56) |
| 报告自动化闭环(§8/§20) | `NextAutoReportCandidates`(终态+冻结摘要存在+无报告行+定义最高修订策略 AUTO_ASYNC)+ 协调器循环(AUTO_REPORT_ENABLED,15s,每 tick 最多 5,经 RequestHumanReportAtomic 幂等请求)+ `ConsumeDownloadTicketAtomic`(票归属/未过期/未使用校验→返回对象键→used_at 一次性)+ POST `/reports/{id}/download` | live PG 集成测试(候选命中/请求后移除/票消费一次/二次 409/跨报告 409)PASS;live:def 4104a7bf 设 AUTO_ASYNC 策略后协调器自动请求 4+ 报告,G05 worker 全部生成 AVAILABLE(MinIO 对象);live 票生命周期:签发→下载 200 对象键→二次 409 already used |
| 自查修复:CH 结果读取注入面(P1) | `GET /runs/{id}/results` 此前以字符串拼接 CH SQL(`escapeCH` 只转义反斜杠不转义单引号,tenant 头可控→注入面) | 改 CH 命名参数化(`{tenant:String}`/`{run:String}` + `param_*` URL 参数)+ 参数白名单校验(短 ASCII 标识,拒绝引号/空白);live:参数化查询 200 返回 detection_rows |
| 自查修复:陈旧 run 关闭(P2/P4) | `CloseStaleRunsAtomic`(tenantScope 可空;ACCEPTED/PREPARING 且 window_end 过 grace→CANCELLED+attempts 取消+队列 EXPIRED+预留 RELEASED+投影取消+审计 STALE_WINDOW_CLOSED;生产面排除测试租户)+ 物化循环每 60s 扫描(grace 10min) | live PG 集成测试(关闭 1 条→六事实齐→二次扫描无重复审计)PASS;live:18 条卡死 ACCEPTED(default)全部关闭 CANCELLED,资源队列视图仅剩 1 RUNNING |
| 自查修复:影子/非终态列表租户噪声(P5) | `ListNonTerminalRunIDs` 排除 integration-% 与 live-gw-demo(影子只对照生产面) | live:影子日志不再混入测试租户 |
| 自查修复:测试残留数据清理(P3) | integration-*/live-gw-demo 租户全表清理(FK 顺序:queue/receipts/reports/tickets/summaries/manifests/projections/attempts/reservations/drr/receipts/outbox/runs/tasks/triggers/schedules/approvals/governance/plans/history/definitions/ledger) | live:analysis_runs 仅剩 default(57);stage_queue 9、outbox 118(均为生产面) |
| 网关登录流修复(验证码失效) | APISIX 路由 `/api/v1/auth*` 全程挂 openid-connect(bearer_only),验证码/登录/刷新/OIDC 回调等匿名端点被网关 401 拦截;APISIX 3.9 的 OIDC 插件无 whitelist 字段 → 拆 5 条匿名路由(11a-11e:captcha/login/refresh/oidc/login/oidc/callback,无 OIDC 插件,更具体 uri 优先于通配),受保护端点(me/logout/scopes/password)仍走 OIDC 路由 | live:captcha 200(返回 captcha_id+base64 PNG)、login 400 业务校验(不再 401)、me 无 token 仍 401(回归);apisix-routes ConfigMap 已同步 + 滚动重启 |
| 视觉开发回路(多模态前端开发) | `web/ui/scripts/visual-inspect.cjs`(Playwright 无头 Chromium 截图 + DOM 文本 + tesseract OCR 三通道);code-inspector-plugin(点击元素跳 IDE 源码,dev-only);analysis MSW mock 11 端点 | 9 页 × 3 通道证据产出;视觉审阅修复 B1-B5(路由串链/原始错误文案/mock 缺失/run 键名契约/插件配置被 js 配置遮蔽);web 345 全绿;镜像 web-ui:analysis-visual-fixes-20260818 + core-v58 上线;live /runs 返回 snake_case 键 |
| 权限体系 P0(角色落地 + 租户绑定强制) | OIDC 回调角色改从 access token 经 JWKS 验签读取(ID token 仅身份;本部署 ID token 无角色声明);`internal/common/authz` 共享中间件(JWKS 验签 + token.tenant_id==X-Tenant-ID 强校验,shadow/enforce,健康豁免);Keycloak tenant_id mapper + admin 用户属性 tenant=default;analysis-service 接入 enforce | live:OIDC 登录 roles=["admin"](user_roles 仅 admin);core-v59 enforce:伪造租户头 → 403 tenant mismatch、匹配 → 200、探针豁免 200;单测 2 例;方案文档 `统一权限体系设计方案-2026-08-18.md` |
| 权限体系 P1(双 token 废除 + 判定合一) | `ValidateToken` 统一入口 HMAC→Keycloak JWKS 回退;auth/alert/graph 三服务 OIDC provider 接线+镜像重建(alert 修复 nil provider 硬编码与 minio secret 缺失);前端单 token+应用 token 401 重试桥+KC refresh 单飞重试(删 per-route 清单);admin 补发 `admin:*`(契约同步);OIDC 回调片段瘦身修复网关 502("upstream sent too big header");KC refresh 服务端兑换 | live:KC token → dashboard/snapshot **200**(P1 前 401)、/me 200、OIDC roles=["admin"];auth 7 包+web 345 全绿;KC refresh 兑换 live 复验 **2026-08-18 通过**:traffic-ui 密码授权捕获的 KC refresh token → POST /v1/auth/refresh → **200**(新 access/refresh、expires_in 1800、roles=["admin"]),刷新后 token → /me **200**(含 admin:*)、伪造租户 **403**;期间发现 OIDC_CLIENT_SECRET 漂移(KC 侧轮换 → k8s secret 同步 + auth-service 重启,已恢复) |
| 权限体系 P2-a(契约解释器逐操作判定) | `internal/common/authz/contract.go`:go:embed 内嵌 m10 契约(与 contracts/authz 权威副本 sha256 奇偶校验测试);`OperationFor`(method+path,/api 前缀自动剥离)、`ScopeCovers`(精确/`svc:*` 通配/`*`,`admin:cross-tenant` 通配不覆盖)、`EnforceContract(provider,mode,ignorePaths)` 按 `required_scope` 判定(shadow 只审计/enforce 拒绝/未命中契约放行);共享中间件 Handler 产出 `Principal`(sub/租户/内部角色/契约权限),auth-service `AUTHZ_CONTRACT_MODE=enforce` 接入(authz-p2-contract-20260818) | 单测(奇偶校验/143 op/4 角色/通配/主体/影子/ignore)全绿;live:KC admin → GET /scopes **200**;admin-cli token(无 traffic 角色→viewer)→ GET /scopes **403** `AUTHZ_403 contract scope required: admin:read`(契约层拦截);viewer → PATCH /me、POST /me/password **403 AUTHZ_403**(user:write);未命中契约的 GET /me 放行 200 |
| 权限体系 P2-b(审计三联,销 SUB-FS-12) | 拒绝留痕:forensics `requirePermission`→`AUTH_PERMISSION_DENIED` 同步审计行、白名单 `requireWrite/Read`→`WHITELIST_ACCESS_DENIED`、rule-manager `writeError`→`RULE_ACCESS_DENIED`;`ActivateModelVersion` 授权顺序修复(先 scope 后锁,viewer 500→403);**audit-materializer 部署**(audit.logs→audit_logs,组 audit-consumer 按 ACL 计划);forensics/rule-manager OIDC provider 接线(镜像 forensics-oidc/rule-oidc2/alert audit-deny);剧本 `doc/03_review/高风险操作验收剧本-2026-08-18.md` | live 四断言全过:白名单 viewer 403 alert:write/admin **201**→归档 **200**(version2);pcap viewer **403** pcap:read/admin **200**;规则 viewer **403** rule:write;模型 viewer **403** model:activate;PG audit_logs 六行(WHITELIST_ACCESS_DENIED/whitelist-create/AUTH_PERMISSION_DENIED/RULE_ACCESS_DENIED×2/whitelist-archive);审计页 API `/api/v1/audit/logs?action=*` 全部可查 200 |
| 权限体系二次复查 R1-R4 | R1 角色映射契约 `contracts/authz/oidc-role-map.v1.json` + authz `OIDCRoleMap`(auth-service 与共享中间件单一解释器,奇偶校验+语义测试);R2 `resolveLocalUser` 统一 KC subject 解析(profile/password/settings)+ user_settings_history 表补建;R3 threat-intel OIDC 子集解析+OIDC env+revision 列补建;R4 forensics EnforceContract 挂载 | live:R2 KC token PUT settings **200**(本地 user_id 行,density 落库);R3 KC token /threat-intel/feeds **200**;R4 viewer /pcap/jobs **403 AUTHZ_403**(pcap:read)、admin 200;单测 authz/auth/service 全绿 |
| 权限体系契约解释器逐服务接入 | alert-service(apiRouter 挂 EnforceContract,provider 适配自有认证层 httpx 键,镜像 contract-20260818);graph-service(认证后→契约→业务顺序修复,OIDC provider 接线补缺,镜像 contract-20260818);asset-service(HTTP 面零校验→共享中间件+契约+API token 回退,`requestIdentity` 优先信任中间件 Principal,镜像 asset-authz-20260818) | live:alert viewer GET /alerts 200、POST /alerts/views **403 AUTHZ_403**(alert:write)、admin 200;graph viewer /workbench 200(契约 viewer 角色含 graph:read)、无 token 401、伪造租户 403、admin 过契约到业务层;asset viewer GET 200、POST **403 AUTHZ_403**(asset:govern)、admin GET 200/POST 至业务校验(400 mac 必填)、无 token 401、伪造租户 403 |
| 网关面机器凭证通行(ADR-6) | APISIX 路由 59(/api/v1/analysis*)与 /api/v1/assets* 移除 OIDC 插件(上游服务=权威判定层,enforce live 验证);其余路由保持网关 OIDC | live:API token 经网关 /analysis/runs **200**、/assets **200**、伪造租户 403;KC token 200(服务侧验签);无 token 401;alerts 路由无 token 网关 401 |
| KC 用户 PATCH /me(AUTH_1006) | UpdateCurrentUser/ChangePassword 按 external_id(KC subject)解析本地用户,actor=本地 user_id | live:PATCH /me 200(user_id=a69e988c…,email 落库,revision 26) |
| 规则/模型允许路径审计 | 规则允许路径 live(201→RULE_CREATE 审计→DELETE→RULE_DELETE 审计,scratch 清理);模型激活允许路径代码级(recordAuditSuccess 已接线,不翻转生产模型) | live:RULE_CREATE/RULE_DELETE 审计行经 materializer 落库+审计 API 可查 |
| 权限体系 P2-c(API token 统一中间件,ADR-6) | `internal/auth/apitoken` 校验器(前缀匹配查询+sha256 十六进制现役方案与存量 bcrypt 双方案+过期/撤销 fail-closed+usage 统计回写);`authz.Config.Fallback TokenValidator`(非 JWT 凭证回退,同一中间件产出 Principal Kind=api-token,租户绑定同样强制);httpx 上下文桥(审计 actor=api-token:<id>);`TokenRepository.GetActiveByTokenPrefixMatch`(不猜历史前缀长度,$1 LIKE token_prefix\|\|'%');analysis-service core-v60 `AUTHZ_API_TOKEN_ENABLED=true` | 单测(前缀形态/双哈希方案/中间件回退 4 断言/httpx 桥)全绿;live 全生命周期:POST /api/v1/tokens **201**(create_token 审计)→ 匹配租户 GET /analysis/runs **200** → 伪造租户 **403** → 伪造 token **401** → usage_count=2/last_used_at 落库 → revoke **200**(revoke_token 审计)→ 原 token **401**(fail-closed);live 修复:COALESCE 类型错误(ip_whitelist 为 jsonb)、现役哈希为 sha256 非 bcrypt、旧格式前缀 14-16 位 |
| Orchestrator 影子接线(等价性证据收集,未切换) | `repository.LoadOrchestratorFacts`(RunState/PlanReady=已发布 PREPARE/SubscriptionActive=已发布 ACTIVE/准入/租约/节点事实)+ `service.OrchestratorShadow`(Load→Advance 只读决策)+ main.go `ORCHESTRATOR_SHADOW` 影子循环(5s 日志 dispatchables/wait,不写) | live PG 集成测试 `TestLivePGOrchestratorShadowDecision`(真实非终态 run:RUNNING→WAIT_PROVIDER_ACK/ACCEPTED 遗留→WAIT_PLAN_READY 决策合理)PASS;live 日志连续输出影子决策;切换写路径仍待证据收敛(IMPLEMENTING) |
| cron 调度样本(CRON_WINDOW) | 调度修订 `5ed955f9`(cron `* * * * *`、window 45s、MISFIRE_FAIL、FORBID_OVERLAP)保存→激活→Tick 触发 | live:fire c-1787009280000-45000 → trigger MATERIALIZED → run `14a42fa5` 全链闭环 SUCCEEDED/THREAT_FOUND/COMPLETE/VERIFIED;下一分钟 fire c-1787009340000-45000 → SUPPRESSED(FORBID_OVERLAP_CONFLICT,正确);重激活后 fire 再物化 run `3c08f6de` 同样全链闭环 SUCCEEDED/THREAT_FOUND/COMPLETE/VERIFIED;触发历史投影含 CRON_WINDOW 多行;验证后 pause(authority_revision 4) |
| allowedActions 服务端驱动 | `service/allowed_actions.go`(ForRun:cancel=非终态非 CANCEL_REQUESTED、retry_stage=非终态且有 FAILED attempt、retry_task=终态、request_report=终态+摘要存在+报告 NOT_REQUESTED/FAILED/CANCELLED;ForDefinition:activate=DRAFT、suspend=ACTIVE)+ GET `/runs/{id}/allowed-actions`、GET `/task-definitions/{id}/allowed-actions`;前端运行详情页按钮(取消/整 Run 重试/生成报告)由该 API 驱动,不再自行推断 | 单测 7 例(六状态判定表)PASS;live curl:RUNNING→{cancel:true}、SUCCEEDED→{retry_task:true,request_report:true}、SUSPENDED def→{activate:false,suspend:false};web 单测 345 全绿,镜像 web-ui:analysis-allowed-actions-20260818 上线 |
| 调度资源页真实前端 | `AnalysisResourcesPage` 四 Tab(容量配额/队列/租约 DRR/执行器 Outbox)接 GET `/resources`(10s 轮询;空数组/错误/loading 齐全);`analysisApi.ts` 增 `fetchAnalysisResources`/`fetchRunAllowedActions`+类型 | web 单测 345 全绿+tsc+vite build;镜像 web-ui:analysis-resources-20260818 上线,网关 /analysis/resources 200;live 资源视图:DRR 行 default/BASELINE、队列聚合(3 RUNNING/18 ACCEPTED) |
| DRR 台账 live 修复 | `RecordDrrServe` 的 `$3-$4` 未定型参数导致 live `operator is not unique: unknown - unknown`(S1 领取记录从未落库,§5.1 原登记为"已 apply"实为 IMPLEMENTING) | 显式 `::bigint` 转型;live PG 集成测试补 DRR 累积断言(deficit=5 epoch=3,种子 3/1+两次服务 cost=3 quantum=2)PASS;live:run `8f7d1ae4` S1 领取后 `analysis_drr_state` 出现 default/BASELINE 行(deficit=0 quantum=1 epoch=1),resources API 可见 |

### 5.2 实施中暴露并修复的回归(live 证据)

| # | 回归 | 修复 |
|---|---|---|
| R1 | 调度保存幂等回放把自动分配的 revision 混入 spec sha → 同 payload 重放 409 载荷不匹配 | sha 排除 revision(revision 是行身份),回放经 `FindScheduleBySHA` 回源既有句柄 |
| R2 | DISPATCHED 投递回执写 stage receipt → SOURCE_ACTIVATE 双 fence 回执 → RECONCILE "want 1 got 2" → 终局 FAILED(run 09f83333/ed06d8b6/b61e6298 现场复现;fail-closed 语义正确生效) | 命令接受改 `MarkAttemptAcceptedAtomic` 直接 CAS(DISPATCHED→RUNNING)不写回执 |
| R3 | 单步行走钩子在"派发先于订阅广播"合法交错下卡 PREPARING | 改事实驱动 Sync(任意调用序收敛) |
| R4 | Sync 化时丢失 S1 领取的准入消费(consume 原在钩子内) | dispatcher 领取后显式 ConsumeReservationAtomic→Sync |
| R5 | 报告策略重放按新计算的 revision 回源(第二次调用 MAX+1=2)查无行 | 重放改按冻结 policy_sha256 回源并返回原始修订号 |
| R6 | 报告重试新建行违反 uq(run_id,summary,template,locale) | 改原地 FAILED/CANCELLED→QUEUED(同一报告行,独立 outbox 事件 event_id);台账先裁决再状态闸(重放不再被 QUEUED 拦截) |
| R7 | retry-task 触发实例携带源 schedule_revision → 混入调度触发历史投影 | 重试语义为 ON_DEMAND,schedule_revision 冻结 0 |
| R8 | 服务错误漏映射(ErrPayloadMismatch/not retryable/ticket 非 AVAILABLE 返回 500) | 统一映射 ANALYSIS_IDEMPOTENCY_PAYLOAD_MISMATCH(409)/ANALYSIS_INVALID_TRANSITION(409)/ANALYSIS_NOT_FOUND |
| R9 | RecordDrrServe `$3-$4` 未定型 → live `operator is not unique`(DRR 台账从未落库) | 显式 `::bigint`;集成测试补累积断言;live 行 default/BASELINE 出现 |
| R10 | 计划冻结哈希漂移:`machine_summary_schema_ref` 从装配链丢失(DefaultTemplate/DefaultPlanResolver/CustomPlanResolver 均未携带;SavePlanDraftAtomic 插入写死 'summary-v1' 而哈希按 '' 计算)→ 同一内容两种冻结哈希、按需 AUTO_DEFAULT 内联提交 409(仅 worker 按冻结 sha 物化可用) | 四层修复:模板结构+装配映射+两 resolver intent+保存命令持久化(字段随行);回归测试 `machine_summary_schema_regression_test.go`(带字段必得 3937277f、解析器必携带)+ live 探针 `TestLivePGPlanFreezeDriftProbe`/`TestLivePGPlanRowConsistency`;新增 SaveDefault(AUTO_DEFAULT 保存)+ POST /tasks/{id}/plans 支持 plan_source;live 重冻结 6 个漂移修订(probe82 AUTO rev3/MANUAL rev4、thin AUTO rev2、ustc AUTO rev2、veth-capture MANUAL rev3、veth-replay MANUAL rev3)后 9 个 ACTIVE 行全部行内容↔哈希一致;live:AUTO/MANUAL 内联提交均 202(run `84d70d92`/`ba8474b5` 全链 RUNNING),preflight 规范 spec 携带 machine_summary_schema_ref |

### 5.3 仍未补齐(下一轮)

Orchestrator 影子→驱动切换(影子 loop 已 live 收集等价性证据;集成测试增收敛不变式:已确立事实不得回退为前置等待,切换需对照收敛后执行)、CompiledRunScopeIndex+fan-out 预算、报告保留清理(AUTO_ASYNC retention 无清理任务)、run 级超时兜底(运行中 run 无超时/取消 drain)、CANCEL_PREVIOUS drain 链、EVENT_DRIVEN 事件桥、Rust Flow 冻结 run、Session/Alert run_id(需影子迁移,按文档迁移规则不得静默改 base 链)、G06-G09、P5 盲评(缺环境如实 BLOCKED)。

**权限体系剩余(下一轮)**:以 `doc/07_alignment/权限体系问题清单-深度梳理-2026-08-18.md` 为准——本轮已销:认证拒绝留痕/analysis 前端权限/创建带 IP 白名单/azp 白名单/网关身份头剥离;剩余:认证服务回退路径的 azp 校验、KC 会话终止、KC 角色变更事件、materializer 修饰与回放、探针凭证核对、asset gRPC 与 nebula 上线前鉴权、G06 浏览器矩阵(BLOCKED)(网关身份头剥离/aud 校验/shadow 主体/双 token 下线/KC 会话终止与角色事件/契约外操作/materializer 修饰与回放/创建带 IP 白名单/探针凭证核对/asset gRPC 与 nebula 上线前鉴权/KC 多租户属性恢复/K4 角色矩阵);G06-G09/P5 缺环境保持 BLOCKED。

**新发现缺口(如实登记)**:KC 用户 PATCH /me → 业务层 401 `AUTH_1006 User not found in authenticated tenant`(UpdateCurrentUser 按 claims.UserID 直查 users 表,KC 用户 subject 与本地 user_id 不同源;契约层已放行 user:write,属 P1 统一 token 后暴露的用户域残留,非本轮回归;GET /me 走 claims 投影不受影响)。

**本轮回滚恢复与运维事项(如实登记)**:① traffic-ui 客户端密钥三次轮换(public/confidential 切换触发 KC 自动重生成,本轮 P2-c 验收续借 token 再触发一次)→ 每次轮换后同步 external-secrets 源 secret(`external-secrets-source/traffic-platform-prod-credentials` 的 OIDC_CLIENT_SECRET)并重启 auth-service,手工 patch 目标 secret 会被 ESO 回写覆盖,必须改源;② rule-manager/forensics/asset/threat-intel 四个部署的"卡死 rollout"恢复:补建 `minio-forensics-service-credentials`/`minio-asset-service-credentials`(accesskey=admin)、导入 `base-threat:remediation-export` 镜像、补齐 PG 表/列(model_action_execution_inbox/model_action_jobs 加法列/deployment_event_projection/deployment_state_projection/model_feedback_inbox 三列/model_action_outbox/whitelist_rule_projection/rule_update_applied_acks/rule_version_rollback/model_rollback_requests/model_feedback_revision_*);③ audit-materializer 新部署(audit.logs→audit_logs,消费组 audit-consumer 依 ACL 计划)。

**重冻结后续登记(已处理)**:`de0e3243`(绑定 def-live-sched-auto rev1,无漂移)此前遗留 ACTIVE 持续触发,已 pause(authority_revision 10,578 条 CONTINUOUS_WINDOW 触发停止增长);`eeef51d3`(绑定 probe82 旧代 rev1 94b3e66a,漂移行)已存重绑定新修订 `918e558a`(approved_plan_revision=3、冻结 sha 114d62e8…,DRAFT 不触发),旧修订行保留。integration-*/live-gw-demo 租户的漂移行属测试/演示残留,非生产面。

**测试卫生如实登记**:本轮回溯性清理命令中一条 SQL(`OR identity_hash IN (SELECT identity_hash ... LIKE '%')`)误删了全部 `analysis_materialization_ledger` 行。台账仅承载幂等重放去重(业务行 task/run/plan/schedule/report 未受影响,已有闭环 run 数据完好);影响=历史提交键失去回放去重(同键重放会新建 task 而非返回原句柄),无业务数据丢失;后续键按租户随机化+失败也清理(t.Cleanup)已修复测试卫生。

---

## 0. 差异总览

| 域 | 已对齐 | 部分对齐 | 未实现/漂移 |
|---|---|---|---|
| 主链聚合与状态机 | 聚合链、六轴枚举、真值表主体、幂等物化、状态机行走(§5.1) | 取消链、命名 | DRR、窗口前置准备、Stage retry |
| 阶段/节点/回执 | receipt inbox 六 outcome、fence CAS、三件套同事务、DISPATCHED 领取、per-input 结果物化+六类计数(§5.1) | 节点粒度、attempt 字段 | PLAN_READY/细粒度节点 attempt、Outbox broker ACK |
| 调度内核 | Schedule 权威链+窗口触发 worker+有效策略+准入生命周期(§5.1) | cron 解析 | 窗口前置准备(prepare_at/迟到三策略)、DRR |
| 数据面隔离 | base v1 key 不动、0..N 派生、固定 group | 订阅字段、索引结构 | CompiledRunScopeIndex、fan-out 预算、Rust Flow 冻结 run、Alert/Evidence run_id |
| Topic 目录 | plan/run/envelopes/receipts/report 五 topic 常驻 | 命名与拆分粒度 | 12-topic 细化拓扑、stage.commands、report.receipts、双写迁移流程 |
| UI | 「任务调度」一级域、五二级菜单名、三步向导、五轴展示 | 运行监控/运行详情、向导 | 报告中心双 Tab、调度管理/调度资源页、六维展示、目录选择、细粒度 scope、浏览器门 |

---

## 1. 对照文档一(对齐方案 + PR 代码级设计 76.43—76.48)

### 1.1 统一聚合链 TaskDefinition → PlanRevision → TriggerInstance → Task → Run → StageAttempt → RunClosure

**已对齐**:
- 对象链与命名基本一致(`analysis_task_definitions`/`analysis_plan_revisions`/`analysis_trigger_instances`/`analysis_tasks`/`analysis_runs`/`analysis_stage_attempts`/`analysis_run_closure_manifests`);物化事务 `MaterializeAnalysisTaskAtomic` 一条链创建 Task+Run+attempts+投影+outbox ✓;TriggerInstance 判别联合(tenant+identity_kind+canonical_identity_hash)+ request_sha256 分列、同键同 hash 重放/异 hash 409 ✓;HTTP 202 在事务提交后返回 ✓。
- 自动/人工仅影响方案来源:同一 `ResolveForPlanSource → PlanCompiler → Materialize` 链,AUTO_DEFAULT 与 MANUAL_CUSTOM live 同链闭环(run 7c790fd8 / e95b6545 / b319eea9 等),无第二套 Topic/group/状态机 ✓(负例 1/2 满足)。

**差异**:
- [中] **节点命名漂移**:代码 `SOURCE_ACTIVATE`、`ENCRYPTED_RECOGNIZER`(trigger.go `defaultNodeExactSet`,L217/L220)与《对齐方案》§11 exact-set 措辞 `SOURCE_ACTIVATION`、`ENCRYPTED_RECOGNITION` 不一致;76.43.2 自身又把后者定为业务阶段名(节点名恰为 ENCRYPTED_RECOGNIZER)。代码与 76.43 节点名一致,但两份设计文档之间存在措辞不一致,需先收敛再回扫代码。
- [中] **PLAN_READY 无 attempt 事实**:对齐方案 §11 中期 exact-set 含 `PLAN_READY` 节点;76.43 将其拆为 PLAN_VALIDATE/ARTIFACT_RESOLVE/PROVIDER_PREPARE/CAPACITY_ADMIT 四节点,并把 RECONCILE 拆为 COVERAGE_RECONCILE/INTEGRITY_RECONCILE、MACHINE_FINALIZATION 拆为 SUMMARY_FINALIZE/RUN_CLOSE。实现侧只有 9 个 attempt 节点(PLAN_READY 是 run 事件/闸门,非 attempt),无逐闸门节点事实与 receipt。
- [中] **StageAttempt 字段缺口**(76.43.1):现表缺 `task_id / run_attempt / stage_attempt_id / authority_revision / required / expected_target_manifest_sha256 / input_replay_manifest_sha256 / lease_epoch / coverage_manifest_sha256 / status`(现用 `state`);attempt 无 authority revision,CAS 全部依赖"当前行 state"而非 expected revision。
- [已补齐→§5.1] ~~状态机未行走~~:Run 状态机行走已接入(事实驱动 `RunStateWalker.Sync`,订阅发布→PREPARING→QUEUED→RUNNING,终局循环→FINALIZING;live 日志连续行走证据);StageAttempt 的 DEDICATED_OPERATION 节点走 PENDING→DISPATCHED→RUNNING→终态。遗留:PARTIAL/SKIPPED 状态的产生路径未实现(completion policy 部分语义),CANCEL_REQUESTED 行走已有枚举但取消链仅 Request+CAS。
- [中] **取消链部分实现**:`CancelService.RequestCancel`(冻结 CancelTargetManifest + CAS 终态先胜)+ FinalizeCancel 存在(finalizer.go:83-105),但缺逐 active-attempt 取消回执、in-flight/outbox drain 判定与 `EvaluateCancelClosureAtomic`(§10.3)。

### 1.2 触发方式、方案来源、调度策略完全正交

**已对齐**:PlanSource 两值不参与执行分叉 ✓;SourceKind 三值(PCAP_REPLAY/PROBE_CAPTURE_WINDOW/LIVE_STREAM_WINDOW)已实现且 live 各验证其一 ✓。

**差异**:
- [高] **TriggerKind 仅 ON_DEMAND**:CONTINUOUS_WINDOW/CRON_WINDOW/EVENT_DRIVEN 无实现;物化命令硬编码 `TriggerKind: "ON_DEMAND"`(trigger.go:160)。
- [已补齐→§5.1] ~~ScheduleRevision 全链未实现~~:登记时误判——保存/窗口 Tick/物化 worker 骨架已存在,真实断点是 `EnqueueMaterialize` 未接线+worker 未启动;激活/暂停/有效策略/并发策略/准入生命周期已于 2026-08-17 晚补齐并 live 闭环(§5.1)。剩余:cron 解析、prepare_at 窗口前置准备与迟到三策略、DRR 公平队列。
- [已补齐→§5.1] ~~EffectiveSchedulingPolicy 未实现~~:`ResolveEffectiveSchedulingPolicy`(类覆盖链/逐维 min cap/超限拒绝/canonical sha)已实现并接入 on-demand 与 schedule 两条物化路径。遗留:authorized trigger override 无授权体系支撑(当前恒为空,fail-closed),tenant policy/definition/trigger 三层 cap 无存储来源(仅 plan budget + schedule restrictions)。
- [部分补齐→§5.1] **AdmissionReservation 生命周期**:CONSUMED(S1 领取)/EXPIRED(过期扫描+过期拒消费)/RELEASED(终态)已实现并 live 证据;遗留:DRR 公平队列(`analysis_drr_state` 无)、节点 lease 与"选中+DRR+扣配额+lease+DISPATCHED+outbox 同事务"合并事务。
- [高] **窗口前置准备未实现**:无 `prepare_at = window_start - prepare_lead_time` 预冻结;`ComputeDueTriggerInstances`/`ComputePrepareAt`/`EvaluateLateActivation`(FAIL_WINDOW/DELAY_WINDOW/BOUNDED_REPLAY_IF_PROVEN)缺失;TriggerInstance 仅 PENDING_MATERIALIZATION(7)/MATERIALIZED(50),SUPPRESSED/QUARANTINED 状态未落地(同 identity 异 hash 直接 409,不落 QUARANTINED 事实);FORBID_OVERLAP/CANCEL_PREVIOUS 并发策略无。

### 1.3 冻结五个业务阶段与内部执行节点、Run 终态真值表

**已对齐**:
- 五段业务投影固定(数据采集/特征处理/加密特征识别/恶意流量检测/机器摘要)✓;S2 含 SESSIONIZATION+FEATURE_EXTRACTION、S4 含 RULE/BEHAVIOR/DETECTION_AGGREGATE ✓;provider_mode 三值与 activation_mode 三值(含 AUTHORITY_LOCAL)冻结 ✓。
- 真值表:`EvaluateRunClosure` 为无 IO 纯函数,10 分支与 §7.6 逐行对应(完整性失败最高优先级→cancel CAS→required 失败→deadline→zero-input(ALLOW_NO_DATA/FAIL_EMPTY)→阳性→阴性→证据不足→部分阈值)✓;`DetectorDisposition` 六值与 `FindingConclusion`/`RiskSeverity`/`Completeness`/`IntegrityState` 枚举值齐全 ✓;三件套同事务 + 唯一终态 ✓(live 1/1/1 × 3 run)。
- 负例多数已闭环:#7(无消息≠NEGATIVE,zero-input 需 source fence 证明)、#9/#11(同键异 payload 409;同 tuple 异 hash quarantine)、#12(旧 fencing token STALE_FENCE)、#13(终态迟到 LATE_TERMINAL)、#21(缺任一 required 节点不得 SUCCEEDED)。

**差异**:
- [高] **每输入×detector 结果未物化**:§7.4/76.44 要求每输入 typed outcome + `AnalysisResult`;实现只有 DETECTION_AGGREGATE 回执 fence 的聚合计数(positive/negative/inconclusive/error),`analysis_results` 0 行,machine summary limitations 自认"per-input disposition rows are not materialized";NOT_RUN/INCOMPATIBLE 计数在聚合 fence 无独立列(判定步骤 5"每 input 恰一个 disposition"无法证明)。
- [中] **RunOutcomeDecision 字段缺口**:无 `HighestRisk` 聚合(live 三 run risk_severity 恒 UNKNOWN)、无 `LimitationCodes / DecisionRuleID / DecisionSHA256`;真值表第 15 步稳定排序+decision hash 未实现。
- [中] **0..N 派发器未接线**:`Orchestrator.Advance` 实现了 PIPELINED_STREAM 不等待上游终态、AFTER_UPSTREAM_CLOSE、AUTHORITY_LOCAL 谓词与 0..N 选择,但 main.go 未调用;实际由 StageDispatcher(S1 单点)+ StageGate(S2-S4 批量开闸)+ FinalizeLoop 驱动;`SelectDispatchableNodes` 的 8 种 WaitReason(WAIT_PLAN_READY/WAIT_WINDOW_START/WAIT_WATERMARK/WAIT_PROVIDER_ACK/WAIT_CAPACITY/READY_TO_RECONCILE/READY_TO_FINALIZE/UNRECOVERABLE_FAILURE)无实现;DETECTION_AGGREGATE 等待 RULE/BEHAVIOR coverage manifest 的依赖未表达(现与 RULE/BEHAVIOR 同批开闸)。
- [中] **Feature 精确选择未实施**:plan 的 selected_feature_ids(["f1"])未在 Flink 数据面执行 exact-set 过滤,RunFeatureReceiptProcessor 恒算全部特征(§3 缺口表"feature_set 标签≠精确选择"仍成立;负例 #4 未闭环)。
- [低] **版本/水位字段**:stage receipt 的 coverage/watermark 语义为计数级,无 input/output schema+hash 与 `ExecutionNodeSpec.SpecSHA256/GraphHash`(BuildExecutionNodeGraph/ValidateExecutionNodeGraph 未实现,节点图硬编码于 defaultNodeExactSet)。

### 1.4 事务边界、幂等、Receipt、Outbox、重试

**已对齐**:receipt 事务 = inbox insert(RECEIVED)→ 锁 attempt → fence 校验 → 插不可变 receipt → CAS attempt → history,同事务 commit(repository/receipt.go);六 outcome(APPLIED/REPLAYED/QUARANTINED_HASH_CONFLICT/STALE_FENCE/LATE_TERMINAL)语义齐全,确定性非法消息 commit 后不重投 ✓;物化身份判别联合与精确重放 ✓。

**差异**:
- [中] **receipt 事务内无 next-stage outbox**:§10.2 要求"insert next-stage outbox if ready"同事务;实现由 StageGateLoop/FinalizeLoop 定时拉取推进,下游驱动与 receipt 提交解耦(功能等价,契约不符)。
- [高] **Outbox 无 broker ACK 台账**:`RunEventRelayer` 发布成功即 `MarkOutboxPublished`,无 topic/partition/offset 记录、无 `MarkBrokerAckAtomic`、无 OUTCOME_UNKNOWN 与事件级 reconciler(76.47.2 的 PublishOne/BrokerAck 合同未实现);MarkOutboxFailed 后无重试 worker。
- [高] **Stage retry 未实现**:无 `ValidateStageRetry`/`STAGE_RETRY_UNSUPPORTED`/RetryTask;输入 replay manifest 字段缺失,SHARED_STREAM 节点重试语义未冻结。
- [中] **人读报告半链**:`RequestHumanReportAtomic`/`ApplyHumanReportReceiptAtomic`/`VerifyAndConfirm` 已实现且不碰 MinIO、不回退 Run ✓;但 worker 未部署(GENERATING 无推进者)、`AutoReportCoordinator`/HumanReportPolicyRevision 未实现、ReportState 的 NOT_REQUESTED(无行)/FAILED/CANCELLED 路径未闭环、报告命令 topic 名为 `analysis.report.requests.v1`(76.34.3 契约名为 `analysis.report.commands.v1` + 独立 `analysis.report.receipts.v1` 缺失)。

### 1.5 v1 Topic 兼容迁移、RunScope 隔离、Flink 状态连续性风险

**已对齐**:base `flow.events.v1` 保持 tenant+community key、未加 run_id ✓;AUTO/MANUAL 共用常驻 group/job ✓;run-scoped 派生 `analysis.envelopes.v1`(key=tenant+run+community)✓;`analysis.plan.events.v1` compact key=tenant+execution_spec_sha256 且不含 run 上下文 ✓;固定 consumer group、不为每 run 建 Topic/Job ✓。

**差异**:
- [中] **Topic 目录名称与粒度漂移**:现目录 5 topic(`analysis.plan.events.v1`/`analysis.run.events.v1`/`analysis.envelopes.v1`/`analysis.receipts.v1`/`analysis.report.requests.v1`);76.34.3 契约为 12 topic:`analysis.run.subscriptions.v1`(现为 run.events)、`analysis.flow/session/feature/recognition.envelopes.v1` + `analysis.detection.outcomes.v1`(现合并为单一 envelopes)、`analysis.stage.commands.v1`(现探针命令走既有 `probe.control.v2`)、`analysis.stage.receipts.v1`、`analysis.task.events.v1`(缺失)、`analysis.report.commands/receipts.v1`。detections.v1 收敛 BLOCKED 项仍 BLOCKED。
- [中] **RunSubscription 字段缺口**:现仅 schema_version/tenant_id/run_id/revision/state/execution_spec_sha256/window_start/end_ms/fence;缺 task_id、plan_revision(+sha256)、source_kind、target_filter_canonical、prepare_at、allowed_lateness、required_provider_set、lease_epoch、effective_policy_sha256、expires_at、scope_sha256、subscription_sha256;state 仅 ACTIVE/CANCELLED,无 PREPARE(PREPARE→ACTIVE 两段订阅未实现)。
- [中] **CompiledRunScopeIndex 未实现**:路由为广播状态全量迭代 + RunScopeRouter.route(每事件 O(active 订阅)),无 tenant/source/time/selector 索引、无 max_active_subscriptions_per_tenant_source、无每事件 candidate/fan-out 上限与 CPU 预算、超限无 ERROR receipt(fail-closed 超限行为缺失)。0..N 重叠派生与稳定排序已实现 ✓。
- [高] **数据面 run 上下文冻结缺口**(76.46.3 逐条仍成立):Rust 探针 run_id 仍为启动静态值,FlowValue/ScopedFlowKey/Snapshot 未冻结 run;base SessionJob keyBy 仍 tenant+community、两个 generateDeterministicSessionId 未加 run_id(analysis 分支 keyBy 仅 runId,run 内仍以 community 聚合);Alert/Evidence Proto、fingerprint、CH/OS/PG 与查询未补 run_id;加密流量查询/cursor 未绑定 run;FeatureFingerprint 仍只写 CH,无"S3 可消费的 原特征+Recognition 复合 envelope"(S4 识别器在行为 job 内联执行,消费信封 feature_observation)。
- [中] **Flink 状态连续性**:实际路径=新增独立 job+新 UID+新 group(run-receipt 系列),即文档允许的"新 UID 影子分支"之一,未改 base keyBy、未触发 savepoint 语义断裂 ✓;但未按 §76.13.2/L9585 输出 fixture/count/hash 对账与影子 diff 证据,也未登记"三选一"决策记录;旧 base 链回滚路径(切回旧投影)未演练。

---

## 2. 对照文档二(菜单与UI详细设计)

### 2.1 菜单收敛

- [低] 一级「任务调度」已存在 ✓;菜单按 requiredScopes 过滤已实现(routes/access.ts visibleNavGroups)✓;但完整一级域 7 组(综合态势/任务调度/研判取证/数据资产/检测能力/分析报告/审计配置)未重组——现仍为 采集监测/威胁分析/资产图谱/检测运营 旧分组,「分析报告」未成为一级入口。
- [中] 任务调度二级当前 6 项:任务管理/调度管理/任务编排/运行监控/**分析报告**/调度资源;设计为 5 项二级 + 一级「报告中心(机器摘要/人读报告双 Tab)」。设计词汇"任务定义/调度计划/分析编排/报告中心"不得用作菜单名,当前未违例(菜单名与设计一致),但"分析报告"挂错层级。
- [中] 兼容迁移矩阵未落地:设计规定 `/analysis/tasks`→`/analysis/runs`、`/analysis/policies`→`/analysis/task-definitions` 别名重定向;当前 `/analysis/tasks` 被占用为"任务管理"页(与设计的旧别名语义相反),任务管理在设计中的路由是 `/analysis/task-definitions`;`/analysis/orchestrations` 现为 `/analysis/orchestration`(单数)。

### 2.2 八个主页面

| 页面 | 设计合同 | 当前实现 | 差异 |
|---|---|---|---|
| 任务管理 | 列表 7 列 + 详情五 Tab(基本信息/方案版本/调度计划/报告策略/审计记录),激活需确认面板+expected revision | 列表 7 列;详情五 Tab 未实现(页面注释自认依赖详情 API 未就绪) | 中 |
| 调度管理 | 列表按下次触发排序 + 四步短表单(exact plan 绑定→TriggerKind→窗口/并发/misfire/class→影响审批) | `Result` 占位"服务未就绪" | 高 |
| 任务编排 | 版本列表+固定五段画布+配置 Drawer+兼容性三态;历史只读 | 只读五段 Steps+PlanReady/Reconcile 说明;无版本列表/Drawer/兼容性 | 中 |
| 运行监控 | 8 列合同、轻摘要三项、四筛选、状态不靠颜色、失败原因摘要 | 运行表存在(状态/机器结论/完整性/证据完整性/风险/操作);无轻摘要、列数不足 8、无"任务定义/目标含负责人与双短 ID"列 | 中 |
| 即时分析向导 | 隐藏路由 `/analysis/runs/new`;①已批准定义目录+source summary/target/window/硬上限 ②默认/自定义(未覆盖字段"继承自模板")③校验提交;稳定幂等键+超时原键恢复 | 页内 Modal 三步;①任务定义**自由文本输入**(设计明令"不得用空白自由输入代替权威定义");①缺范围/窗口/上限;幂等键 `ui-{taskId}-{Date.now()}`(每次新键,违反稳定键+原键恢复合同) | 高 |
| 运行详情 | 概览/分析结果/证据/技术详情四 Tab;顶部只留目标/阶段/结论/完整性/关键发现/报告状态 | Descriptions 五轴+规格哈希+阶段表+报告按钮;无四 Tab 分层、无证据/技术详情页签 | 中 |
| 调度资源 | 管理员四 Tab(容量配额/队列/租约/执行器),首屏只答阻塞/影响/动作 | `Result` 占位"服务未就绪" | 高 |
| 报告中心 | 机器摘要默认可开+人读报告双 Tab,列表含 summary hash/模板/语言/状态/下载审计 | info 占位说明,无列表、无状态列、无下载票 | 高 |

### 2.3 六轴正交(运行状态、机器结论、风险级别、完整性、完整性校验、报告状态)

- [低] 后端五轴在 `analysis_runs`(state/finding_conclusion/risk_severity/completeness/integrity_state),ReportState 独立于 `analysis_human_reports.state`(QUEUED→VERIFYING→AVAILABLE),后端未压成一个状态 ✓。
- [高] 前端 `AnalysisRunView` 仅 5 字段、无 report_state:运行监控与运行详情只展示 5/6 维,「报告状态」缺失(仅有"生成人读报告"按钮 toast,无 NOT_REQUESTED/QUEUED/GENERATING/VERIFYING/AVAILABLE/FAILED/CANCELLED 状态列与文案)。
- [中] 类型化六组件(RunStateTag/FindingConclusionView/RiskSeverityTag/CompletenessView/IntegrityStateView/ReportStateTag)与 §18 结论文案矩阵(如"在所选检测范围内未检出威胁"、"分析已完成,人读报告生成失败")未实现;unknown enum fail-closed("状态无法确认"+锁关键动作)未实现。
- [中] 刷新频率合同(运行列表 active 10s/全终态 30s、详情 3s→10s 退避、报告排队 5s)未按合同实现(详情 5s 固定轮询)。

### 2.4 字段、动作、权限、空态、异常态、响应式

- [高] 权限 scope 仅 `analysis:read`/`analysis:admin` 两档;设计 15 档(definition/schedule/plan/run/report/resource/audit × read/write/approve/activate/trigger/cancel/retry/request/download)。
- [高] `allowedActions` 服务端驱动缺失:操作按钮由客户端状态推断(非终态可取消),无 server allowedActions × route scope 交集。
- [中] 空态三文案(无批准定义/有定义无 Run/筛选无结果)未实现;TRANSPORT_OUTCOME_UNKNOWN/STALE_PREFLIGHT/INTEGRITY_FAILURE/UNKNOWN_ENUM 状态板未实现。
- [中] 幂等键与操作恢复:向导用时间戳新键,无"提交结果待确认+原键恢复"(`recoverAnalysisOperation` 未实现)。
- [低] 响应式与浏览器门:无 1920/1440/1024/390 同视口 Design QA;八张视觉候选 PNG 未吸收进页面;键盘/焦点/对比度门未跑。
- [中] 目录/边界:`features/analysis-scheduling/`(contracts/viewModels/components/forms)、`routePageRegistry.tsx`、`pageDesignContracts.v1.json`、`services/analysisSchedulingApi.ts` 未建(现为 pages/ 下散页 + services/analysisApi.ts);`services/pageApiPlans.ts` 已存在(7 个 analysis 页主端点计划,analysis-resources 计划主端点 `/v1/analysis/queue`、analysis-reports 计划主端点 `/v1/analysis/runs`,与设计第 20 章端点合同仍有出入);六个 analysis 页面与 analysisApi.ts 为 git 未跟踪 WIP。
- [低] 测试:345 单测全绿但 analysisApi 无专属单测、e2e 无 analysis 路由覆盖、msw handlers 无 analysis mock。

---

## 3. 文档间自身不一致(先收敛再回扫,不得"后文覆盖前文")

- [低] 节点名:对齐方案 §11 的 `SOURCE_ACTIVATION`/`ENCRYPTED_RECOGNITION` vs 76.43.2 的节点名 `SOURCE_ACTIVATE`/`ENCRYPTED_RECOGNIZER`(后者与代码一致)。
- [低] 报告域菜单:对齐方案 §13 为「分析报告」一级(机器摘要/人读报告);UI 设计为一级「报告中心」(路由 /analysis/reports?tab=machine|human);76.48 称「报告中心」。三处措辞需统一。
- [中] Topic 名:76.34.3 用 `analysis.run.subscriptions.v1`/`analysis.stage.receipts.v1`/`analysis.report.commands.v1`,而 76.46.3/L9583 文本写作 `analysis.run.subscriptions.v1`(一致)但现实现用 `analysis.run.events.v1`/`analysis.receipts.v1`/`analysis.report.requests.v1`;若实现名被视为既定事实,应反向更新 76.34.3 目录或执行迁移,不能两套并存。
- [低] 76.43 的 StageAttempt 字段 `status` vs 现表 `state`、`run_attempt` 维度(现表无)——字段名需裁决。

---

## 4. 建议的处理顺序(不改变执行授权)

1. **契约冻结**:节点命名(1.1)、Topic 目录名(1.5)、StageAttempt 字段表(76.43)三处先出 CTR 裁决,再回扫代码,避免实现与文档互相追逐。
2. **主链硬缺口**(按依赖):Schedule/TriggerKind 正交 → EffectiveSchedulingPolicy + Reservation CONSUMED/EXPIRED + DRR → 窗口前置准备;per-input AnalysisResult 物化 + DETECTION_AGGREGATE 全 disposition 计数;Outbox broker ACK 台账;Stage retry 合同;状态机行走接入物化/回执/终局事务;Orchestrator 接线替代双 loop。
3. **数据面**:RunSubscription 字段补全(PREPARE 两段)、CompiledRunScopeIndex + fan-out 预算、Rust Flow 冻结 run、Session 确定性 ID 加 run、Alert/Evidence run_id(legacy-unattributed)、Flink 影子对账 diff 证据。
4. **UI**:报告中心独立一级 + 双 Tab;六维展示与类型化组件 + 文案矩阵;向导改"批准定义目录选择 + 稳定幂等键 + 原键恢复";调度管理/调度资源页按合同实现;allowedActions 服务端驱动与细粒度 scope;补 1920/1440/1024/390 浏览器门与 e2e。
5. **如实非声明**:本清单只登记差异;任何条目修复后须按文档证据等级(E1-S/E1-H/E2/E3/D)重验,禁止把"静态合同存在"写成闭环。

---

更新时间:2026-08-17
状态:<code>DIFF_REGISTERED / NOT_EXECUTED</code>(对照登记,不含执行授权;证据等级按 doc/07_alignment/README.md 规则)
