# 前后端逻辑对齐整改执行态差异清单

采集时间：2026-08-07

本清单记录会话 `019fb27f-8e98-76a0-aa2f-c4272abd68b0` 后的执行态，与 `编制基线清单-2026-07-30-10万字扩展版.md` 互补。历史基线不回写，本清单不把仓库静态验证、单次 HTTP 2xx、单张截图或候选环境 PASS 解释为生产完成。

## 1. Git 与工作树基线

| 对象 | 状态 |
|---|---|
| 主工作树 | `/home/wangwt/phase_2/code/traffic-analysis-platform`，`main@d97b8f96c267dd128efd452289fc32015e82ae05`，存在专题与 UI 的独立未提交变更，本整改不触碰 |
| 整改候选工作树 | `/home/wangwt/phase_2/code/traffic-analysis-platform-remediation-w0`，`codex/remediation-w0-20260730@d593fc1c3c77a9403c5d2a1c045faff99ef29d19` |
| 干净集成工作树 | `/home/wangwt/phase_2/code/traffic-analysis-platform-remediation-integration`，最新G0实现候选为 `codex/remediation-integration-20260807@077aec85589a5e2164306d307ec40a9a0ef0bb43`；采集前后源码哈希稳定 |

整改候选的 Git 默认折叠口径为575个已跟踪修改和337个未跟踪状态项，共912项；将未跟踪目录展开后，实际为575个已跟踪修改和744个未跟踪文件，共1319个文件。两种口径都保留，后续不再把折叠状态项数当作实际文件数。

## 2. Canonical 整改账本

- 功能54项、技术48项，共102个 canonical ID，无删除、无改名。
- 当前状态：`BLOCKED 3 / IMPLEMENTING 37 / OBSERVING 1 / OPEN 35 / VERIFYING 26 / CLOSED 0`。
- 38个 P0：`IMPLEMENTING 18 / VERIFYING 19 / OBSERVING 1`，无 P0 处于 `BLOCKED`，也无 P0 满足正式关闭条件。
- 前端 Product Design 审计已登记 occurrence 的开放项为0；该状态不替代 canonical 关单、G7 或 G8。

## 3. 候选源码与证据绑定

| 证据 | 状态 |
|---|---|
| 最新已完成 G0 | `20260808-remediation-g0-full-v200`，`PASS`，2389个受管文件；alignment 81.660秒、full 232.927秒、python 210.811秒，三项分别PASS |
| 最新资产expand护栏 | `20260807-remediation-asset-expand-guardrails-v1`，仓库专项 `PASS`、覆盖 `PARTIAL_REPOSITORY_DEFAULT_OFF_AND_SUSPENDED_EXPAND`；十项PG migration、15个默认关闭开关和审批绑定渲染器已固化，`production_applied=false` |
| 最新资产PG G1 | `20260807-remediation-asset-expand-g1-v2`，`PASS_ISOLATED_POSTGRES_EXPAND_DOUBLE_REPLAY`；十项migration双重回放、两次schema指纹一致、22/22表与基线回填通过，容器已删除，`production_applied=false` |
| 最新资产OpenSearch G1 | `20260807-remediation-asset-projection-opensearch-g1-v1`，`PASS_ISOLATED_ASSET_OPENSEARCH_PROJECTION`；生产映射authority、read/write alias、确定性文档ID、`external_gte`同版本重放和旧版本拒绝通过，容器已删除，`production_applied=false` |
| 最新资产Kafka G1 | `20260807-remediation-asset-projection-kafka-g1-v1`，`PASS_ISOLATED_ASSET_KAFKA_DURABLE_INBOX`；`asset.events.v2` broker ACK、完整headers/payload消费、durable inbox offset和精确重放幂等通过，PG/Redpanda容器已删除，`production_applied=false` |
| 最新资产MinIO导出 G1 | `20260807-remediation-asset-export-minio-g1-v1`，`PASS_ISOLATED_ASSET_EXPORT_MINIO_MANIFEST`；同一trace下PG job、四项watermark、requested/completed outbox、三类审计、MinIO Stat和下载SHA-256对账通过，PG/MinIO容器已删除，`production_applied=false` |
| 最新资产NebulaGraph G1 | `20260807-remediation-asset-projection-nebula-g1-v1`，`PASS_ISOLATED_ASSET_NEBULAGRAPH_PROJECTION`；真实metad/storaged/graphd下确定性重放、跨tenant VID隔离和有界读取通过，三容器与专用网络已删除，`production_applied=false` |
| 最新资产ClickHouse G1 | `20260808-clickhouse-driver-v2322-compat-g1-v2`，`PASS`；同v200源码将clickhouse-go/ch-go升级到v2.32.2/v0.65.1，真实reader与生产alert writer均通过，tenant隔离、`as_of`上界、告警最新态、硬截断、水位和源不可用失败关闭通过，候选源码稳定，容器与专用网络已删除，`production_applied=false` |
| 最新资产七源 G1 | `20260807-remediation-asset-seven-source-g1-v6`，`PASS_OWNED_BOUNDED_SEVEN_SOURCE_ASSET_RECONCILIATION`；一个资产revision以同一event/trace/revision对账PG、Kafka、ClickHouse、OpenSearch、NebulaGraph、MinIO和审计，计划型oracle为PASS；MinIO明确为适配写入且未包含Flink，8个容器及网络已删除，`production_applied=false` |
| 最新告警OpenSearch writer G1 | `20260807-remediation-alert-opensearch-writer-g1-v2`，`PASS_OWNED_STRICT_ALERT_OPENSEARCH_WRITER`；生产Go单条与bulk writer均被精确alerts-v2严格mapping接受，`updated_at/state_version/event_id/trace_id`回读一致，固定摘要容器已删除，`production_applied=false` |
| 最新告警投影repair终态回执 G1 | `20260807-remediation-alert-projection-terminal-receipt-g1-v2`，`PASS_OWNED_BOUNDED_ALERT_PROJECTION_REPAIR_TERMINAL_RECEIPT`；missing/stale完成受控修复，extra不删除，刷新精确V2 alias并同scope回读后remaining missing/stale为0、extra为1，容器已删除，`production_applied=false` |
| 最新告警投影PG watermark G1 | `20260807-remediation-alert-projection-watermark-postgres-g1-v2`，`PASS_OWNED_ALERT_PROJECTION_POSTGRES_WATERMARK_RECEIPT`；真实PG检测缺失receipt、补写后重查、同毫秒SHA漂移修复和target-version隔离均通过，容器已删除，`production_applied=false` |
| 最新告警投影PG+OpenSearch组合 G1 | `20260807-remediation-alert-projection-postgres-opensearch-g1-v1`，`PASS_OWNED_ALERT_PROJECTION_POSTGRES_OPENSEARCH_TERMINAL_RECEIPT`；同一运行真实OS修复/回读与真实PG水位/manifest回读通过，删除receipt后的第二次运行以0次OS写入恢复水位，两容器均删除，`production_applied=false` |
| 最新告警投影CH+PG+OS三存储 G1 | `20260807-remediation-alert-projection-three-store-g1-v1`，`PASS_OWNED_ALERT_PROJECTION_CLICKHOUSE_POSTGRES_OPENSEARCH_RECEIPT`；生产CH writer/repository提供权威源，逐alert的CH/OS/PG SHA与source_version一致，extra保留，三容器均删除，`production_applied=false` |
| 最新告警投影Kafka五存储 G1 | `20260808-remediation-alert-projection-kafka-five-store-g1-v6`，`PASS_OWNED_ALERT_PROJECTION_KAFKA_REDIS_CLICKHOUSE_POSTGRES_OPENSEARCH_RECEIPT`；PG回执故障同组重启后先恢复到offset=2，乱序与精确重投到offset=5；两个生产consumer稳定分配2个partition并各自产生回执后commit，关闭一员后剩余consumer接管其partition，最终offset=8、lag=0、七event的CH/OS/PG回执一致；该运行仅1个broker副本，不证明复制故障转移；五容器均删除，`production_applied=false` |
| 最新告警投影CH+OS只读shadow G1 | `20260808-remediation-alert-projection-shadow-g1-v2`，`PASS`；固定摘要无卷回环CH/OS以生产reader识别1 missing、1 stale、1 extra，shadow前后两侧count和逐文档SHA不变，不连接PG，两个容器均删除；审批门禁允许纯证据HEAD推进但拒绝源码内容漂移，`production_applied=false` |
| 最新告警投影工具镜像 G1 | `20260808-remediation-alert-projection-tools-image-g1-v1`，`PASS`仅限本地构建；同v197源码的scratch镜像包含两个静态命令、使用`65532:65532`并绑定revision/content SHA；本地image ID为`0e0181c4…de910`但`RepoDigests=[]`，未发布、不可作为审批或Kubernetes镜像引用，`production_applied=false` |
| 最新告警投影工具供应链 G1 | `20260808-remediation-alert-projection-tools-supply-chain-g1-v4`，`HOLD/PREPUBLISH_NON_AUTHORIZING`；同v200源码本地镜像ID为`58b5e6ea…b9c4`，CycloneDX 1.5 SBOM SHA为`486a08b1…dcab`，两个二进制可达与不可达已知漏洞均为0；扫描异常退出最多重试3次且漏洞返回码不重试，仍因漏洞库超过24小时、无RepoDigest、签名和SLSA provenance而不具审批资格 |
| 最新Kafka DLQ/commit屏障 | `T-KAFKA-003` 仓库专项 `PASS`、覆盖 `PARTIAL_SHARED_GO_CONSUMER`；合同、静态verifier、5项负向变异测试及Go race回归已纳入CI，`production_applied=false` |
| 最新PCAP元数据ACK屏障 | `F-PROBE-001 / T-KAFKA-003` 仓库专项 `PASS`、覆盖 `PARTIAL_PROBE_TO_KAFKA_RECEIPT`；ACK只表示MinIO已journal且Kafka RequireAll确认，下游索引仍pending，`production_applied=false` |
| 最新Feature Contract专项 | `20260807-remediation-feature-contract-registry-v5`，`PARTIAL`，仓库专项 `PASS`，标准范围38/38、OpenAPI绑定25/38、非draft绑定缺口0、`production_applied=false` |
| 最新公共响应专项 | `20260807-remediation-common-response-adoption-v4`，`PARTIAL`，仓库专项 `PASS`，审计批次202明确为Kafka ACK后的非最终态，`production_applied=false` |
| 最新同哈希 PG/outbox | `20260807-remediation-pg-transaction-outbox-v16`，`PARTIAL`，专项步骤 `PASS`，包含审计event identity碰撞防护，`production_applied=false` |
| 最新G0、ClickHouse兼容 G1与告警投影工具供应链 G1共同候选哈希 | `016f280e9c38e61136b1eaa98b6b40d9bbd7f53bcdfc74da9159933280bf6d40`；只读shadow G1 v2仍绑定上一v196哈希 |
| G0采集工作树不变性 | 采集前后均为 `322b0bf11f28293633dd24b5981f4e5f7cb3486b`、空状态；状态哈希均为 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |

`contracts/alignment/evidence-index.json` 的最新G0、ClickHouse驱动兼容G1与本地告警投影工具供应链G1绑定v200同一候选哈希；告警投影CH+OS只读shadow G1仍绑定v196，Kafka五存储 G1保留v193，CH+PG+OS三存储 G1保留v187，其余专项继续保留各自历史候选，不冒充v200同哈希证据。当前最终G0候选以v200为准。G0仍排除100轮 live、生产候选部署、Windows Chrome、G2—G6和G8；本地供应链G1虽证明两个二进制可达与不可达漏洞结果均为0，仍因漏洞库时效、registry digest、签名和provenance缺口保持HOLD；ClickHouse兼容G1和shadow G1均只证明自有回环范围，不是生产G2/G3。历史固定窗口`ClickHouse=1,442,312 / OpenSearch=559,140 / delta=883,172`的引用manifest在当前候选中缺失，不能直接转为执行ID或视为已重新确认。生产当前shadow、镜像发布/签名、四方审批、expand/backfill/repair和alias切换仍开放。候选APISIX新增route 45，而实时集群仍为57条旧路由，网关专项因此按事实记为FAIL、`candidate_route_match=false`。

## 4. 与2026-07-30历史基线的差异

- 7月30日的 `NOT_EXECUTED` 仍是报告编制时点的真实记录；本清单只记录后续新证据，不将其回填为历史 PASS。
- 当前已有候选运行时、100轮、Windows Chrome、PG/outbox、OpenSearch、MinIO和PKI等证据，但仍须按各 manifest 的 `production_applied`、`gate_status`和排除项逐项判定。
- 主线在历史基线后新增的报告、索引和架构提示词提交予以保留；架构提示词不纳入本整改交付和验收范围。

## 5. W0 集成约束

1. 整改候选按“合同与工具、平台与数据、领域后端、前端、部署、证据文档”形成可审查提交。
2. 从干净集成工作树逐提交移植，不在脏主工作树中直接 merge/rebase。
3. 发生文件冲突时，先保留主线已批准功能，再以 Feature Contract、Proto、OpenAPI、迁移和测试作为合并裁决依据。
4. 集成后重新计算候选哈希并重跑G0；如果哈希变化，旧证据只能作为历史证据。
5. 本W0不授权生产迁移、回填、流量切换、破坏性HA、现场变更或第三方验收。

## 6. W1合同冻结增量

- 新增 `F-ALERT-003—006`、`F-COMMON-001`、`F-EXPORT-001`、`F-GRAPH-002—004`、`F-TOPIC-003—004` 共11份合同；24周标准范围由27/38提升为38/38，11项标准缺口清零。
- 合同状态与证据分离：已有事务或任务基础的响应进度、告警证据、保存视图标为 `verifying`；服务端批量、统一导出、图谱保存/授权/持续投影、专题对象报告和同源snapshot仍标为 `draft`。
- 注册表状态仍为 `implementing`，覆盖结论为 `STANDARD_SCOPE_COMPLETE_BACKLOG_PARTIAL`；16份backlog合同、运行时contract version/candidate hash、旧客户端命中量、未知字段遥测及G2—G7保持开放。
- 本次仅完成合同编制与仓库门禁，不代表11项功能实现，不改变102项canonical账本状态，不产生任何 `CLOSED`。
- 告警响应动作、告警证据读取、告警视图保存/列表、认证scope目录和探针配置的 `method + path + operation_id + feature_id + scope` 已与OpenAPI对齐；公共稳定动作以显式 `PROFILED` 方式绑定具体告警操作。生成TypeScript客户端同步新增证据、视图和scope目录方法。
- 告警证据、保存视图、视图列表、认证scope目录及审计批次入口改用加法兼容的Contract Envelope；保存视图保持既有HTTP 201，认证scope目录保留 `/api/v1/tokens/scopes` 兼容路由。当前38份合同中25份已绑定，非draft OpenAPI绑定缺口为0。
- F-AUDIT-001新增默认关闭的 `POST /internal/v1/audit/batches`：认证tenant是唯一租户真源，整批任一事件无效即拒绝，调用方必须提供稳定`created_at`，Kafka未获得RequireAll ACK不得返回202；202仅表示批次入Kafka，返回`final=false`和逐event状态URL。消费端同一event_id同载荷可重放，异载荷成为永久identity collision并阻止offset静默前移。
- 上述审计入口尚未部署。实时网关没有候选route 45，生产audit schema迁移、Kafka/PG/CH同trace对账、DLQ barrier、性能、故障、Windows Chrome、灰度回滚和T+观察均未完成，因此F-AUDIT-001仍为VERIFYING，不新增任何CLOSED。
- T-KAFKA-003公共Go批处理已消除“commit排队即ACK”和commit错误吞没：批量只在broker确认后增加processed，提交失败直接返回并保留offset；后台单消息批量提交失败保留pending重试。DLQ初始化遇到非法TLS/SASL配置即fail closed，批内任一序列化失败整批拒绝，批量与单条统一使用canonical TargetTopic。该切片已有合同、静态verifier、5项负向变异、race回归和v173三道G0，但真实broker毒消息/DLQ中断、PG/CH/审计对账、重启/rebalance、非Go consumer、性能和发布观察未完成，因此T-KAFKA-003保持VERIFYING。
- F-PROBE-001/T-KAFKA-003的PCAP元数据入口不再返回含混最终成功：ingest启动只接受`KAFKA_REQUIRED_ACKS=all`，请求tenant/probe必须与认证上下文一致，Kafka失败返回gRPC `Unavailable`，成功message明确`downstream index pending`，审计记录`ack_scope=kafka_durable/final_indexed=false`；探针端只有成功receipt才标metadata_synced，且本地证据仍保留。合同、静态verifier、4项负向测试、全Go和v174三道G0通过；真实MinIO manifest/hash、Kafka故障与Agent重启、Flink/CH最终索引、容量清理、灰度回滚和观察未完成，F-PROBE-001保持OBSERVING、T-KAFKA-003保持VERIFYING。

## 7. W2资产expand增量

- `ASSET_EVENT_OUTBOX_ENABLED`、`ASSET_DISCOVERY_OUTBOX_ENABLED`、`ASSET_PROJECTION_ENABLED` 已在Go运行时默认值、主Kubernetes清单和兼容部署清单中统一改为 `false`；既有 `asset.bindings.v1` consumer 的 `ASSET_KAFKA_ENABLED=true` 保持不变，避免把兼容入口误当成新投影开关。
- 新增 `asset-expand-control.v1` 和 `render_asset_postgres_expand.py`：十项资产migration以不可变ConfigMap打包，渲染必须绑定同源码PASS G0、PostgreSQL `system_identifier`、逐migration前置状态、独立请求/审批身份、不可变nonce和不超过四小时的窗口；Job默认`suspend: true`、`backoffLimit: 0`，执行前会再次计算挂载migration bundle哈希。
- 静态verifier与正负向测试已纳入`make alignment-test`；全量373项alignment测试、全Go、`tests/run_tests.sh full`和`make python-test`在v184通过。配置、服务身份、DR与PKI派生目录保持同步。
- G1使用固定PostgreSQL repo digest创建无卷、loopback、专用sentinel容器，十项资产migration连续回放两次；两次schema指纹均为`c7e00282…b3bc9`，22/22预期表、IP/revision/lifecycle回填和migration registry通过，五类业务outbox均为0，容器已删除。
- 原OpenSearch 2.14.0固定摘要已无法从registry拉取；v177将基础设施、HA镜像和镜像锁统一到当前可拉取的不可变repo digest。专用无卷、loopback、sentinel OpenSearch容器按生产资产模板验证`dynamic=strict`、`assets-v2-write/read`、确定性文档ID、`external_gte`同版本幂等重放和旧版本409拒绝，测试后容器已删除。
- v178以固定digest启动无卷、loopback Redpanda与PostgreSQL，创建单分区`asset.events.v2`并加载14份PG schema入口。真实producer取得RequireAll ACK后outbox才进入published；consumer校验event headers、partition key和authoritative asset history后持久化inbox partition/offset，精确broker重放只产生新提交offset且不复制inbox。首次旧readiness文本判定和第二次topic描述文本判定均安全失败并清理，修正为元数据请求与topic list/partition双检后正式运行通过。
- v179以固定digest启动无卷、loopback PostgreSQL与MinIO，加载14份PG schema入口并使用专用sentinel。资产导出job、snapshot与四项source watermark、requested/completed outbox、requested/completed/downloaded审计、MinIO对象Stat元数据和下载内容SHA-256均绑定`trace-real-minio`；两次测试守卫问题安全失败且容器均清理，修正JSON key计数、UUID文本显式转换和bucket defer清理后正式运行通过。
- v180以固定digest启动无卷、专用bridge的单metad/storaged/graphd，graph端点仅映射到loopback。真实SDK完成相同资产投影精确重放、相同asset ID跨tenant确定性VID隔离、event/revision水位和有界读取；TCP过早就绪与schema传播延迟两次安全失败均完整清理，最终以SDK握手及真实UPSERT作为readiness。正式运行后三容器与网络均删除。
- 仓库原ClickHouse `b052…`摘要已无法从registry解析，但锁文件中的image ID与当前24.3.18.7 amd64固定清单一致；v181将当前镜像锁、四个Kubernetes运行入口及派生配置/身份/DR目录同步到可解析的`458f…`摘要，未回写历史release manifest，未应用生产。
- v181从`common/sql/ch/00-all-tables.sql`提取`traffic.sessions/alerts`完整列权威，只把集群引擎降为隔离单节点`MergeTree`；固定摘要、无卷、loopback ClickHouse中，真实reader验证tenant隔离、快照之后才结束的会话不进入`as_of`聚合、告警最新态、`limit+1`硬截断、水位及表不可用时返回错误。前三次分别因缺少旧摘要、网络账号安全默认值和驱动参数安全失败且均清理，正式运行后容器与网络也已删除。
- v182修复生产ClickHouse告警writer对canonical `Int64`毫秒时间列、`updated_at`和`state_version`的写入，并保留旧`DateTime64/updated_ts`兼容；OpenSearch v2严格mapping加法保留`updated_ts`并加入`updated_at`。七源G1在同一次自有运行中用生产资产PG upsert/outbox、Kafka producer/consumer/durable inbox、OpenSearch和NebulaGraph worker路径处理一个revision，生产ClickHouse writer及显式MinIO适配写入同一event/trace/revision，审计作为第七源，计划型oracle无差异。Nebula首次schema传播失败按生产退避重试收敛；正式v6运行PASS，8个固定摘要无卷容器与网络全部删除。
- v183新增生产Go OpenSearch单条与bulk writer的真实严格mapping回归。固定摘要、无卷、loopback OpenSearch同时装载资产模板和`common/opensearch/alerts-v2/mappings-component.json`，三条告警写入均成功，`updated_at/state_version/event_id/trace_id`按external_gte版本回读一致；同容器资产投影确定性ID、同版本重放和旧版本拒绝继续通过，测试后容器删除。
- v184为T-OS-004补齐repair终态回执：修复前固定存在missing=1、stale=1、extra=1，生产writer只写missing/stale，随后刷新精确`alerts-v2-write`并以同tenant和同scope回读；remaining missing=0、stale=0、extra=1且`repair_converged=true`。PG run manifest结构保留修复前和修复后清单；refresh失败、写ACK但不可见、watermark失败或remaining差异均由负向测试阻止伪收敛。该G1未连接生产PG/CH/Kafka，也未处理883172条现网差额。
- v185进一步消除跨运行伪收敛：即使OpenSearch目标内容在后续运行开始时已经一致，repair仍按同一有界权威集合批量比较PG watermark的`source_version + source_sha256`，缺失或漂移receipt补写后必须再次查询。固定摘要真实PG G1验证缺失receipt、同毫秒内容SHA漂移、同版本补写和target-version隔离；该运行未连接真实OpenSearch/ClickHouse/Kafka，不能与v184两份单组件证据拼接成G3。
- v186将前两项单组件证据合并为一次真实PG+OpenSearch运行：生产Reconciler修复missing/stale、保留extra、刷新精确alias并同scope回读，再写入和重查PG watermark及durable run manifest。测试随后只删除一个PG receipt，保持OpenSearch内容不变；第二次repair记录0次OpenSearch写调用并补回receipt。权威源仍为有界测试源且未包含真实ClickHouse/Kafka，因此只提升G1强度，不进入G2/G3或关单。
- v187把组合G1的内存权威源替换为生产ClickHouse writer与authoritative repository：2条权威告警经真实CH表与latest投影读取，真实OS预置missing/stale/extra后只修复前两类，PG保存watermark与收敛run manifest。每条权威告警的CH authoritative SHA、OS target SHA、PG receipt SHA和source_version逐项一致；extra作为第3条目标记录保留。Kafka broker offset、committed last event_id和lag未进入该运行，故仍不属于G3。
- v188补齐正常成功路径的真实Kafka边界：生产alert consumer从固定摘要Redpanda消费canonical DetectionBatch，经真实Redis去重后由生产writer写入ClickHouse和OpenSearch；OS成功后PG在Kafka提交前原子写入applied watermark。consumer生成的`updated_at`统一截断到UTC毫秒，消除CH权威像与OS/PG纳秒像在同一source version下的SHA分叉。正式G1中broker committed offset=1、lag=0、post-commit last event_id匹配，CH/OS/PG逐项SHA/version一致，五个无卷loopback容器全部删除。PG回执真实故障、重启/rebalance及重复乱序重放仍开放，不能把该G1解释为G2/G3。
- v189在同一生产consumer五存储路径注入真实临时PG applied receipt表故障：第二个event已成功写入CH/OS，但PG回执失败后consumer停止，broker committed offset保持1、lag为1且post-commit last event仍是首个event。恢复PG并以同一group重启后，broker重投同一个第二event，offset推进到2、lag归零，CH/OS/PG的event、source version和SHA重新一致。该G1只覆盖单consumer同组重启，不替代多副本rebalance、重复/乱序重放、批准环境G2/G3或883172条现网差额回填。
- v190修复v189重投仍会二次增加Redis dedup count并用处理时刻重算source version的问题：生产consumer改用Redis原子`event_id -> fingerprint`回执，精确重投复用原count/first_seen/last_seen；source version绑定canonical header `event_ts`毫秒。正式五存储G1中PG故障前后Redis count均为2，第二event在重启前后的CH/OS SHA与source version不变；同一event_id配不同fingerprint由真实Redis脚本失败关闭且不改变聚合。不同event乱序、多副本rebalance及生产部署仍开放。
- v193在v192 source-time兼容基础上把正式五存储G1扩展为2个partition和2个同组生产consumer。group稳定后每个member各处理一个定向partition事件并在CH/OS/PG回执后commit；关闭一员、确认剩余member接管两个partition后，再向离开者原partition写入并收敛。最终7个不同event、总offset=8、lag=0；单broker副本故障、legacy envelope真实broker与生产部署仍开放。
- v194在候选`2213489eb`加入严格无状态投影shadow与repair review package：shadow只连接CH/OS，强制单租户闭窗口、稳定期、数量上限、cluster/alias/write-index绑定，截断或歧义失败关闭；review package始终`execution_authorized=false`，repair启动时在连接PG前重新核验全部目标身份。v194 G0三阶段通过，随后为可复现G1 runner继续演进，故只保留为中间候选历史证据。
- v195在候选`44bdabeb7`加入固定摘要、无卷、loopback的CH+OS shadow G1。生产CH writer/repository与OS V2 reader在同一范围识别1 missing、1 stale、1 extra；shadow阶段前后两侧count与逐文档SHA一致、无PG依赖、无生产变更，两个容器均删除。该证据不复核或修复883172条历史生产差异，不授权expand/backfill/repair，也不进入G2/G3。
- v196在候选`90129decc`修正审批门禁自锁：G0仍记录源码提交HEAD，但实时漂移判定改为候选源码内容SHA，因此证据/账本提交可以推进HEAD，任何受管源码变化仍会失败关闭。G0三阶段与同哈希shadow G1 v2均PASS；G1 v2重复验证1 missing、1 stale、1 extra、无PG依赖、两侧count/SHA不变和容器清理，边界仍仅为G1。
- v197在候选`392a7c0e5`补齐投影工具打包和执行前强门禁：固定摘要builder/runtime生成scratch双命令镜像；repair在连接存储前验证review/approval文件SHA、15分钟shadow时效、完整实际argv、精确repository digest、四方不同身份审批和非自批。渲染器只生成immutable工件、专用SA、NetworkPolicy及`suspend=true/backoffLimit=0` Job。G0三阶段PASS；同哈希本地镜像G1验证两个静态命令、`65532:65532`和OCI源码标签，但`RepoDigests=[]`，故未形成可发布/可审批镜像证据。
- v198在候选`077aec855`完成投影工具漏洞修复和发布前供应链门禁：Go升级到1.25.12，gRPC升级到1.82.1，OpenTelemetry升级到1.44.0，并同步修复x/net、x/text、compress等依赖；全Go与G0三段PASS。取证脚本从同哈希镜像提取两个静态二进制，生成确定性CycloneDX 1.5 SBOM，同时用govulncheck文本与SARIF交叉判定，防止SARIF退出0掩盖error结果。双二进制可达已知漏洞为0，但漏洞库距采集约11天、每个二进制有1个不可达模块发现待安全裁决，且本地镜像无RepoDigest、cosign签名与SLSA provenance，因此结果明确为HOLD，不授予repair审批资格。
- v199在候选`a17dd7fe1`将clickhouse-go从v2.17.1升级到v2.32.2、ch-go从v0.58.2升级到v0.65.1，消除`GO-2025-3603`不可达模块发现；完整Go测试、G0三段及G0绑定的自有ClickHouse真实reader/生产writer兼容验证通过。两次供应链捕获分别在不同二进制遇到漏洞库下载`unexpected EOF`并按失败关闭，暴露扫描器缺少有界重试，故v199不作为最终候选。
- v200在候选`322b0bf11`为govulncheck异常退出增加最多3次有界重试并记录尝试次数；返回码3代表真实漏洞结果，立即终止而不重试。G0三段、同哈希ClickHouse兼容G1和同哈希供应链G1 v4均完成；两个二进制文本/SARIF均为0可达、0不可达，供应链阻塞收敛为漏洞库超过24小时、缺RepoDigest、缺签名、缺SLSA provenance四项，因此仍为HOLD且不授权生产repair。
- 本次没有渲染可应用清单、解除suspend、连接共享数据库、启用生产worker或写入生产服务。F-ASSET-001—006状态不变；G1新增资产七源有界样例，但Flink、MinIO正式下游、两分片两副本、真实候选部署、G2—G6、Windows Chrome、性能、部分分片故障、灰度回滚和T+观察继续开放。
