# 外部工程标准纠偏:验证状态说明

更新时间:2026-08-17
状态:<code>IMPLEMENTING / PARTIAL</code>(依据 `doc/00_sources/external-engineering-standards/00_Project_Engineering_Constraints/园区网络全流量采集与分析系统工程约束总则.md` §16 完成定义与状态词)
范围:DLQ 回放审批权威迁移 PostgreSQL、ICMP Community ID 跨语言对齐、proto 加法式字段、MLOps 锁与模板同步、部署环境变量同步。

## 1. 已执行并通过的 Gate

| Gate | 范围 | 证据 | 结果 |
|---|---|---|---|
| G01 static | Go 全仓 build;Rust cargo check;Java mvn compile;Web npm build;buf lint;YAML safe_load | 本会话工具输出 | PASS |
| G02 unit | Go 90 包(含新增 sqlmock 审批测试 4 项、审批 HTTP round-trip);Rust cargo test 全过;Java 全模块 mvn test(含 ICMP pin 向量 2 项、session/pcap 契约测试);MLOps pytest 51 | 本会话工具输出 | PASS |

## 2. BLOCKED(缺真实环境/证据,不得宣称通过)

| Gate | 缺什么 | 状态 |
|---|---|---|
| G03 contract | 未跑 buf breaking/round-trip/unknown enum 测试;PG 审批表未在真实 PG 执行 | BLOCKED |
| G04 integration | 审批链路未在真实 PostgreSQL 上执行(仅 sqlmock);Redis/CH 无本次变更集成 | BLOCKED |
| G05 stream | 新 proto 字段未在真实 Kafka/Flink 链路验证 producer→consumer | BLOCKED |
| G06 UI browser | Web 改动未跑 Playwright/正式 Windows Chrome 与 320/断点±1/1440/1920 矩阵 | BLOCKED |
| G07 fault/HA | PG 故障时审批 fail-closed、kill/restart 未演练 | BLOCKED |
| G08 performance | 无目标环境 profile/EXPLAIN(审批表单行查询,静态推断不作数) | BLOCKED |
| G09 release | 未执行 canary/rollback/观察窗;live Secret(OPENSEARCH_USER 等)未注入 | BLOCKED |

## 3. 未满足的约束项(如实登记)

| constraint_id | 偏差 | 处置 |
|---|---|---|
| ENG-FUNC-006 | 函数级合同的 `ast_node_sha256`/`resolver_receipt_ref` 未生成 | 合同卡 DRAFT;resolver 接入后补齐 |
| ENG-SW-004 | Strategy 模式 ADR 缺 debate_receipt_ref(红队答辩未执行) | pattern-proposal DRAFT;答辩排期 |
| ENG-EVOL-001 | proto 字段 producer/consumer 同步上线,未走双读→canary→观察窗 | live 滚动时执行 canary |
| ENG-CMD-002 | 审批同 key 异 hash 的 409 语义仅由唯一约束兜底,未独立测试 | 后续补负例测试 |

## 4. 本轮纠偏落地文件

- `common/sql/pg/24-dlq-replay-approval-v1.sql`(新增)
- `deployments/kubernetes/init-jobs/02-postgres-schema.yaml`(镜像 key 50-m10-*)
- `go/control-plane/internal/ingest/dlq/replay_approval.go`(移除 Redis 权威实现,保留接口+测试内存实现)
- `go/control-plane/internal/ingest/dlq/replay_approval_pg.go`(新增 PG 权威实现)
- `go/control-plane/internal/ingest/dlq/replay_approval_pg_test.go`(新增 sqlmock 测试)
- `go/control-plane/internal/ingest/dlq/{replay_manager,replay_http}.go`、`cmd/ingest-gateway/main.go`(装配 PG)
- `contracts/alignment/code-unit-contract-dlq-replay-approval.v1.json`(DRAFT)
- `contracts/alignment/pattern-proposal-dlq-replay-approval.v1.json`(DRAFT)

## 5. 结论

按总则状态词:本批次只能声明 **G01/G02 通过 + IMPLEMENTING/PARTIAL**;任何"闭环/通过验收"表述在 G03-G09 补齐前均不成立。
