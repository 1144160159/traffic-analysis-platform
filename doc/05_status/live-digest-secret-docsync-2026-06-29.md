# Live digest / Secret / evidence sync status (2026-06-29)

This note records the current repo/live evidence after the 2026-06-29 app, codex-loop, and Kafka digest rollout plus documentation sync. It supersedes older same-day counts that mention 13 or 1 live workload images without digest pins.

## What changed

- Pinned the live Go service, Web UI, probe-agent, and codex-loop queue-service workload specs to `image@sha256` references.
- Added `POSTGRES_DSN` to the Kubernetes Secret provisioning path and to the Secret/ExternalSecret templates so graph-service can use a Secret-backed PostgreSQL DSN.
- Removed an unnecessary `POSTGRES_DSN` env var from ingest-gateway after confirming graph-service is the workload that requires the DSN.
- Added the codex-loop digest to `deployments/kubernetes/image-digests.lock.json` and changed codex-loop deployment/sandbox defaults to `docker.io/traffic-analysis/codex-loop@sha256:8c5b02614836432992780e6a8e7550d73fbffabd13f834150f960f8f374a4ee7`.
- Pinned Kafka repo/live images to the dual-node local CRI manifest digest `docker.io/apache/kafka@sha256:32217f809d3fba75ee46e93b1fdbea4aacc0821139443efb73691a913948c31a`.
- Pinned forensics-service repo/live images to `docker.io/traffic/forensics-service@sha256:7d37a072f6e470b00d7170496b87f671d50469afbddacecf62b337df42fd3c44` after the Forensics PCAP task cancel state-machine fix.
- Pinned rule-manager repo/live images to `docker.io/traffic/rule-manager@sha256:d614aa785ddc742407e083eaaceba06869478ab8fa365121ff31e75019f8b045` after the Rule, Deployment, and Model Version state-machine fixes.
- Pinned auth-service repo/live images to `docker.io/traffic/auth-service@sha256:b5a247f01baf26c4939711bf25c22cb3668718d2ff4f303b8c40cac572c5dc11` after adding asset scope catalog entries.
- Pinned asset-service repo/live images to `docker.io/traffic/asset-service@sha256:0ff5bc4b1084b610e8508e47195dddbc692d8a10ff28daa653508a00af5b95bd` after adding SNMP/LLDP active discovery RBAC and audit-log persistence.
- Pinned alert-service repo/live images to `docker.io/traffic/alert-service@sha256:77a5e9b4f5b8680e3c2b3451915f84ac8e2d07b3d2ea06adfcd28f4ce2086e93` after the probe operations governance API and APISIX route rollout.
- Extended the production security preflight so ExternalSecret/SealedSecret is a live operator-backed reconciliation gate, not only a repo-template presence check.
- Installed External Secrets Operator 2.7.0 through the local Helm chart cache with digest-pinned controller images, added an `external-secrets.io/v1` canary SecretStore/ExternalSecret profile, and verified canary source-to-target Secret reconciliation.
- Added `deployments/kubernetes/security/external-secrets-prod-store.yaml` and `tests/e2e/live_external_secret_production_reconciliation.sh` so the live cluster has a Kubernetes-provider `ClusterSecretStore` backed by `external-secrets-source`, plus a reproducible production ExternalSecret reconciliation gate that records key presence/equality only and never writes secret values to evidence.
- Added `tests/e2e/live_kafka_security_rollout_preflight.sh` and seeded Kafka SCRAM client/broker users on the live plaintext cluster without rolling the StatefulSet.

## Live rollout evidence

- `ingest-gateway`: rolled after env cleanup, `2/2` ready.
- `graph-service`: rolled with Secret-backed `POSTGRES_DSN`, `1/1` ready.
- `codex-loop-queue-service`: rolled to digest image, `1/1` ready on `8-2tb`.
- codex-loop health endpoint returned HTTP 200 with `QUEUE_SERVICE_HEALTHY`.
- `kafka`: partitioned StatefulSet rollout completed across `kafka-2`, `kafka-1`, and `kafka-0`; all three brokers are `1/1` ready on the digest image.
- `alert-service`: rolled to `docker.io/traffic/alert-service@sha256:77a5e9b4f5b8680e3c2b3451915f84ac8e2d07b3d2ea06adfcd28f4ce2086e93` after the probe operations governance fix and APISIX `/api/v1/probes/*` route addition, `1/1` ready.
- `forensics-service`: rolled to `docker.io/traffic/forensics-service@sha256:7d37a072f6e470b00d7170496b87f671d50469afbddacecf62b337df42fd3c44` after the PCAP cancel state-machine and `PCAP_CANCEL` audit fix, `1/1` ready.
- `rule-manager`: rolled to `docker.io/traffic/rule-manager@sha256:d614aa785ddc742407e083eaaceba06869478ab8fa365121ff31e75019f8b045` after the Model Version state-machine fix, `1/1` ready.
- `auth-service`: rolled to `docker.io/traffic/auth-service@sha256:b5a247f01baf26c4939711bf25c22cb3668718d2ff4f303b8c40cac572c5dc11` after adding `asset:read` and `asset:discover` to the scope catalog, `1/1` ready.
- `asset-service`: rolled to `docker.io/traffic/asset-service@sha256:0ff5bc4b1084b610e8508e47195dddbc692d8a10ff28daa653508a00af5b95bd` after the SNMP/LLDP scanner worker, default-off scheduler, RBAC and audit update, `1/1` ready.
- Current live workload image debt is `0` containers.

## Verification evidence

- `python3 tests/e2e/image_digest_lock.py validate --root deployments/kubernetes --lock deployments/kubernetes/image-digests.lock.json`: pass, missing lock lines `0`, mutable tag lines `0`.
- `kubectl apply --dry-run=client -f deployments/kubernetes/applications/go-services.yaml`: pass.
- Kafka post-rollout checks: 3 brokers ready on digest, headless DNS resolves for all brokers, broker API reports 3 brokers, and topic list reports 29 topics.
- `tests/e2e/live_business_flow_api_preflight.sh` with run `20260630-business-flow-api-r26-baseline-governance`: pass, `46/46` checks after storage cleanup, Flink job recovery, PDB selector repair, Playbook/Forensics/Model Version state-machine rollout, compliance/audit page APIs, notification-governance rollout, topic-governance rollout, whitelist-governance rollout, and baseline-governance rollout validation through APISIX.
- `tests/e2e/live_notification_governance_preflight.sh` with run `20260630-notification-governance-preflight-r1`: pass, `26/26` checks, `0` blockers, `0` warnings; validates notification settings update, inline-secret rejection, notification test send, silence rule create/disable, viewer write denial, cross-tenant isolation, PostgreSQL `notification_silence_rules` persistence, and `NOTIFICATION_*` audit-log queryability.
- `tests/e2e/live_topic_governance_preflight.sh` with run `20260630-topic-governance-preflight-r2`: pass, `42/42` checks, `0` blockers, `0` warnings; validates topic tunnel/exfil/APT reads, saved view create/share/favorite, topic scope update, subscription create/disable, report export, evidence package export, viewer write/export denial, cross-tenant isolation, PostgreSQL `topic_*` persistence, and `TOPIC_*` audit-log queryability.
- `tests/e2e/live_whitelist_governance_preflight.sh` with run `20260630-whitelist-governance-preflight-r2`: pass, `26/26` checks, `0` blockers, `0` warnings; validates whitelist draft create, approval submit, approve/activate, expiry extension, disable, match-check behavior, viewer write denial, cross-tenant isolation, PostgreSQL `whitelist` persistence, and `WHITELIST_*` audit-log queryability.
- `tests/e2e/live_baseline_governance_preflight.sh` with run `20260630-baseline-governance-preflight-r2`: pass, `16/16` checks, `0` blockers, `0` warnings; validates baseline list/detail reads, baseline reset, viewer write denial, cross-tenant audit isolation, PostgreSQL `behavior_baseline_resets` persistence, and `BEHAVIOR_BASELINE_RESET` audit-log queryability.
- `tests/e2e/live_settings_governance_preflight.sh` with run `20260630-settings-governance-preflight-r1`: pass, `45/45` checks, `0` blockers, `0` warnings; validates `/settings` frontend action contract, display settings save/read through auth-service, token scope catalogs, API token create/scope update/regenerate/revoke/validate, viewer write denial, cross-tenant isolation, PostgreSQL `user_settings` / `api_tokens` persistence, and `create_token` / `regenerate_token` / `revoke_token` audit-log queryability. Token-bearing create/regenerate responses are kept only in temp files; latest artifacts are redacted.
- `tests/e2e/live_probe_ops_governance_preflight.sh` with run `20260630-probe-ops-governance-r2`: pass, `27/27` checks, `0` blockers, `0` warnings; validates `/probes` frontend action contract, probe list, config push, connectivity test, mTLS SecretRef rotation, batch upgrade, viewer write denial, cross-tenant isolation, PostgreSQL `probe_operations` persistence, probe software-version update, `PROBE_*` audit-log queryability, and no plaintext secret or Bearer token leakage in acceptance artifacts.
- `tests/e2e/live_release_manifest.sh` with run `20260701-release-manifest-r80-ha-review-r1`: pass, `12/12` checks; stable baseline now captures 143 source file hashes, K8s core manifest dry-run 124 objects, 58 workloads, 146 pod image IDs, 29 live Kafka topics, 656 acceptance runs, asset-discovery-rbac r1, probe-ops r2, settings-governance r1, baseline-governance r2, whitelist-governance r2, topic-governance r2, notification-governance r1, compliance-audit r1, production-security r49, deployment r60, production ExternalSecret reconciliation, Kafka security rollout preflight r9, and HA readiness r10.
- `tests/e2e/live_deployment_preflight.sh` with run `20260630-deployment-preflight-r60-fusion-value-report`: pass, `16/16` checks, `0` blockers, `0` warnings; release package count `138`, repo image lock missing count `0`, live unpinned image count `0`, Pending PVC count `0`, and APISIX business entry HTTP 200.
- `tests/e2e/live_playbook_state_machine.sh` with run `20260629-playbook-state-machine-r1`: pass, `21/21` checks, `0` blockers, `0` warnings; validates Playbook catalog/executions, disabled execution guard, manual `max_runs` enforcement, PostgreSQL execution persistence, and `/playbooks` page consumption through APISIX.
- `tests/e2e/live_forensics_task_state_machine.sh` with run `20260629-forensics-task-state-machine-r1`: pass, `7/7` checks, `0` blockers, `0` warnings; validates processing task cancel, completed-task 409, cross-tenant 403, task persistence and queryable `PCAP_CANCEL` audit through APISIX/PostgreSQL.
- `tests/e2e/live_rule_state_machine.sh` with run `20260629-rule-state-machine-r5-model-version-state`: pass, `15/15` checks, `0` blockers, `0` warnings; validates `rule:enable`, cross-tenant/viewer 403, version increment, `rule_versions`, `rule_outbox`, and `RULE_ENABLE` / `RULE_DISABLE` audit evidence.
- `tests/e2e/live_deployment_state_machine.sh` with run `20260629-deployment-state-machine-r3-model-version-state`: pass, `25/25` checks, `0` blockers, `0` warnings; validates gray, activate, pause, resume, rollback, invalid 409, cross-tenant/viewer 403, `deployment_history`, and deployment audit evidence.
- `tests/e2e/live_model_version_state_machine.sh` with run `20260629-model-version-state-machine-r1`: pass, `20/20` checks, `0` blockers, `0` warnings; validates model version registration, activation, previous active deprecation, active deprecation, invalid deprecation 409, cross-tenant/viewer 403, and `MODEL_VERSION_*` audit evidence.
- `tests/e2e/live_asset_discovery_preflight.sh` with run `20260629-asset-discovery-r2-report`: pass, `6/6` checks, `0` blockers, `0` warnings; validates SNMP/LLDP SecretRef credential registration, discovery run completion, queryable run, queryable LLDP topology link, source asset upsert and neighbor asset upsert through real APISIX/asset-service/PostgreSQL.
- `tests/e2e/live_asset_discovery_preflight.sh` with run `20260630-asset-discovery-worker-r2`: pass, `7/7` checks, `0` blockers, `0` warnings; validates the existing SNMP/LLDP control-plane path plus the no-observations scanner worker path, which now creates a failed `asset_discovery_runs` row with an error message instead of silently remaining queued when SNMP runtime credentials or targets are unavailable.
- `tests/e2e/live_asset_discovery_preflight.sh` with run `20260630-asset-discovery-rbac-r1`: pass, `11/11` checks, `0` blockers, `0` warnings; validates `asset:discover` in auth-service scope catalog, viewer write denial with only `asset:read`, SecretRef credential registration, discovery run completion, scanner worker failed-run persistence, `audit_logs` rows for credential/run writes, LLDP topology query, source asset upsert and neighbor asset upsert through real APISIX/auth-service/asset-service/PostgreSQL.
- Local targeted regression after the Model Version fix: `go test ./internal/rules/service ./internal/rules/api ./internal/rules/rbac ./cmd/rule-manager` passed; `npm run test -- --run src/services/pageApiPlans.test.ts` passed with `10/10` tests.
- `tests/chaos/live_ha_readiness_preflight.sh` with run `20260629-ha-readiness-preflight-r5-pdb-selector`: blocked, `12/13` passed, `0` warnings; Flink `9/9` expected jobs are RUNNING, MinIO health is checked cluster-locally, Kafka/Redis PDB selectors now match live Pods, and the only blocker is missing destructive RTO/RPO drill evidence.
- `tests/e2e/live_kafka_security_rollout_preflight.sh` with run `20260629-kafka-security-rollout-preflight-r2-scram-seeded`: blocked, `7/9` passed, `0` warnings; prerequisite blockers are `0` because Kafka Secret/TLS material is present, TLS material parses with live passwords, and SCRAM client/broker users exist. Remaining rollout blockers are live ACL authorizer disabled and live listener still plaintext.
- `tests/e2e/live_external_secret_operator_canary.sh` with run `20260629-external-secret-operator-canary-r2-local-chart`: pass, `11/11` checks; ESO has `3` Ready controller deployments, `3` digest-pinned controller images, CRDs established, and canary SecretStore/ExternalSecret/source-target Secret reconciliation passed without touching production `traffic-credentials` or Kafka TLS Secrets.
- `kubectl apply --dry-run=server -f deployments/kubernetes/security/external-secrets-template.yaml`: pass after upgrading the template to `external-secrets.io/v1`; all 13 production ExternalSecret objects are schema-valid.
- `tests/e2e/live_external_secret_production_reconciliation.sh` with run `20260629-external-secret-production-reconciliation-r1`: pass, `10/10` checks; `ClusterSecretStore/traffic-platform-secret-store` is Ready, `13/13` production ExternalSecrets are live and Ready, all 58 expected keys have source/target presence and equality, and evidence contains only names, types, key lists, and booleans.
- `tests/e2e/live_production_security_preflight.sh` with run `20260630-production-security-preflight-r49-waiver-registry`: blocked, `20/21` passed, `1` blocker, `0` warnings; ESO CRD count is `1`, ready controller pod count is `3`, live ExternalSecret readiness is `14/14`, production ExternalSecrets are `13/13` Ready, live unpinned image count remains `0`, Secret reconciliation blockers are `0`, Kafka TLS/SASL is enabled, Keycloak TLS/SecretRef profile is enabled, and the structured waiver registry leaves `0` unwaived local/dev Kafka plaintext, raw Secret template, privileged-container, or host-namespace findings. The remaining blocker is NetworkPolicy-capable CNI count `0`.
- `tests/e2e/live_ui_contract_preflight.sh` latest run `20260701-ui-contract-preflight-r17-desktop-login-pass-business-redirect-current`: blocked because Codex Desktop Chrome extension backend can open `/login`, but a protected `/alerts` business-page open redirects to `/login`; repo/UI/API contract checks remain `19/19` with no non-browser blockers, and overall result is `20/21` passed.
- `tests/e2e/live_ui_visual_interaction_preflight.sh` added as the stricter real-frontend 1:1 gate. Run `20260702-ui-visual-interaction-preflight-r29-bridge-r3-templates` is `blocked`: `28/28` React page components exist, `30/30` visual target source images exist at `1920x1080`, direct source design-PNG embedding blockers are `0`, and Desktop Chrome captured real `/login` evidence plus authenticated `/screen`, `/dashboard`, and `/alerts` interaction evidence; visual diff evidence is still `0/30`, the `/alerts` screenshot is `2559x1271` rather than `1920x1080`, and business interaction evidence is now `4/28`. r29 requires receiver `capture-meta.json` for every visual target to prove the uploaded Desktop Chrome screenshot was 1920x1080 before storage and was not post-capture resized. A follow-up auth-storage probe proves the short-lived JWT is accepted by `/api/v1/auth/me`; direct localStorage injection remains unavailable, but a short `DESKTOP_SMOKE_TOKEN_ENABLED=true` hash-smoke acceptance window produced the `/alerts` interaction pass and is now kept `true` in repo and live runtime for continued controlled capture. This prevents treating UI contract success, generated PNGs, cropped screenshots, resized screenshots, or historical browser smoke as proof of 1:1 React page implementation.
- `tests/e2e/ui_desktop_capture_plan.mjs` now generates `02-regression/ui-visual-interaction/capture-plan-latest.json` / `.md`, a Desktop Chrome work queue for all 30 visual targets and 28 interaction routes, including dynamic detail URLs, receiver upload endpoints, missing evidence, capture metadata requirements, and metrics commands. This plan drives real capture work only; it is not a substitute for screenshots, capture metadata, diff metrics, or interaction evidence. Since r29, `ui-visual-interaction-matrix-latest.json` also embeds `safeRedirectUrlPattern`, `safeWrapperCall`, and `interactionRequirements` for each missing route.

## Remaining blockers

- Replace or augment Flannel-only networking with an enforcement-capable CNI before claiming NetworkPolicy enforcement.
- Replace the temporary Kubernetes-provider source namespace with the target site SecretStore/rotation flow if the production environment requires an external vault or enterprise rotation policy; the current cluster now has all 13 production ExternalSecrets live and Ready, but this does not by itself prove enterprise vault governance.
- Execute the destructive Kafka/Flink/ClickHouse/PostgreSQL/MinIO HA drills in a maintenance window and produce RTO/RPO plus data-consistency reports; current readiness is still blocked on this evidence, not on PVC/PV drift.
- Continue authenticated Desktop Chrome business-page capture route by route; `/alerts` is now proved via controlled smoke hash, but 26/28 route interactions and all 30 visual diffs still remain before the 1:1 frontend gate can pass.

## 2026-06-30 Kafka SASL_SSL and Flink Recovery Addendum

- Kafka live StatefulSet rollout to `SASL_SSL` / `SCRAM-SHA-512` / TLS / ACL is now complete. `20260630-kafka-sasl-ssl-rollout-r6-controller-mtls-live` passed `11/11` with `0` blockers, and post-rollout `20260630-kafka-security-rollout-preflight-r9-post-sasl-ssl` passed with `0` blockers.
- Superseded rollout failures are documented: r4 found ExternalSecret source truststores with `0` trusted cert entries; r5 found KRaft controller authorization failures from `User:ANONYMOUS`. Both are closed by regenerating source and target PKCS12 truststores, forcing ESO sync, enabling controller mTLS, and adding the broker cert DN super user.
- Flink JobManager was recovered by backing up `flink-traffic-cluster-config-map`, removing stale `jobGraph-*` entries whose `submittedJobGraph*` files were absent, and restarting `flink-jobmanager-0`.
- Flink analysis was resubmitted with complete submit-side environment: Kafka `SASL_SSL`/PKCS12, ClickHouse password, and PostgreSQL password. Current HA readiness r10 reports `9/9` RUNNING jobs with checkpoint and exception checks healthy.
- Kafka-dependent Go services were rolled after the listener switch: `alert-service`, `asset-service`, `ingest-gateway`, `rule-manager`, and `threat-intel-service` all reached desired ready replicas.
- Current verification: production security r49 is `blocked` only by NetworkPolicy-capable CNI count `0`; HA readiness r10 is `blocked` only by missing destructive RTO/RPO drill reports and now rejects renamed bootstrap/review-template artifacts; asset discovery coverage r3 is `blocked` by a review-marker guard after the r1 site inventory review packet; capture performance r4 is `blocked` after the r1 review packet and formal bootstrap/template guard; deployment preflight r60, release manifest r80, asset-discovery-rbac r1, business-flow API r26, compliance-audit r1, notification-governance r1, topic-governance r2, whitelist-governance r2, baseline-governance r2, settings-governance r1, and probe-ops r2 all passed. UI contract r17 remains `blocked` only by protected business-page auth redirect; repo/UI/API contract checks are `19/19` with no non-browser blockers and Desktop Chrome login-page reachability is restored.
- Added `tests/e2e/live_network_policy_enforcement_preflight.sh` as a dedicated NetworkPolicy enforcement gate. Run `20260630-network-policy-enforcement-preflight-r1-flannel-blocked` is `blocked`: repo NetworkPolicy dry-run passed, `20` live NetworkPolicy objects exist, policy-capable CNI pods are `0`, and the default-deny / allow-list probe is skipped to avoid a false pass on Flannel-only networking.
- Added compliance report generation as an audited admin action in `web/ui/src/services/pageApiPlans.ts` and enforced `admin:*` on `POST /api/v1/compliance/reports/generate` in alert-service. `alert-service` rolled to `docker.io/traffic/alert-service@sha256:5302515a84340940a539f458cd7ed648a5f83369131577678003a5449f2cd4c2` after dual-node containerd import, and `20260630-compliance-audit-preflight-r1` passed `19/19`: admin report generation, report query, compliance audit trail, audit-log API, PostgreSQL `compliance_reports`/`audit_logs` persistence, cross-tenant isolation, and viewer write denial all passed.

## 2026-06-30 Notification Governance Addendum

- Added notification governance as an audited admin action set in `web/ui/src/services/pageApiPlans.ts`, connected the `/notifications` snapshot adapter to `/v1/notifications/silence-rules`, and enforced `admin:*` on notification settings, test sends, and silence rule mutations in alert-service.
- Added the PostgreSQL `notification_silence_rules` table and indexes to the alert-service repository schema plus the shared PostgreSQL init SQL/YAML sources, keeping live DB evidence and bootstrap manifests aligned.
- `alert-service` rolled to `docker.io/traffic/alert-service@sha256:d46cffeb90f649eacd07da2b8f34713706d4819c72adbff4dd8c74c55972061c` after dual-node containerd import, and `20260630-notification-governance-preflight-r1` passed `26/26`: settings update, inline-secret rejection, notification test send, silence rule create/disable, viewer write denial, cross-tenant isolation, PostgreSQL persistence, and audit-log queryability all passed.

## 2026-06-30 Topic Governance Addendum

- Added topic governance as an audited action set in `web/ui/src/services/pageApiPlans.ts`; `/topics` now declares saved-view, scope, subscription, report-export, evidence-package-export, share and favorite flows against control-plane APIs.
- Added `/topics` snapshot adaptation for `/v1/topics/views` and `/v1/topics/subscriptions`, so the Topic Workbench evidence rail reflects saved views, shared/favorite state, subscriptions, export readiness and audit coverage instead of only read-only topic payloads.
- Added PostgreSQL `topic_saved_views`, `topic_scope_overrides`, `topic_subscriptions` and `topic_exports` tables to the alert-service repository schema plus shared PostgreSQL init SQL/YAML sources, keeping runtime auto-init and bootstrap manifests aligned.
- `alert-service` rolled to `docker.io/traffic/alert-service@sha256:ea3a74019e53f18c2cadd9c460234eb0fd634fd3babed9279205aff0c09ae919` after dual-node containerd import. The first r1 image replaced `/usr/local/bin/alert-service` while the container entrypoint was `/usr/local/bin/app`; r4 explicitly replaced both paths, and `/proc/1/exe` now matches the new binary checksum.
- `20260630-topic-governance-preflight-r2` passed `42/42`: topic tunnel/exfil/APT reads, saved view create/share/favorite, scope update, subscription create/disable, report export, evidence package export, viewer write/export denial, cross-tenant isolation, PostgreSQL persistence, and audit-log queryability all passed.

## 2026-06-30 Whitelist Governance Addendum

- Added whitelist governance as an audited action set in `web/ui/src/services/pageApiPlans.ts`; `/whitelist` now declares create, submit-approval, expiry-extension and disable flows against `/v1/whitelist` and `/v1/whitelist/{id}`.
- Updated `go/control-plane/internal/alert/whitelist` so tenant scope comes from authenticated context, write actions require `alert:write`/admin scope, `PATCH /whitelist/{id}` supports approval/expiry/status updates, disabled rows stop matching `/v1/whitelist/check`, empty lists return `[]`, and `WHITELIST_*` actions are written directly to `audit_logs`.
- Added PostgreSQL `whitelist` governance columns and indexes to the runtime repository schema plus shared PostgreSQL init SQL/YAML sources, keeping runtime auto-init and bootstrap manifests aligned.
- `alert-service` rolled to `docker.io/traffic/alert-service@sha256:c0bf72ca45d3c22acf2c3586e3ef88c49c79edd507a870b704131d6678d27f22` after dual-node containerd import and digest alias tagging. r1 exposed the empty-list `entries:null` contract drift; r2 fixed it to `entries:[]`.
- `20260630-whitelist-governance-preflight-r2` passed `26/26`: whitelist list shape, draft create, approval submit, approve/activate, active match-check, expiry extension, viewer write denial, cross-tenant 404, disable, disabled match-check false, PostgreSQL persistence, and audit-log queryability all passed.

## 2026-06-30 Baseline Governance Addendum

- Added behavior baseline governance as an audited action in `web/ui/src/services/pageApiPlans.ts`; `/baselines` now declares `/v1/baselines/{id}` evidence detail and `POST /v1/baselines/{id}/reset` as the `alert:write` reset flow.
- Updated `go/control-plane/internal/alert/api` so baseline reset requires `alert:write`/admin scope and write tenant scope comes from the authenticated context before persisting `behavior_baseline_resets` and `BEHAVIOR_BASELINE_RESET` audit rows.
- `alert-service` rolled to `docker.io/traffic/alert-service@sha256:b72cabec23b3cce37332501fe4a90fc05a54020b132b69c9121d2db6fcc6ba4f` after dual-node containerd import and digest alias tagging. `/usr/local/bin/app` and `/usr/local/bin/alert-service` both match checksum `a6a837bfe6223f5d296ce3a411ea1426dfc24cccf2115d737a83823d0e082d01`.
- `20260630-baseline-governance-preflight-r2` passed `16/16`: baseline list/detail reads, reset response, viewer write denial, cross-tenant audit isolation, PostgreSQL `behavior_baseline_resets` persistence, and audit-log queryability all passed.

## 2026-06-30 Settings Governance Addendum

- Added settings governance action contracts in `web/ui/src/services/pageApiPlans.ts`; `/settings` now declares display preferences save, API token create, token scope update, regenerate, revoke, and validate flows against `/v1/auth/settings/{category}` and `/v1/tokens*`.
- Added `tests/e2e/live_settings_governance_preflight.sh` as the system settings live gate. The script prepares real PG users for JWT foreign-key safety, avoids writing plaintext token responses to acceptance artifacts, and copies only redacted token create/regenerate JSON to `doc/02_acceptance/02-regression/`.
- `20260630-settings-governance-preflight-r1` passed `45/45`: frontend settings action contract, token scope catalogs, display settings save/read, invalid category rejection, viewer create-token 403, token create, invalid scope rejection, token scope update, raw token validate, cross-tenant 404, regenerate old-token rejection/new-token validation, revoke rejection, PostgreSQL `user_settings` / `api_tokens` persistence, and `create_token` / `regenerate_token` / `revoke_token` audit-log queryability all passed.

## 2026-06-30 Probe Operations Governance Addendum

- Added probe operations action contracts in `web/ui/src/services/pageApiPlans.ts`; `/probes` now declares batch upgrade, config push, connectivity test and certificate rotation flows against `/v1/probes*` with `probe:write` and `PROBE_*` audit events.
- Added alert-service probe operations APIs and PostgreSQL `probe_operations` persistence, with tenant-scoped writes, `probe:write` enforcement, plaintext certificate/private-key/token/password rejection, and audit rows for config push, connectivity test, certificate rotation and batch upgrade.
- Added APISIX route `44` for `/api/v1/probes/*`, updated shared PostgreSQL init SQL/YAML sources, and pinned repo/live alert-service to `docker.io/traffic/alert-service@sha256:77a5e9b4f5b8680e3c2b3451915f84ac8e2d07b3d2ea06adfcd28f4ce2086e93`. The r1-r3 images exposed the entrypoint mismatch (`/usr/local/bin/app` vs `/usr/local/bin/alert-service`); r4 replaces `/usr/local/bin/app`, and startup logs now include `Probe operations API registered`.
- `20260630-probe-ops-governance-r2` passed `27/27`: probe list, config push, viewer 403, cross-tenant 404, connectivity checks, SecretRef certificate rotation, plaintext material rejection, batch upgrade, PostgreSQL persistence, software version update, `PROBE_*` audit-log queryability, and artifact secret-leak scan all passed.

## 2026-06-30 Asset Discovery Worker Addendum

- Added an SNMP/LLDP scanner worker path to asset-service. `RunActiveDiscovery` now preserves the existing observation-ingest behavior, and when a request supplies `target_cidr` or `credential_id` without observations it invokes the scanner path, loading the registered credential endpoint and using `ASSET_DISCOVERY_SNMP_COMMUNITY` only from runtime Secret/env.
- Added default-off scheduler settings (`ASSET_DISCOVERY_SCHEDULER_ENABLED=false`, mode, interval and max-host limits) to the K8s asset-service workload, so live rollout is inert until a site explicitly supplies target/credential and SNMP community Secret material.
- `asset-service` rolled to `docker.io/traffic/asset-service@sha256:dcf0334ff8d5b793c1e221a278bb77aff9f41ac16dd2c4ca74fadc1a634e1f24` after dual-node containerd import and digest alias tagging. `/usr/local/bin/app` matches checksum `561b2c9613a464ab19693c8b899419743dc48612271ceea0f65580d502d5d61d`.
- `20260630-asset-discovery-worker-r2` passed `7/7`: SecretRef credential registration, observation-based discovery completion, run query, worker-path failed run persistence, LLDP topology query, source asset upsert and neighbor asset upsert all passed through real APISIX/asset-service/PostgreSQL.

## 2026-06-30 Asset Discovery RBAC/Audit Addendum

- Added `asset:read` and `asset:discover` to the auth-service scope catalog and default role mapping. Viewer keeps asset read-only access; operator/admin can run active discovery.
- Added JWT/header scope enforcement on asset-service discovery write APIs. `POST /api/v1/assets/discovery/credentials` and `POST /api/v1/assets/discovery/runs` now require `asset:discover`, `asset:*`, `admin:*` or `*`; tenant and actor for writes are taken from the authenticated token or verified request context.
- Added synchronous `audit_logs` rows for `ASSET_DISCOVERY_CREDENTIAL_REGISTER` and `ASSET_ACTIVE_DISCOVERY_RUN`, keeping Secret values out of audit detail and evidence artifacts.
- `auth-service` rolled to `docker.io/traffic/auth-service@sha256:b5a247f01baf26c4939711bf25c22cb3668718d2ff4f303b8c40cac572c5dc11`; `/usr/local/bin/app` matches checksum `5a00a991f286a5967d36c5d7e27efb3d7d3387ca84fd304aae3c9a3b03cd214b`.
- `asset-service` rolled to `docker.io/traffic/asset-service@sha256:0ff5bc4b1084b610e8508e47195dddbc692d8a10ff28daa653508a00af5b95bd`; `/usr/local/bin/app` matches checksum `7ff339836c4a4e3e313ac29139c59c3fdf38f291ae4f7993ac2d0aed7aa2e7b3`.
- `20260630-asset-discovery-rbac-r1` passed `11/11`: scope catalog, viewer write denial, SecretRef credential registration, observation-based discovery completion, run query, worker-path failed run persistence, credential/run audit rows, LLDP topology query, source asset upsert and neighbor asset upsert all passed through real APISIX/auth-service/asset-service/PostgreSQL.

## 2026-06-30 Asset Frontend Detail Linkage Addendum

- `/assets` Web UI local adapter now consumes `/v1/assets/discovery/runs` and `/v1/assets/discovery/neighbors` secondary payloads and exposes latest run, status, discovered asset/link counts, LLDP/SNMP neighbor count and neighbor summary in the asset detail rail.
- Evidence: `doc/02_acceptance/runs/20260630-asset-frontend-detail-local/local-report.md`.
- Local verification passed: `npm --prefix web/ui run test -- --run src/services/pageSnapshotAdapters.test.ts src/services/pageApiPlans.test.ts src/routes/routeManifest.test.ts` (`49/49` tests), `npm --prefix web/ui run build`, and `node doc/04_assets/ui_suite_gpt_v1/validate_frontend_contracts.mjs` (`181` manifest items, `28` route contracts, `70` overlay contracts, `errors: 0, warnings: 0`).
- This addendum is local frontend evidence only. Desktop Chrome bridge still returns `Transport closed`, so it does not close the browser-facing UI contract blocker.

## 2026-06-30 Asset Discovery Coverage Report Addendum

- Added `tests/e2e/live_asset_discovery_coverage_report.sh` as a read-only SNMP/LLDP asset discovery coverage report gate. It mints an admin JWT from the live Secret, reads APISIX `/api/v1/assets`, `/api/v1/assets/discovery/runs` and `/api/v1/assets/discovery/neighbors`, and cross-checks PostgreSQL `assets`, `asset_discovery_runs` and `asset_topology_links`.
- `20260630-asset-discovery-coverage-r2-review-required-guard` was run with `ALLOW_BLOCKERS=true` and `SITE_ASSET_INVENTORY_JSON=doc/02_acceptance/02-regression/asset-discovery-site-inventory.bootstrap-latest.json`; result is `blocked`, `6/7` checks passed, `1` blocker, `0` warnings. The bootstrap inventory matched 27/27 observed assets with raw coverage 100%, but it carries `review_required=true`, so the formal coverage threshold remains failed until the site owner approves or replaces the inventory.
- Live statistics from the run: 27 total assets, 10 active discovery assets, 10 assets seen in the last 24 hours, 20 VLAN-tagged assets, 6 completed discovery runs, 2 failed runs, 6 discovered assets, 6 discovered links, 5 topology links, 5 source assets with links and 5 neighbor assets with links.
- Stable artifacts were written to `doc/02_acceptance/02-regression/asset-discovery-coverage-latest.json`, `doc/02_acceptance/02-regression/asset-discovery-coverage-latest.md` and `doc/02_acceptance/02-regression/asset-discovery-site-inventory.template.json`; the run-local report is `doc/02_acceptance/runs/20260630-asset-discovery-coverage-r2-review-required-guard/local-report.md`.
- To convert the gate from blocked to a real coverage assertion, provide `SITE_ASSET_INVENTORY_JSON=/path/to/site-assets.json` using the template and rerun with `MIN_DISCOVERY_COVERAGE_PCT=95` or the site-agreed threshold.

## 2026-06-30 Screen Live Snapshot Addendum

- `/screen` now has a dedicated snapshot adapter in `web/ui/src/services/pageSnapshotAdapters.ts`. It consumes `/v1/dashboard/stats`, `/v1/dashboard/encrypted/trend` and `/v1/dashboard/attack-phases`, then maps the live dashboard payload into building coverage, probe online rate, capture throughput, parser success rate, Kafka lag, Flink P95, evidence completeness, high-risk alert count, attack phases and response actions.

## 2026-06-30 Fusion Value Report Addendum

- `alert-service` rolled to `docker.io/traffic/alert-service@sha256:89d2e52463f7bcadf103b8c6274dd5559570fc7a2ff0da7103c1faf9d83e0569` after building `traffic/alert-service:fusion-value-report-20260630-r1`, copying the new binary to both `/usr/local/bin/app` and `/usr/local/bin/alert-service`, and importing the image into both `8-2tb` and `zeus-server` containerd `k8s.io` namespaces.
- The live pod runs the new digest with `/proc/1/exe -> /usr/local/bin/app`; `/usr/local/bin/app` and `/usr/local/bin/alert-service` both have checksum `eb71f94a9e8bacc2a65e80a222c4eb8572ecb435cfa87782fe0203b39a3b67d4`.
- `20260630-fusion-value-report-preflight-r2-live-rollout` passed `19/19` with `0` blockers and `0` warnings. It validates repo route/formula/API-plan/adapter/UI-contract presence, local Go/Vitest/UI suite checks, live JWT Secret access, APISIX `/api/v1/fusion/stats`, `/api/v1/fusion/entities`, and `/api/v1/fusion/value-report?window_hours=168` response shape.
- The live response uses `fusion-value-ablation-v1` and returns single-source baseline, multi-source metrics, lead-time / false-positive / MTTR deltas, quality gates, and evidence. The current live sample still reports blocked quality gates for alert sample size and feedback/MTTR, so real value acceptance still needs a frozen sample window, actual single-vs-multi source ablation data, and pilot or third-party signoff.
- `web/ui/src/pages/SituationalScreen.tsx` now renders those snapshot metrics into the screen coverage cards, full-traffic pipeline strip, PCAP evidence ring, response-action strip, risk legend and runtime evidence instead of using only static local constants plus row count.
- Evidence: `doc/02_acceptance/runs/20260630-screen-live-snapshot-local/local-report.md`.
- Local verification passed: `npm --prefix web/ui run test -- --run src/services/pageSnapshotAdapters.test.ts src/routes/routeManifest.test.ts` (`32/32` tests), `npm --prefix web/ui run build`, and `node doc/04_assets/ui_suite_gpt_v1/validate_frontend_contracts.mjs` (`181` manifest items, `28` route contracts, `70` overlay contracts, `errors: 0, warnings: 0`).
- Desktop Chrome bridge recheck with `desktop_chrome_open_url("http://10.0.5.8:30180/login")` still returned `Transport closed`; this addendum does not close the browser-facing UI contract blocker.

## 2026-06-30 Desktop Bridge Recheck Addendum

- Added `doc/02_acceptance/runs/20260630-desktop-bridge-recheck-r2/local-report.md` for the current Codex Desktop bridge availability recheck.
- `desktop_iab_list_targets`, `desktop_chrome_list_tabs`, `desktop_chrome_open_url(url="http://10.0.5.8:30180/login", keep=true, wait_ms=2500)`, and `js_reset` all returned `Transport closed`.
- This keeps the browser-facing UI smoke blocked at the `codex-desktop-node-repl` transport layer. It does not indicate a repo/UI regression because the Chrome extension backend was not reachable and the login page was not opened.
- Added `doc/02_acceptance/runs/20260630-desktop-bridge-recheck-r3/local-report.md` after tool discovery exposed the `codex-desktop-node-repl` wrapper namespace. The wrapper calls `desktop_chrome_list_tabs`, `desktop_chrome_open_url`, `desktop_iab_list_targets`, and `js_reset` still returned `Transport closed`, narrowing the issue to runtime transport rather than `tool_missing`.
- Added current UI contract recheck `20260701-ui-contract-preflight-r15-desktop-open-url-transport-closed` after tool discovery again exposed the wrapper namespace. The live wrapper calls `desktop_chrome_list_tabs`, `js_reset`, and required `desktop_chrome_open_url(http://10.0.5.8:30180/login, keep=true)` all returned `Transport closed` in this r15 recheck.
- The r13 UI contract preflight reran repo/UI/API checks and produced stable latest evidence: `19/19` non-browser checks passed, no non-browser blocker, and the only two blockers are the Desktop Chrome login page and protected business page browser checks. This confirms the latest browser-facing blocker is current Desktop bridge transport, not a proved Web UI regression.
- Added UI visual/interaction dual gate `20260701-ui-visual-interaction-preflight-r2-topic-states`. It checks `visual-acceptance.json` against `route-page-map`, source image dimensions, React page component presence, forbidden source design image embedding, per-visual-target `actual-1920.png` / `diff-1920.png` / `metrics.json`, and per-route `interaction.json`. The run is intentionally `blocked` until real Desktop Chrome screenshots and diff metrics are captured for all 30 visual targets, plus route-specific business actions for all 28 routes.

## 2026-06-30 Asset Inventory Bootstrap Addendum

- Added `tests/e2e/live_asset_inventory_bootstrap.sh` as a read-only helper that mints an admin JWT from the live Secret, reads APISIX `/api/v1/assets`, and writes a review-required site inventory draft compatible with `SITE_ASSET_INVENTORY_JSON`.
- Run `20260630-asset-inventory-bootstrap-r1` passed and exported 27 observed live assets to `doc/02_acceptance/02-regression/asset-discovery-site-inventory.bootstrap-latest.json`; stable human-readable evidence is `doc/02_acceptance/02-regression/asset-discovery-site-inventory.bootstrap-latest.md`.
- Draft-format validation was run with `20260630-asset-discovery-coverage-bootstrap-r1` using a run-local `STABLE_DIR` so the official `asset-discovery-coverage-latest.json` stayed blocked. The draft matched 27/27 observed assets and proved the bootstrap file is consumable by the coverage script.
- This does not close the formal asset discovery coverage blocker: the bootstrap has `review_required=true` and must be reviewed/corrected by the site owner before it can become the authoritative `SITE_ASSET_INVENTORY_JSON`.

## 2026-07-01 Asset Inventory Review Packet Addendum

- Added `tests/e2e/live_asset_inventory_review_packet.sh` to convert the live observed bootstrap inventory into a review-ready site-owner packet without treating it as formal coverage evidence.
- Run `20260701-asset-inventory-review-r1` passed package generation: 27 assets, `duplicate_key_count=0`, 3/3 checks passed, 0 blockers, and 0 warnings. Stable artifacts are `doc/02_acceptance/02-regression/asset-inventory-review/asset-inventory-review-latest.json` and `.md`; packet files are under `doc/02_acceptance/02-regression/asset-inventory-review/latest/`.
- The packet writes `review-assets.csv`, `formal-site-inventory.template.json`, `review-checklist.md`, and `review-summary.json`. The template carries `TBD` / review markers until the site owner approves, modifies, or excludes each row.
- Strengthened `tests/e2e/live_asset_discovery_coverage_report.sh` so a formal `SITE_ASSET_INVENTORY_JSON` containing `TBD`, `review-template`, `needs_site_owner_review`, or `bootstrap` markers cannot pass even if raw coverage is 100%.
- Run `20260701-asset-discovery-coverage-r3-review-packet-guard` is `blocked`: 6/7 checks passed, 1 blocker, 0 warnings, 27/27 matched, raw threshold passed, but formal `threshold_passed=false` because `inventory_review_marker_detected=true`.

## 2026-06-30 Detection Quality Bootstrap Addendum

- Added `tests/e2e/live_detection_quality_package_bootstrap.sh` as a read-only helper that mints an admin JWT from the live Secret, reads APISIX `/api/v1/alerts` and `/api/v1/fusion/value-report`, and writes a review-required blind-package draft.
- Run `20260630-detection-quality-bootstrap-r1` passed and exported 45 live alert candidate samples. Stable draft artifacts are under `doc/02_acceptance/04-detection-quality/bootstrap/latest/`, with summary `doc/02_acceptance/04-detection-quality/bootstrap/detection-quality-bootstrap-latest.json` and report `doc/02_acceptance/04-detection-quality/bootstrap/detection-quality-bootstrap-latest.md`.
- The bootstrap deliberately creates only `*.bootstrap.*` and `*.review-template.*` files: `sample-index.bootstrap.csv`, `labels/labels.review-template.csv`, `predictions/predictions.review-template.csv`, `threshold-lock.review-template.json`, and `reports/third-party-attestation.review-template.yaml`.
- Formal detection quality preflight was rerun as `20260630-detection-quality-preflight-r4-integrity-active` and remains `blocked`, proving the bootstrap does not fake GATE-P0-06. `mlops/scripts/evaluate_blind_package.py` now scans formal artifacts for bootstrap/review-template markers and rejects unsigned third-party attestation fields; a temporary synthetic package that renamed bootstrap/template files into formal paths was also blocked by this guard. The remaining required formal artifacts are frozen `dataset-manifest.yaml`, numeric `threshold-lock.json`, real `labels.csv`, real `predictions.csv`, and signed `third-party-attestation.yaml`.

## 2026-07-01 Detection Quality Review Packet Addendum

- Added `tests/e2e/live_detection_quality_review_packet.sh` as a non-formal review helper that reads `doc/02_acceptance/04-detection-quality/bootstrap/latest/` and writes a blind evaluation review board under `doc/02_acceptance/04-detection-quality/review/latest/`.
- Run `20260701-detection-quality-review-r1` passed package generation: 45 candidate samples, `duplicate_sample_id_count=0`, 6/6 checks passed, 0 blockers, and 0 warnings. Stable artifacts are `doc/02_acceptance/04-detection-quality/review/detection-quality-review-latest.json` and `.md`.
- The packet creates `sample-review.csv`, `labeling-worklist.csv`, `prediction-worklist.csv`, `formal-package-manifest.template.yaml`, `threshold-lock.template.json`, `third-party-attestation.template.yaml`, `review-checklist.md`, and `review-summary.json`. These are review worklists/templates only and do not write formal artifacts into `mlops/eval_packages/topic1_blind/`.
- Formal detection quality preflight was rerun as `20260701-detection-quality-preflight-r5-review-packet` and remains `blocked`: 5/10 checks passed, 5 blockers, 0 warnings. The blockers are still the absence of real `dataset-manifest.yaml`, numeric `threshold-lock.json`, `labels.csv`, `predictions.csv`, and signed `third-party-attestation.yaml`.
- Release manifest `20260701-release-manifest-r80-ha-review-r1`, signoff readiness `20260701-third-party-signoff-readiness-r20-release-r80-ha-review-r1`, project completion audit `20260701-project-completion-audit-r32-release-r80-ha-review-r1`, and closure board `20260701-completion-blocker-closure-readiness-r24-ha-review-r1` were refreshed after the detection-quality and capture-performance review packets.

## 2026-06-30 Capture Performance Bootstrap Addendum

- Added `tests/perf/100g_capture/live_capture_performance_package_bootstrap.sh` as a read-only helper that gathers the latest live node summary, probe capture profile, repo 500k stress context, templates, schema, and capture-performance preflight result into a review-required hardware-window draft.
- Run `20260630-capture-performance-bootstrap-r1` passed `13/13` and wrote stable draft artifacts under `doc/02_acceptance/03-performance/bootstrap/latest/`, with summary `doc/02_acceptance/03-performance/bootstrap/capture-performance-bootstrap-latest.json` and report `doc/02_acceptance/03-performance/bootstrap/capture-performance-bootstrap-latest.md`.
- The bootstrap deliberately creates only `hardware-inventory.bootstrap.yaml`, `traffic-profile.bootstrap.yaml`, `results/10x100g-summary.review-template.json`, `results/512mpps-summary.review-template.json`, `evidence-manifest.bootstrap.json`, and `operator-runbook.md`.
- Formal capture performance preflight was rerun as `20260701-capture-performance-preflight-r4-review-packet` after the review packet and remains `blocked`: 11/18 checks passed, 4 blockers, 3 warnings. The remaining formal artifacts are real `hardware-inventory.yaml`, `traffic-profile.yaml`, `results/10x100g-summary.json`, and `results/512mpps-summary.json`; the live probe is still `af_packet` with 2 CPU cores, and the 500k stress context is not a 10 x 100Gbps or 512Mpps acceptance result. The preflight scans formal performance artifacts for bootstrap/review-template markers, and a temporary synthetic package that renamed bootstrap/template files into formal paths was blocked by this guard.

## 2026-07-01 Capture Performance Review Packet Addendum

- Added `tests/perf/100g_capture/live_capture_performance_review_packet.sh` as a non-formal review helper that reads `doc/02_acceptance/03-performance/bootstrap/latest/` and writes a hardware-window review board under `doc/02_acceptance/03-performance/review/latest/`.
- Run `20260701-capture-performance-review-r1` passed package generation: 2 performance targets, 7 review files, `formal_artifact_count=0`, 6/6 checks passed, 0 blockers, and 0 warnings. Stable artifacts are `doc/02_acceptance/03-performance/review/capture-performance-review-latest.json` and `.md`.
- The packet creates `hardware-review.csv`, `traffic-profile-review.csv`, `result-summary-worklist.csv`, `formal-artifact-manifest.template.json`, `operator-approval.template.md`, `review-checklist.md`, and `review-summary.json`. These are review worklists/templates only and do not write formal artifacts into `tests/perf/100g_capture/`.
- Formal capture performance preflight was rerun as `20260701-capture-performance-preflight-r4-review-packet` and remains `blocked`: 11/18 checks passed, 4 blockers, 3 warnings. The blockers are still the absence of real `hardware-inventory.yaml`, `traffic-profile.yaml`, `results/10x100g-summary.json`, and `results/512mpps-summary.json`.
- Release manifest `20260701-release-manifest-r80-ha-review-r1`, signoff readiness `20260701-third-party-signoff-readiness-r20-release-r80-ha-review-r1`, project completion audit `20260701-project-completion-audit-r32-release-r80-ha-review-r1`, and closure board `20260701-completion-blocker-closure-readiness-r24-ha-review-r1` were refreshed after this review packet.

## 2026-06-30 Third-party Signoff Readiness Addendum

- Added `tests/e2e/live_third_party_signoff_readiness.sh` as a read-only helper that inventories `doc/02_acceptance/08-third-party` templates, reads `pilot-package-manifest.json` evidence inputs, and writes a review-required signoff readiness package.
- Run `20260630-third-party-signoff-readiness-r10-release-r70-capture-performance-integrity-active` passed package generation with 10/12 checks and 2 warnings. Stable artifacts are under `doc/02_acceptance/08-third-party/readiness/latest/`, with summary `doc/02_acceptance/08-third-party/readiness/third-party-signoff-readiness-latest.json` and report `doc/02_acceptance/08-third-party/readiness/third-party-signoff-readiness-latest.md`.
- The readiness package deliberately creates `readiness-manifest.bootstrap.json`, `evidence-ledger.bootstrap.json`, `placeholder-inventory.bootstrap.csv`, `placeholder-owner-summary.bootstrap.json`, `signoff-checklist.review-template.md`, `exception-register.review-template.csv`, and `claim-boundary.review-template.md`. It records 159 remaining template placeholders and 5 upstream non-pass or blocked evidence inputs that require reviewer decision. Placeholder owner counts are `project_review=84`, `user_signoff=45`, `site_operations=15`, `third_party_lab=8`, `project_team=3`, `performance_lab=2`, and `maintenance_window=2`. The stable summary now also carries `evidence_inputs` and `evidence_input_run_ids`; the latest readiness r20 binds baseline to `20260701-release-manifest-r80-ha-review-r1` and performance to `20260701-capture-performance-preflight-r4-review-packet`.
- This does not close the formal user/third-party signoff gate. `user-acceptance-signoff.md` still contains `TBD`, and no user signature, third-party report, pilot signoff, or economic-benefit confirmation has been provided.

## 2026-06-30 NetworkPolicy Enforcement Readiness Addendum

- Added `tests/e2e/live_network_policy_enforcement_readiness.sh` as a read-only helper that packages the latest formal NetworkPolicy enforcement preflight, CNI capability summary, live NetworkPolicy inventory, and repo `00-network-policies.yaml` into a review-required CNI migration and negative-probe readiness package.
- Run `20260630-network-policy-enforcement-readiness-r1` passed package generation with 7/9 checks, 0 blockers, and 2 warnings. Stable artifacts are under `doc/02_acceptance/05-security/network-policy-readiness/latest/`, with summary `doc/02_acceptance/05-security/network-policy-readiness/network-policy-enforcement-readiness-latest.json` and report `doc/02_acceptance/05-security/network-policy-readiness/network-policy-enforcement-readiness-latest.md`.
- The package creates `cni-migration-readiness.bootstrap.json`, `cni-migration-runbook.md`, `cni-selection.review-template.yaml`, `rollback-checklist.review-template.md`, `enforcement-probe.review-template.md`, `preflight-command.review-template.sh`, and `evidence-manifest.bootstrap.json`.
- This does not close the formal NetworkPolicy enforcement gate. The latest formal preflight is still `20260630-network-policy-enforcement-preflight-r1-flannel-blocked`, with policy-capable CNI pods `0`, Flannel markers `2`, and the default-deny/allow-list negative probe skipped to avoid a false pass on Flannel-only networking.

## 2026-06-30 HA Drill Evidence Bootstrap Addendum

- Added `tests/chaos/live_ha_drill_evidence_bootstrap.sh` as a read-only helper that packages the HA destructive drill plan, latest HA readiness result, operator approval, timeline, snapshot index, RTO/RPO table, data-consistency report, failover report templates, and evidence manifest into a review-required maintenance-window draft.
- Run `20260630-ha-drill-evidence-bootstrap-r1` passed package generation with 7/8 checks, 0 blockers, and 1 warning. Stable artifacts are under `doc/02_acceptance/06-resilience/bootstrap/latest/`, with summary `doc/02_acceptance/06-resilience/bootstrap/ha-drill-evidence-bootstrap-latest.json` and report `doc/02_acceptance/06-resilience/bootstrap/ha-drill-evidence-bootstrap-latest.md`.
- The bootstrap deliberately creates only `*.bootstrap.*` and `*.review-template.*` files. It records `formal_artifact_count=0`, so it does not create root-level `kafka-failover.md`, `flink-failover.md`, `clickhouse-failover.md`, `postgres-failover.md`, `minio-failover.md`, or `ha-rto-rpo-latest.json`.
- Formal HA readiness was rerun as `20260630-ha-readiness-preflight-r8-post-bootstrap` and remains `blocked`: 12/13 checks passed, 1 blocker, 0 warnings. The remaining blocker is still missing destructive Kafka/Flink/ClickHouse/PostgreSQL/MinIO RTO/RPO and data-consistency drill reports.

## 2026-06-30 HA Integrity Addendum

- Added a formal-artifact integrity guard to `tests/chaos/live_ha_readiness_preflight.sh`. The gate now requires all 6 root-level formal artifacts: `kafka-failover.md`, `flink-failover.md`, `clickhouse-failover.md`, `postgres-failover.md`, `minio-failover.md`, and `ha-rto-rpo-latest.json`.
- Run `20260701-ha-readiness-preflight-r10-review-packet` remains `blocked`: 13/14 checks passed, 1 blocker, 0 warnings. Kafka 29 topics, 9 Flink jobs, 13 ClickHouse replicated tables, 2 PostgreSQL streaming replicas, Redis Sentinel, MinIO health, and APISIX are healthy; the remaining blocker is still the absence of the 6 formal maintenance-window reports.
- A synthetic temp package that renamed `bootstrap/latest` review-template files into formal paths was blocked by the integrity guard, proving the review-required HA draft cannot be used as formal GATE-P0-08 evidence.

## 2026-06-30 Completion Blocker Closure Readiness Addendum

- Added `tests/e2e/live_completion_blocker_closure_readiness.sh` as a non-destructive helper that reads the latest project completion audit and turns the current blockers into a review-required closure board.
- Run `20260701-completion-blocker-closure-readiness-r24-ha-review-r1` passed package generation with 13/13 checks, 0 blockers, and 0 warnings. Stable artifacts are under `doc/02_acceptance/09-completion/blocker-closure/latest/`, with summary `doc/02_acceptance/09-completion/blocker-closure/completion-blocker-closure-readiness-latest.json` and report `doc/02_acceptance/09-completion/blocker-closure/completion-blocker-closure-readiness-latest.md`.
- The package creates `closure-ledger.bootstrap.json`, `closure-board.review-template.csv`, `blocker-owner-matrix.review-template.md`, `evidence-readiness-map.json`, `exception-register.review-template.csv`, and `formal-rerun-commands.md`.
- It maps all 8 current completion blockers, 19 ready input packages or evidence links, 8 external or maintenance-window actions, and 9 formal rerun commands. This does not close any formal gate; `project-completion-audit-latest.json` remains `blocked` until the upstream blockers are resolved and the formal project audit is rerun.

## 2026-06-30 Production Security Waiver Registry Addendum

- Added `doc/02_acceptance/05-security/production-security-waivers.yaml` as a reviewable waiver registry for local/dev Kafka plaintext fixtures, the placeholder raw Secret template, and required host-level infrastructure workloads.
- Extended `tests/e2e/live_production_security_preflight.sh` so waiver-backed findings are checked by exact path or workload identity and copied into stable artifacts instead of remaining free-text warnings.
- Run `20260630-production-security-preflight-r49-waiver-registry` is still `blocked`, but now with `20/21` checks passed, `1` blocker, and `0` warnings. The unwaived counters are all `0`: local/dev Kafka plaintext files, repo raw Secret files, privileged containers, and host namespace workloads.
- The only remaining production-security blocker is unchanged: live NetworkPolicy-capable CNI pods are `0` on the current Flannel-only cluster. The waiver registry does not waive this CNI/enforcement blocker.

## 2026-07-01 Desktop Bridge and Evidence Refresh Addendum

- Current Codex Desktop bridge recheck exposed the `codex-desktop-node-repl` wrapper namespace and the Chrome extension backend is reachable again for login-page navigation. `desktop_chrome_open_url(http://10.0.5.8:30180/login, keep=true)` opened the production login page with title `园区网络全流量采集与分析系统`; the historical UI contract r17 browser record still has protected `/alerts` redirecting to `/login`, while UI visual/interaction r29 added a controlled hash-smoke `/alerts` authenticated interaction pass. The frontend remains blocked on full 1920x1080 visual diff evidence and the remaining route interactions.
- UI contract preflight `20260701-ui-contract-preflight-r17-desktop-login-pass-business-redirect-current` remains `blocked` with `20/21` passed and 1 browser blocker. Repo/UI/API non-browser checks remain `19/19` with `0` non-browser blockers.
- UI visual/interaction dual gate `20260702-ui-visual-interaction-preflight-r29-bridge-r3-templates` remains `blocked` with React page components `28/28`, design-PNG embedding blockers `0`, source images `30/30`, Desktop Chrome login-page status `pass`, real `/login`, `/screen`, `/dashboard`, and `/alerts` interaction evidence present, visual diff evidence `0/30`, missing/failed `capture-meta.json` for visual targets, and business interaction evidence `4/28`; this is now the acceptance boundary for “1:1 front-end page replication, not images.”
- Added `tests/e2e/ui_visual_diff_metrics.py` to generate `diff-1920.png` and `metrics.json` from a Desktop Chrome `actual-1920.png` screenshot and the matching source UI PNG. A self-compare smoke against `dashboard.png` returned `pixel_mismatch_ratio=0.0`, proving the metrics schema can be generated without adding frontend dependencies.
- Added `tests/e2e/ui_desktop_capture_receiver.py` as a short-lived Linux-side receiver for Desktop Chrome screenshots. A local dummy smoke proved `/health` returns ok, unauthorized `/token` returns 403, authorized token retrieval works with `X-Codex-Capture-Key`, and PNG/JPEG upload stores `actual-1920.png` plus `capture-meta.json` without logging the token.
- Added Desktop viewport capability evidence at `doc/02_acceptance/02-regression/ui-visual-interaction/desktop-chrome-viewport-probe-latest.json` / `.md`. The current Chrome extension bridge can claim `/login`, evaluate page metrics, inspect DOM, and capture a real JPEG screenshot, but the screenshot remains `2559x1271`, `window.resizeTo` is unavailable, and no viewport/window/bounds/emulation control method is exposed. The formal visual gate therefore still requires a real `1920x1080` Desktop capture path; cropped, resized, or renamed evidence remains invalid.
- Added Desktop authenticated-route evidence at `doc/02_acceptance/02-regression/ui-visual-interaction/desktop-chrome-auth-storage-probe-latest.json` / `.md`. It records that backend auth accepts the short-lived token, but current Desktop Chrome constraints still block reproducible protected business-page interaction evidence without submitting the login form or enabling an approved smoke-token path.
- Release manifest `20260701-release-manifest-r80-ha-review-r1` passed 12/12 and now indexes 656 acceptance runs, 143 source file hashes, 58 workloads, 146 pod image IDs and 29 live Kafka topics.
- Project completion audit `20260702-project-completion-audit-r54-ui-r29-bridge-r3-templates` remains `blocked`: 7 gates pass and 9 gates block. The latest UI evidence now appears as two separate blockers: historical UI contract browser lineage and the `ui_visual_interaction` 30-target diff / 28-route interaction gate.
- Closure board `20260702-completion-blocker-closure-readiness-r38-ui-r29-bridge-r3-templates` remains `pass` as a readiness package and maps the 9 current blockers to owners, 24 ready inputs, 9 external actions, and 14 formal rerun commands.

## 2026-07-01 Third-party Signoff Prefill Addendum

- Prefilled `doc/02_acceptance/08-third-party/user-acceptance-signoff.md`, `pilot-deployment-proof.md`, and `demo-script.md` with current internal evidence statuses where release/deployment/business-flow/UI/security/HA/performance/detection-quality evidence already exists.
- Strengthened `tests/e2e/live_project_completion_audit.sh` so `trial_third_party_signoff` blocks on the full third-party readiness placeholder inventory and upstream non-pass/blocker inventory, not only the presence of `TBD` in `user-acceptance-signoff.md`.
- Run `20260701-third-party-signoff-readiness-r20-release-r80-ha-review-r1` remains a readiness `pass` with 2 warnings: `template_tbd_count=159` and `upstream_blocked_or_nonpass_inputs=5`. It reduces blank fields, classifies placeholder owners, and binds the signoff package to release r80, HA r10, and performance r4, but does not replace user signatures, third-party reports, site inventory approval, performance results, HA drill results, or Desktop browser recovery.

## 2026-07-01 Project Completion Audit Addendum

- Added `tests/e2e/live_project_completion_audit.sh` as a non-destructive project-level gate that reads the latest documented evidence under `doc/02_acceptance` and writes stable completion artifacts.
- Run `20260702-project-completion-audit-r54-ui-r29-bridge-r3-templates` was executed with `ALLOW_BLOCKERS=true` to capture the current blocked state after UI contract r17, UI visual/interaction r29, the detection-quality review packet and r5 formal preflight, asset inventory review packet, asset-coverage review-marker guard, capture-performance review packet and r4 formal preflight, third-party signoff readiness, HA integrity guard, and NetworkPolicy enforcement readiness bootstraps. Stable artifacts are `doc/02_acceptance/09-completion/project-completion-audit-latest.json` and `doc/02_acceptance/09-completion/project-completion-audit-latest.md`; run-local evidence is under `doc/02_acceptance/runs/20260702-project-completion-audit-r54-ui-r29-bridge-r3-templates/`.
- Result is `blocked`: `7` gates pass and `9` gates block. Passing gates are release manifest, deployment preflight, business-flow API, static UI contracts, governance/state-flow evidence, Kafka security, and Fusion value-report structure.
- Blocking gates are Desktop browser smoke lineage (UI contract r17 still records `/alerts` redirect, while r29 visual/interaction now has `/alerts` pass through controlled smoke hash), UI visual/interaction (`0/30` visual diff, missing/failed capture metadata, and `4/28` interaction evidence; `/alerts` screenshot currently `2559x1271`), production security / NetworkPolicy enforcement on Flannel-only CNI, missing destructive HA RTO/RPO drill evidence, missing real 10 x 100Gbps / 512Mpps hardware-window performance package, missing detection-quality third-party package, review-marker asset discovery coverage inventory, and template-only user/third-party signoff. The Desktop browser blocker now points to UI contract run `20260701-ui-contract-preflight-r17-desktop-login-pass-business-redirect-current`; the UI visual/interaction blocker points to `20260702-ui-visual-interaction-preflight-r29-bridge-r3-templates` plus `desktop-chrome-auth-storage-probe-latest.json`. The NetworkPolicy blocker points to readiness run `20260630-network-policy-enforcement-readiness-r1`, which packages the CNI migration and negative-probe workflow but still records policy-capable CNI count `0`. The HA blocker points to readiness run `20260701-ha-readiness-preflight-r10-review-packet`; the HA bootstrap package `20260630-ha-drill-evidence-bootstrap-r1` is available for the maintenance window but has `formal_artifact_count=0`, and r10 refuses renamed bootstrap/review-template artifacts. The capture performance blocker points to `20260701-capture-performance-preflight-r4-review-packet`, which keeps formal GATE-P0-03/04 blocked while refusing bootstrap/review-template renamed artifacts; the ready input package is `20260701-capture-performance-review-r1`. The detection-quality blocker points to `20260701-detection-quality-preflight-r5-review-packet`; the ready input package is `20260701-detection-quality-review-r1`, but formal GATE-P0-06 still needs real manifest, threshold lock, labels, predictions, and signed attestation. The asset coverage blocker points to `20260701-asset-discovery-coverage-r3-review-packet-guard`, which proves 27/27 review-template matches and raw threshold passes but refuses formal coverage while `inventory_review_marker_detected=true`; the ready input package is `20260701-asset-inventory-review-r1`. The signoff blocker points to readiness run `20260701-third-party-signoff-readiness-r20-release-r80-ha-review-r1`, which inventories 159 remaining placeholders/signature markers, owner-classifies them, records 5 upstream non-pass or blocked evidence inputs, and proves the readiness baseline matches current release r80. A deliberate intermediate project audit `20260630-project-completion-audit-r17-stale-signoff-guard` demonstrated the guard blocks a stale readiness package with `stale_release`.
- Follow-up closure board `20260702-completion-blocker-closure-readiness-r38-ui-r29-bridge-r3-templates` now maps these 9 blockers to owner/review templates, 24 ready inputs, 9 external or maintenance-window actions, and 14 rerun commands, but the formal project completion result is unchanged.

## 2026-06-30 Capture Performance Integrity Addendum

- Added a formal-artifact integrity guard to `tests/perf/100g_capture/live_capture_performance_preflight.sh` so `hardware-inventory.yaml`, `traffic-profile.yaml`, `results/10x100g-summary.json`, and `results/512mpps-summary.json` are scanned for bootstrap/review-template markers before GATE-P0-03/04 can pass.
- Run `20260701-capture-performance-preflight-r4-review-packet` remains `blocked`: 11/18 checks passed, 4 blockers, 3 warnings. The blockers are still the absence of real hardware inventory, traffic profile, and two signed result summaries; warnings remain the small 500k stress context and current live probe profile.
- A synthetic temp package that renamed `bootstrap/latest` files into formal paths was blocked by the integrity guard, proving the review-required hardware-window draft cannot be used as formal performance evidence.
- Release manifest `20260701-release-manifest-r80-ha-review-r1`, signoff readiness `20260701-third-party-signoff-readiness-r20-release-r80-ha-review-r1`, project completion audit `20260701-project-completion-audit-r32-release-r80-ha-review-r1`, and closure board `20260701-completion-blocker-closure-readiness-r24-ha-review-r1` were refreshed after the latest performance review packet and HA integrity evidence.

## 2026-07-01 HA Drill Review Packet Addendum

- Added `tests/chaos/live_ha_drill_review_packet.sh` as a read-only helper that converts `doc/02_acceptance/06-resilience/bootstrap/latest/` into a maintenance-window review board under `doc/02_acceptance/06-resilience/review/latest/`.
- Run `20260701-ha-drill-review-r1` passed package generation: 5 HA components, 7 review files, `formal_artifact_count=0`, 6/6 checks passed, 0 blockers, and 0 warnings. Stable artifacts are `doc/02_acceptance/06-resilience/review/ha-drill-review-latest.json` and `.md`.
- The packet creates `component-drill-review.csv`, `rto-rpo-evidence-worklist.csv`, `maintenance-window-approval.template.md`, `formal-artifact-manifest.template.json`, `data-consistency-review-checklist.md`, `operator-review-checklist.md`, and `review-summary.json`.
- Formal HA readiness was rerun as `20260701-ha-readiness-preflight-r10-review-packet` and remains `blocked`: 13/14 checks passed, 1 blocker, 0 warnings. Kafka 29 topics, 9 Flink jobs, 13 ClickHouse replicated tables, 2 PostgreSQL streaming replicas, Redis Sentinel, MinIO health, and APISIX are healthy; the remaining blocker is still the absence of the 6 formal maintenance-window reports.
- Release manifest `20260701-release-manifest-r80-ha-review-r1` passed 12/12 and now indexes 656 acceptance runs, 143 source file hashes, 58 workloads, 146 pod image IDs, and 29 live Kafka topics. Signoff readiness `20260701-third-party-signoff-readiness-r20-release-r80-ha-review-r1` binds baseline r80 and HA r10. Project completion audit `20260702-project-completion-audit-r54-ui-r29-bridge-r3-templates` remains `blocked` with 7 gates passing and 9 gates blocked. Closure board `20260702-completion-blocker-closure-readiness-r38-ui-r29-bridge-r3-templates` passes 14/14 and records `ready_input_count=24`.
