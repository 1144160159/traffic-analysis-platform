# 代码实证状态核对（2026-06-19）

## 1. 核对口径

本文件基于当前工作区源码、部署清单、测试脚本和已生成契约进行静态核对，用于校正 `未开发项梳理-2026-06-19.md` 中的“代码真实状态”。本文件不是验收通过报告；凡未运行专项压测、第三方评测、故障演练或现场安全测试的条目，均按“功能存在但未验收闭环”处理。

判定分为五类：

| 状态 | 含义 |
|---|---|
| 已实装 | 有服务/页面/API/Topic/测试或核心逻辑，且闭环路径在代码中可追踪 |
| 部分实装 | 主体代码存在，但缺关键环节、状态一致性、权限、报告或运维入口 |
| 已实装但未验收闭环 | 功能代码存在，但缺任务书级专项测试、第三方证据或可复现报告 |
| 未生产化 | 能跑或可演示，但生产安全、发布基线、凭据、网络边界不达标 |
| 未开发 | 未发现可运行服务、页面、API、Topic、采集器、脚本或证据包 |

## 2. 总体结论

当前系统不是“主体功能大量未开发”。真实状态是：采集、Kafka/Flink、告警、取证、资产、图谱、MLOps、Web UI 等主链路已具备大量工程实现；主要缺口集中在任务书验收红线、生产安全、权限边界、发布基线、专项演练和增强数据源产品化。

最新校正（2026-06-30）：本轮已将 live 应用、Probe Agent、codex-loop queue-service、Kafka StatefulSet、Playbook 状态机后的 alert-service、Forensics 状态机后的 forensics-service、模型版本状态机后的 rule-manager，以及资产发现 RBAC/audit 后的 auth-service 与 asset-service 工作负载推进到 digest 镜像，并同步 graph-service 所需 Secret-backed `POSTGRES_DSN`。最新实证为：business flow API r26 `46/46` 通过，Playbook state machine r1 `21/21` 通过，Forensics task state machine r1 `7/7` 通过，Rule state machine r5 `15/15` 通过，Deployment state machine r3 `25/25` 通过，Model version state machine r1 `20/20` 通过，SNMP/LLDP asset discovery RBAC/audit r1 `11/11` 通过，asset discovery coverage r1 `blocked` 且 `6/7` 通过、1 blocker、0 warnings，唯一 blocker 是缺 `SITE_ASSET_INVENTORY_JSON` 现场期望资产清单，live 统计为 27 个资产、10 个 active discovery 资产、6 个 completed run 和 5 条拓扑边；`20260630-screen-live-snapshot-local` 证明 `/screen` 已消费真实 dashboard API snapshot 并通过 32/32 frontend tests、Web build 和 UI suite contract 0 error/0 warning；UI contract r9 repo/UI/API contract 19/21 通过但 Desktop Chrome bridge `Transport closed` blocked，Kafka security rollout preflight r9 `9/10` 通过且 `blockers=0`，ExternalSecret operator canary r2 `11/11` 通过，production ExternalSecret reconciliation r1 `10/10` 通过且 `13/13` 生产 ExternalSecrets Ready，release manifest r63 `12/12` 通过，deployment preflight r60 为 `pass` 且 `16/16`、0 warnings，release package 为 138 files；auth-service live digest 为 `docker.io/traffic/auth-service@sha256:b5a247f01baf26c4939711bf25c22cb3668718d2ff4f303b8c40cac572c5dc11`，asset-service live digest 为 `docker.io/traffic/asset-service@sha256:0ff5bc4b1084b610e8508e47195dddbc692d8a10ff28daa653508a00af5b95bd`，rule-manager live digest 为 `docker.io/traffic/rule-manager@sha256:d614aa785ddc742407e083eaaceba06869478ab8fa365121ff31e75019f8b045`。HA readiness r7 仍为 `blocked` 但 Flink 9/9、MinIO health、PVC/PV 漂移和 PDB selector 漂移已恢复，唯一 blocker 是缺破坏性 RTO/RPO 演练；production security preflight r48 仍为 `blocked`，`15/20` 通过、1 blocker、4 warnings；ESO CRD=1、ready controller pods=3、live ExternalSecrets 14/14 Ready、生产 ExternalSecrets 13/13 Ready、Secret reconciliation blocker 为 0；live 未 digest 镜像为 0，Keycloak live profile blocker 为 0；`doc/02_acceptance/08-third-party/` 已新增试点/第三方材料模板包。详见 `live-digest-secret-docsync-2026-06-29.md`。

需要从文档清单修正的重点：

| 条目 | 原判断风险 | 代码实证后的真实状态 |
|---|---|---|
| DLQ/replay | 容易误判为只有 topic | 已有 DLQ Topic、Proto、Go DLQ Producer、Flink DLQ Sink、ingest fallback replay；缺运维重放 API、审批、审计和端到端恢复报告 |
| Threat Intel | 容易误判为仍未服务化 | 告警侧已有内嵌威胁情报富化；2026-06-29 已新增独立 `threat-intel-service`、`threat.intel.v1` Topic/K8s/APISIX/API、JWT/RBAC read-write gates 和 live preflight；同日已把 `/v1/threat-intel/entries` 接入 Fusion 页面契约并完成 live Web rollout contract，Web UI 镜像已从 `traffic/web-ui:fusion-threat-intel-20260629-r1` 推进到 `traffic/web-ui:fusion-write-20260629-r1`；r6 已补 `threat_intel` 租户字段与 tenant-scoped lookup/list/upsert/import、跨租户 lookup/list 负例、过期 token 401、`threat_intel_feeds` 订阅源表、scheduled feed scheduler、upsert/import/scheduled feed 同步 `audit_logs` 落库和 `threat.intel.v1` publish/consume 证据；Fusion 写链路 `20260629-fusion-write-r2` 为 29/29 passed，新增 `POST /v1/fusion/conflicts/{id}/resolve`、`PATCH /v1/fusion/rules/{id}`、viewer 403、`fusion_conflict_resolutions`/`fusion_rule_overrides`/`audit_logs` 行级证据，Alert 服务镜像为 `traffic/alert-service:fusion-write-20260629-r1`，Threat Intel 服务镜像为 `traffic/threat-intel-service:threat-intel-20260629-r6` |
| UI 告警处置 | 容易误判为只读页面 | 前后端已有指派、状态更新、批量操作、反馈、白名单；2026-06-29 已把告警主状态收敛为 `new/triage/assigned/closed` canonical 状态机并兼容 `in_progress/resolved/false_positive` 等历史/展示别名，前端 `alertStatus`、route manifest 与后端 handler 权限/原因门禁已补首轮 live 证据；同日追加单条 `/status` `state_version`/`If-Match` 乐观锁契约并用真实告警 stale version 验证 `409 BIZ_3005` 且状态/版本未变化；同日追加批量 `/api/v1/alerts/batch/status` `items[].state_version` 乐观锁契约并用真实告警 stale version 验证 0 success/2 failed、`BIZ_3005` 且状态/版本未变化；同日追加真实告警 `new -> closed -> reopen` 正向写入/回滚、`audit_logs` 同步落库、`/audit/logs` 对象过滤查询和 `GetByID` 最新版本读取修复；同日追加 FP 反馈生成 `draft` 白名单草案、`whitelist_draft` 响应和告警详情页草案跳转，live 已创建/查询/删除草案并用 Playwright 验证反馈控件；同日补齐完整 Playwright 权限矩阵和合法登录态实时通道自动连接证据 |
| 白名单治理 | 容易把旧 26/26 API 预检或历史 raster 截图当成最终 UI/安全闭环 | 2026-07-19 r351/r352 已补 draft-first 与合法状态对、重复创建、两人审批、乐观锁、停用后带版本删除；FP 反馈白名单在告警查询前校验 `alert:write`，草案与 `WHITELIST_CREATED` 同事务提交，源 SQL/Docker/K8s schema 同步。Alert Service digest `d623d9e...` 的 live 预检为 46/46，真实删除后业务行 0、审计行严格为 6；Web UI digest `082270a...` 经 Xshell/CDP Windows Chrome 150 对生产 `/whitelist` 完成 11/11 视觉状态和创建→提交→独立审批→延期→停用→删除业务交互，0 个业务浏览器错误，像素差异率全部小于 0.125。独立逻辑/布局复审均 P0=0/P1=0，证据入口为 `doc/02_acceptance/02-regression/whitelist-development-progress-latest.json` |
| 合规审计 | 容易把旧 19/19 生成/查询预检、前端样例数或“completed”状态误报为完整合规通过 | 2026-07-19 r367/r368 已改为 9 section fail-closed：缺数据保存 `insufficient_evidence`，历史零证据 pass 报告迁移为 `invalidated` 并从列表、导出、整改、固化路径隔离；新增 dedicated `compliance:read/write/export/remediate/finalize` 与 `audit:read`、真实 ZIP/英文 PDF/中文 DOCX、canonical report hash、整改任务幂等复用、唯一且数据库不可变固化。Alert Service digest `8b364a7...` 的 live APISIX/JWT/PostgreSQL 预检 52/52；Web UI digest `48e92b7...` 经 Xshell/CDP Windows Chrome 150 完成生成→导出→整改→固化及 4/4 页面/浮层视觉门禁，应用错误 0，差异率全部小于 0.125，逻辑/布局复审均 P0=0/P1=0/P2=0。证据入口为 `doc/02_acceptance/02-regression/compliance-development-progress-latest.json` |
| SOAR Playbook | 容易误判为只有目录和执行历史，或把模拟动作误报为真实处置 | 2026-07-19 r346 已升级为 PostgreSQL 租户剧本定义、乐观版本、提交审批、两人审批、启停、仅模拟 drill、rollback 记录、定向 workbench、完整 evidence export、`playbook:read/write/drill/approve/export` 权限和 mode/status/rollback 数据库约束。未知 severity/asset risk 均 fail-closed，未配置真实 provider 时 live execute 固定 501；Windows Chrome 经 Xshell CDP 对生产 `/playbooks` 验证 stale version 409、自审批 409、viewer 403、跨租户 404、三项 simulated action、`external_effect_applied=false`、205/205 边界导出、回滚证据计数 1 与清理，视觉差异率 `0.07687100161233736 < 0.125`。alert-service、auth-service、web-ui 均以 digest 部署，证据见 `doc/02_acceptance/02-regression/playbook-development-progress-latest.json` |
| PCAP 取证 | 容易误判为基础不足 | 下载、预签名、租户隔离、路径防护、审计基础已实现；2026-06-29 已补 `result_sha256`、下载 `X-Content-SHA256`、`/api/v1/pcap/verify`、presign 过期上限和对象存在校验、同步 `audit_logs` 落库，live 证据见 `doc/02_acceptance/runs/20260629-pcap-forensics-integrity/`；同日新增 `PCAP_CANCEL` 审计事件、Forensics 页面 `forensics-cancel-job` action contract 和 `tests/e2e/live_forensics_task_state_machine.sh`，r1 7/7 passed，验证 processing 取消、completed 取消 409、跨租户 403、任务状态持久化和审计可查；forensics-service 已滚动到 `traffic/forensics-service:forensics-state-20260629-r2` digest |
| MLOps | 容易误判为只有脚本 | 训练、评估、SHAP、注册、编排触发已存在；2026-06-29 已补冻结盲测包契约、unknown recall/95% CI 计算器和预检入口，但缺真实冻结样本、预测输出和第三方质量签认 |

## 3. P0 条目真实状态

| 功能/能力 | 代码证据 | 真实状态 | 还缺什么 |
|---|---|---|---|
| 10 x 100Gbps / 512Mpps 采集 | `rust/probe-agent/README.md:7` 声明 100Gbps/512Mpps；`rust/probe-agent/probe-agent/src/main.rs:859-975` 有 capturer 启动、XDP 失败转 AF_PACKET、pps/Mbps 指标；`rust/probe-agent/tests/reports/stress_500k_report.txt:5-23` 只有 50 万包、约 0.94 Mpps、1.3 Gbps 报告；2026-06-29 已新增 `tests/perf/100g_capture/`、`tests/perf/100g_capture/live_capture_performance_preflight.sh` 和 `doc/02_acceptance/03-performance/` 稳定证据 | 已实装性能验收包/预检，但未验收闭环 | 当前 r1 `blocked`：缺真实硬件清单、流量 profile、10x100G 结果和 512Mpps 结果；live 探针 ready 但为 `af_packet`/2 cores 小 profile，不能作为任务书性能通过证据 |
| 端到端 P95 <= 60s | `common/sql/ch/00-all-tables.sql`、`02-latency-chain.sql` 和 K8s CH init job 已补 `event_ts/ingest_ts/kafka_ts/flink_out_ts`；`go/control-plane/internal/common/dataquality/monitor.go` 提供 latency-chain 分段统计；`go/control-plane/internal/alert/api/handler_advanced.go` 暴露 `/api/v1/data-quality/latency-chain`；`tests/e2e/live_latency_chain_report.sh` 合并 ClickHouse/API/UI 证据；`doc/02_acceptance/runs/20260629-latency-chain/live-latency-chain-20260629-latency-chain-r15-3m-final-r8-slot32-summary.json` 为最终证据 | 已验收闭环 | 2026-06-29 r15 真实 K8s/APISIX/ClickHouse/Playwright 3 分钟窗口 `result=pass`、`full_chain_closed=true`、0 gaps、0 command failures；各分段 P95 均小于 60s，后续仅需纳入 release manifest 并继续观察 Feature/Session ratio warn |
| 检测率 95% / 误报率 5% / 未知攻击召回 | `mlops/scripts/train_model.py:451-486`、`evaluate_model.py:183-277` 有准确率、精确率、召回率、F1、AUC、混淆矩阵；`explain_model.py:40-75` 有 SHAP；`mlops_orchestrator.go:45-61,226-338` 有反馈、FP、PSI 重训触发；2026-06-29 已新增 `mlops/eval_packages/topic1_blind/`、`mlops/scripts/evaluate_blind_package.py`、`tests/e2e/live_detection_quality_preflight.sh` 和 `doc/02_acceptance/04-detection-quality/` 稳定证据 | 已实装盲测包契约/预检，但未验收闭环 | 当前 r1 `blocked`：契约文件齐全但缺冻结 `dataset-manifest.yaml`、阈值锁、真实标签、真实预测和第三方签认；因此不能声明 95%/5% 或 CNAS 通过 |
| Kafka TLS/SASL/ACL、ExternalSecret、生产 Secret | `deployments/kubernetes/infrastructure/01-kafka.yaml` 已切到 `SASL_SSL` + `SCRAM-SHA-512` + TLS + ACL，生产清单 Kafka PLAINTEXT 扫描为 0；Kafka broker/init job、Go producers/consumers/DLQ、Flink source/sink、Go/Flink K8s env/mount、`deploy.sh` TLS/SCRAM 种子和 preflight 均已补齐。`deploy.sh secrets` 已在 live 执行，deployment preflight r50 证明 required Secret references 15 个齐备、repo 镜像 lock 缺口 0、repo mutable/latest 0、repo Service 非业务外部端口 0、live workload digest pin 缺口 0、Pending PVC 0；本轮又将 Kafka StatefulSet repo/live 镜像 pin 到 `docker.io/apache/kafka@sha256:32217f809d3fba75ee46e93b1fdbea4aacc0821139443efb73691a913948c31a`，partitioned rollout 后 3 brokers ready、broker API 3 brokers、topic list 29；Kafka PDB selector 已对齐 `app=kafka` live Pod 标签；business flow API r20 46/46 passed；Kafka security rollout preflight r2 已用 `SEED_SCRAM=true` 种入 live SCRAM client/broker 用户，并证明 Secret/TLS material 齐全、TLS 可解析、`prereq_blockers=0`；production ExternalSecret reconciliation r1 已通过 `ClusterSecretStore/traffic-platform-secret-store` 将 13 个生产 ExternalSecrets 同步为 Ready，58 个期望 key 源/目标一致且证据不含 secret value | repo profile、Secret/TLS readiness、SCRAM rollout prerequisites、生产 ExternalSecret reconciliation、repo 镜像 lock、repo Service 暴露面、live workload digest pin 和 Kafka PDB readiness 已闭环，整体仍未生产化闭环 | production security preflight r44 仍为 `blocked`：live Kafka listener 尚未从 plaintext 滚动到 SASL_SSL，live ACL authorizer 仍 disabled，20 个 live NetworkPolicy 对象已创建但 Flannel-only CNI 不执行策略、enforcement-capable CNI 为 0；live workload 镜像未 digest pin 已清零，live 外部 Service 暴露已收敛到仅 APISIX 业务端口，生产 ExternalSecret blocker 和 aggregate Secret reconciliation blocker 已清零。仍需维护窗口 Kafka live TLS/SASL rollout、按现场要求替换/接入企业外部 Vault 或轮转策略、镜像签名/准入、NetworkPolicy enforcement-capable CNI 与负例、secret scan 门禁 |
| NetworkPolicy 与攻击面收敛 | 2026-06-29 已新增 `deployments/kubernetes/security/00-network-policies.yaml`，默认拒绝和 APISIX 业务入口白名单 profile client/server dry-run 通过；repo Service profile 已收敛为仅 `gateway/apisix:http:30180` 外露；live 已创建 20 个 NetworkPolicy 对象，`live_production_security_preflight.sh` r44 报告 live Service 已收敛为仅 APISIX 业务端口，但当前 CNI 为 Flannel-only、enforcement-capable CNI 为 0 | Service 暴露面已闭环，NetworkPolicy enforcement 未闭环 | 仍需维护窗口接入或切换 enforcement-capable CNI 后执行默认拒绝/白名单负例测试，补管理面 VPN/堡垒机入口 |
| 路由权限 Manifest、`/screen` 边界、启动态鉴权 | 2026-06-29 已以新 `web/ui/src/routes/routeManifest.tsx`、`web/ui/src/App.tsx`、`web/ui/src/layouts/AppShell.tsx`、`web/ui/src/services/realtime.ts` 替换旧抽样问题：受保护路由启动拉 `/v1/auth/me`，菜单按 scope 过滤，`/screen` 受保护且支持显式 masked-demo，WebSocket 只在授权 AppShell 中连接；真实 APISIX 权限矩阵 11/11、合法登录态实时通道自动连接和 `/screen` 只读 token 首轮矩阵均通过；同日新增 `tests/e2e/live_ui_contract_preflight.sh`，r5 证明设计菜单 6 组/24 路由、routeManifest、UI suite route-page-map、业务流、代码差距、APISIX `/login` GET、route/access Vitest、Codex Desktop Chrome wrapper 登录页 smoke 和合法登录态 `/dashboard -> /alerts` 业务页点击 21/21 通过，证据含 `desktop-chrome-login-smoke-20260629-r1.json`、`desktop-chrome-business-smoke-r4.json`；同日新增 `tests/e2e/live_business_flow_api_preflight.sh`，修复 graph-service ClickHouse 自动重连/查询超时并滚动 `traffic/graph-service:business-flow-20260629-r1` 后，又在 Nebula Meta/Storage、Redis、Flink、ClickHouse StatefulSet 与 Playbook state rollout 后以 r19 证明业务流 46 个检查经 APISIX 全部通过、`/api/v1/graph/explore` 200；同日新增 `tests/e2e/live_token_lifecycle_matrix.sh`，修复 auth-service API token hash/prefix 契约和同步 `audit_logs`，滚动 `traffic/auth-service:token-lifecycle-20260629-r2`，r2 证明 token create/read/validate、viewer 403、跨租户 404、regenerate、revoke、expiry 和审计 23/23 通过 | 已实装，API 契约、token 生命周期负例、登录页 Desktop Chrome 门禁和受保护业务页 Desktop 登录态点击均已闭环 | 后续扩展为更多业务页视觉巡检，不再作为 P0 blocker |
| 基线冻结与发布证据包 | 2026-06-29 已新增 `tests/e2e/live_release_manifest.sh`，真实采集当前 repo 与 live K8s/API/Kafka 状态；最新 r50 `20260630-release-manifest-r50-prod-externalsecret` 为 12/12 checks passed，稳定副本写入 `doc/02_acceptance/00-baseline/release-manifest-latest.json`，覆盖 commit、dirty status、124 个 source file hashes、123 个 core K8s dry-run objects、58 个 workloads、146 个 pod image IDs、repo/live Topic、模型/规则/部署 API 目录和 461 个历史证据索引，并纳入 production-security r44、production ExternalSecret reconciliation r1、ESO canary 和 Kafka security rollout preflight r2；同日新增并更新 `deployments/kubernetes/site-values.template.yaml`、`deployments/kubernetes/image-digests.lock.json`、`tests/e2e/image_digest_lock.py`、`tests/e2e/k8s_service_exposure.py`、`tests/e2e/live_deployment_preflight.sh` 和 `doc/02_acceptance/07-deployment/`，最新 r50 证明 core K8s dry-run 123 objects、release package 127 files、repo 镜像 lock 缺口 0、repo Service 非业务外部端口 0、2 节点/local-hdd/runtime workload/APISIX 入口均可复核，required Secret references 15 个齐备、live workload spec 镜像 digest pin 缺口 0、Pending PVC 0 | 已实装但未完全验收闭环 | 当前 deployment preflight 已 `pass` 且 16/16、0 warnings。仍需 site values 现场实例、Kafka live TLS/SASL rollout、production security blocker 清零和 release package 签发；当前证据不代表 100G/512Mpps、第三方检测质量或 HA 验收 |
| HA 故障恢复与 RTO/RPO | `deployments/kubernetes/flink/flink-configmap.yaml:34-62` 有 checkpoint；`BehaviorDetectionJob.java:278-321` 配置 EXACTLY_ONCE、unaligned checkpoint、RocksDB；`03-postgresql.yaml:276-342` 有 PG failover CronJob；2026-06-29 已新增 `tests/chaos/live_ha_readiness_preflight.sh`、`tests/chaos/ha_drill_plan.yaml` 和 `doc/02_acceptance/runs/20260629-ha-readiness-preflight/`，r5 证明 Kafka ISR、Flink 9 job checkpoint/异常、ClickHouse 复制、PG 复制、Redis Sentinel、MinIO cluster-local health、APISIX readiness 和关键 PDB voluntary disruption 可读检查通过，并确认旧 Redis Pending PVC/Released PV drift 已清理 | 已实装但未验收闭环 | 当前 readiness preflight 仍 `blocked`：唯一 blocker 是缺 Kafka/Flink/CH/PG/MinIO 维护窗口内破坏性故障注入、恢复时间、数据一致性、RTO/RPO 和演练报告；当前 warnings 为 0 |
| DLQ/replay/幂等恢复 | `common/kafka/create-topics.sh:13-27` 有 `dlq.v1`；`proto/traffic/v1/audit.proto:52-68` 有 DeadLetter；`FeatureJob.java:195-220` 写 DLQ Sink；`DLQSinkFactory.java:29-44` 至少一次投递；`go/control-plane/internal/common/kafka/dlq.go:25-57,160-231` 有 DLQ envelope 与 producer；`internal/ingest/dlq/producer.go:488-698` 有 fallback replay；2026-06-28/29 已新增受保护 `POST /api/v1/dlq/replay/fallback`、`dlq:replay` 权限、审批/审计/Redis 幂等和 Web UI `DataQualityPage` dry-run modal；2026-06-29 修复 replay writer topic 契约、新增 `tests/e2e/live_dlq_replay_recovery.sh`、`tests/e2e/live_dlq_replay_failure_regression.sh`、`tests/e2e/live_kafka_flink_bad_message_dlq.sh`，并新增 `tests/e2e/live_data_quality_dlq_business.sh`；同日 ingest-gateway replay HTTP 增加用户 access JWT 校验并注入 `JWT_SIGNING_KEY` | 已实装并完成核心验收闭环 | API dry-run、重复幂等和 Web UI r2 部署已有证据：`doc/02_acceptance/runs/20260628-dlq-replay-control/`、`doc/02_acceptance/runs/20260629-data-quality-dlq-dryrun/`；合法 JWT 业务页 modal dry-run 4/4 通过并真实命中 replay API：`doc/02_acceptance/runs/20260629-data-quality-dlq-business/`；真实 fallback 文件非 dry-run 重放、Kafka 消费核对和跨 Pod 幂等已有证据：`doc/02_acceptance/runs/20260629-dlq-replay-recovery/`；无效 fallback payload 的 partial 失败样本回归和隔离保留已有证据：`doc/02_acceptance/runs/20260629-dlq-replay-failure-regression/`；live Kafka/Flink 坏消息注入进入 `dlq.v1` 已有证据：`doc/02_acceptance/runs/20260629-kafka-flink-bad-message-dlq/`；Desktop Chrome 合法登录态业务页点击已有 `/alerts` P0 smoke 证据，DLQ modal 的 Desktop 多页巡检可作为后续扩展 |

## 4. P1 条目真实状态

| 功能/能力 | 代码证据 | 真实状态 | 还缺什么 |
|---|---|---|---|
| Threat Intel 独立服务和数据源闭环 | `go/control-plane/cmd/threat-intel-service/main.go` 提供 `/api/v1/threat-intel/lookup|enrich|entries|import|feeds`、JWT/RBAC read-write gates、upsert/import/scheduled feed 同步 `audit_logs` 和 `threat.intel.v1` Kafka event publish；`internal/alert/threatintel/threat_intel.go` 提供 schema、`threat_intel_feeds`、tenant-scoped PostgreSQL lookup/list/upsert/import、feed source repository 和内置 C2/扫描器富化；`common/kafka/create-topics.sh` 与 live Kafka 已有 `threat.intel.v1`；K8s/APISIX 清单已接入 scheduler tick；`tests/e2e/live_threat_intel_service.sh` r6 47/47 passed，已核对跨租户 lookup/list 负例、过期 token 401、PG audit rows、scheduled feed status 和 Kafka consumer event ids/types；`web/ui` Fusion API plan、route manifest、snapshot adapter 和 Workbench 数据源卡已接入 `/v1/threat-intel/entries`；`tests/e2e/live_fusion_threat_intel_contract.sh` `20260629-fusion-write-r2` 29/29 passed，live Web UI 镜像 `traffic/web-ui:fusion-write-20260629-r1` 与 bundle marker 已验证，且真实 APISIX 写入 `POST /v1/fusion/conflicts/{id}/resolve`、`PATCH /v1/fusion/rules/{id}`、viewer 403、PG 行和 `audit_logs` 行均已核对 | 已实装服务化 MVP，Fusion 读链路、写链路、订阅源调度、租户隔离、导入审计和 Kafka publish/consume 已闭环 | 独立服务、Topic、API、表结构、基础 RBAC、跨租户隔离、Fusion 读写链路、scheduled feed、同步审计、Kafka publish/consume 和真实 APISIX/K8s/PG/Web UI 验证已补齐；后续进入 SNMP/LLDP、融合价值量化和试点材料 |
| SNMP/LLDP 主动资产发现 | `go/control-plane/internal/asset/config/discovery.go` 定义 SNMP/LLDP 凭据、发现任务、观测和拓扑边；`repository/discovery.go` 与 `postgres.go` 新增 `asset_discovery_credentials`、`asset_discovery_runs`、`asset_topology_links` 表和查询/写入，并对发现写操作同步写入 `audit_logs`；`service/discovery.go` 拒绝明文凭据、只登记 SecretRef，并把发现观测 upsert 到 `assets`、LLDP 邻居写入拓扑边；`service/scanner.go` 与 `discovery_scheduler.go` 新增默认关闭的 SNMP/LLDP scanner worker 和周期调度路径，SNMP community 只从 runtime Secret/env 读取；`api/http_handler.go` 暴露 `/api/v1/assets/discovery/*` 并强制 `asset:discover`/`asset:*`/`admin:*`/`*`；auth-service scope catalog 已纳入 `asset:read` 与 `asset:discover`；K8s schema、auth-service/asset-service digest、Web UI API plan/route manifest 和 `tests/e2e/live_asset_discovery_preflight.sh` 已同步，`20260630-asset-discovery-rbac-r1` 为 11/11 passed；`web/ui/src/services/pageSnapshotAdapters.ts` 与 `web/ui/src/pages/AssetInventoryPage.tsx` 已把发现任务和 LLDP/SNMP 邻居合入 `/assets` 详情侧栏，`20260630-asset-frontend-detail-local` 本地通过 49 个 Vitest、Web build 和 UI suite contract 0 error/0 warning；`tests/e2e/live_asset_discovery_coverage_report.sh` 已新增只读覆盖率报告门，`20260630-asset-discovery-coverage-r1` 真实读取 APISIX 与 PostgreSQL 后为 blocked：6/7 passed、1 blocker、0 warnings，统计 27 个资产、10 个 active discovery 资产、6 个 completed run、5 条拓扑边 | 已实装控制面、scanner worker、拓扑入库、failed-run 安全落库、RBAC viewer 403、审计可查、前端详情本地联动和覆盖率报告门；现场覆盖率仍 blocked，不得声明全网设备发现率达标 | 继续补真实交换机/园区网扫描窗口、周期任务结果覆盖率、Desktop Chrome 资产页浏览器验证，并提供 `SITE_ASSET_INVENTORY_JSON` 现场期望资产清单后复跑覆盖率门 |
| 多源融合价值量化 | Fusion 页面/API 和 `threat_intel` 数据源类型存在；2026-06-30 已新增 `GET /api/v1/fusion/value-report`、`fusion-value-ablation-v1` 指标公式、`single_source_baseline`/`multi_source`/`delta`/`quality_gates` 响应、`/fusion` snapshot adapter 价值指标映射和 UI suite 契约更新；本地 `go test ./internal/alert/api`、Fusion adapter/route Vitest 和 code-gap 46/46 已通过；同日新增 `tests/e2e/live_fusion_value_report_preflight.sh`，把 repo 契约、本地 Go/Vitest/UI suite、K8s JWT Secret、真实 APISIX `/api/v1/fusion/stats|entities|value-report` 和 value-report 响应结构纳入稳定证据门；alert-service 已滚动到 `docker.io/traffic/alert-service@sha256:89d2e52463f7bcadf103b8c6274dd5559570fc7a2ff0da7103c1faf9d83e0569`，`20260630-fusion-value-report-preflight-r2-live-rollout` 为 `pass`：19/19 passed、0 blockers、0 warnings | 已实装价值量化 MVP，并完成 live APISIX/JWT/K8s 结构门；未完成试点/第三方价值验收 | 仍缺冻结样本窗口、单源/多源真实消融结果、第三方/试点签认和可交付报告模板 |
| 态势感知大屏概念 UI 产品化落地 | `web/ui/src/App.tsx:19,75` 已有 `/screen` 与 `SituationalScreen`；`web/ui/src/components/ScreenLayout.tsx:2`、`web/ui/src/styles/screen.css:2` 有大屏布局；`CampusMap`、`AttackMap`、`WorldAttackMap` 等态势组件存在；参考图已落盘到 `doc/04_assets/generated/situation_screen_concept_chatgpt_20260619.png`；`web/ui/src/services/pageSnapshotAdapters.ts` 已新增 `screen` 专用 adapter，消费 `/v1/dashboard/stats`、`/v1/dashboard/encrypted/trend` 和 `/v1/dashboard/attack-phases`，并把楼宇覆盖、探针在线、采集吞吐、协议解析率、Kafka 积压、Flink P95、证据完整度、高危告警、攻击阶段和闭环动作映射到 `/screen` 页面；`20260630-screen-live-snapshot-local` 本地通过 32/32 Vitest、Web build 和 UI suite contract 0 error/0 warning | 已实装大屏只读边界、脱敏模式、视觉骨架和真实 dashboard API snapshot 一屏联动，但未完成 Desktop browser 视觉验收 | 继续补 4K/2K/1080p 大屏巡检、自动轮播/指标解释、Desktop Chrome 大屏浏览器验证和生产演示窗口证据 |
| UI 告警与 Playbook 处置闭环 | 告警侧已覆盖列表、详情、状态更新、指派、关闭、重开、反馈和乐观锁；Playbook 侧 2026-07-19 r346 已把 `PlaybookAutomationPage` 接到真实 PostgreSQL catalog/workbench/lifecycle API，完成版本化草稿、两人审批、启停、安全 drill、回滚、导出、RBAC、审计、数据库约束、回滚 KPI 真值与按 1/2/3/4 动作数闭合的连接位图，且明确 `max_runs/cooldown` 仅在 provider 接入后约束实行动作 | 告警和 Playbook 核心状态机均已形成数据、权限、审计、浏览器和视觉证据闭环；当前 Playbook 只接受模拟演练，不声称真实封禁或隔离 | 后续真实 provider 接入必须重新开启 live execute，并补供应商回执、超时/补偿、幂等、外部故障和可执行回滚证据；r346 保留为 drill-only 回归基线，完整导出快照/流式化列为非阻断 P2 |
| PCAP 取证闭环 | `go/control-plane/internal/forensics/api/handler.go` 统一 `results/{tenant}/...` key 校验，`/api/v1/pcap/presign` 校验对象存在和 86400 秒过期上限，`/api/v1/pcap/verify` 流式计算 SHA-256，下载返回 `X-Content-SHA256`，同步写 `audit_logs`；`task_repository.go` 持久化 `result_sha256`；`tests/e2e/live_pcap_forensics_integrity.sh` 固化 live 矩阵 | 已验收闭环 | 2026-06-29 live 9/9 通过：jobs hash、presign hash/过期上限、verify、路径遍历 400、跨租户 403、下载 body/header hash 一致、PCAP 审计可查；另有 Desktop Chrome 匿名门禁和合法登录态 `/forensics` Playwright 证据。后续纳入 release manifest 与生产安全总包 |
| 试点材料、售前脚本、经济效益模板 | 2026-06-29 已新增 `doc/02_acceptance/08-third-party/README.md`、`pilot-package-manifest.json`、`pilot-deployment-proof.md`、`demo-script.md`、`pilot-weekly-report-template.md`、`economic-benefit.md`、`user-acceptance-signoff.md` 和 `ipr-index.md`，覆盖部署确认、演示剧本、周报、价值测算、用户签认和成果转化索引 | 已实装模板包，但未真实试点闭环 | 缺真实试点数据、用户盖章确认、第三方检测报告、经济效益签认和可复测演示数据集 |

## 5. 必须修正的实现不一致

| 问题 | 代码位置 | 风险 |
|---|---|---|
| 前端状态值多于后端状态机 | 2026-06-29 已在 `state.ParseStatus` 和 `web/ui/src/services/alertStatus.ts` 同步别名归一；`resolved/in_progress/false_positive` 不再作为写入状态落库，而是映射为 `closed/triage/closed` canonical 值 | 旧风险已降级；后续风险转为真实告警写入、版本冲突和审计证据是否完整 |
| `/screen` 不经登录保护 | 旧抽样问题已修正：新路由把 `/screen` 纳入保护路由，生产 `SCREEN_ACCESS_MODE=protected`；仅显式 `masked-demo` 才脱敏公开；2026-06-29 已补 `screen:view` 后端 scope、只读大屏 token live 矩阵，以及 API token 跨租户/重签发/撤销/过期矩阵 | 后续仅扩展 4K/2K/1080p 大屏巡检和 Desktop 登录态点击 |
| WebSocket 在 App 启动即连接 | 旧抽样问题已修正：`useAuthorizedRealtime` 只在 `ENABLE_REALTIME=true`、存在授权 token 和 currentUser 后连接；2026-06-29 live 页面已验证自动连接 `/ws/events` ready 帧；API token 生命周期矩阵已补 create/regenerate/revoke/expiry | 后续扩展 JWT session 轮换/撤销与高并发实时推送 |
| P95 命名与真实口径不一致 | `handler_dashboard.go:397-413`、`go/control-plane/internal/common/dataquality/monitor.go` | Dashboard 旧字段仍需逐步迁移到 latency-chain 口径；GATE-P0-05 的验收真源已切到 `/api/v1/data-quality/latency-chain` 与 `live_latency_chain_report.sh`，并以 `alert_last_seen_to_created` 避免把去重窗口生命周期误记为处理延迟 |
| DLQ replay 只有 fallback 文件自动重放 | `internal/ingest/dlq/producer.go:488-698`、`go/control-plane/internal/ingest/auth/replay_token_validator.go`、`tests/e2e/live_dlq_replay_recovery.sh`、`tests/e2e/live_dlq_replay_failure_regression.sh`、`tests/e2e/live_kafka_flink_bad_message_dlq.sh`、`tests/e2e/live_data_quality_dlq_business.sh` | 旧风险已降级：人工修复字段、审批、受保护 API、审计、Redis 幂等、真实 fallback 文件非 dry-run 重放、跨 Pod duplicate 验证、partial 失败样本隔离、live Kafka/Flink 坏消息注入进入 `dlq.v1`、合法 JWT 业务页 modal dry-run 和清理/门禁验证已有证据 |
| 生产安全 live Kafka plaintext 和镜像签名债务 | Kafka repo profile 已切到 SASL_SSL/SCRAM/TLS/ACL，生产清单 PLAINTEXT 扫描为 0；live Secret/TLS readiness 已通过 `deploy.sh secrets` 补齐到 15/15 ok；Kafka security rollout preflight r2 已证明 Secret/TLS/SCRAM 前置条件为 0 blockers；production ExternalSecret reconciliation r1 已证明 13/13 生产 ExternalSecrets Ready，aggregate Secret reconciliation blocker 为 0；repo 镜像 evidence lock 缺口、repo mutable/latest、repo Service 非业务外部端口均已降为 0；live workload image specs 已全部 digest pin。2026-06-29 已清理明文 secret/默认口令命中，并将管理、观测、入口、存储、流处理、身份、应用、Probe、codex-loop 与 Kafka broker 等 live workload 推进到 digest；Kafka digest rollout 后 3 brokers ready、topic list 29、business flow API r20 46/46 passed | 不满足现场生产安全和专家评审红线 | 仍需维护窗口推进 Kafka listener 到 SASL_SSL/SCRAM/TLS/ACL 并启用 ACL authorizer、补镜像签名/准入策略、NetworkPolicy enforcement-capable CNI 与负例 |

## 6. 真实开发优先级

### P0：先补任务书验收和生产红线

1. 增加 `routeManifest`，统一前端菜单、路由、权限、验收点和状态机；同步修复告警状态值。
2. 增加生产安全 profile：Kafka TLS/SASL/ACL repo profile、repo Service 暴露面收敛、live Secret/TLS readiness、production ExternalSecret reconciliation 和 live workload digest pin 已完成，继续补 Kafka live rollout、NetworkPolicy enforcement-capable CNI 与负例、镜像签名/准入、live 攻击面、默认口令清零，以及按现场要求接入企业外部 Vault/轮转。
3. 建立验收证据包：P95 <= 60s 已有 2026-06-29 3 分钟 live 闭环报告；10 x 100Gbps/512Mpps 性能验收包和预检已有 r1 blocked 证据；检测质量盲测包契约/预检已有 r1 blocked 证据；HA readiness preflight 已有 r7 证据但缺破坏性 RTO/RPO 演练；deployment preflight/site-values/release package manifest 已推进到 r60 pass 且 0 warnings 证据，production security preflight 已推进到 r48 blocked 证据，ExternalSecret operator canary 已推进到 r2 11/11，production ExternalSecret reconciliation 已推进到 r1 10/10，Kafka security rollout preflight 已推进到 r9 且 blockers 为 0，release manifest 已推进到 r63 12/12，Playbook state machine 已推进到 r1 21/21，Forensics task state machine 已推进到 r1 7/7，Rule state machine 已推进到 r5 15/15，Deployment state machine 已推进到 r3 25/25，Model version state machine 已推进到 r1 20/20，SNMP/LLDP asset discovery RBAC/audit 已推进到 r1 11/11，asset discovery coverage r1 已建立报告门但因缺 `SITE_ASSET_INVENTORY_JSON` 现场期望资产清单 blocked；继续补真实性能硬件窗口结果、真实第三方盲测数据、RTO/RPO、NetworkPolicy enforcement-capable CNI、安全负例和真实设备发现率清单。
4. 把 DLQ/replay 从“fallback 能力”提升为“运维任务闭环”：查询、修复、审批、重放、审计、幂等验证。
5. P95 时间链已推进到完整 live 闭环：后续把 r15 证据纳入 release manifest，并将 Dashboard/SituationalScreen 的旧展示口径逐步迁移到 latency-chain API。

### P1：再补竞争力增强

1. Threat Intel 硬化：服务化 MVP、Fusion 读写链路、订阅源调度、租户隔离、导入审计和 Kafka publish/consume 已有 live Web/PG/Kafka 证据；后续补多源融合价值量化。
2. SNMP/LLDP 主动发现：凭据、发现任务、邻居拓扑、scanner worker、RBAC viewer 403、auth scope catalog 和 `audit_logs` 行级证据已有 live MVP 证据；资产台账前端已本地联动发现任务和 LLDP/SNMP 邻居上下文；覆盖率报告门已能读取 live APISIX/PostgreSQL 并生成稳定证据，但当前因缺 `SITE_ASSET_INVENTORY_JSON` 现场期望资产清单保持 blocked；后续补真实扫描窗口、周期调度结果覆盖率、Desktop Chrome 资产页验证和现场覆盖率验收。
3. 多源融合消融实验：已补 value-report MVP 并跑通 live APISIX/JWT/K8s 结构门；下一步要冻结真实样本窗口，产出单源/多源提前量、误报下降、MTTR 降低和试点签认。
4. 态势感知大屏产品化：以 `situation_screen_concept_chatgpt_20260619.png` 为视觉目标；`/screen` 已补真实 dashboard API snapshot 一屏联动和只读脱敏边界，本地前端/契约验证通过；继续补自动轮播、指标解释、Desktop Chrome 大屏浏览器验证和 1080p/2K/4K 验收。
5. PCAP 法证完整性：2026-06-29 已完成 sha256 必填、下载校验、签名 URL 负例和审计证据包，后续纳入 release manifest 与生产安全总包。
6. 试点/售前材料：模板包已建立；继续补真实试点数据、用户确认、第三方报告和经济效益签认。

## 7. 一句话结论

代码层面已经具备“工程系统雏形”和大量真实业务闭环；当前不能再按旧文档简单说“未开发”。更准确的研发判断是：主链路基本成型，P0 工作应转向验收可复现、生产安全、权限边界、状态一致性和专项证据包，P1 继续补 SNMP/LLDP、融合价值和试点商业化材料。
