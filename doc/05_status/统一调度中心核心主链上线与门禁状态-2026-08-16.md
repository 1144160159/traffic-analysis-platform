# 统一分析任务调度中心核心主链:开发与上线门禁状态

更新时间:2026-08-17 傍晚(测试阶段虚拟网卡回放→实时采集数据集生成 live 闭环;流水线闸门竞态修复)
状态:<code>IMPLEMENTING / PARTIAL</code>(外部工程标准状态词;G05-G09 与 P5 盲评缺环境,如实 BLOCKED,禁止把部分验证写成闭环)

## 0.6 测试阶段虚拟网卡回放→实时采集数据集生成(2026-08-17 傍晚)

**目标**:实时采集缺少恶意流量与细粒度 ground truth 数据集——在探针宿主机创建 veth 对,
输入虚拟网卡端由 Rust 探针回放 pcap 产生真实流量,输出虚拟网卡端由探针实时采集,把冻结
样本变成"真实时间线上的实时采集数据集"。

### 拓扑与组件归属(每环节唯一归属)
- 基础设施(测试阶段一次性):宿主 8-2tb 创建 veth 对 `ta-veth-in ↔ ta-veth-out`(MTU 65535,均 UP,不配 IP)。
- 回放执行(**Rust probe-agent**,回放发生在探针位置):`pcap_replay` 命令携带 `interface` 时切换 **wire 回放执行臂**——取对象→sha256 冻结校验→有界回放(时间线 120× 压缩 + 帧间 100µs 下限兜底)→ 经 AF_PACKET 原始帧注入 ta-veth-in;接口须在探针配置 `capture.wire_replay_interfaces` allowlist 内(生产探针空列表=全部拒绝,fail-closed);wire 模式不喂进程内共享分支(该 run 下游消费共享分支同窗口流量——由输出端探针实时采集流入),回执 watermark 用真实墙钟。
- 实时采集(**probe-agent-veth 测试探针**):独立 DaemonSet(仅宿主 8-2tb),常驻 AF_PACKET 采集 ta-veth-out,M02 生产者只对该探针放行;窗口内流量经共享分支按订阅窗口归属 run。
- 调度链(analysis-service):两个任务定义(def-live-veth-replay / def-live-veth-capture)均走人工列车(MANUAL_CUSTOM 草稿→maker/checker 审批→触发),回放 run 与采集 run 共享窗口 [t0,t0+10min]。
- 分析(共享分支组件):RunScopeRouter 按 tenant+时间窗口派生 run envelope → S2/S3/S4 → S5 终局,与既有主链完全一致。

### 代码/契约变更
- Rust:`capture/wire_inject.rs`(新增,AF_PACKET TX + lo 回环自检测试)、`pcap_replay_op.rs` 抽取 `stage_validated_object` + 新增 `execute_wire_replay_window`(压缩时间线 pacing + 注入失败 fail-closed,4 项测试)、`ReplayWindowCommand.interface`(serde default,canonical hash 与 Go omitempty 对齐)、`CaptureConfig.wire_replay_interfaces`、main.rs `ProbeReplayExecutor` 双执行臂。探针测试 108 绿 + lo 注入自检(注入帧字节级回显一致)。
- Go:`contract/replay_command.go` Interface(omitempty)、`SourceSpecPcap.Interface` + `buildPcapReplayCommand` 贯通、`adapters/pcap_replay.go` 透传 + 接口名校验(`^[A-Za-z0-9._-]{1,15}$`),3+1 项测试。
- 部署:`probe-agent-veth.yaml`(新增)、`probe-agent.yaml` 显式 `wire_replay_interfaces: []`(生产 fail-closed)、analysis-service core-v31→core-v32、probe-agent `wire-replay-20260817v1`。
- 数据集:zeus-server 的 USTC-TFC2016 恶意样本 `Zeus.pcap`(sha256 `d38e6cd7…`,93,141 帧/21,741 五元组流)上传 `analysis-bench/datasets/ustc-tfc2016/malware/Zeus.pcap`。

### live 闭环证据(3 次独立运行,真实数据)
| run | 链 | S1 回执 | 终局 | 三件套 | 摘要 |
|---|---|---|---|---|---|
| b319eea9(采集) | MANUAL_CUSTOM → PROBE_CAPTURE_WINDOW(ta-veth-out) | capture_window_fence,窗口确认 | SUCCEEDED/THREAT_FOUND/COMPLETE/VERIFIED | 1/1/1 | sessions=11624,检测 total=21166/positive=9 |
| 70758751(回放) | MANUAL_CUSTOM → PCAP_REPLAY(interface=ta-veth-in) | source_fence,packets=**93141**,error=0,applied=true | SUCCEEDED/THREAT_FOUND/COMPLETE/VERIFIED | 1/1/1 | sessions=11608,检测 total=21014/positive=9 |
| 9faf4115(回放,第三轮) | 同上(绑定同一 ACTIVE 计划修订) | source_fence,packets=93141,revision=3 | SUCCEEDED/THREAT_FOUND/COMPLETE/VERIFIED | 1/1/1 | 检测 flows=20310/positive=6 |

- 探针 archiver 实测捕获 **93,000/93,141 帧(99.8%)**;veth 层计数器零丢帧/零错误。
- 宿主机 wire 抓包(ta-veth-out,测试观察手段)产出的实时采集数据集 artifact:
  **93,141/93,141 帧字节级一致;21,741/21,741 五元组流一致(交集 100%,零缺失零多余)**;
  已上传 `analysis-bench/datasets/ustc-tfc2016/malware/captured/Zeus.rt-capture-20260817.pcap`(sha256 `c35499de…`,仅时间戳为真实采集时间)。
- 检测规则确定性(RDT-001 洪泛形态/BDT-001 持续高带宽阈值),两轮同窗口回放+采集双 run 结果一致(positive=9)。
- ground truth:Zeus.pcap 五元组流 21,741(192.168.0.x ↔ C2 端点 63.217.115.x/85.233.160.70 等),采集 run 检测流 21,166≈97% 覆盖。

### 根因修复(本轮新增)
| # | 根因 | 修复 | 载体 |
|---|---|---|---|
| 22 | 流水线闸门以"S1 SUCCEEDED"为开闸条件;wire 回放等长 S1 可晚于 window_end 完成,S2-S4 回执先于开闸到达 → 回执被确定性丢弃(attempt PENDING 时 CAS 失败)→ run 永久卡死(run 9faf4115 现场复现) | 开闸条件改为"源阶段已派发(离开 PENDING)";S1 失败由闭包真值表 RequiredNodeFailed 裁决。集成测试 `TestLivePGGatingBatchOpensOnSourceDispatch`(live PG 双场景)通过 | dispatch.go NextGatingBatch + core-v32 |
| 23 | 被丢弃回执已提交 offset,重启不重投 | 消费组 offset 回卷(--reset-offsets --to-datetime)→ 回执经 inbox 去重重放 → run 9faf4115 九阶段全 SUCCEEDED 闭环 | K8s/运维 |

### 已知限制(如实登记)
- 探针命令串行执行(心跳循环逐个 `.await`):长 wire 回放会推迟同探针 capture_window ACK;10 分钟窗口内可收敛,窗口更短时需并发化(待办)。
- 探针测试基线:整机高负载下 `sender::asset_binding::tests::spool_survives_reopen_…` 偶发闪断(约 1/4 全量轮次;sled 重开锁窗口,单测隔离 8/8 绿,与本次变更无关,文件为既往未提交 WIP,待修复登记)。
- 测试探针 archiver 在本次采集画像下未触发旋转/上传(捕获驻留内存缓冲;>64MB 才必旋),持久化数据集 artifact 由宿主机 wire 抓包补齐;生产探针连续采集旋转路径不受影响(待办:核实 spool barrier)。
- AF_PACKET 采集在 10kpps 注入突发下出现 UMEM free-queue 争用告警("frame 0 lost"),流级完整率仍≈97%(5 元组级 100% 经 wire 抓包证实);帧间下限调大可消除(待办)。
- 共享分支按 tenant+时间窗口归属(run 无探针维度过滤):同窗口并发 run 会互相看见流量,实时采集 run 的流集合含生产探针环境流量;ground truth 以数据集网段(192.168.0.0/24)过滤或以专用 veth 探针隔离。

## 0.2.1 本轮(2026-08-17)G1+G2 live 闭环:根因修复全记录

### live 主链证据(真实数据集 USTC-TFC2016 BitTorrent.pcap,sha256 f4c4c59d…)
- **run c7c78365(S1 闭环)**:探针真实回放执行 packets=15000 → ACK(applied=true) → gateway StageReceipt → `analysis.receipts.v1` → ReceiptApplier → **SOURCE_ACTIVATE SUCCEEDED**(回执 input=15000/error=0,provider=probe-agent,fencing 匹配)→ StageGateLoop 批量开闸 **SESSIONIZATION+FEATURE_EXTRACTION 共享同一 fencing token**。
- **run 9ab70a39(fail-closed 语义验证)**:命令 revision 撞探针持久化 revision → 探针 stale_command_revision 拒绝(applied=false)→ 回执 error_count=1 → **SOURCE_ACTIVATE FAILED**(新的 FAILED 语义正确)。
- **run 817f5976(全链闭环 G1+G2+S2 数据面)**:revision 计数器生效 → 回放真实执行(packets=15000,flows=7517 入 flow.events.v1)→ S1 SUCCEEDED → S2 开闸(共享 fence 68c22ccb)→ RunScopeRouter 派生 6149 条 run envelope → RunReceiptJob 聚合 → **SESSIONIZATION SUCCEEDED**(回执 input=6149/output=6149/error=0,fencing_token=68c22ccb 与 attempt 完全匹配)。c7c78365 的 S2 同链补闭环(SUCCEEDED,2759 flows)。
- 终态边界(如实):FEATURE_EXTRACTION 与 S3/S4/S5 阶段回执接线(G3 剩余)与终局循环(G4)未开发,attempt 停留在 RUNNING/PENDING;不宣称超出已实现范围。

### 根因与修复(8 项,全部代码/部署级修复并重新构建镜像)
| # | 根因 | 修复 | 载体 |
|---|---|---|---|
| 1 | gateway 镜像 `replay-receipt-20260817` 为 overlay 构建,继承的 ENTRYPOINT 仍指向旧二进制 `/usr/local/bin/app`(8-04,无 response_revision/ack_scope proto),探针流式上报收到 revision=0 → "unsupported StreamFlows response revision" → 连接循环断开、心跳饥饿 | 重建镜像(ENTRYPOINT 指向新二进制) | `receipt-bridge-20260817v3/v4` |
| 2 | gateway 代码默认 DLQ topic `dlq.ingest-gateway` 不存在且无 ACL(目录契约是 `dlq.v1`),永久错误消息无法入 DLQ → 消费分区卡死、generation consumer 退出 | 配置对齐目录:KAFKA_DLQ_TOPIC=dlq.v1 + config 默认值改 dlq.v1 | go-services.yaml + config.go |
| 3 | probe.control.v2 消息 Kafka key 契约不符:analysis-service 发 operation_id,gateway 校验 tenant:probe(目录 key_contract=tenant_id+probe_id) | 发布器 key 改 `tenant:probe` | probe_command_publisher.go |
| 4 | 探针 ACK 出箱对已释放命令重投 → Exchange Get 失败 → 每次心跳 "probe control exchange unavailable" livelock | Idempotent Receiver:NotFound ACK 幂等接受并 WARN | bridge.go Exchange |
| 5 | 命令 ExpiresAt=5min < 心跳排队窗口(60s 心跳+连接重建),命令排队期间过期被拒 | 延长至 30min | pcap_replay.go |
| 6 | 命令 revision 恒为 1,探针要求严格单调 → 同探针第二个命令必被 stale_command_revision 拒绝 | `analysis_probe_command_revisions` (tenant,probe) 原子递增计数器 | dispatch.go + schema DDL |
| 7 | 未应用 ACK 的 detail 缺 run 身份 → publishStageReceipt 失败 → Exchange 永久失败 | 从冻结命令负载回填 run_id/fencing_token | bridge.go |
| 8 | ReceiptApplier 恒 SUCCEEDED(无视 error_count),失败执行被误标成功并开闸下游 | error_count>0 → FAILED | receipt_applier.go |
| 9 | PG 缺 `kafka_dlq_acknowledgement_receipts` 表(迁移文件在仓库但从未 apply)→ DLQ 屏障 INSERT 失败 → consumer 反复死亡 | live apply 迁移 202608131330 | PG |
| 10 | 17 天前遗留 `ingest-gateway-probe-canary`(旧二进制)在独立消费组消费同一 probe.control.v2,向共享 Redis 写旧 protobuf 格式命令记录 → 新格式 List 解码失败 → exchange 失败 | 两个 canary Deployment 缩容到 0(可逆) | K8s |
| 11 | RunReceiptProcessor 仅事件时间定时器(window_end+grace),回放数据水位永远到不了 window_end → S2 回执饥饿 | 增加处理时间(墙钟)兜底定时器,窗口闭合后必定产出 | RunReceiptProcessor.java(ptfix2 镜像) |
| 12 | 回执 JSON 缺 event_id → ReceiptApplier "missing identity" 隔离 | 确定性 UUIDv5 event_id | RunReceiptProcessor.java(ptfix4) |
| 13 | 窗口已过期时 PT 定时器在首个 envelope 即触发 → flows=1 早产回执,其余 envelope 因 firedState 丢弃 | PT 触发点 = max(window_end+grace, now+grace),积压先聚合 | RunReceiptProcessor.java(ptfix4) |
| 14 | envelope 聚合 fence 取首个值(开闸前 envelope 无 fence)→ 回执 fence 为空被 stale-fence 隔离 | 聚合取最新非空 fence | RunReceiptProcessor.java(ptfix3) |
| 15 | 双 Receipt Job 同组并发(取消失败残留)导致重放/偏移冲突 | 精确取消+offsets 重置(129531)后单作业重建 | K8s/运维 |

### 数据面接线进展(G3 部分)
- flow.events.v1 收到回放流(probe-8-2tb,ts 平移至 run 窗口,7517 条)✓
- RunScopeRouter(live):订阅 ACTIVE(fence 随 rev-2 广播)→ 窗口内流派生 envelope(含 fencing_token)✓(订阅/流两组 lag=0)
- RunReceiptProcessor:envelope 聚合 → SESSIONIZATION StageReceipt(fencing_token 透传)✓
- 待验证/待办:S2 回执落地后的 S3(ENCRYPTED_RECOGNIZER)/S4(RULE_DETECTION 等)阶段 receipt 接线(G3 剩余)+ 终局循环(G4)。

## 0.4 live 全链闭环证据(回放 + 实时采集双数据源)

- **回放 run 7c790fd8**(AUTO_DEFAULT + PCAP_REPLAY):S1-S5 九阶段全 SUCCEEDED → 终局 **SUCCEEDED / THREAT_FOUND / COMPLETE / VERIFIED**;三件套同事务落地(1/1/1;摘要 sessions=19359,检测 total=28561/positive=152)。
- **实时采集 run e95b6545**(MANUAL_CUSTOM 草稿→审批→触发 + PROBE_CAPTURE_WINDOW):S1-S5 九阶段全 SUCCEEDED → 终局 **SUCCEEDED / THREAT_FOUND / COMPLETE / VERIFIED**;三件套落地(摘要 sessions=11702,检测 total=20731/positive=8)。
- 人工选择两种数据源(实时采集/回放)均已闭环;fail-closed 语义经 run 9ab70a39(S1 FAILED)与对账失败路径实证。

### 0.3.1 终局链上的根因修复(续)
| # | 根因 | 修复 |
|---|---|---|
| 16 | 路由器重启后流处理先于订阅广播,rev-1 无 fence 的订阅派生信封 → 回执 fence 为空被 stale-fence 隔离 | **流水线 fence 物化时预分配**(S2/S3/S4 共享一个 token,S5 共享另一个;rev-1 订阅即携带);闸门不再生成新 token;批内兜底共享一个 token |
| 17 | uber jar 对 protobuf 做了 shaded 重定位,补丁类引用未重定位的 `com.google.protobuf.*` → 运行时 NoClassDefFound | catch 具体异常改 `Exception`(类引用 0 处未重定位 protobuf) |
| 18 | 终局候选查询不覆盖 ACCEPTED(调度器状态机未推进)且旧残缺 run(无 S5 节点)卡死循环 | 候选状态加 ACCEPTED/PREPARING;候选须具备 RECONCILE+MACHINE_FINALIZATION 两节点 |
| 19 | 共享流多匹配下跨阶段计数不构成不变量(实时流量落入回放窗口;检测输入=流数≠会话数) | 守恒校验改为如实登记;对账完整性以 fence/回执匹配为准 |
| 20 | gateway SOURCE 回执 payload_hash 为空 → applier 合成哈希 ≠ spec → 身份完整性判 FAILED | gateway 回执 payload_hash=desired_version(spec 哈希) |
| 21 | capture 窗口回执 fence 未标记类型 → 终局误走 zero-input(NO_DATA) | fence kind=capture_window_fence(覆盖确认语义,放行守恒+关闭 zero-input) |

## 0.3 本轮(2026-08-17 下午)G3/G4 续作:终局循环 + 实时采集人工选择

### G4 终局循环(调度权威自身)
- `repository/finalize.go`(新增):`NextFinalizeCandidate`(S1-S4 全终态且 S5 未终态的 run,SKIP LOCKED)+ `LoadRunClosureFacts`(run+attempt+receipt 事实快照)。
- `service/finalize_loop.go`+`finalize_loop_reconcile.go`(新增):S5 权威自产回执(RECONCILE 对账 + MACHINE_FINALIZATION 五轴判定,经与外部回执同一 `ApplyStageReceiptAtomic` 不可变路径)→ 对账(每终态 attempt 恰一条 fence 匹配回执;计数守恒)→ 事实装配 → `EvaluateRunClosure` 真值表 → `FinalizeRunAtomic` 三件套同事务 + 唯一终态。6 项单测(clean/fence 失配/NO_THREAT/THREAT_FOUND/FAILED/ZERO_INPUT)。
- main.go `FINALIZE_ENABLED` 门控循环(3s)。
- **流水线闸门修正**:S2/S3/S4 为 `PIPELINED_STREAM` 共享流阶段——S1 SUCCEEDED 后一次性批量开闸,共享一个 run 级 fencing token(原按 phase 逐级开闸与流水线激活语义不符,且下游回执无法取得本阶段 token)。live 证据:run f6857d92 六个阶段同 token `651d7f92` 一次开闸。

### G3 数据面回执接线(流水线阶段)
- 路由器信封富化:`RunRouterProcessFunction.envelopeJson` 携带完整流特征(tuple/pps/bps/tcp flags/tos/pktlen/iat/active/idle stats + 探针 DPI `feature_observation`:transport_security/tls_version/ja3/ja4/sni/entropy/missing_fields)。修复 envelope 重复字段 bug;修复 uber jar shaded-protobuf 引起的运行时 NoClassDefFound(catch 具体异常→Exception,类引用 0 处 unshaded protobuf)。
- flink-feature-job:`RunEnvelopeRecord` 扩展 + 新增 `RunFeatureReceiptProcessor`(S2 FEATURE_EXTRACTION:18 项流级派生特征 + feature_hash + 诚实 modality_missingness_mask)→ RunReceiptJob union 双回执。
- flink-behavior-job:新增 `receipt/RunBehaviorReceiptJob`+`RunBehaviorReceiptProcessor`(S3/S4):每流 识别→检测→聚合 纯函数管线(识别器用真实 DPI 指纹;检测器 RDT-001 洪泛形态/BDT-001 持续高带宽真实阈值)→ ENCRYPTED_RECOGNIZER/RULE_DETECTION/BEHAVIOR_DETECTION/DETECTION_AGGREGATE 四条回执。ACL:flink-behavior-job 增 envelopes READ(run-behavior-receipt-v1)+ receipts WRITE,目录同步。

### 人工选择实时采集(PROBE_CAPTURE_WINDOW)
- Go:`contract/source.go` 采集字段(interface/bpf/spool/lease)、`SourceSpecCaptureWindow` + `buildCaptureWindowCommand`(PROBE_CAPTURE_WINDOW|LIVE_STREAM_WINDOW)、`adapters/capture_window.go`(协议转换:冻结校验+canonical hash+capture_window 命令发布)、派发器执行器注册表(SetExecutor OCP)。4+3 项单测。
- Rust:探针 `BuiltinProbeExecutor` 增 `capture_window` 执行臂(窗口覆盖确认:validate+interface 匹配+窗口未过期→applied=true;fail-closed)+ `with_capture_interface`。探针全量测试 103+ 绿。
- live 证据(run 94c0a83c):人工列车草稿(custom_overrides source_kind=PROBE_CAPTURE_WINDOW)→ maker/checker 审批 ACTIVE → MANUAL_CUSTOM 触发(编译哈希与冻结哈希一致)→ 探针窗口确认 → **S1 SUCCEEDED**;探针真实流量捕获(1687 pps/6.0 Mbps/14195 流事件)入共享分支。
- 环境:探针 producer 打开(M02_CAPTURE_PRODUCER_V1_ENABLED=true + CANARY probe-8-2tb 精确放行)。

## 0. 本轮(2026-08-16 晚间)新增落地

### 0.1 网关链路修复(全平台 API 恢复)
- **根因 1(全局 404)**:`deployments/kubernetes/configmaps/apisix-routes.yaml` 在既往 audit 提交中丢失了 APISIX standalone 模式必需的 `#END` 尾标记,导致整个 route 表加载失败(`read_apisix_yaml(): missing valid end flag`),全平台 API 经网关一律 404。
- **根因 2(route id 冲突)**:`- id: 29`/`- id: 30` 曾同时用于 analysis/tokens/topics 路由,已改为 analysis=59、tokens=29、topics=30(全表 59 条路由 id 唯一,已脚本校验)。
- **根因 3(带 token 仍 401)**:openid-connect 插件 `ssl_verify: true` 拉取 Keycloak discovery 失败(`18: self-signed certificate`)。修复:创建 `gateway/traffic-platform-ca` Secret(Keycloak 自签证书为信任锚)、`config.yaml` 增加 `apisix.ssl.ssl_trusted_certificate`、`08-gateway.yaml` 挂载 CA 卷与 `SSL_CERT_FILE`/`APISIX_OIDC_CLIENT_SECRET` 环境。
- **根因 4(Secret 漂移)**:`gateway/traffic-credentials` 由 ExternalSecret 托管且仅映射 `APISIX_ADMIN_KEY`;已扩展 live ExternalSecret 与 `site-values.template.yaml`,增加 `OIDC_CLIENT_SECRET` 映射。
- **安全对齐**:`/api/v1/analysis*` 路由补齐与其他 `/api/v1/*` 同级的 request-id + limit-req + openid-connect(bearer_only)+ client-control + request-validation 插件栈(此前仅 request-id)。

### 0.2 analysis-service 触发主链接通(镜像 core-v1 → core-v6)
- 新增 `adapters/plan_catalog.go`:`PGPlanTemplateProvider.LoadTemplate` + 纯函数 `BuildTemplateFromPlanRow`(激活计划行 → DefaultTemplate + CatalogSnapshot;冻结字段损坏 fail-closed,不猜测)。
- `service/trigger.go`:
  - `SetTemplateLoader` 装配侧钩子(AUTO_DEFAULT 以模板为全部输入,MANUAL_CUSTOM 以模板为覆盖基座);
  - 触发实例先插入(`PENDING_MATERIALIZATION`,判别联合冲突复用既有 id),修复物化事务 `FOR UPDATE` 锁空行 500;
  - 计划修订绑定:AUTO_DEFAULT 绑定激活默认计划修订;**MANUAL_CUSTOM 在人工定制计划持久化列车(P2)接入前占位 0,物化期 def+revision+双哈希 校验 409 拒绝,不静默挂到默认修订**;
  - 幂等重放:同 identity 同 hash 回源已物化 task/run,返回同一句柄(不再 500)。
- `contract/topics.go`:`ParseError`(契约码前缀错误解析)+ `AllErrorCodes` + `ErrCodeNotFound`;`api/handler.go` `writeServiceError` 统一把仓储层契约码前缀错误映射为稳定 HTTP 语义码(修复 MANUAL_CUSTOM 绑定失败误报 500)。
- 测试:新增 `adapters/plan_catalog_test.go`(4 例)、`service/trigger_test.go`(2 例)、`contract/topics_test.go`(6 断言);`live_pg_integration_test.go` 清理语句修复(ledger 清理不再带多余参数)。

### 0.3 镜像与部署
- 镜像 `docker.io/traffic/analysis-service:core-v8`(CGO_ENABLED=0 静态二进制 + alpine 叠加),双节点(8-2tb 本机 + zeus-server)containerd 导入,`applications/analysis-service.yaml` 更新 tag,`imagePullPolicy: Never`。

### 0.4 P2 人工选择列车 live 闭环(core-v7/v8)
- 新增 `service/plan_author.go`:`PlanAuthorService.SaveCustom`(preflight→CustomPlanResolver→Compile→SavePlanDraftAtomic,修订号事务内自动分配)+ `Approve`(maker/checker,同人审批拒绝,已激活幂等)。
- 仓储增强:`SavePlanDraftAtomic` 修订自动分配(定义级 advisory lock + MAX+1)、`GetPlanGovernanceHead`、`FindPlanByExecutionSpec`(草稿幂等回源)、`GetActivePlanForDefinitionBySource`(计划来源隔离)、`GetPlanByDefinitionAndRevision`(调度触发绑定冻结修订,不随激活头漂移)、`ApproveOrActivatePlanAtomic` 补 approvals.id 并同步定义 active_plan_revision。
- HTTP:`POST /api/v1/analysis/tasks/{id}/plans`(201 草稿)、`POST /api/v1/analysis/plans/{id}/approve`(200 ACTIVE)。
- 触发绑定:MANUAL_CUSTOM 绑定 ACTIVE 定制修订且编译哈希必须等于冻结哈希(异覆盖 409),AUTO_DEFAULT 始终绑定默认修订——两链互不串扰。
- 测试:sqlmock `plan_author_test.go` 6 例、handler 路由守卫测试、live PG 集成 `TestLivePGCustomPlanSaveApproveTriggerChain`(草稿修订=2→幂等回源→审批 ACTIVE→定制触发绑定修订 2→异覆盖 409)。
- **live 网关证据**:草稿 201(plan_id+rev2+双哈希)→ approve 200 ACTIVE → MANUAL_CUSTOM 触发 202 且 DB 绑定 plan_revision=2 → 异覆盖 409 → 同键重放同句柄;AUTO_DEFAULT 触发在定制 ACTIVE 并存时仍绑定修订 1(来源隔离回归通过)。
- Web 向导(FP-108):`analysisApi.ts` 新增 saveAnalysisCustomPlan/approveAnalysisPlan;plan_source 契约改为 `AUTO_DEFAULT`/`MANUAL_CUSTOM`(原 'default'/'custom' 与后端键不一致,属真实缺陷);custom 分支改为 保存草稿→maker/checker 审批→触发 三步链;Web 345/345 测试+build 通过;镜像 `web-ui:p2-manual-train-f6072c454` 已双节点上线(新 bundle 经网关 200 生效)。
- 合同治理:`kafka-topic-catalog.v1.json` 五个 analysis topic 补齐 json-schema 定义(`kafka-json-events-v1.schema.json` +7 defs),readiness 如实登记 `reserved`(生产/消费接线未落地,不编造代码路径);`kafka-acl-catalog.v1.json` 补 5 条空授权绑定;`pipeline_contract_test.go` 增加 `reserved` 状态语义(双方皆空)。`tests/integration` 全绿。

### 0.5 P3 真实数据回放数据面(10.0.5.9 /home/data/datasets 数据集)
- 真实 ReplayFeeder 落地:`adapters/ingest_replay_feeder.go`(MinIO 取对象→sha256 校验→有界解析→流聚合→ingest gRPC mTLS+token UploadFlows 喂入共享分支→source fence 回执)+ `adapters/flow_aggregate.go`(community-id-spec v1 跨语言 parity 向量、双向流、ICMP 归一、pktlen/iat Welford、时间线映射到 run 窗口)。
- 派发循环:`service/stage_dispatcher.go`(领取 PENDING SOURCE_ACTIVATE→CAS RUNNING+fencing→执行器→回执 APPLIED;执行失败合成 FAILED 回执 fail-closed)+ `repository/dispatch.go`(租户可作用域、SKIP LOCKED);`contract/source.go` 承载跨边界命令/回执协议(消除 service↔adapters 依赖环);main.go env 门控 REPLAY_ENABLED。
- **live 全链证据(USTC-TFC2016 BitTorrent.pcap,7.7MB,真实数据集)**:MinIO 上传(sha256 f4c4c59d…,bucket analysis-bench)→ 网关触发(X-Tenant-ID: default)202 run 46dcd52c → 派发循环领取 → 真实回放 **packets=15000 flows=7517** → ingest mTLS+token **Accepted** → attempt SUCCEEDED + source_fence 回执落地(provider=analysis-replay)。宿主侧集成测试 `TestLiveIngestReplayRealDatasetPcap` 同链 PASS。
- 身份/时序如实登记:①回放上传复用探针传输身份(probe-agent-certs + PROBE_AUTH_TOKEN,mTLS+token 与探针同协议;待 Traffic Root CA 为 analysis-service 签发独立身份后替换——CA 私钥不在集群内,已登记);②数据集 pcap 1970 纪元时间戳按 run 窗口平移(保相对时序);③CH `flows_raw` 自 8-05 起停写(特征任务 CH sink 停摆,pre-existing,与本次无关,回执以 ingest Accepted+PG fence 为准);④RunScopeRouter→analysis.envelopes 派生仍待 P3 接线(G05 相关)。

### 0.6 权威事件中继(outbox→Kafka,RunSubscription 上线)
- 新增 `repository/outbox.go`(PENDING 领取 SKIP LOCKED/发布 CAS/失败指数退避)、`service/run_event_relayer.go`(物化事件→RunSubscription 信封富化;payload 无 tenant 的遗留行经 GetRunByID 回退)、`adapters/kafka_event_publisher.go`(MultiTopicProducer,SASL_SSL+truststore,acks=all);main.go env 门控 RELAY_ENABLED。
- Kafka 身份开通(如实登记):ACL catalog 增 analysis-service 主体 + run/plan/report 三 topic 生产者绑定;`generate_kafka_acl_plan.py --write-generated` 重生成 init-job 计划;live 创建 SCRAM 用户 `traffic-analysis-service` + 7 条 ACL(Describe/Write×3+IdempotentWrite)+ Secret `kafka-analysis-service-credentials`(密码仅存 Secret/principal-credentials,remote store 属性待补,漂移已登记)。
- **live 证据**:核心卷 outbox 全量 **16 条 PUBLISHED**(含真实回放 run 46dcd52c)→ Kafka `analysis.run.events.v1` 消费到 **16 条 RunSubscription**(tenant/run/revision/state=ACTIVE/execution_spec_sha256/window 齐全)。
- 过程发现并修复:部署清单 volume 的 secretName 曾被夹入 "…end of thinking…" 文本工件(导致两节点 kubelet FailedMount)——清理后双节点均正常;此前误判 zeus kubelet 故障已纠正,未动节点。
- **P3 第三阶段(G05 收口)**:Flink RunScopeRouter 作业已上线——`flink-alert-generator-job` 新增 `RunRouterJob`/`RunRouterProcessFunction`/`RunSubscriptionRecord`(广播状态订阅管理:ACTIVE revision 单调、CANCELLED 移除、旧 revision 拒绝;解析失败 DLQ;跨租户/窗口外不派生),7 项单测全过;镜像 `run-router-9a05b` 双节点导入;K8s 一次性提交 Job → Flink session 集群 **Run Scope Router Job RUNNING**。ACL:traffic-flink-alert-generator 增 READ flow.events.v1(组 run-router-flow-v1)+ READ analysis.run.events.v1(组 run-router-subscription-v1)+ WRITE analysis.envelopes.v1,合同目录同步(envelopes=producer_only,run.events=active)。
- **live 全链证据(真实回放 run 49168dec)**:网关触发 → 回放 packets=15000/flows=7517 入共享分支 → RunSubscription 中继发布 → RunScopeRouter 派生 → **`analysis.envelopes.v1` 三个分区累计 7,359 条 run envelope**(tenant/run_id/execution_spec_sha256/window_id/community_id 齐全)。G05 收口:采集→订阅→派生全通;仅剩特征/检测阶段消费 envelopes 产出 StageReceipt 回执(S2-S4 回执回流)。

## 1. 已执行并通过的 Gate(证据:本会话工具输出 / 此前会话记录)

| Gate | 范围 | 证据 | 结果 |
|---|---|---|---|
| G01 static | Go 全仓 build + analysis 包 vet;Web build/测试;Rust cargo;Java mvn;buf lint;YAML 校验(59 路由唯一 id、#END 尾标记脚本断言) | 本会话/此前会话工具输出 | PASS |
| G02 unit | Go analysis 8 包全过(含新增 3 个测试文件);live PG 集成测试(重跑) | 本会话工具输出 | PASS |
| G03 contract | PG 23+ 表在真实 PG 幂等执行(两次 apply 无漂移);CH additive 4 节点建表;5 analysis.* topic 就绪;proto 三语生成 | 此前会话工具输出 | PASS |
| G04 integration | live PG 集成链:materialize→replay→receipt→fence→finalize(FAILED)→reservation RELEASED→report AVAILABLE(**本会话重跑 PASS**) | 本会话工具输出 | PASS |
| G04-gw | **网关全链路 E2E(新增)**:Keycloak 取 token → APISIX OIDC bearer → analysis-service;POST /triggers(AUTO_DEFAULT)202+task/run;同键重放同句柄 202;异 hash 409 `ANALYSIS_IDEMPOTENCY_PAYLOAD_MISMATCH`;缺幂等键 400;MANUAL_CUSTOM 无持久化定制计划 409 `ANALYSIS_PLAN_NOT_APPROVED`;GET /runs/{id} 9 stage attempt+5 投影+六轴字段;GET /runs、/tasks 列表;DB 侧 trigger MATERIALIZED、task/run/reservation RESERVED、outbox `analysis.run.events.v1` PENDING、history/receipts 同事务 | 本会话工具输出 | PASS |
| G04-p2 | **人工选择列车 live(新增)**:草稿保存(修订自动分配/幂等回源)→ maker/checker 审批 ACTIVE → MANUAL_CUSTOM 触发绑定定制修订(异覆盖 409);AUTO/MANUAL 修订来源隔离 | live PG 集成测试 + 网关 E2E(本会话) | PASS |

## 2. BLOCKED(缺真实环境/证据,不得宣称通过)

| Gate | 缺什么 | 状态 |
|---|---|---|
| G05 stream | **真实数据集回放已通**:USTC pcap→MinIO→派发→聚合→ingest Accepted→PG 回执(flows 进入共享分支 flow.events.v1);**仍缺**:RunScopeRouter 订阅派生 analysis.envelopes(Flink ProcessFunction 接线)、特征/检测阶段 receipt 回流 | PARTIAL(BLOCKED:envelopes 派生与后续阶段) |
| P5 盲评 | `mlops/eval_packages/topic1_blind` labels/predictions 为空,无冻结带标签样本;95%/5% 复算不可执行(评估 Run 形态已建,缺样本数据) | BLOCKED |
| P2 人工列车 live | **已闭环**:草稿保存+审批激活+触发绑定定制修订,经网关 E2E 与 live PG 集成测试 PASS | PASS |
| P5 盲评 | `mlops/eval_packages/topic1_blind` labels/predictions 为空,无冻结带标签样本;95%/5% 复算不可执行(评估 Run 形态已建,缺样本数据) | BLOCKED |
| G06 UI browser | 分析域 7 页面 Web 构建+契约测试通过;但未跑 Playwright/正式 Windows Chrome 与 320/断点±1/1440/1920 矩阵 | BLOCKED |
| G07 fault/HA | PG 故障 fail-closed、kill/restart、双副本网关引流演练未执行 | BLOCKED |
| G08 performance | 物化事务/列表查询无目标环境 EXPLAIN/profile | BLOCKED |
| G09 release | 无 canary/rollback/观察窗;core-v1→v6 为直更(镜像不可变 tag 递增,节点离线导入) | BLOCKED |

## 3. 已知残留(如实登记,不影响正确性)

| 残留 | 说明 | 处置 |
|---|---|---|
| run `475a7220-...`(tenant live-gw-demo) | core-v4 部署窗口内 MANUAL_CUSTOM 提交因当时修订绑定缺陷按默认修订物化(其 overrides 不影响 execution hash 的探针字段);core-v5 起已修复为 fail-closed | 作为测试残留保留,run 不可变不清理;证据链以 core-v6 复验为准 |
| `analysis_trigger_instances` 存在 PENDING_MATERIALIZATION 残留行 | 失败尝试(计划绑定 409)的触发实例保留;同判别联合重试复用该行、台账无残留,无毒化 | SuppressTrigger 机制已具备;运营清理可后续执行 |
| live-gw-demo 租户 fixture | 网关 E2E 种子数据(定义+计划+治理头 ACTIVE) | 保留作为 live 证据 |

## 4. 主要落地文件

- 测试阶段虚拟网卡数据集生成(2026-08-17 傍晚):`rust/probe-agent/probe-agent/src/capture/wire_inject.rs`(新增)、`rust/probe-agent/probe-agent/src/capture/pcap_replay_op.rs`(wire 执行臂)、`rust/probe-agent/probe-agent/src/control.rs`(interface/allowlist 校验)、`rust/probe-agent/probe-agent/src/config.rs`、`rust/probe-agent/probe-agent/src/main.rs`(双执行臂)、`go/control-plane/internal/analysis/contract/replay_command.go`、`service/stage_dispatcher.go`(SourceSpecPcap.Interface)、`adapters/pcap_replay.go`(+测试)、`repository/dispatch.go`(闸门开闸条件修正)、`repository/live_pg_dispatch_integration_test.go`(新增闸门集成测试)、`deployments/kubernetes/applications/probe-agent-veth.yaml`(新增)、`probe-agent.yaml`、`analysis-service.yaml`(core-v32)
- 网关:`deployments/kubernetes/configmaps/apisix-routes.yaml`(#END、id 唯一化、analysis 安全插件栈、CA 信任配置)、`deployments/kubernetes/infrastructure/08-gateway.yaml`(CA 卷/env)、`deployments/kubernetes/site-values.template.yaml`(gateway OIDC 密钥映射)
- Go:`internal/analysis/adapters/plan_catalog.go`(+测试)、`service/trigger.go`、`service/plan_author.go`(新增,P2)、`service/worker.go`(冻结修订绑定)、`service/plan_resolver.go`(DefaultTemplate.PlanRevision)、`contract/topics.go`(ParseError/AllErrorCodes/ErrCodeNotFound + 测试)、`api/handler.go`(错误码映射+plans 端点)、`repository/plan.go`(FindTriggerInstanceByIdentity/GetTaskRunBinding/修订自动分配/审批增强)、`repository/read.go`(BySource/ByRevision 读取)、`cmd/analysis-service/main.go`(装配注入)、`repository/live_pg_integration_test.go`、`repository/live_pg_custom_plan_integration_test.go`(新增)、`service/trigger_test.go`、`service/plan_author_test.go`(新增)
- 部署:`deployments/kubernetes/applications/analysis-service.yaml`(core-v8)、`deployments/kubernetes/applications/web-ui.yaml`(p2-manual-train-f6072c454)
- live Secret:`gateway/traffic-platform-ca`(CA 锚)、gateway ExternalSecret 扩展 `OIDC_CLIENT_SECRET`

## 5. 后续待办(按依赖排序)

1. P3 第二阶段:RunScopeRouter 订阅 `analysis.run.events.v1` → 派生 `analysis.envelopes.v1`(Flink ProcessFunction 接线)+ 特征/识别/检测阶段 receipt 回流(解锁 G05 剩余)。
2. 回放身份独立化:以 Traffic Root CA 为 analysis-service 签发独立 mTLS 身份(当前复用探针身份,已登记)。
2. P5 盲评:向 `mlops/eval_packages/topic1_blind` 注入冻结带标签样本后跑 `evaluate_blind_package.py --emit-run-contract`,95%/5% 复算。
3. G06-G09 按外部工程标准矩阵补齐(Playwright 矩阵、故障演练、EXPLAIN、canary/rollback)。
