# Web 前端重开发状态

更新时间：2026-07-17

## 1. 本轮范围

本轮按 `agent.md` 与 `doc/01_design`、`doc/04_assets/ui_suite_gpt_v1` 的 UI 真源，停止沿用旧 `web/ui/src` 工程，完成旧前端备份、新 Vite React 脚手架、K8s-first 部署基础、全菜单路由骨架和本地验证。

## 2. 旧工程备份

- 旧工程备份目录：`web/ui_legacy_20260625-090823/`
- 备份清单：`web/ui_legacy_20260625-090823/BACKUP_MANIFEST.md`
- 文件清单：`web/ui_legacy_20260625-090823/BACKUP_FILE_LIST.txt`
- 备份排除：`node_modules`、`dist`、`playwright-report`、`test-results`

恢复方式：把备份目录复制回 `web/ui`，执行 `npm install` 与 `npm run build`。

## 3. 新前端状态

已完成：

- 新 `web/ui` 脚手架：React 18、Vite、TypeScript、Ant Design、ECharts、React Query、MSW、Playwright。
- 运行时配置：`public/config.js` 与生产 Nginx `config.js.template` 双路径，生产由容器启动脚本注入。
- 全局 AppShell：顶部状态条、左侧 6 个业务域导航、当前域二级菜单、底部状态栏。
- 路由真源：`src/routes/routeManifest.tsx` 覆盖 6 个一级域、24 个导航路由、2 个详情路由；`topics` 是综合态势下唯一专题菜单，标题为“专题面板”，三类专题在 `/topics` 页内切换；旧 `/topics/tunnel`、`/topics/exfil`、`/topics/apt` 仅保留兼容深链并映射到 `/topics?topic=...`。
- 页面骨架：登录、仪表盘、态势大屏、告警中心、告警详情、战役详情、系统设置、404 异常页与主要业务页已逐步专用化；其余未完成页面暂保留通用工作台。
- 本地 mock：`VITE_USE_MOCK=true` 时由 MSW 提供页面快照；生产默认 `VITE_USE_MOCK=false`。
- 真实 API 边界：`VITE_USE_MOCK=false` 时页面快照通过 `services/api.ts` 请求 APISIX 后端接口；接口失败时页面显示错误态和重试入口，不再本地合成假成功数据。
- API 契约门禁：`src/services/pageApiPlans.ts` 维护页面级生产接口计划；`src/services/pageApiPlans.test.ts` 校验所有 `routeManifest` 页面都有 API plan，且 endpoint 根路径被 `deployments/kubernetes/configmaps/apisix-routes.yaml` 覆盖。
- 路由权限门禁：`src/routes/routeManifest.tsx` 已为 24 个导航路由、2 个详情路由和旧专题兼容深链补充 `requiredScopes`、`accessMode` 与 `acceptance`；`src/App.tsx` 的受保护路由启动后通过 `/v1/auth/me` 校验会话并按 scope 判定 403；`src/layouts/AppShell.tsx` 按当前用户权限过滤菜单并调用 `/v1/auth/logout`；`src/pages/LoginPage.tsx` 已适配真实后端 `access_token/user/permissions` 登录响应和 captcha。
- 真实权限矩阵：2026-06-29 已新增 `tests/e2e/live_permission_matrix.sh`，使用 K8s Secret 内存签发短期 admin/viewer JWT，面向真实 APISIX 验证 API RBAC 与 Playwright 路由权限；anonymous/admin/viewer 共 11 项通过；同日后续用 Desktop Chrome extension backend 完成合法登录态 `/dashboard -> /alerts` 点击，证据见 `doc/02_acceptance/runs/20260629-live-permission-matrix/` 与 `doc/02_acceptance/runs/20260629-desktop-chrome-business-smoke/`。
- `/screen` 脱敏与只读边界：生产默认仍为 `SCREEN_ACCESS_MODE=protected`；仅当运行时显式设置 `SCREEN_ACCESS_MODE=masked-demo` 时，匿名访问 `/screen` 会使用 `screen_demo` 只读身份、`screen:view` scope 和脱敏指标渲染大屏，不暴露资产、用户、IP、证据文件或处置动作明细；2026-06-29 已将 `screen:view` 纳入 auth-service scope catalog，并通过 live 只读大屏 token 矩阵验证可看 `/screen`、不能列 API tokens、不能写告警、不能进入 `/settings`。
- WebSocket 授权时序：`src/services/realtime.ts` 已新增授权后连接客户端；`src/layouts/AppShell.tsx` 仅在 `ENABLE_REALTIME=true` 且存在授权 token 时，才在受保护应用壳内按 `WS_URL` 建立 WebSocket，并携带 `token` 与可选 `tenant_id` 查询参数；登录页、匿名跳转和 `/screen` 脱敏公开模式无 token 时不连接，退出/卸载会关闭连接；2026-06-29 已将 live K8s `deployment/web-ui` 与源码清单切到 `ENABLE_REALTIME=true`，并完成合法登录态业务页自动连接可视巡检。
- 真实字段适配：`src/services/pageSnapshotAdapters.ts` 已为 `/dashboard`、`/probes`、`/data-quality`、`/alerts`、`/assets`、`/graph`、`/fusion`、`/baselines`、`/campaigns`、`/attack-chains`、专题接口 `/v1/topics/tunnel`、`/v1/topics/exfil`、`/v1/topics/apt`、`/encrypted-traffic`、`/forensics`、`/rules`、`/deployments`、`/models`、`/mlops`、`/playbooks`、`/whitelist`、`/compliance`、`/audit-log`、`/notifications`、`/settings` 建立页面级 payload 适配，优先映射后端真实字段，再由通用归一化兜底其他页面。
- 仪表盘专用工作台：`src/pages/DashboardOperationsPage.tsx` 已按 `screens/pages/dashboard.png` 的信息架构拆分为脱敏运营 KPI、优先级待办队列、采集与数据健康门禁、验收缺口与建议动作、告警处置阶段工作篮、证据与反馈质量摘要、Top Talkers 风险贡献，不再套用通用页面骨架。
- 探针管理专用工作台：`src/pages/ProbesManagementPage.tsx` 已按 `screens/pages/probes.png` 的信息架构拆分为探针 KPI、API 动态 2D/3D SVG 部署拓扑、探针状态矩阵、吞吐与丢包趋势、选中探针详情、配置闭环、批量运维和心跳日志。2026-07-16 已将 `topology_x/y/z`、区域、角色、链路和链路带宽，以及趋势/PPS/阈值全部接入 `/v1/probes` + PostgreSQL fixture；2D 使用 API 平面坐标，3D 使用同一 API 坐标的等距投影，节点、链路、带宽和状态均动态重算。六类运维表单均提交真实控制面接口，操作与审计在同一事务中持久化为 `queued`；viewer 403、跨租户 404、54 条操作记录、6 类审计事件一致性和 Windows Chrome 1920x1080 视觉/交互验收 31/31 通过；证据见 `windows-chrome-cdp-probes-latest.json`。控制面不会伪报探针已执行，最终完成仍等待探针 ACK 消费者。
- 数据质量专用工作台：`src/pages/DataQualityPage.tsx` 已按 `screens/pages/data-quality.png` 的信息架构拆分为质量页签、7 项质量 KPI、Kafka Topic 健康表、Topic 分区倾斜热力图、Flink 处理质量概览、字段质量矩阵、存储写入质量、对账报告、质量异常告警、快速定位、质量修复建议和证据报告动作；真实数据通过 `/v1/data-quality` 的 `overall`、`checks`、`metrics.flow_rate`、`metrics.data_completeness`、`metrics.p95_latency_ms`、`metrics.insert_rate_per_min`、`metrics.session_count_1h`、`metrics.feature_count_1h` 映射。
- Data Quality DLQ 业务闭环：2026-06-29 ingest-gateway replay HTTP 已新增用户 access JWT 校验并注入 `JWT_SIGNING_KEY`，Web UI 合法 JWT 会话进入 `/data-quality?tab=replay-reconcile` 后打开 `DLQ fallback replay dry-run` modal，提交真实 `POST /api/v1/dlq/replay/fallback` 返回 `status=dry_run`、`duplicate=false`，无 console/pageerror/requestfailed/http 4xx/5xx；证据见 `doc/02_acceptance/runs/20260629-data-quality-dlq-business/`。Desktop Chrome 合法登录态业务页点击已由 `/dashboard -> /alerts` P0 smoke 闭环，DLQ modal 可作为后续多页巡检扩展。
- 专题面板工作台：`src/pages/TopicWorkbenchPage.tsx` 已合并为单个 `/topics` 菜单页，左侧二级菜单只显示“专题面板”；页内提供 `加密隧道专题`、`数据外传专题`、`APT 战役专题` 三种切换状态，分别使用 `bg-topic-encrypted-tunnel-4k.png`、`bg-topic-data-exfiltration-4k.png`、`bg-topic-apt-campaign-4k.png` 作为视觉基准；真实数据分别通过 `/v1/topics/tunnel` 的 `summary/protocols/users`、`/v1/topics/exfil` 的 `summary/top_sources/risk_types/paths`、`/v1/topics/apt` 的 `summary/campaigns/phase_distribution` 映射。旧专题深链重定向到 `/topics?topic=...`。
- 告警中心专用工作台：`src/pages/AlertTriagePage.tsx` 已按 `screens/pages/alerts.png` 的信息架构拆分为告警队列、筛选检索、高密度告警表格、右侧告警详情、风险评分环、研判时间线、关联告警簇和处置反馈表单，不再套用通用页面骨架。
- 告警详情专用工作台：`src/pages/AlertDetailPage.tsx` 已按 `screens/pages/alert-detail.png` 的信息架构拆分为研判摘要、风险评分、告警事实矩阵、资产上下文、事件时间线、证据链、攻击阶段轨迹、影响范围、处置响应和反馈学习；真实数据通过 `src/services/alertDetailApi.ts` 请求 `/v1/alerts/{id}`、`/v1/alerts/{id}/evidence`、`/v1/alerts/{id}/feedback`，主详情失败进入错误态，证据/反馈二级接口失败保留告警主详情并显式标记证据不足。
- 资产台账专用工作台：`src/pages/AssetInventoryPage.tsx` 已按 `screens/pages/assets.png` 的信息架构拆分为资产筛选、资产 KPI、高密度资产表格、右侧资产详情、风险画像、资产上下文、流量画像、协议分布、Top 对端、周期性连接和关联证据，不再套用通用页面骨架。
- 实体图谱专用工作台：`src/pages/GraphEntityPage.tsx` 已按 `screens/pages/graph.png` 的信息架构拆分为实体搜索、邻居图谱画布、关系图例、路径分析、实体详情、关联证据、查询治理和跨模块下钻动作，不再套用通用页面骨架。
- 数据融合专用工作台：`src/pages/FusionWorkbenchPage.tsx` 已按 `screens/pages/fusion.png` 的信息架构拆分为数据源健康卡、多源融合编排、融合规则管理、冲突队列、融合事件审计、融合质量看板和右侧冲突处理抽屉，不再套用通用页面骨架；2026-06-29 已接入 `POST /v1/fusion/conflicts/{id}/resolve` 和 `PATCH /v1/fusion/rules/{id}`，冲突确认、修复任务入口和规则编辑动作会通过真实 APISIX API 写入后端；2026-06-30 已接入 `/v1/fusion/value-report`，在质量看板映射检出提前量、误报下降和 MTTR 下降，并新增 `tests/e2e/live_fusion_value_report_preflight.sh` 作为部署后 live APISIX/JWT/K8s 结构证据门；alert-service 已滚动到 `docker.io/traffic/alert-service@sha256:89d2e52463f7bcadf103b8c6274dd5559570fc7a2ff0da7103c1faf9d83e0569`，r2 live preflight `19/19` passed。
- 行为基准专用工作台：`src/pages/BaselineWorkbenchPage.tsx` 已按 `screens/pages/baselines.png` 的信息架构拆分为基线页签、筛选条件、基线 KPI、基线状态机、行为分布分析、偏离列表、基线版本管理、偏离解释和治理操作，不再套用通用页面骨架。
- 战役列表专用工作台：`src/pages/CampaignWorkbenchPage.tsx` 已按 `screens/pages/campaigns.png` 的信息架构收紧为顶部 6 KPI + 风险分布、左侧高密度战役表、中部 ATT&CK 阶段视图、右侧当前战役/影响范围/证据完整度/状态流转/战役操作栏；真实数据通过 `/v1/campaigns` 的 `campaign_id`、`campaign_type`、`attack_phases`、`entities`、`alerts`、`score`、`ts_start/ts_end` 映射。
- 战役详情专用工作台：`src/pages/CampaignDetailPage.tsx` 已按 `screens/pages/campaign-detail.png` 的信息架构拆分为战役画像、风险评分、事实矩阵、攻击时间轴、关联告警、影响范围、证据包、处置流程、处置动作和复盘结论；真实数据通过 `src/services/campaignDetailApi.ts` 请求 `/v1/campaigns/{id}`，映射 `campaign_id`、`ts_start/ts_end`、`entities`、`alert_ids/alerts`、`score`、`summary`、`campaign_type`、`attack_phases`、`rule_ids`、`model_ids`，主详情失败进入错误态，不在生产态本地伪造成成功。
- 攻击链分析专用工作台：`src/pages/AttackChainAnalysisPage.tsx` 已按 `screens/pages/attack-chains.png` 的信息架构拆分为顶部筛选/动作条、6 阶段攻击链画布、实体/资产泳道、告警事件泳道、证据锚点泳道、处置动作泳道、右侧证据锚点表、处置建议表、底部 ATT&CK 阶段矩阵和路径明细表；真实数据通过 `/v1/attack-chains` 的 `chain_id`、`title`、`phases`、`key_events`、`risk_score`、`source_ip`、`entity_count`、`alert_count` 映射。
- 加密流量专用工作台：`src/pages/EncryptedTrafficPage.tsx` 已按 `screens/pages/encrypted-traffic.png` 的信息架构拆分为顶部页签/筛选/一键分析、7 项加密流量 KPI、协议分布与趋势、Top JA3 指纹表、JA3 分布散点、隧道检测与异常特征、异常隧道列表、右侧外联画像、处置与分析建议、关联下钻、生成导出和握手元数据证据表；真实数据通过 `/v1/encrypted-traffic/stats`、`/v1/encrypted-traffic/sessions`、`/v1/encrypted-traffic/ja3`、`/v1/encrypted-traffic/tunnels`、`/v1/encrypted-traffic/exfiltration` 映射。
- 取证分析专用工作台：`src/pages/ForensicsWorkbenchPage.tsx` 已按 `screens/pages/forensics.png` 的信息架构拆分为来源上下文、取证筛选、取证任务状态机、会话复放、取证任务列表、PCAP 索引、证据导出包、hash 校验结果、返回来源、右侧证据完整性、签名 URL、取证操作和审计日志；真实数据通过 `/v1/pcap/jobs` 与 `/v1/pcap/stats` 映射。2026-06-29 已补 `pageApiPlans` 中 `forensics-cancel-job` action contract，绑定 `POST /v1/pcap/jobs/{id}/cancel`、`pcap:write`、`PCAP_CANCEL` 审计事件和 queued/processing 取消门禁，并以 `tests/e2e/live_forensics_task_state_machine.sh` r1 验证 processing 取消、completed 取消 409、跨租户 403、任务持久化和审计查询 7/7 通过；当前 live forensics-service 为 `traffic/forensics-service:forensics-state-20260629-r2` digest。
- 规则管理专用工作台：`src/pages/RuleManagementPage.tsx` 已按 `screens/pages/rules.png` 及四张规则 Tab UI 图完成规则 KPI、无滚动规则表、条件/DSL、生命周期、版本/审批/发布、底部六模块，以及测试验证、依赖引用、PCAP/Session/日志样本全状态；Tab 仅在 `/rules` 原页面内切换，不产生独立路由或 `view` 查询参数。2026-07-17 r126 将六个规则状态 KPI 的内部 Ant Design SVG 实际扩大到 48×48px，固定版本历史四条记录并补齐完整时间、操作者和底部入口；依赖引用重构为中心规则加六类依赖的 ECharts 动态关系图，并增加版本/状态徽标和六类图例；生命周期重构为原图六个单圆形节点与五段连续连接线，箭头、线和圆心对齐，状态/时间/操作人完整且无重复圆点；规则定义四条条件和例外条件均为字段、运算符、值三个真实下拉框，箭头位于各自边框内；DSL 改为可编辑代码区，MITRE 左侧 TA0011 指挥与控制可删除、右侧可添加；PCAP、Session、日志样本 Tab 按各自原 UI 重构表头、四行数据、字段徽标、操作按钮和误报开关。测试验证结果和依赖关系表均保留原编辑容器内滚动条，所有 Tab 切换前后模块外框几何不变。真实数据通过 `/v1/rules` 的 `rule_id`、`name`、`type`、`engine`、`severity`、`enabled`、`priority`、`version`、`status`、`labels`、`updated_at` 映射；Windows Chrome 150、1920x1080 的 r252 验收 15/15 通过，报告为 `evidence/ui-image-breakdowns/pages/rules/interaction-r252-rules-embedded-tabs.json`。
- 部署管理专用工作台：`src/pages/DeploymentManagementPage.tsx` 已按 `screens/pages/deployments.png` 的信息架构拆分为发布 KPI、发布清单、灰度策略、发布健康、版本对比、回滚管理和发布证据；真实数据通过 `/v1/deployments` 的 `deployment_id`、`name`、`rule_version`、`model_version`、`feature_set_id`、`scope`、`status`、`created_by`、`created_at`、`updated_at` 映射。
- 模型管理专用工作台：`src/pages/ModelManagementPage.tsx` 已按 `screens/pages/models.png` 的信息架构拆分为模型 KPI、模型列表、Champion/Challenger 状态机、数据集与样本、模型指标、解释与特征、激活与回滚；真实数据通过 `/v1/models` 的 `model_id`、`name`、`model_type`、`metadata.status`、`metadata.current_version`、`metadata.online_version`、`metadata.candidate_version`、`metadata.metrics`、`metadata.owner`、`metadata.trained_at`、`created_at`、`updated_at` 映射。
- MLOps 编排专用工作台：`src/pages/MlopsOrchestrationPage.tsx` 已按 `screens/pages/mlops.png` 的信息架构拆分为编排 KPI、MLOps 闭环 DAG、反馈样本池、训练任务队列、评估与门禁、混淆矩阵、注册与发布、效果回流、反馈漏斗和漂移趋势；真实数据通过 `/v1/mlops/status` 的 `running_workflows`、`max_concurrent`、`min_feedback_count`、`max_fp_rate`、`clickhouse_connected` 与 `/v1/mlops/conditions` 的 `triggers` 映射。
- SOAR 剧本专用工作台：`src/pages/PlaybookAutomationPage.tsx` 已按 `screens/pages/playbooks.png` 的信息架构拆分为剧本 KPI、剧本列表、剧本编排画布、节点配置/触发策略、风险控制、执行历史、处置效果和审计证据；真实数据通过 `/v1/playbooks/catalog` 的 `name`、`description`、`enabled`、`trigger`、`actions`、`cooldown`、`max_runs`、`run_count` 与 `/v1/playbooks/executions` 的 `execution_id`、`playbook`、`alert_id`、`success_actions`、`failed_actions`、`duration_ms` 映射。2026-06-29 已补 `pageApiPlans` 中 `playbook-execute` 与 `playbook-state-update` action contract，并以 `tests/e2e/live_playbook_state_machine.sh` r1 验证禁用门禁、手动 `max_runs`、执行落库、执行历史 API 和 `/playbooks` 页面消费 21/21 通过；当前 live alert-service 为 `traffic/alert-service:playbook-state-20260629-r1` digest。
- 白名单专用工作台：`src/pages/WhitelistGovernancePage.tsx` 已按 `screens/pages/whitelist.png` 的信息架构拆分为 6 项白名单 KPI、白名单列表、条件构造器/新增草案、审批流程状态机、命中监控、到期治理、反馈关联链路和影响矩阵/来源链路卡；真实数据通过 `/v1/whitelist` 的 `entries`、`total`、`type`、`value`、`reason`、`description`、`created_by`、`expires_at`、`created_at` 映射，并从来源告警、到期时间、责任角色和对象类型推导审批、到期治理、覆盖告警和潜在漏报风险证据。
- 告警详情反馈闭环：2026-06-29 已把 `src/pages/AlertDetailPage.tsx` 的“反馈与学习”从静态控件接入 `submitAlertFeedback`，支持 TP/FP、误报原因、备注、生成白名单审批草案；顶部“加入白名单”会切到 FP + 生成草案，后端返回 `whitelist_draft` 后可跳转 `/whitelist?source_alert=...&draft_id=...`，对应服务契约与测试见 `src/services/alertDetailApi.ts`、`src/services/alertDetailApi.test.ts`，live 证据见 `doc/02_acceptance/runs/20260629-alert-feedback-whitelist/`。
- 合规审计专用工作台：`src/pages/ComplianceAuditPage.tsx` 已按 `screens/pages/compliance.png` 的信息架构拆分为 6 项合规 KPI、验收门禁矩阵、指标映射追踪表、证据包完整度、运行报告预览、缺口治理看板和第三方评测批次；真实数据通过 `/v1/compliance/reports` 的 `reports`、`total`、`report_id`、`report_type`、`generated_at`、`status`、`summary`、`sections` 与 `/v1/compliance/audit-trail` 的 `trails` 映射，并从分项状态、SLA 违规、已处置告警和审计记录推导门禁通过率、未达标项、证据完整度和复验通过率。
- 审计日志专用工作台：`src/pages/AuditLogPage.tsx` 已按 `screens/pages/audit-log.png` 的信息架构拆分为 6 项审计 KPI、日志检索、高密度审计表、操作详情 / Diff 视图、高风险审计、留存状态、关联链路、操作时间线和导出取证；真实数据通过 `/v1/audit/logs` 的 `trails`、`total`、`log_id`、`tenant_id`、`user_id`、`action`、`resource_type`、`resource_id`、`details`、`ip_address`、`timestamp`、`result` 映射，并从动作类型、对象类型、结果、request_id、trace_id 和详情字段推导失败操作、高风险操作、导出下载、PCAP 访问、完整性校验、关联链路和留存证据。
- 通知配置专用工作台：`src/pages/NotificationConfigPage.tsx` 已按 `screens/pages/notifications.png` 的信息架构拆分为 6 项通知 KPI、通知渠道健康、订阅规则、条件构造器、升级策略流程、模板管理、发送历史和抑制静默；真实数据通过 `/v1/notifications/settings` 的 `enabled`、`min_severity`、`rate_limit_per_min`、`channels`、`secret_ref`、`rules`、`history`、`escalation_rules`、`silence_rules`、`templates` 映射，并从启用通道、失败/待确认投递、secret 引用、升级策略和静默窗口推导通知路由与审计证据。
- 系统设置专用工作台：`src/pages/SettingsGovernancePage.tsx` 已按 `screens/pages/settings.png` 的信息架构拆分为 6 项设置 KPI、租户与站点树、RBAC 权限矩阵、API 令牌表、数据留存策略、集成配置健康、安全策略与系统参数、闭环动作入口；真实数据通过 `/v1/tokens/scopes`、`/v1/tokens`、`/v1/tokens/scopes/probe` 的 `scopes`、`tokens`、`total`、`tenant_id`、`name`、`token_prefix`、`expires_at`、`last_used_at`、`status`、`rotation_enabled` 映射，并从权限范围、有效/即将过期令牌、探针默认权限和轮换状态推导设置审计证据。2026-06-30 已补 `pageApiPlans` 中 `settings-preferences-save`、`settings-token-create`、`settings-token-scope-update`、`settings-token-regenerate`、`settings-token-revoke` 和 `settings-token-validate` action contract，并以 `tests/e2e/live_settings_governance_preflight.sh` r1 验证 display 设置保存/读取、token 创建/Scope 更新/轮换/吊销/validate、viewer 403、跨租户 404、`user_settings` / `api_tokens` 持久化和 token 审计查询 45/45 通过；latest token 响应证据已脱敏。
- 404 异常页专用工作台：`src/pages/NotFoundPage.tsx` 已按 `screens/pages/not-found.png` 的信息架构拆分为 404 摘要、追踪事实矩阵、返回入口、安全提示、辅助操作、最近可用入口和相关系统状态；页面不展示未知路由原文、堆栈、内部接口细节或凭据，支持返回上一页、进入仪表盘/态势大屏/告警中心/审计日志/系统设置与复制追踪 ID。
- 构建分包治理：`src/App.tsx` 已改为路由级 `React.lazy` 加载；`src/components/charts.tsx` 已改为 ECharts core 按需注册 Line/Gauge/Grid/Tooltip/Title/CanvasRenderer；`vite.config.ts` 按 React、Router、Data、ECharts、AntD、rc 与包级 vendor 进行稳定拆分，当前生产构建不再出现 Vite 大 chunk、circular chunk 或 empty chunk warning。
- UI 资产：从 `doc/04_assets/ui_suite_gpt_v1/backgrounds/4k` 复制关键背景资产到 `web/ui/public/ui-assets/backgrounds/`，本轮新增三个专题背景 `bg-topic-encrypted-tunnel-4k.png`、`bg-topic-data-exfiltration-4k.png`、`bg-topic-apt-campaign-4k.png`。

## 4. K8s-first 部署口径

- `web/ui/deployments/Dockerfile` 继续使用多阶段构建。
- `web/ui/deployments/nginx.conf` 继续提供 SPA 静态服务、`/health`、`/config.js`、`/api/` 与 `/ws/` 代理。
- `web/ui/deployments/kubernetes/deployment.yaml` 已改为通过 K8s 内部 APISIX Gateway：`apisix.gateway.svc:9080`。
- 生产 live 环境为 `AUTH_ENABLED=true`、`USE_MOCK=false`、`ENABLE_REALTIME=true`；本地和镜像构建默认值仍可通过运行时 `config.js` 覆盖。
- `public/config.js` 只作为本地/静态预览占位；容器运行时由 `deployments/docker-entrypoint.sh` 生成最终 `config.js`。

## 5. 已验证证据

在 `web/ui` 下通过：

```text
npm run lint:check
npm run test -- --run
npm run build
PLAYWRIGHT_BASE_URL=http://127.0.0.1:4190 npm run test:e2e
```

完整基线结果：`npm run lint:check` 通过；`npm run test -- --run` 为 5 个测试文件、33 个测试全部通过；`npm run build` 通过；`PLAYWRIGHT_BASE_URL=http://127.0.0.1:4190 npm run test:e2e` 为 26 项 Playwright 测试全部通过。lint/test 中的 `Cannot open netlink socket` 与 Vitest WebSocket `EPERM` 为当前沙箱环境提示，命令退出码均为 0。

构建分包补充验证：`npm run lint:check` 通过；`npm run test -- --run src/routes/routeManifest.test.ts src/services/pageSnapshotAdapters.test.ts` 为 26 项通过；`npm run build` 通过，最大 JS chunk 为 `vendor-antd` 456.25 kB、`vendor-rc` 417.73 kB、`vendor-echarts` 342.14 kB，未出现 Vite 大 chunk、circular chunk 或 empty chunk warning；当时的 dashboard、settings、not-found 与专题相关 Playwright 子集为 3 项通过，专题结构已由后续“专题面板合并补充验证”覆盖。

路由权限门禁补充验证：`npm run lint:check` 通过；`npm run test -- --run src/routes/routeManifest.test.ts src/routes/access.test.ts` 为 7 项通过；`npm run build` 通过且仍无 Vite 大 chunk warning；dashboard、settings、not-found 与专题相关 Playwright 子集为 3 项通过；`PLAYWRIGHT_BASE_URL=http://127.0.0.1:4195 npm run test:e2e -- e2e/auth-gate.spec.ts` 为 2 项通过，覆盖匿名用户跳转 `/login` 和 viewer 访问 `/settings` 显示 403 scope 证据。

`/screen` 脱敏模式补充验证：`npm run lint:check` 通过；`npm run test -- --run src/routes/routeManifest.test.ts src/routes/access.test.ts` 为 7 项通过；`npm run build` 通过且仍无 Vite 大 chunk warning；`PLAYWRIGHT_BASE_URL=http://127.0.0.1:4196 npm run test:e2e -- e2e/auth-gate.spec.ts` 为 3 项通过，新增覆盖 `SCREEN_ACCESS_MODE=masked-demo` 下匿名访问 `/screen`、显示 `脱敏公开演示` 与 `screen_demo`；dashboard、settings、not-found 与专题相关 Playwright 子集为 3 项通过；`env -u HTTP_PROXY -u HTTPS_PROXY -u ALL_PROXY -u http_proxy -u https_proxy -u all_proxy kubectl apply --dry-run=client -f web/ui/deployments/kubernetes/deployment.yaml` 通过。

WebSocket 授权时序补充验证：`npm run lint:check` 通过；`npm run test -- --run src/services/realtime.test.ts src/routes/access.test.ts src/routes/routeManifest.test.ts` 为 3 个测试文件、10 项通过，覆盖相对/绝对 WS URL、`ws/wss` 协议转换、token/tenant query 和启用鉴权时必须有 token 才连接；新增后 `npm run test -- --run` 为 7 个测试文件、40 项通过；`npm run build` 通过且仍无 Vite 大 chunk warning；`env -u HTTP_PROXY -u HTTPS_PROXY -u ALL_PROXY -u http_proxy -u https_proxy -u all_proxy kubectl apply --dry-run=client -f web/ui/deployments/kubernetes/deployment.yaml` 通过。浏览器 preview 常驻验证在当前 Codex 沙箱内未能完成：`npm run preview -- --host 127.0.0.1 --port 4199` 与 `CODEX_NETWORK_ALLOW_LOCAL_BINDING=1 npm run preview -- --host 127.0.0.1 --port 4199` 均因 `listen EPERM: operation not permitted 127.0.0.1:4199` 失败，当前命令运行器带 `--unshare-net`，所以不能把非沙箱 preview 结果作为该项关闭证据。

真实 APISIX 权限矩阵补充验证：`LOG_DIR=/tmp/perm-matrix-20260629015619 tests/e2e/live_permission_matrix.sh` 通过，API RBAC 6 项、Playwright route matrix 5 项，合计 11/11；`npm run test -- --run src/routes/access.test.ts src/routes/routeManifest.test.ts` 为 2 个测试文件、10 项通过；Codex Desktop Chrome wrapper 匿名 `/dashboard` 门禁仍回 `/login`，后续 `20260629-desktop-chrome-business-smoke-r4` 已用受控 smoke token 完成 `/dashboard -> /alerts` 合法登录态点击。证据见 `doc/02_acceptance/runs/20260629-live-permission-matrix/` 与 `doc/02_acceptance/runs/20260629-desktop-chrome-business-smoke/`。

合法登录态实时通道补充验证：`env -u HTTP_PROXY -u HTTPS_PROXY -u ALL_PROXY -u http_proxy -u https_proxy -u all_proxy kubectl -n traffic-analysis set env deployment/web-ui VITE_ENABLE_WEBSOCKET=true ENABLE_REALTIME=true` 后 rollout 通过，APISIX `/config.js` 返回 `ENABLE_REALTIME: "true"`；`LOG_DIR=/tmp/realtime-appshell-20260629020212 tests/e2e/live_realtime_appshell.sh` 为 2/2 通过，合法 token 进入 `/dashboard` 后页面显示 `实时通道` / `已连接`，Playwright 捕获 `/ws/events` WebSocket 且收到 `ready` 帧；打开实时通道后回归 `tests/e2e/live_permission_matrix.sh` 仍为 11/11；`npm run test -- --run src/services/realtime.test.ts src/routes/access.test.ts src/routes/routeManifest.test.ts` 为 3 个测试文件、13 项通过；Desktop Chrome 合法登录态 `/alerts` 页同样可见 `实时通道` 和 `数据质量`。证据见 `doc/02_acceptance/runs/20260629-realtime-appshell-live/` 与 `doc/02_acceptance/runs/20260629-desktop-chrome-business-smoke/`。

态势大屏只读 token 补充验证：auth-service 镜像滚动到 `traffic/auth-service:screen-scope-20260629-r1`；`go test ./internal/auth/model ./internal/auth/service ./cmd/auth-service` 通过；`LOG_DIR=/tmp/screen-readonly-20260629020953 tests/e2e/live_screen_readonly_matrix.sh` 为 8/8 通过，覆盖 `screen:view` scope catalog、`/api/v1/auth/me` 只含 `screen:view`、`/api/v1/dashboard/stats` 可读、`/api/v1/tokens` 和告警状态写入拒绝、`/screen` 可渲染、`/settings` 403、写操作导航隐藏；打开后回归权限矩阵 11/11、实时通道 2/2；Desktop Chrome 匿名 `/screen` 门禁仍重定向 `/login`，合法登录态业务页点击已在 `/alerts` smoke 闭环。证据见 `doc/02_acceptance/runs/20260629-screen-readonly-token/` 与 `doc/02_acceptance/runs/20260629-desktop-chrome-business-smoke/`。

API token 生命周期补充验证：auth-service 镜像继续滚动到 `traffic/auth-service:token-lifecycle-20260629-r2`；`go test ./internal/auth/...` 通过；`RUN_ID=20260629-token-lifecycle-r2 LOG_DIR=doc/02_acceptance/runs/20260629-token-lifecycle tests/e2e/live_token_lifecycle_matrix.sh` 为 23/23 通过，覆盖 token create/read/validate、SHA-256 hash 与 `token_prefix` 持久化、viewer 403、跨租户 404、regenerate 后旧 token 401/新 token 200、revoke 后 401、短期过期 401 和同步 `audit_logs`；权限矩阵回归 `RUN_ID=20260629-token-lifecycle-permission-r1 LOG_DIR=doc/02_acceptance/runs/20260629-token-lifecycle tests/e2e/live_permission_matrix.sh` 为 11/11 通过；release manifest r9 已绑定该 auth-service 镜像。证据见 `doc/02_acceptance/runs/20260629-token-lifecycle/` 与 `doc/02_acceptance/02-regression/token-lifecycle-matrix-latest.md`。

404 异常页补充验证：`npm run build` 通过；`npm run test -- --run src/routes/routeManifest.test.ts src/services/pageSnapshotAdapters.test.ts` 为 26 项通过；`PLAYWRIGHT_BASE_URL=http://127.0.0.1:4193 npm run test:e2e -- --grep "not found route"` 为 1 项通过；浏览器截图检查 `events=[]`，且未把 `/missing/not-found` 或 `stack` 暴露到页面正文。

专题面板合并补充验证：`npm run test -- --run src/routes/routeManifest.test.ts src/services/pageApiPlans.test.ts src/services/pageSnapshotAdapters.test.ts` 为 3 个测试文件、30 项通过；`npm run build` 通过；`VITE_USE_MOCK=true VITE_AUTH_ENABLED=false npm run build -- --base=/__codex_preview/ --outDir dist-codex-preview` 通过并更新沙箱常驻 preview 产物；`PLAYWRIGHT_BASE_URL=http://127.0.0.1:4197 npm run test:e2e -- --grep "topic panel|legacy topic"` 为 2 项通过；Codex Desktop 内置浏览器访问 `/__codex_preview/topics` 无非 warning console/pageerror、无横向溢出，左侧二级菜单为 `仪表盘`、`态势大屏`、`专题面板`，点击 APT 后进入 `/__codex_preview/topics?topic=apt`，旧 `/__codex_preview/topics/apt` 自动映射到 `/__codex_preview/topics?topic=apt`。

其中 `npm run test -- --run` 当前包含：

- `routeManifest.test.ts`：校验 6 个一级域、24 个菜单路由、2 个详情路由，确认导航菜单包含 `/topics` 和“专题面板”，且不再把 `/topics/tunnel`、`/topics/exfil`、`/topics/apt` 放入左侧菜单；旧专题路径只保留在 `allRoutes` 作为兼容深链。
- `realtime.test.ts`：校验 WebSocket URL 归一化、`ws/wss` 协议转换、token/tenant 参数和 `ENABLE_REALTIME` + auth token 才允许连接的门禁。
- `pageApiPlans.test.ts`：校验页面 API plan 与 APISIX 路由覆盖关系。
- `alertDetailApi.test.ts`：校验 `/v1/alerts/{id}`、`/v1/alerts/{id}/evidence`、`/v1/alerts/{id}/feedback` 映射为告警详情研判模型，并覆盖二级接口失败时不伪造成成功的证据状态。
- `campaignDetailApi.test.ts`：校验 `/v1/campaigns/{id}` 映射为战役故事板模型，覆盖战役画像、攻击阶段、关联告警、影响范围、证据包、处置流程和复盘结论字段。
- `pageSnapshotAdapters.test.ts`：校验 dashboard、probes、data-quality、alerts、assets、graph、fusion、baselines、campaigns、attack-chains、encrypted-traffic、forensics、rules、deployments、models、mlops、playbooks、whitelist、compliance、audit-log、notifications、settings 的真实 payload 映射为页面指标、表格和证据字段；dashboard 覆盖 `待复核` 与 `业务系统` 字段，probes 覆盖 `probe_id`、`status`、`capture_mode`、`bandwidth_mbps`、`drop_rate`、`config_version`、`last_heartbeat` 与 `/v1/probes` 证据字段，data-quality 覆盖 `overall`、`checks.status`、`metrics.data_completeness`、`metrics.p95_latency_ms`、`metrics.insert_rate_per_min`、`质量总分`、`DLQ 数量`、`flow_original` Topic 行与 `/v1/data-quality` 证据字段，alerts 覆盖 `受影响资产`、`规则/模型`、`置信度` 字段，assets 覆盖 `主机名`、`操作系统`、`漂移资产`、`暴露服务数` 字段，graph 覆盖 `data.graph.nodes/edges`、`meta.node_count`、`meta.edge_count`、`告警关联` 与路径行字段，fusion 覆盖 `data_source_stats`、`quality_metrics`、`entities`、`来源覆盖`、`可信度` 与冲突处理字段，baselines 覆盖 `baseline_id`、`baseline_type`、`metrics.deviation_score`、`status`、`基线稳定度` 与偏离解释字段，campaigns 覆盖 `campaign_id`、`attack_phases`、`entities`、`alerts`、`score`、`ts_start/ts_end` 与 `/v1/campaigns` 证据字段，attack-chains 覆盖 `chain_id`、`phases.key_events`、`risk_score`、`entity_count`、`source_ip` 和 `/v1/attack-chains` 证据字段，encrypted-traffic 覆盖 `total_sessions`、`tls_sessions`、`quic_sessions`、`malicious_ja3_matches`、`sessions`、`certificate_issuer`、`ja3/ja3s`、`tunnels` 与 `exfiltration` 证据字段，forensics 覆盖 `job_id`、`status`、`progress`、`params`、`result_file_key`、`download_url`、`expires_at` 与 `/v1/pcap/stats` 证据字段，rules 覆盖 `rule_id`、`name`、`type`、`severity`、`enabled`、`version`、`status`、`labels`、生命周期门禁和 `/v1/rules` 证据字段，deployments 覆盖 `deployment_id`、`name`、`rule_version`、`model_version`、`feature_set_id`、`scope`、`status`、`created_by`、发布时间、灰度门禁和 `/v1/deployments` 证据字段，models 覆盖 `model_id`、`name`、`model_type`、`metadata.status`、`metadata.current_version`、`metadata.online_version`、`metadata.candidate_version`、`metadata.metrics.f1_score`、`metadata.metrics.drift`、`metadata.owner` 与 `/v1/models` 证据字段，mlops 覆盖 `running_workflows`、`max_concurrent`、`min_feedback_count`、`max_fp_rate`、`clickhouse_connected`、`triggers`、训练/评估/注册/发布任务指标和 `/v1/mlops/*` 证据字段，playbooks 覆盖 `Playbook` 目录、触发器、动作、启用状态、执行记录、失败步骤、平均耗时和 `/v1/playbooks/*` 证据字段，whitelist 覆盖 `/v1/whitelist` 的 `entries`、`total`、对象类型、匹配条件、来源告警、审批状态、长期生效、覆盖告警和命中监控证据字段，compliance 覆盖 `/v1/compliance/reports` 的 `reports`、`summary`、`sections`、门禁通过率、未达标项、证据完整度、复验通过率、验收门禁行和 `/v1/compliance/audit-trail` 审计证据字段，audit-log 覆盖 `/v1/audit/logs` 的 `trails`、`action`、`resource_type`、`details.request_id`、`details.trace_id`、失败操作、高风险审计、PCAP 访问、留存状态和导出取证证据字段，notifications 覆盖 `/v1/notifications/settings` 的 `enabled`、`min_severity`、`rate_limit_per_min`、`channels`、`secret_ref`、`rules`、`history`、`escalation_rules`、`silence_rules` 和通知投递审计证据字段，settings 覆盖 `/v1/tokens/scopes`、`/v1/tokens`、`/v1/tokens/scopes/probe` 的 `scopes`、`tokens`、`tenant_id`、`name`、`token_prefix`、`expires_at`、`last_used_at`、`status`、`rotation_enabled` 和 Token/RBAC/Probe Scopes 证据字段。
- `pageSnapshotAdapters.test.ts` 额外覆盖三个专题接口：`/v1/topics/tunnel` 的 `summary/protocols/users`、`/v1/topics/exfil` 的 `summary/top_sources/risk_types/paths`、`/v1/topics/apt` 的 `summary/campaigns/phase_distribution`。
- `product-navigation.spec.ts`：除基础路由巡检外，校验 `/topics` 作为单个“专题面板”菜单页渲染三种专题状态，并校验旧 `/topics/apt` 深链重定向到 `/topics?topic=apt`；同时覆盖 `/dashboard`、`/missing/not-found`、`/probes`、`/data-quality`、`/alerts`、`/alerts/:alertId`、`/campaigns/:campaignId`、`/assets`、`/graph`、`/fusion`、`/baselines`、`/campaigns`、`/attack-chains`、`/encrypted-traffic`、`/forensics`、`/rules`、`/deployments`、`/models`、`/mlops`、`/playbooks`、`/whitelist`、`/compliance`、`/audit-log`、`/notifications` 与 `/settings` 渲染专用工作台关键模块。

K8s YAML 通过：

```text
env -u HTTP_PROXY -u HTTPS_PROXY -u ALL_PROXY -u http_proxy -u https_proxy -u all_proxy kubectl apply --dry-run=client -f web/ui/deployments/kubernetes/deployment.yaml
```

浏览器巡检通过：

```text
routes=/dashboard,/alerts,/screen,/settings failures=0
```

专用页面截图巡检通过：

```text
/assets nonWarningEvents=[] eventCount=17
/alert-detail events=[] checks={summaryFacts:12,metrics:6,assetCards:2,timelineRows:5,stageNodes:5,responseActions:5,evidenceRows:6,gridBottom:1011,overflowX:false,overflowY:false}
/graph failures=[] hasGraph=true
/fusion failures=[] hasFusion=true
/baselines failures=[] hasBaselines=true
/probes events=[] checks={kpis:8,topologyNodes:8,tableRows:7,detail:true,actions:true,heartbeat:true,shellBottom:1036,footerTop:1080,overflowX:false}
/data-quality events=[] checks={score:92,kpis:7,topics:7,heatCells:72,modules:10,shellBottom:1030,lowerBottom:1023,overflowX:false}
/campaigns events=[] checks={title:1,kpis:6,riskDistribution:1,attack:1}
/campaign-detail events=[] checks={profileFacts:10,metrics:6,phaseCards:7,phaseDots:7,alertRows:5,impactTabs:6,topAssets:5,evidenceChecks:6,evidenceSummaryRows:5,responseSteps:6,actionRows:5,reviewRows:6,gridBottom:1029,footerTop:1036,overflowX:false,overflowY:false}
/attack-chains events=[] checks={title:1,columns:6,matrix:1,evidence:1,recommendations:1}
/encrypted-traffic events=[] checks={kpis:7,links:true,exports:true,exportBottom:962,evidenceBottom:1002,footerTop:1036,overflowX:false}
/forensics events=[] checks={stateNodes:6,taskRows:5,session:true,pcap:true,hash:true,audit:true,shellBottom:999,footerTop:1080,overflowX:false}
/rules events=[] checks={kpis:6,rows:7,dsl:true,lifecycle:6,modules:12,shellBottom:1042,bottomBottom:1035,overflowX:false}
/deployments events=[] checks={kpis:6,rows:7,gray:true,health:6,evidence:7,modules:6,shellBottom:981,bottomBottom:975,overflowX:false}
/models events=[] checks={kpis:6,rows:8,champion:true,datasetRows:4,metrics:7,features:6,reviews:4,actions:["激活到线上","停用模型","回滚到 v1.8.0"],modules:6,shellBottom:1019,workbenchBottom:1013,overflowX:false}
/mlops events=[] checks={kpis:6,dagNodes:8,feedbackRows:8,tableRows:6,gateRows:6,packages:4,loopMetrics:5,modules:6,shellBottom:935,workbenchBottom:929,footerTop:1080,overflowX:false}
/playbooks events=[] checks={kpis:6,panels:7,rows:5,flowNodes:8,policyControls:11,riskRows:7,historyRows:5,effectCards:4,auditRows:4,shellBottom:923,workbenchBottom:917,footerTop:1080,overflowX:false}
/whitelist events=[] checks={kpis:6,panels:7,rows:5,approvalSteps:5,expiryRows:4,feedbackRows:3,impactRows:5,heatCells:20,shellBottom:939,workbenchBottom:933,footerTop:1036,overflowX:false}
/compliance events=[] checks={kpis:6,panels:6,gateRows:7,indicatorRows:6,evidenceRows:5,gapRows:5,batchRows:5,trendCards:4,shellBottom:979,workbenchBottom:973,footerTop:1036,overflowX:false}
/audit-log events=[] checks={kpis:6,tableRows:8,diffRows:6,chainNodes:6,riskRows:5,retentionRows:4,timelineRows:5,exportControls:6,shellBottom:951,workbenchBottom:945,footerTop:1036,overflowX:false}
/notifications events=[] checks={kpis:6,channelCards:6,ruleRows:6,builderLabels:6,escalationSteps:5,templateRows:4,historyRows:5,silenceRows:3,shellBottom:947,workbenchBottom:941,footerTop:1036,overflowX:false}
/settings events=[] checks={kpis:6,tenantRows:11,rbacRows:5,tokenRows:5,retentionRows:6,integrationRows:7,securityParams:10,loopActions:9,tokenFooterVisible:true,shellBottom:1001,workbenchBottom:995,footerTop:1036,overflowX:false}
/topics events=[] checks={h1:"专题面板",activeItem:"专题面板",secondaryItems:["仪表盘","态势大屏","专题面板"],topicButtons:3,kpis:6,signals:4,lanes:5,actions:4,evidence:6,overflowX:false,overflowY:false}
/topics?topic=apt events=[] checks={h1:"专题面板",activeTopic:"APT 战役专题",signals:["战役态势信号","APT 战役阶段线","APT 战役线索"],overflowX:false}
/topics/apt redirect=/topics?topic=apt events=[] checks={h1:"专题面板",activeItem:"专题面板"}
/missing/not-found events=[] checks={h1:1,facts:4,returnLinks:3,returnButtons:1,entries:4,statusRows:4,auxActions:3,mainBottom:1020,railBottom:1020,footerTop:1036,overflowX:false,overflowY:false,bodyTextHasPath:false,bodyTextHasStack:false}
```

本轮补充验证结论：

```text
生产态不再使用本地合成快照；mock 仅在 VITE_USE_MOCK=true 时启用。
真实 API 主接口失败会暴露为页面错误态，不作为成功验收。
```

截图证据：

- `/tmp/traffic-ui-rebuild-dashboard-fixed.png`
- `/tmp/traffic-ui-rebuild-screen-fixed2.png`
- `/tmp/traffic-ui-rebuild-alerts.png`
- `/tmp/traffic-ui-rebuild-settings.png`
- `/tmp/traffic-ui-dashboard-ops-20260625-auth.png`
- `/tmp/traffic-ui-probes-management-20260625-auth.png`
- `/tmp/traffic-ui-data-quality-20260625-auth.png`
- `/tmp/traffic-ui-alert-triage-20260625-auth.png`
- `/tmp/traffic-ui-alert-detail-20260626-auth.png`
- `/tmp/traffic-ui-asset-inventory-20260625-auth.png`
- `/tmp/traffic-ui-graph-entity-20260625-auth.png`
- `/tmp/traffic-ui-fusion-workbench-20260625-auth.png`
- `/tmp/traffic-ui-baselines-workbench-20260625-auth.png`
- `/tmp/traffic-ui-campaigns-workbench-20260625-auth.png`
- `/tmp/traffic-ui-campaign-detail-20260626-auth.png`
- `/tmp/traffic-ui-attack-chains-analysis-20260625-auth.png`
- `/tmp/traffic-ui-encrypted-traffic-20260625-auth.png`
- `/tmp/traffic-ui-forensics-workbench-20260625-auth.png`
- `/tmp/traffic-ui-rules-management-20260625-auth.png`
- `/tmp/traffic-ui-deployments-management-20260625-auth.png`
- `/tmp/traffic-ui-models-management-20260625-auth.png`
- `/tmp/traffic-ui-mlops-orchestration-20260625-auth.png`
- `/tmp/traffic-ui-playbooks-automation-20260625-auth.png`
- `/tmp/traffic-ui-whitelist-governance-20260625-auth.png`
- `/tmp/traffic-ui-compliance-audit-20260625-auth.png`
- `/tmp/traffic-ui-audit-log-20260625-auth.png`
- `/tmp/traffic-ui-notifications-20260625-auth.png`
- `/tmp/traffic-ui-settings-governance-20260625-auth.png`
- `/tmp/traffic-ui-not-found-20260626-auth.png`
- `/tmp/traffic-ui-not-found-compare-20260626.png`
- `design-qa.md`：`/probes`、`/data-quality`、`/topics` 专题面板、`/campaigns`、`/campaigns/:campaignId`、`/attack-chains`、`/encrypted-traffic`、`/forensics`、`/rules`、`/deployments`、`/models`、`/mlops`、`/playbooks`、`/whitelist`、`/compliance`、`/audit-log`、`/notifications`、`/settings`、`/not-found` 与 `/alerts/:alertId` 参考图/4K 背景对照 QA，`final result: passed`。

## 6. 后续未完成边界

- 目前是可运行的新脚手架与全菜单骨架，不是 163 张 UI suite 的逐页像素级复刻。
- 生产真实 API 已有页面级 endpoint plan，但仍需继续把每个页面的业务字段、筛选条件、详情抽屉和写操作逐项绑定到后端契约。
- 路由权限已完成前端首轮门禁，真实 APISIX API + Playwright 权限矩阵、合法登录态 WebSocket 自动连接可视巡检和 `/screen` 只读 token 首轮矩阵均已补证据；`/screen` 脱敏公开模式和 WebSocket 授权后连接已在前端落地；沙箱内常驻 preview 浏览器证据仍需专项闭环。
- 构建分包已完成首轮治理；后续如继续加入大型图谱、3D、编辑器或低代码依赖，应维持路由级懒加载和 500 kB 默认 chunk 预算。
- `npm install` 报 npm audit 漏洞和 Node 20.10 低于部分间接依赖 engine 声明，后续应单独做依赖治理。
