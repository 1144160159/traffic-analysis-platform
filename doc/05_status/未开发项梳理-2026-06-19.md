# doc 未开发项梳理（2026-06-19）

## 1. 梳理口径

本文件基于 `doc/` 当前主线文档、归档文档、`agent.md` 当前状态摘要，以及代码目录抽样反查形成。更细的源码实证校正见 `代码实证状态核对-2026-06-19.md`。状态分为四类：

| 状态 | 判定口径 |
| --- | --- |
| 未开发 | 文档有明确能力要求，但代码中没有可运行服务、页面、路由、Topic、Manifest 或工程脚本 |
| 已开发但未闭环 | 代码已有主体能力，但缺生产安全、验收报告、专项压测、第三方数据包、演练或证据包 |
| 旧文档待更新 | 旧文档仍写“待开发”，但当前代码或 `agent.md` 已显示能力已补齐 |
| 非当前主线 | 仅在归档草案中出现，未进入当前课题一验收主线 |

当前最重要的结论：2026-06-15 旧系统计划已经合并到本文件、`代码实证状态核对-2026-06-19.md` 和主设计稿，不再作为当前未开发清单；`doc/01_design/课题一产品与技术总体设计.md`、`doc/02_acceptance/README.md`、`doc/03_review/专家深评整改清单.md` 和 `agent.md` 才是当前判断主线。

最新校正（2026-06-30）：应用/Probe/codex-loop digest rollout 与 Secret-backed `POSTGRES_DSN` 同步后，业务流 API preflight 已推进到 `20260629-business-flow-api-preflight-r20-model-version-state`，为 `46/46` 通过；SOAR Playbook 状态机 `20260629-playbook-state-machine-r1` 为 `21/21` 通过，Forensics 任务状态机 `20260629-forensics-task-state-machine-r1` 为 `7/7` 通过，规则状态机 `20260629-rule-state-machine-r5-model-version-state` 为 `15/15` 通过，部署状态机 `20260629-deployment-state-machine-r3-model-version-state` 为 `25/25` 通过，模型版本状态机 `20260629-model-version-state-machine-r1` 为 `20/20` 通过，SNMP/LLDP 主动资产发现 `20260629-asset-discovery-r2-report` 为 `6/6` 通过；UI contract 最新 r8 `20260629-ui-contract-preflight-r8-current-desktop-bridge-transport` 为 `blocked`，repo/UI/API contract 19/21 通过，唯一阻塞仍是当前 Desktop Chrome bridge `Transport closed`；Kafka security rollout preflight `20260629-kafka-security-rollout-preflight-r2-scram-seeded` 为 `blocked` 但 `7/9` 通过且 `prereq_blockers=0`；production ExternalSecret reconciliation `20260629-external-secret-production-reconciliation-r1` 为 `10/10` 通过，`13/13` 生产 ExternalSecrets Ready；release manifest `20260630-release-manifest-r50-prod-externalsecret` 为 `12/12` 通过；production security preflight `20260630-production-security-preflight-r44-prod-externalsecret` 仍为 `blocked`，`14/20` 通过、2 blockers、4 warnings；ESO CRD=1、ready controller pods=3、live ExternalSecrets 14/14 Ready、Secret reconciliation blocker 为 0；deployment preflight `20260630-deployment-preflight-r50-prod-externalsecret` 为 `pass` 且 `16/16`、0 warnings，release package 为 127 files，HA readiness `20260629-ha-readiness-preflight-r5-pdb-selector` 仍 blocked 但 Flink 9/9、MinIO health、PVC/PV 漂移和 PDB selector 漂移已恢复；live workload 未 digest 镜像已从旧记录的 13 个收敛到 0；`doc/02_acceptance/08-third-party/` 已新增试点/第三方材料模板包。本文件下方历史长表中若仍出现 13 个未 pin 镜像、旧 Pending PVC/PV drift warning、旧生产 ExternalSecret missing 或旧版 SNMP/LLDP 状态描述，以 `live-digest-secret-docsync-2026-06-29.md` 和 `doc/02_acceptance/*-latest` 为准。

## 2. 未开发或未工程闭环总表

| 优先级 | 功能/能力 | 文档来源 | 代码核对结论 | 当前判定 | 下一步 |
| --- | --- | --- | --- | --- | --- |
| P0 | 10 x 100Gbps 多口线速采集、512Mpps 小包能力 | `01_design` GATE-P0-03/04、`02_acceptance` | Rust 探针已有 AF_XDP/AF_PACKET/PCAP、压力测试报告痕迹；2026-06-29 已新增 `tests/perf/100g_capture/`、`live_capture_performance_preflight.sh`、结果 schema 和 `doc/02_acceptance/03-performance/` 稳定证据 | 已开发性能验收包/预检，但未验收闭环 | 当前 r1 `blocked`：10/17 checks passed，缺 `hardware-inventory.yaml`、`traffic-profile.yaml`、`results/10x100g-summary.json`、`results/512mpps-summary.json`；500k stress 仅为 0.94Mpps/1.3Gbps 级别，live 探针为 `af_packet`/2 cores 小 profile。继续补 TRex/pktgen 硬件窗口、NIC/NUMA、丢包率、Kafka lag、Flink backpressure、资源水位和签名报告，使预检从 `blocked` 变为 `pass` |
| P0 | 端到端 P95 <= 60s 验收链 | `01_design` GATE-P0-05、`03_review` QA-02 | Dashboard/SituationalScreen 有 P95 展示字段；2026-06-29 已新增并闭环 `/api/v1/data-quality/latency-chain`、`tests/e2e/live_latency_chain_report.sh` 和 `doc/02_acceptance/runs/20260629-latency-chain/`，可复核 `event_ts/ingest_ts/kafka_ts/flink_out_ts/api_seen_ts/ui_seen_ts` 及分段 P50/P90/P95/P99；最终 r15 3 分钟 live 报告 `result=pass`、`full_chain_closed=true`、0 gaps、0 command failures | 已验收闭环 | 纳入 release manifest；继续观察 Feature/Session ratio warn，不把本报告外推为 10 x 100Gbps、512Mpps 或第三方检测质量结论 |
| P0 | 95% 检出率、5% 误报率、未知攻击召回的第三方盲测包 | `01_design` GATE-P0-06、`03_review` 算法整改 | MLOps 有训练、评估、SHAP、注册、热更新脚本；2026-06-29 已新增 `mlops/eval_packages/topic1_blind/` 契约、`mlops/scripts/evaluate_blind_package.py` 指标计算器和 `tests/e2e/live_detection_quality_preflight.sh` 预检，稳定证据写入 `doc/02_acceptance/04-detection-quality/` | 已开发盲测包契约/预检，但未验收闭环 | 当前 r1 `blocked`：缺冻结 `dataset-manifest.yaml`、阈值锁 `threshold-lock.json`、真实 `labels.csv`、真实 `predictions.csv` 和第三方签认；补齐后由脚本输出混淆矩阵、FPR/FNR、unknown recall、95% CI 和阈值锁定证据 |
| P0 | Kafka TLS/SASL/ACL、ExternalSecret、生产 Secret 治理 | `01_design` GATE-P0-07、`03_review` CTO/IMPL/QA | 生产 K8s 清单 Kafka PLAINTEXT 扫描已为 0；repo 镜像 evidence lock、repo mutable/latest、repo Service 非业务外部端口均为 0；live workload 未 digest 镜像已清零。2026-06-29 已新增 `deployments/kubernetes/security/external-secrets-template.yaml`、`tests/e2e/live_production_security_preflight.sh` 和 `tests/e2e/live_kafka_security_rollout_preflight.sh`；Kafka rollout preflight r2 证明 Secret/TLS material 齐全、TLS 可解析、SCRAM client/broker 用户已种入 live plaintext Kafka、`prereq_blockers=0`，剩余 rollout blockers 是 live ACL authorizer disabled 和 live listener plaintext。production ExternalSecret reconciliation r1 已通过 `ClusterSecretStore/traffic-platform-secret-store` 将 13 个生产 ExternalSecrets 同步为 Ready，58 个期望 key 源/目标一致且证据不含 secret value。production security r44 当前仍 blocked：20 个 live NetworkPolicy 对象已创建但 enforcement-capable CNI 为 0，live Kafka listener 仍 plaintext；生产 ExternalSecret blocker 和 aggregate Secret reconciliation blocker 已清零。Kafka repo profile 已切到 `SASL_SSL` + `SCRAM-SHA-512` + TLS + ACL，覆盖 broker/init job、ACL 授权、Go Kafka producer/consumer/DLQ、Flink source/sink、Go/Flink K8s env/mount、ExternalSecret、site-values、deploy.sh TLS/SCRAM 种子和 preflight；live Secret/TLS readiness 已由 deployment preflight r50 证明 required Secret references 15 个齐备，且 live workload digest pin 缺口/Pending PVC/repo lock 缺口均为 0；Kafka StatefulSet repo/live 镜像已 pin 到 `docker.io/apache/kafka@sha256:32217f809d3fba75ee46e93b1fdbea4aacc0821139443efb73691a913948c31a`，partitioned rollout 后 3 brokers ready、topic list 29、业务流 API r20 46/46 passed，Kafka PDB selector 已对齐 live Pod | repo profile、Secret/TLS readiness、Kafka SCRAM rollout prerequisites、生产 ExternalSecret reconciliation、repo 镜像 lock、repo Service 暴露面、live workload digest pin 和 Kafka PDB readiness 已闭环，整体仍未生产化闭环 | 维护窗口中滚动 Kafka SASL_SSL/SCRAM/TLS/ACL 与所有生产者消费者并启用 ACL authorizer；按现场要求替换/接入企业外部 Vault 或 Secret 轮转策略；继续处理 NetworkPolicy enforcement-capable CNI 与负例、镜像签名/准入、安全负例和 secret scan 门禁；preflight 必须从 `blocked` 变为 `pass` 才能声明生产安全 |
| P0 | NetworkPolicy 与攻击面收敛 | `01_design` GATE-P0-10、`03_review` SUB-IMPL-05 | 2026-06-29 已新增 `deployments/kubernetes/security/00-network-policies.yaml`，默认拒绝和 APISIX 业务入口白名单 profile client dry-run 通过；repo Service profile 已由 `tests/e2e/k8s_service_exposure.py` 证明仅 APISIX 业务端口外露；live preflight r44 仍 blocked：20 个 live NetworkPolicy 对象已创建，但 CNI 为 Flannel-only、enforcement-capable CNI 为 0；live Service 已收敛为仅 APISIX 业务端口外露 | Service 暴露面已闭环，NetworkPolicy enforcement 未闭环 | 维护窗口中接入或切换 enforcement-capable CNI 后执行默认拒绝/白名单负例连通性测试，补管理面 VPN/堡垒机入口 |
| P0 | 路由权限 Manifest、`/screen` 边界、启动态鉴权 | `03_review` SUB-FS-01/02/03、SUB-UI-01/02 | 新 `web/ui` 已有 routeManifest 权限元数据、`/v1/auth/me` 启动态会话校验、菜单 scope 过滤、403 证据页、真实登录响应与 captcha 适配；`/screen` 已作为受保护只读路由登记，并支持显式 `SCREEN_ACCESS_MODE=masked-demo` 脱敏公开模式；WebSocket 前端已改为授权后仅在受保护 AppShell 且有 token 时连接，并在退出/卸载时断开；2026-06-29 已新增 alert-service `/ws/events` 后端鉴权、APISIX `/ws*` WebSocket 路由、Secret-backed auth PG 配置、live 401/403/ready 正例和 Desktop Chrome 登录门禁证据，见 `doc/02_acceptance/runs/20260629-realtime-ws-auth/`；同日新增 `tests/e2e/live_permission_matrix.sh`、`tests/e2e/live_realtime_appshell.sh`、`tests/e2e/live_screen_readonly_matrix.sh` 和 `tests/e2e/live_token_lifecycle_matrix.sh`，已覆盖权限矩阵、实时通道、大屏只读 token、API token 生命周期和审计负例；`tests/e2e/live_ui_contract_preflight.sh` 历史 r5 证明设计菜单 6 组/24 路由、routeManifest、UI suite、APISIX `/login` GET、route/access Vitest、Codex Desktop Chrome wrapper 登录页 smoke 和合法登录态 `/dashboard -> /alerts` 业务页点击 21/21 通过；最新 r8 因当前 Desktop Chrome bridge `Transport closed` blocked，但 repo/UI/API contract 为 19/21 passed；`tests/e2e/live_business_flow_api_preflight.sh` 最新 r20 证明 46/46 业务流 API 经 APISIX 通过，`/api/v1/graph/explore` 返回 200 | API 契约、token 生命周期负例和权限边界已闭环；最新 Desktop Chrome UI contract 受桥接传输阻塞 | 恢复 Codex Desktop Chrome bridge 后扩展更多业务页 Desktop 视觉巡检 |
| P0 | 基线冻结与可复现发布证据包 | `01_design` GATE-P0-01/09、`03_review` PM/IMPL | 2026-06-29 已新增 `tests/e2e/live_release_manifest.sh`；最新 r50 `20260630-release-manifest-r50-prod-externalsecret` 为 12/12 checks passed；稳定副本 `doc/02_acceptance/00-baseline/release-manifest-latest.json` 绑定 commit、dirty status、124 个 source file hashes、123 个 core K8s dry-run objects、58 个 workloads、146 个 pod image IDs、repo/live Topic、模型/规则/部署 API 目录、461 个历史证据索引、production-security r44、production ExternalSecret reconciliation r1 和 Kafka security rollout preflight r2；同日新增并更新 `deployments/kubernetes/site-values.template.yaml`、`deployments/kubernetes/image-digests.lock.json`、`tests/e2e/image_digest_lock.py`、`tests/e2e/k8s_service_exposure.py`、`tests/e2e/live_deployment_preflight.sh` 和 `doc/02_acceptance/runs/20260630-deployment-preflight-r50-prod-externalsecret/`，最新 r50 覆盖 core manifest dry-run 123 objects、release package 127 files、repo 镜像 lock 缺口 0、repo Service 非业务外部端口 0、节点/StorageClass/workload/APISIX 入口，并证明 required Secret references 15 个齐备、live workload digest pin 缺口 0、Pending PVC 0 | 已开发 site-values/preflight/package manifest、repo 镜像 lock 与 repo Service 暴露面，但未完全验收闭环 | 当前 deployment preflight 已 pass 且 16/16、0 warnings。继续补 Kafka live TLS/SASL rollout、NetworkPolicy/攻击面、生产安全 blockers 清零和签发版 release package；不能外推为 10 x 100Gbps、512Mpps、第三方检测质量或 HA 验收 |
| P0 | HA 故障恢复演练和 RTO/RPO | `01_design` GATE-P0-08、`03_review` SUB-QA/SUB-IMPL | 2026-06-29 已新增 `tests/chaos/live_ha_readiness_preflight.sh`、`tests/chaos/ha_drill_plan.yaml` 和 `doc/02_acceptance/runs/20260629-ha-readiness-preflight/`；r5 结果 `blocked` 但 readiness 12/13 通过、0 warnings：Kafka ISR、Flink 9 job checkpoint/异常、CH 复制、PG 2 replicas、Redis Sentinel、MinIO cluster-local health、APISIX 和关键 PDB 均正常；本轮恢复了缺失的 Flink 作业、清理旧 Redis Pending PVC/Released PV drift，并修复 Kafka/Redis PDB selector 漂移 | 已开发 readiness，但未验收闭环 | 在维护窗口按 `ha_drill_plan.yaml` 执行 broker/Flink TM/CH replica/PG/MinIO 破坏性故障注入，输出 RTO/RPO、数据一致性、恢复时间和操作者审批记录 |
| P0 | DLQ/replay/幂等恢复 | `01_design` P0 整改、`03_review` QA-06 | `dlq.v1` Topic、DeadLetter 契约、Go DLQ Producer、Flink DLQ Sink、ingest fallback replay 已存在；2026-06-28/29 已新增 `POST /api/v1/dlq/replay/fallback` 受保护 API、`dlq:replay` 权限、人工修复/双人审批、Redis-backed 幂等 key、审计轨迹、fail-closed 单测和 Web UI `data-quality` 重放对账 action contract；Web UI 镜像 `traffic/web-ui:dlq-contract-20260628-r1`、ingest-gateway 镜像 `traffic/ingest-gateway:dlq-replay-20260628-r2` 已滚动到 K8s，APISIX `/api/v1/dlq* -> ingest-gateway:8080` 已生效；live 已通过未授权 401、`dry_run=true` 200、重复 idempotency key 返回 `duplicate:true` 和临时 token 清理验证，证据见 `doc/02_acceptance/runs/20260628-dlq-replay-control/`；2026-06-29 继续将 Web UI action contract 提升为可操作 dry-run modal，滚动 `traffic/web-ui:dq-dlq-dryrun-20260629-r2` 到 live，定向 Vitest 13/13、Web build、K8s rollout、APISIX 200、live bundle marker 和 Desktop Chrome 登录页桥接证据均已归档到 `doc/02_acceptance/runs/20260629-data-quality-dlq-dryrun/`；同日修复 replay Kafka writer topic 双重指定问题，滚动 `traffic/ingest-gateway:dlq-replay-recovery-20260629-r1`，新增 `tests/e2e/live_dlq_replay_recovery.sh` 并完成真实 fallback 文件非 dry-run 重放、Kafka `dlq.v1` 消费核对、跨 Pod Redis 幂等 `duplicate:true`、fallback 文件行为和 token/Redis/端口清理验证，证据见 `doc/02_acceptance/runs/20260629-dlq-replay-recovery/`；同日继续修复 partial replay 剩余文件统计，滚动 `traffic/ingest-gateway:dlq-replay-failure-20260629-r2`，新增 `tests/e2e/live_dlq_replay_failure_regression.sh` 并完成无效 fallback payload 的 partial 失败样本回归、文件隔离保留、重复幂等和清理验证，证据见 `doc/02_acceptance/runs/20260629-dlq-replay-failure-regression/`；同日将 Flink Session 输入解析失败从静默过滤改为 `DeadLetter` side output，滚动 `flink-session-job-parse-dlq-20260629-r2.jar`，新增 `tests/e2e/live_kafka_flink_bad_message_dlq.sh` 并完成 live 坏 `flow.events.v1` 注入、checkpoint 前进和 `dlq.v1` protobuf DeadLetter 字段级核对，证据见 `doc/02_acceptance/runs/20260629-kafka-flink-bad-message-dlq/`；同日新增 ingest-gateway 用户 access JWT replay validator、注入 `JWT_SIGNING_KEY`，滚动 `traffic/ingest-gateway:dq-dlq-jwt-20260629-r1`，`tests/e2e/live_data_quality_dlq_business.sh` 完成合法 JWT 业务页 modal dry-run 4/4，证据见 `doc/02_acceptance/runs/20260629-data-quality-dlq-business/`；Desktop Chrome 合法登录态业务页点击已有 `/alerts` P0 smoke 证据 | 已开发并完成核心闭环 | 后续仅扩展 Desktop 已登录会话多页巡检 |
| P1 | Threat Intel 独立服务和 `threat.intel.v1` 数据源闭环 | 归档 `数据采集分工设计.md`、`01_design` 多源融合 | 2026-06-29 已新增 `cmd/threat-intel-service`、PostgreSQL schema/upsert/list/import/lookup/enrich API、`threat_intel_feeds` 订阅源表、scheduled feed scheduler、`threat.intel.v1` Topic 清单和 live Topic、K8s Deployment、APISIX `/api/v1/threat-intel*` 路由；`tests/e2e/live_threat_intel_service.sh` r6 47/47 passed，含匿名 401、过期 token 401、只读 lookup 200、只读写入 403、tenant-scoped upsert/lookup/list/import、跨租户 lookup/list 负例、upsert/import/scheduled feed 响应与状态证据、同步 `audit_logs` 落库和 `threat.intel.v1` 事件消费；`tests/e2e/live_fusion_threat_intel_contract.sh` 已推进到 `20260629-fusion-write-r2`，29/29 passed，证明 Fusion 页面契约消费 Threat Intel 服务，且冲突处理/规则编辑经 APISIX 写入 `fusion_conflict_resolutions`、`fusion_rule_overrides` 和 `audit_logs`，viewer 写入 403，live Web UI 已滚动 `traffic/web-ui:fusion-write-20260629-r1` 并通过 bundle marker 验证 | 已开发服务化 MVP，Fusion 读链路、写链路、订阅源调度、租户隔离、导入审计和 Kafka publish/consume 已闭环 | 当前 evidence 证明独立服务 API、基础 RBAC、跨租户隔离、Fusion 读写链路、真实 PG/K8s/APISIX/Kafka Topic/Web UI、scheduled feed、同步审计和事件发布消费可用；后续转入 SNMP/LLDP、融合价值量化和试点材料 |
| P1 | SNMP/LLDP 主动资产发现 | 归档 `数据采集分工设计.md`、MLOps 业务文档 | 2026-06-29 已新增 asset-service 主动发现控制面：`/api/v1/assets/discovery/credentials` 只登记 `k8s://...` SecretRef，`/api/v1/assets/discovery/runs` 接收 SNMP/LLDP 观测并写入 `asset_discovery_runs`、`assets` 和 `asset_topology_links`，`/api/v1/assets/discovery/neighbors` 查询 LLDP 拓扑；K8s PostgreSQL init schema、asset-service digest 镜像和 UI API plan/route manifest 已同步；`tests/e2e/live_asset_discovery_preflight.sh` `20260629-asset-discovery-r2-report` 为 6/6 passed | 已开发控制面/拓扑入库 MVP，但未完成真实网络扫描器和现场覆盖率验收 | 继续补 SNMP v2/v3/LLDP 实际扫描 worker、周期调度、RBAC 审计细化、页面联动和真实交换机/园区网覆盖率报告；当前不能外推为全网自动发现率验收 |
| P1 | 多源融合价值量化 | `01_design` P1、`03_review` 产品/算法 | 2026-06-30 已补 `GET /api/v1/fusion/value-report`、`fusion-value-ablation-v1`、单源基线/多源当前/delta/quality gates/evidence 响应，前端 `/fusion` 质量看板映射检出提前量、误报下降和 MTTR 下降，UI suite 契约已同步；同日新增 `tests/e2e/live_fusion_value_report_preflight.sh` 作为 live APISIX/JWT/K8s 结构证据门；alert-service 已滚动到 `docker.io/traffic/alert-service@sha256:89d2e52463f7bcadf103b8c6274dd5559570fc7a2ff0da7103c1faf9d83e0569`，r2 live preflight `19/19` passed | 已开发价值量化 MVP，并完成 live 结构门；未完成真实价值验收闭环 | 补冻结真实样本窗口、只流量/流量+资产/流量+日志+用户行为消融结果、检出提前量、误报下降、定位时间下降、试点签认和可交付报告模板 |
| P1 | 态势感知大屏概念 UI 产品化落地 | `04_assets/generated/situation_screen_concept_chatgpt_20260619.png`、`01_design` 第 4.3/4.4 章、`03_review` UI-10/SUB-UI-01 | `/screen`、`SituationalScreen`、`ScreenLayout`、`screen.css` 等基础大屏已存在，但未按概念图形成园区数字孪生、采集链路、告警研判、PCAP 取证、资产上下文、响应动作、反馈学习的一屏闭环；4K/拼接屏、演示模式、指标口径和只读脱敏边界未闭环 | 已开发但未闭环 | 以概念图为 UI 目标重构 `/screen`：真实 API 指标、4K/2K/1080p 自适应、只读 token/脱敏演示、自动轮播、指标解释和 Playwright 大屏巡检 |
| P1 | UI 闭环动作的验收状态机 | `01_design` P1、`03_review` UI | Alert/Forensics/Rule/Deployment/MLOps/DataQuality/Playbook 页面和部分动作已存在；2026-06-29 已先修正告警状态枚举漂移，补 `new/triage/assigned/closed` canonical 状态机、历史/展示别名归一、`alert:write` 权限门禁、关闭原因必填、route manifest 状态机 API hints，并滚动 live 镜像验证 read-only 403、缺原因 400、别名 `resolved` 进入服务层和 Desktop Chrome 登录门禁；同日追加单条 `/status` 与批量状态更新 `state_version` 乐观锁、同步审计、真实告警 `new -> closed -> reopen`、FP 反馈生成白名单草案跳转、viewer 写拒绝和权限矩阵证据；本轮已补 Playbook 手动执行状态机，`tests/e2e/live_playbook_state_machine.sh` r1 为 21/21 passed；同日新增 Forensics PCAP 取消 action contract、`PCAP_CANCEL` 审计事件和 `tests/e2e/live_forensics_task_state_machine.sh`，r1 为 7/7 passed；同日已补规则启停、部署灰度/激活/暂停/恢复/回滚、模型版本注册/激活/弃用状态机，最新证据分别为 rule r5 `15/15`、deployment r3 `25/25`、model-version r1 `20/20` | Alert、DataQuality、Playbook、Forensics、Rule、Deployment、MLOps/Model Version 均已有 live 状态机/动作证据 | 保持 DataQuality replay/DLQ、规则、部署和模型版本状态机回归；后续 UI 闭环工作转向更多 Desktop 登录态业务页视觉巡检、MLOps 盲测质量证据和试点演示材料 |
| P1 | 试点材料、售前脚本、经济效益模板 | `01_design` 第 7/8/11 章、`03_review` PM/销售 | 2026-06-29 已新增 `doc/02_acceptance/08-third-party/` 试点/第三方材料包，包含 `pilot-package-manifest.json`、部署证明、演示脚本、周报、经济效益、用户确认和成果转化索引模板 | 已开发模板包，但未真实试点闭环 | 用真实试点数据填充模板，补用户盖章确认、第三方报告、经济效益签认和可复测演示数据集 |

## 3. 旧文档误报或已补齐项

以下条目仍在历史文档中出现“待开发/待修复”，但从当前代码和 `agent.md` 看，不能再直接列为未开发：

| 历史条目 | 历史来源 | 当前核对 |
| --- | --- | --- |
| Web UI 真实 API 对接阶段 1-4 | 2026-06-15 旧系统计划 | `web/ui/src/pages` 已包含 Dashboard、Alert、Graph、Rule、Campaign、Forensics、Probe、Whitelist、Deployment、Audit、Fusion、Compliance 等页面；`agent.md` 记录真实 API/Chrome 全路由巡检通过 |
| 新增 6 页面 | 2026-06-15 旧系统计划 | `AssetInventory`、`NotificationSettings`、`PlaybookManagement`、`ModelManagement`、`DataQualityPage`、`MLOpsOrchestration` 已存在 |
| EvidenceCard 下载 TODO | 2026-06-15 旧系统计划 | `EvidenceCard.tsx` 已调用 `forensicsApi.downloadPcapByKey`；`ForensicsPage.tsx` 已有 completed 下载；`agent.md` 记录 PCAP completed 下载真实链路通过 |
| Asset Service / Asset Proto / Ingest MAC 提取 | 归档 `数据采集分工设计.md` | `cmd/asset-service`、`proto/traffic/v1/asset.proto`、`asset.bindings.v1`、asset API 和页面已存在 |
| Flink Log Job / User Behavior Job | 归档 `数据采集分工设计.md` | `flink-log-job`、`flink-user-behavior-job`、`device.logs.v1`、`user.events.v1` 已存在；agent 记录 9 个 live 业务作业 RUNNING |
| MLOps 手动重训、模型注册/激活 | 旧 P1/P2 计划 | `mlops`、Argo Events、`model-updates`、Go orchestrator、Flink hot update 已有；缺的是第三方质量和生产门禁，不是主体功能 |
| PCAP 跨租户保护/预签名/审计基础 | 旧取证缺口 | `forensics` handler 已有路径遍历防护、跨租户拒绝、presign 和 audit 调用；2026-06-29 已补 hash 必填/下载校验、签名 URL 过期上限、路径和跨租户负例、同步审计可查与浏览器证据，见 `doc/02_acceptance/runs/20260629-pcap-forensics-integrity/`；同日已补 PCAP 取消状态机和 `PCAP_CANCEL` 同步审计，见 `doc/02_acceptance/runs/20260629-forensics-task-state-machine/` |

## 4. 当前真正的开发排期建议

### P0：验收红线

1. 生产安全 Profile：Kafka TLS/SASL/ACL repo profile、Kafka Secret/TLS/SCRAM rollout prerequisites、Keycloak live TLS/SecretRef profile、production ExternalSecret reconciliation 和 live workload digest pin 已闭环；继续补 Kafka live listener/ACL authorizer rollout、现场企业外部 Vault/Secret 轮转策略、NetworkPolicy enforcement、镜像签名/准入和安全负例门禁。
2. 性能与延迟证据：P95 <= 60s 已有 2026-06-29 3 分钟 live 闭环报告；10 x 100Gbps/512Mpps 已有可复现实验包和预检，仍需补真实硬件窗口清单、流量 profile、结果摘要和原始报告。
3. 检测质量证据：盲测包契约和预检已建立；继续补第三方冻结样本、95%/5%、unknown recall、置信区间、阈值锁定和签认证据，使预检从 `blocked` 变为 `pass`。
4. 权限边界：route manifest、`/auth/me` 启动态鉴权、`/screen` 只读 token/脱敏、WebSocket 后端鉴权和 live 权限矩阵。
5. 基线与演练：release manifest 回归基线、deployment preflight/site-values/package manifest、repo 镜像 evidence lock、APISIX-only Service exposure profile、production ExternalSecret reconciliation、HA readiness preflight 与演练计划均已建立；live Secret/TLS readiness 已闭环到 15/15 ok，live workload digest pin、Pending PVC、旧 PV drift 和 PDB selector 漂移已清零；继续补维护窗口内 HA chaos/RTO-RPO 报告、镜像签名/准入、现场 site-values 实例、Kafka 安全 profile live rollout 和 release package 签发。

### P1：竞争力增强

1. Threat Intel 服务化 MVP、Fusion 读写链路、订阅源调度、租户隔离、导入审计和 `threat.intel.v1` publish/consume 已有 live 证据，Alert 服务已从 `traffic/alert-service:fusion-write-20260629-r1` 继续滚动到 `traffic/alert-service:playbook-state-20260629-r1` 对应 digest，当前 Web UI 已滚动 `traffic/web-ui:desktop-smoke-token-20260629-r3` 并继承 Fusion 写链路契约，Threat Intel 服务已滚动 `traffic/threat-intel-service:threat-intel-20260629-r6`；Fusion 价值量化已补 MVP 和 live APISIX/JWT/K8s 结构门，后续补真实消融样本和试点签认。
2. SNMP/LLDP 主动发现控制面和拓扑入库已有 live MVP 证据；继续补真实扫描 worker、周期调度、页面联动和现场覆盖率。
3. 多源融合消融实验和业务价值指标：value-report MVP 与 live 结构门已补，仍需冻结样本窗口、真实单源/多源消融和签认材料。
4. 态势感知大屏概念 UI 产品化落地：以 `04_assets/generated/situation_screen_concept_chatgpt_20260619.png` 为视觉目标，补齐一屏闭环、真实指标、演示模式和多分辨率巡检。
5. UI 操作闭环状态机与验收 Manifest：Alert、DataQuality、Playbook 和 Forensics 已有 live 状态机/动作证据，继续补 Rule、Deployment、MLOps。
6. 试点交付包、销售演示脚本、经济效益模板已建立模板包；后续补真实试点数据、客户确认和第三方签认。

### P2：工程质量

1. Java Flink backpressure 监控接入真实 Flink Metrics API。
2. ClickHouse sink 外部告警通知。
3. Go/Java/Web/Playwright live matrix 扩展到权限、重放、故障、跨租户拒绝。
4. Grafana 业务指标看板和告警规则专项验收。

## 5. 建议的文档处理

| 文档 | 处理建议 |
| --- | --- |
| `doc/01_design/课题一产品与技术总体设计.md` | 保持主设计入口；把本文件 P0/P1 表合并到第 9 章整改总表 |
| `doc/02_acceptance/README.md` | 增加本文件的“未闭环证据项”作为验收门禁清单 |
| `doc/03_review/专家深评整改清单.md` | 保留角色评审视角；将已开发项标注为“已补齐/待验收” |
| 2026-06-15 旧系统计划 | 已合并删除，不再作为当前待开发基线 |
| `doc/archive/merged_20260619/数据采集分工设计.md` | 仅保留为数据源扩展参考；Threat Intel 已进入服务化 MVP，SNMP/LLDP 已进入主动发现控制面 MVP，真实扫描器和现场覆盖率仍按 P1 增强推进 |

## 6. 一句话结论

当前系统不是“主体功能大量未开发”，而是“主体链路已基本成型，验收红线、生产安全、权限边界、专项压测、第三方检测质量和少数增强数据源还没有工程闭环”。下一阶段应按 P0 红线先补证据和生产化，再继续推进 SNMP/LLDP、多源融合价值量化和试点商业化材料等竞争力增强。
