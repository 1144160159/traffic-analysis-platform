# 全项目代码复查报告(2026-08-19 第二轮)

- 审查范围: 全仓库 8 个域(Go 核心 / Go 服务 / Rust / Java / 前端 TS / K8s+SQL / MLOps / 契约+工具链)
- 审查方式: 8 个独立只读审查代理 + 自研模式扫描;区分"最近改动引入"与"存量"
- 统计: 高危 3(均为存量)+ 中危 20 + 低危 27 + 信息若干
- 本轮修复: 16 项(3 高 13 中低),提交 `934b0ad41` 已推送

## 一、已修复(16 项)

### 高危(6)
| # | 域 | 问题 | 修法 |
|---|---|---|---|
| 1 | Go | notification/evaluator 注册表 map 查找在锁外(运行期注册数据竞争 panic) | 查找移入锁内 |
| 2 | Go | audit logger Close 正常路径静默丢弃残留缓冲审计事件 | done 分支落 backup |
| 3 | Rust | xdp_socket SocketGuard 与 XskSocket 双重持有 fd,失败路径 double-close | guard 在构造成功后立即 release |
| 4 | 契约 | 状态机契约缺 alert_status_in_progress/false_positive 线名,告警被脏修复重置为 new | JSON+Go 别名补齐 |
| 5 | SQL | clickhouse_merged.sql 双 ENGINE 语法错误/自引用 MV/ON CLUSTER 字面量 | 3 处修复 |
| 6 | MLOps | extract_data.py:482 二元组解包崩溃(治理抽取生产路径必挂) | 改调 preprocess_data_with_notes |

### 中/低危(10)
- persistence 落库词表大小写兼容(状态机小写 canonical 读回不再丢状态)
- GetUserSettings 补租户边界校验;httpx body_limit 双 WriteHeader(413 生效)
- authz keyFor 网络 IO 移出锁(refresh 内部自锁)
- replay 幂等锁只覆盖判定,长 I/O 锁外执行 + 最终写入重查
- kafka commitChan 带 ctx/关闭感知;同步提交失败指数退避防忙循环
- Rust start 失败降级路径包 FallbackCapturer(降级事实可观测)
- MatcherFactory.close 置 null 一致;DLQ/ACK 工厂 DEFAULT 单例 readResolve

## 二、暂缓/报告清单(按域)

### Go 核心域
- 【低】authz looksLikeJWT 点计数启发式可误判;【低】shadow 模式默认放行依赖业务层 fail-closed(设计如此,记录)

### Go 服务域
- 【低】kafka dlq 外重试与 Writer MaxAttempts 双重重试叠加(改单一层需重排重试语义);重试 sleep 部分路径仍不感知 ctx

### Rust 域
- 【中】`unsafe impl Sync for XdpCapture`(XskSocket 非原子,需原子化环状态后重新声明)
- 【中】umem get_frame_mut/get_data_mut `&self→&mut` 别名 UB(需句柄化重构)
- 【低】对象池失效、UmemInner Drop 15s 忙等、写失败误计、热路径每帧分配、partitioned_flow_table 疑似死代码

### Java 域
- 【中】Rule.conditions Map<String,Object> 依赖 Kryo 无界类型;广播态无 TTL(tombstone 无限增长);双 sink 无跨 sink 事务且 JDBC 无 DLQ
- 【低】BaseJob 未采用致双套 checkpoint 纪律;TlsFingerprintMatcher 反射查不存在字段恒不命中;keyBy 拼接无转义;BruteForceMatcher 吞端口异常;MatchContext 黑名单死代码

### 前端域(依用户约束"前端不涉及修改",10 项全部只报告未动)
- 【高】KC 令牌 remember 默认 true 恒入 localStorage,"记住登录"对真实凭证无效
- 【中】AppShell MutationObserver 全 shell 布局抖动;AnalysisResourcesPage setInterval 轮询闪烁/乱序
- 【低】渲染期副作用、OidcCallback 无卸载守卫、终态后仍轮询、refetchInterval 魔数、.catch(()=>{}) 吞错、windowFrameState 双实例化等 7 项

### K8s/SQL 域
- 【高-治理】PG/CH 各有三套发散 DDL 权威源(common/sql vs init-jobs vs merged),建议以 common/sql 唯一源收敛;postgres_merged.sql 缺 40 表/多 27 遗留/whitelist 重复建表
- 【中】common/kafka/create-topics.sh 缺 5 个 analysis.* topic;OpenSearch alerts-template 未随 alerts-v2 迁移;credentials.yaml 硬编码 default_auth_token;credentials-secret 示例命令缺 OPENSEARCH 键
- 【低】资源限额缺失、probe privileged 漂移、init 容器 runAsUser:0、nebula README 漂移等

### MLOps 域(已修 1 高)
- 【中】镜像 tag 未 digest 绑定;unknown_recall 门禁口径错配(拒识 vs 检测);legacy SQL 自研转义注入面;空值 Secret 覆盖真实凭证;test_workflow.sh set -e/引号
- 【低】密码明文 argv;git_auto_commit.sh 硬编码绝对路径;注册 payload metrics 未与 manifest 交叉核对

### 契约域(已修 1 高)
- 【中】super_admin 仅独立 role 条目未绑定任何 operation(authorized_roles 判定下全拒)
- 【低】Web 侧无契约一致性门禁;rules/proto.md 示例与真源漂移;proto 文件残留悬空注释

## 三、正面结论(各域代理一致)

- 最近改动方向正确:策略化重构语义保持、合并损伤去重正确、proto 再生成与真源一致
- 全库无 XSS 面(前端)、Rust 无 unsafe 所有权问题(除上述两处存量 Sync 声明)、TTL/异常纪律良好
- 回归:go build+test 全量 ✓、cargo check+test --workspace ✓、mvn test ✓、py_compile+test_mlops(24 tests)✓
