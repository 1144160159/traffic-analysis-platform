# Encrypted Traffic Evidence Center r201

Windows Chrome CDP production-route evidence for `/encrypted-traffic?tab=evidence-center` after the evidence-center rework.

- `actual-1920.png`, `target-1920.png`, `diff-business.png`, and `metrics-business.json` record the valid 1920 x 1080 capture: ratio `0.10801938657407407`, configured business threshold `0.35`, tolerance `64`.
- `rework-runtime.json` proves the five fixed-size tabs, three ECharts evidence canvases, adapted certificate and handshake details, and synchronized table-to-anchor-to-right-rail selection for `s-23a9b7d4c1e8`.
- `capture-meta-business.json` and `production-route-report.json` record the visible Windows Chrome route and error-free capture runtime.
- `verification.json` records the API-first fallback boundary and the evidence-action audit semantics.

This package is business and interaction evidence only. It does not grant strict pixel acceptance because the ratio remains above `0.015`. Evidence preservation is recorded as an audited request; it is not represented as a completed external forensic task.
