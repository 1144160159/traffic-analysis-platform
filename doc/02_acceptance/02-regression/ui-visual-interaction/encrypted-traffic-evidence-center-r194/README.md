# Encrypted Traffic Evidence Center r194

Windows Chrome CDP production-route evidence for `/encrypted-traffic?tab=evidence-center`.

- `actual-1920.png`, `target-1920.png`, `diff-business.png`, and `metrics-business.json` record the 1920 x 1080 business visual gate: ratio `0.11718605324074075`, configured threshold `0.35`, tolerance `64`.
- `echarts-interaction-runtime.json` proves two ECharts canvases, Session-to-detail-to-rail selection synchronization, a `write_audit` POST with status `200`, and a matching persisted audit trail.
- `evidence-api-runtime.json` records the real evidence API schema and the current empty live collections, which activate the explicit simulated-data state.
- `capture-meta-business.json` and `production-route-report.json` record the visible Windows Chrome route and error-free runtime.

This package is evidence for r194 business implementation and interaction rework. It is not a strict pixel-acceptance declaration because the ratio remains above `0.015`.
