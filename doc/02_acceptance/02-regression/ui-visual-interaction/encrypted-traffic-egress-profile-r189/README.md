# Encrypted Traffic Egress Profile r189

Windows Chrome CDP acceptance for `/encrypted-traffic?tab=egress-profile` on the real APISIX route.

- `actual-1920.png`, `target-1920.png`, `diff-business.png`, and `metrics-business.json`: 1920 x 1080 business visual gate, ratio `0.13109230324074075` with threshold `0.35` and tolerance `64`.
- `echarts-interaction-runtime.json`: five rendered canvases, map/graph selection synchronization for `104.16.12.34`, and a persisted `write_audit` action read back from `audit_logs`.
- `exfiltration-api-runtime.json`: live API schema and the currently empty external-destination data state that activates the explicit simulated-data view.
- `capture-meta-business.json` and `production-route-report.json`: route, browser, and runtime evidence.
