# 全局业务 ROI 门槛

- 生效时间：`2026-07-11`
- 门槛：业务 ROI 必须严格 `<0.125`。
- 适用范围：所有带有显式业务评分区域的页面证据，例如 `content-root`。公共 topbar、sidebar 和 bottombar 仅保留为整图诊断，不参与业务通过判定。
- 实现：`capture_image_breakdown_production_route.mjs` 会将带 ROI 的采集限制到 `min(调用阈值, 0.125)`；调用方不能用更宽的阈值绕过该上限。
- 历史处理：旧采集值可作为可追溯证据保留，但 `>=0.125` 的旧“通过”状态不再有效，必须回到业务区回修并重新截图。

## 当前判定

| 页面 | 最新业务 ROI | 新门槛判定 | 处理 |
|---|---:|---|---|
| `data-quality-field-quality` r235 | `0.12001590805750711` | pass | 已复验交互、分页和滚动容器 |
| `encrypted-traffic-evidence-center` r231 | `0.12906050692310234` | fail | 标记为业务区回修待办，不修改公共壳层 |
