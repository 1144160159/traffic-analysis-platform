# G-01～G-08 ImageGen九段式提示词

本文件引用并继承同目录下的 [`00-统一视觉母版.md`](./00-统一视觉母版.md)。逐图提示词只定义该图特有的目的、时态、布局、节点、连线、禁止关系、文字和验收条件；未重复说明的画布、语义色、节点形状、连线语义、全局禁止关系与全局验收要求，以统一视觉母版为准。

为避免把双向交互误解为无语义双向关系，以下 `A ↔ B [TYPE]` 在渲染时表示两条可辨认的有向边 `A → B [TYPE]` 与 `B → A [TYPE]`，不得画成无箭头直线。当前批次仅形成提示词，暂不生成图片。

## G-01 SYSTEM CONTEXT

### 1. 目的／问题

生成“系统上下文与边界图”，回答：谁使用平台、哪些系统位于平台外、哪些交互必须经过平台边界、平台不拥有哪些外部副作用。只表达一级上下文，不展开内部技术组件。

### 2. 时态

`TARGET`。这是目标边界图，不代表外部SOAR、通知或第三方测试集成当前已经完成。右上角必须显示状态胶囊 `TARGET`。

### 3. 画布与布局

16:9横版，建议3840×2160。三栏布局：左侧 `ACTORS`，中间 `PLATFORM`，右侧 `EXTERNAL`。中间用一条深蓝色虚线圆角边界包住节点06和07。所有节点水平或垂直对齐，连线使用正交折线，避免交叉。

### 4. 节点白名单

只允许以下12个节点，节点文字必须原样显示：

- `01 Analyst`
- `02 Operator`
- `03 Tenant Admin`
- `04 SRE`
- `05 Auditor`
- `06 Web Portal`
- `07 Traffic Platform`
- `08 Network TAP`
- `09 Identity Provider`
- `10 SOAR Provider`
- `11 Notify Provider`
- `12 Test Lab`

### 5. 允许连线

只允许以下连线：

- `01 → 06 [SYNC]`
- `02 → 06 [SYNC]`
- `03 → 06 [SYNC]`
- `04 → 06 [SYNC]`
- `05 → 06 [SYNC]`
- `06 ↔ 07 [SYNC]`
- `08 → 07 [EVENT]`
- `06 ↔ 09 [SYNC]`
- `07 ↔ 10 [SYNC]`
- `07 → 11 [SYNC]`
- `07 → 12 [OBJECT]`

### 6. 禁止关系

禁止Actor绕过Web Portal直连Traffic Platform；禁止Network TAP直连Web Portal；禁止SOAR Provider、Notify Provider或Test Lab反向修改平台PASS状态；禁止Identity Provider被画成业务数据库；禁止增加PG、Kafka、Flink等内部组件；禁止画平台直接控制交换机、防火墙或终端；禁止任何白名单外节点和连线。

### 7. 统一视觉母版

白灰背景 `#F7F9FC`，深蓝描边 `#16324F`，扁平矢量架构图。Actor与外部节点使用浅灰 `#EEF1F5`；平台节点使用浅蓝 `#DCEBFF`；边界使用深蓝虚线。`SYNC`为蓝色实线箭头，`EVENT`为橙色虚线箭头，`QUERY`为青色点线箭头，`OBJECT`为绿色双线箭头。圆角矩形、轻微无方向阴影、无渐变、无3D、无照片、无厂商Logo、无装饰插画。字体为清晰无衬线英文，标题左上、状态右上、图例右下。

### 8. 文字白名单

图中只允许出现：

`G-01 SYSTEM CONTEXT`、`TARGET`、`ACTORS`、`PLATFORM`、`EXTERNAL`、12个节点白名单文字、`SYNC`、`EVENT`、`OBJECT`。

### 9. 验收标准

必须是16:9；恰好12个节点；所有节点拼写完全正确；只有规定的11条连线；方向和线型正确；06和07位于PLATFORM边界内，其余节点在边界外；无额外文字、图标、数字、数据库或云服务；无交叉线、遮挡、截字和重复节点。

---

## G-02 SYSTEM OVERVIEW

### 1. 目的／问题

生成“系统总体架构图”，回答Web控制面、Probe数据面、实时处理和多存储如何协作，以及控制面业务事实如何通过Outbox进入事件链。

### 2. 时态

`TARGET`。图中Outbox传播、投影和对象链均为目标责任关系，不代表当前生产环境已经全部验证。

### 3. 画布与布局

16:9横版，建议3840×2160。上方为 `CONTROL PLANE`，下方为 `DATA PLANE`，右侧为 `DATA STORES`。控制面从左向右；数据面从左向右；存储纵向排列。连线使用正交折线，控制面与数据面在Kafka处汇合。

### 4. 节点白名单

只允许以下12个节点：

- `01 Web UI`
- `02 APISIX`
- `03 Go Control`
- `04 PostgreSQL`
- `05 Probe Agent`
- `06 Ingest`
- `07 Kafka`
- `08 Flink`
- `09 ClickHouse`
- `10 OS + NebulaGraph`
- `11 Redis`
- `12 MinIO`

### 5. 允许连线

只允许以下连线：

- `01 ↔ 02 [SYNC]`
- `02 ↔ 03 [SYNC]`
- `03 ↔ 04 [SYNC]`
- `04 → 07 [EVENT, OUTBOX]`
- `05 → 06 [EVENT]`
- `06 → 07 [EVENT]`
- `07 → 08 [EVENT]`
- `08 → 09 [EVENT]`
- `08 → 10 [EVENT]`
- `03 ↔ 09 [QUERY]`
- `03 ↔ 10 [QUERY]`
- `03 ↔ 11 [QUERY]`
- `03 ↔ 12 [OBJECT]`
- `08 ↔ 12 [OBJECT]`

### 6. 禁止关系

禁止Web UI直连任何存储；禁止Probe Agent直连PostgreSQL或ClickHouse；禁止Go Control绕过PostgreSQL直接发布业务命令事件；禁止Kafka直连Web UI；禁止OpenSearch、NebulaGraph或Redis反向覆盖PostgreSQL；禁止把MinIO画成业务权限真源；禁止新增Service Mesh、Data Lake、Cloud SOC等节点；禁止把TARGET画成AS-IS。

### 7. 统一视觉母版

白灰背景 `#F7F9FC`，深蓝描边 `#16324F`，扁平矢量。控制面节点浅蓝 `#DCEBFF`；数据面节点浅橙 `#FFF0D5`；存储节点浅绿 `#E0F4EA`。`SYNC`蓝色实线，`EVENT`橙色虚线，`QUERY`青色点线，`OBJECT`绿色双线。统一圆角卡片、技术节点可使用极简几何符号但禁止厂商Logo。无渐变、无3D、无照片、无复杂背景。标题左上，状态右上，图例右下。

### 8. 文字白名单

只允许：

`G-02 SYSTEM OVERVIEW`、`TARGET`、`CONTROL PLANE`、`DATA PLANE`、`DATA STORES`、12个节点文字、`SYNC`、`EVENT`、`QUERY`、`OBJECT`、`OUTBOX`。

### 9. 验收标准

恰好12个节点；三大区域清晰；只有允许连线；PostgreSQL到Kafka的事件线必须标记OUTBOX；Web UI无存储直连；Probe链必须经过Ingest和Kafka；查询、事件、对象线型可区分；无额外组件、性能数字、中文段落、交叉线或拼写错误。

---

## G-03 SECURITY LOOP

### 1. 目的／问题

生成“安全业务闭环图”，回答从流量采集到反馈优化的完整业务链是否闭合。只表达业务阶段，不出现数据库、消息中间件和部署组件。

### 2. 时态

`TARGET`。表示整改后的完整业务闭环，不声明每一阶段当前均已达到生产验收。

### 3. 画布与布局

16:9横版，建议3840×2160。采用顺时针椭圆闭环或两行回环布局。主链从左上开始，沿顺时针方向推进，最后由Optimize返回Detect。中心保留空白，不放装饰图案。

### 4. 节点白名单

只允许以下11个节点：

- `01 Capture`
- `02 Parse`
- `03 Sessionize`
- `04 Feature`
- `05 Detect`
- `06 Alert`
- `07 Investigate`
- `08 Evidence`
- `09 Respond`
- `10 Feedback`
- `11 Optimize`

### 5. 允许连线

只允许：

- `01 → 02 [FLOW]`
- `02 → 03 [FLOW]`
- `03 → 04 [FLOW]`
- `04 → 05 [FLOW]`
- `05 → 06 [FLOW]`
- `06 → 07 [FLOW]`
- `07 → 08 [FLOW]`
- `08 → 09 [FLOW]`
- `09 → 10 [FLOW]`
- `10 → 11 [FLOW]`
- `11 → 05 [FEEDBACK]`

### 6. 禁止关系

禁止Capture直接到Alert；禁止Alert绕过Investigate和Evidence直接到Respond；禁止Evidence改变业务状态；禁止Optimize直接回写Capture；禁止加入Kafka、Flink、PG、CH、SOAR等技术节点；禁止加入未经批准的“Auto Block”；禁止任何额外捷径和双向箭头。

### 7. 统一视觉母版

白灰背景 `#F7F9FC`，深蓝描边 `#16324F`。全部业务阶段使用浅蓝卡片，Detect与Alert可使用浅橙强调，Evidence使用浅绿，Feedback与Optimize使用浅紫。`FLOW`为蓝色实线箭头；`FEEDBACK`为橙色虚线回环箭头。扁平矢量、圆角矩形、无3D、无照片、无厂商Logo、无装饰性盾牌或黑客人物。标题左上、TARGET右上、图例右下。

### 8. 文字白名单

只允许：

`G-03 SECURITY LOOP`、`TARGET`、11个节点文字、`FLOW`、`FEEDBACK`。

### 9. 验收标准

恰好11个阶段；主链顺序完全正确；只有Optimize回到Detect；闭环一眼可读；无技术组件；无自动阻断暗示；无额外文字、数字、图标、交叉线、重叠和拼写错误。

---

## G-04 DOMAIN OBJECTS

### 1. 目的／问题

生成“核心业务对象关系图”，回答核心对象之间的所有权、派生、聚合、引用和审计关系。它是领域关系图，不是数据库ER图。

### 2. 时态

`TARGET`。对象边界与关系为目标领域模型，不表示所有对象和关系当前已完整落库。

### 3. 画布与布局

16:9横版，建议3840×2160。顶部放Tenant；中部主脊为Asset到Alert；Campaign和Topic位于Alert上方；Evidence与Operation位于Alert下方；Deployment位于Detection上方；AuditEvent位于右下。连线尽量不交叉。

### 4. 节点白名单

只允许以下12个节点：

- `01 Tenant`
- `02 Asset`
- `03 Session`
- `04 Flow Event`
- `05 Detection`
- `06 Alert`
- `07 Campaign`
- `08 Topic`
- `09 Evidence`
- `10 Operation`
- `11 Deployment`
- `12 Audit Event`

### 5. 允许连线

只允许：

- `01 → 02 [OWNS]`
- `01 → 08 [OWNS]`
- `01 → 11 [OWNS]`
- `02 → 03 [CONTEXT]`
- `03 → 04 [CONTAINS]`
- `04 → 05 [DERIVES]`
- `05 → 06 [TRIGGERS]`
- `07 → 06 [GROUPS]`
- `08 → 06 [CURATES]`
- `06 → 09 [REFERENCES]`
- `06 → 10 [STARTS]`
- `11 → 05 [ACTIVATES]`
- `10 → 12 [AUDITS]`
- `11 → 12 [AUDITS]`

### 6. 禁止关系

禁止把Campaign或Topic画成Alert的权威所有者；禁止Evidence反向修改Alert；禁止Operation直接激活Detection；禁止Deployment拥有Tenant；禁止出现数据库表、外键、Kafka Topic或物理存储；禁止跨Tenant关系；禁止把关系画成无方向双向线；禁止额外对象。

### 7. 统一视觉母版

白灰背景 `#F7F9FC`，深蓝描边 `#16324F`。业务主对象浅蓝；分析聚合对象浅紫；Evidence浅绿；Operation与Deployment浅橙；Audit Event浅灰紫。关系线为细深蓝实线箭头，关系动词置于线中部白色小标签。扁平矢量、无乌鸦脚ER符号、无表格字段、无3D、无照片、无Logo。标题左上、TARGET右上。

### 8. 文字白名单

只允许：

`G-04 DOMAIN OBJECTS`、`TARGET`、12个节点文字、`OWNS`、`CONTEXT`、`CONTAINS`、`DERIVES`、`TRIGGERS`、`GROUPS`、`CURATES`、`REFERENCES`、`STARTS`、`ACTIVATES`、`AUDITS`。

### 9. 验收标准

恰好12个对象；所有14条关系存在且方向正确；没有物理存储或字段；主脊Asset—Session—Flow Event—Detection—Alert清晰；Evidence、Operation、Deployment、Audit位置明确；没有未批准关系、截字、重线或交叉线。

---

## G-05 APPLICATION DOMAINS

### 1. 目的／问题

生成“应用领域模块图”，回答平台逻辑能力如何分域，以及为什么不能按页面一对一拆成微服务。只表达逻辑领域，不表达物理Pod和数据库。

### 2. 时态

`TARGET`。这是目标逻辑边界，节点数量不等于未来物理服务数量。

### 3. 画布与布局

16:9横版，建议3840×2160。Gateway/BFF位于顶部中央；其余10个领域以两行五列规则网格排列。跨域事件线在网格后方走外圈，避免穿过卡片。底部可放一句状态标签，但不得放长说明。

### 4. 节点白名单

只允许以下11个节点：

- `01 Gateway / BFF`
- `02 IAM + Settings`
- `03 Asset + Fusion`
- `04 Alert + Campaign`
- `05 Topic Analysis`
- `06 Graph`
- `07 Forensics`
- `08 Rule + Model`
- `09 Deploy + Orchestrate`
- `10 Compliance + Audit`
- `11 Task + Notify`

### 5. 允许连线

只允许：

- `01 ↔ 02 [SYNC]`
- `01 ↔ 03 [QUERY]`
- `01 ↔ 04 [QUERY]`
- `01 ↔ 05 [QUERY]`
- `01 ↔ 06 [QUERY]`
- `01 ↔ 07 [SYNC]`
- `01 ↔ 08 [SYNC]`
- `01 ↔ 09 [SYNC]`
- `01 ↔ 10 [QUERY]`
- `01 ↔ 11 [QUERY]`
- `03 → 04 [EVENT]`
- `03 → 06 [EVENT]`
- `04 → 05 [EVENT]`
- `04 → 06 [EVENT]`
- `04 → 07 [SYNC]`
- `08 → 09 [SYNC]`
- `09 → 11 [EVENT]`

### 6. 禁止关系

禁止加入PostgreSQL、Kafka、Flink等技术组件；禁止把每个页面画成独立Service；禁止所有领域之间全互连；禁止Graph拥有Asset或Alert状态；禁止Forensics直接改变Alert状态；禁止Task + Notify拥有业务任务真相；禁止Gateway/BFF承载领域状态；禁止新增SOC、EDR、ITSM领域。

### 7. 统一视觉母版

白灰背景 `#F7F9FC`，深蓝描边 `#16324F`。Gateway/BFF使用深浅蓝强调；业务领域浅蓝；治理领域浅紫；取证浅绿；部署与任务浅橙。`SYNC`蓝色实线，`EVENT`橙色虚线，`QUERY`青色点线。统一圆角卡片、扁平矢量、无服务器或数据库图标、无3D、无照片、无Logo。标题左上、TARGET右上、图例右下。

### 8. 文字白名单

只允许：

`G-05 APPLICATION DOMAINS`、`TARGET`、`LOGICAL DOMAINS`、11个节点文字、`SYNC`、`EVENT`、`QUERY`。

### 9. 验收标准

恰好11个逻辑域节点；Gateway/BFF与领域网格关系清楚；只有允许连线；没有任何数据库、Topic、Pod或页面名称；不能暗示逻辑域必须一域一服务；无额外文字、交叉线、拼写错误或重复节点。

---

## G-06 FRONTEND TO BACKEND

### 1. 目的／问题

生成“前后端交互架构图”，回答Route、React页面、类型客户端、网关、Handler、Use Case、Domain Guard和Repository如何形成可追踪链，并明确页面不能直连数据库或自行猜测响应。

### 2. 时态

`TARGET`。表示整改后的目标交互模式，不声明现有所有页面均已完成生成客户端和Page View Model改造。

### 3. 画布与布局

16:9横版，建议3840×2160。采用上下双轨：上轨为请求链，从左向右；下轨为响应模型回传。PostgreSQL和Read Models放在最右侧上下排列。Page View Model位于下轨中央，返回React Page。所有线正交，响应线不得与请求线混淆。

### 4. 节点白名单

只允许以下11个节点：

- `01 Route`
- `02 React Page`
- `03 Page View Model`
- `04 TS Client`
- `05 APISIX`
- `06 Go Handler`
- `07 Use Case`
- `08 Domain Guard`
- `09 Repository`
- `10 PostgreSQL`
- `11 Read Models`

### 5. 允许连线

只允许：

- `01 → 02 [SYNC]`
- `02 → 04 [SYNC]`
- `04 ↔ 05 [SYNC]`
- `05 ↔ 06 [SYNC]`
- `06 → 07 [SYNC]`
- `07 → 08 [SYNC]`
- `08 → 09 [SYNC]`
- `09 ↔ 10 [SYNC]`
- `09 ↔ 11 [QUERY]`
- `07 → 03 [RESPONSE]`
- `03 → 04 [RESPONSE]`
- `04 → 02 [RESPONSE]`

### 6. 禁止关系

禁止React Page直接连接APISIX以外的后端；禁止组件内直接fetch；禁止React Page直连PostgreSQL或Read Models；禁止Go Handler直连数据库；禁止Handler承载业务状态机；禁止Page View Model反向写库；禁止从payload信任tenant；禁止把数据库行直接作为页面合同；禁止增加Redux、GraphQL或Service Mesh等未要求节点。

### 7. 统一视觉母版

白灰背景 `#F7F9FC`，深蓝描边 `#16324F`。前端节点浅蓝；Gateway与Go节点稍深蓝；领域守卫浅紫；Repository和数据节点浅绿。`SYNC`蓝色实线，`QUERY`青色点线，`RESPONSE`紫色实线回传箭头。扁平矢量、圆角卡片、无代码截图、无浏览器截图、无3D、无照片、无Logo。标题左上、TARGET右上、图例右下。

### 8. 文字白名单

只允许：

`G-06 FRONTEND TO BACKEND`、`TARGET`、`REQUEST`、`RESPONSE`、11个节点文字、`SYNC`、`QUERY`。

### 9. 验收标准

恰好11个节点；请求链和响应轨道可区分；React Page没有任何存储直连；Handler必须经过Use Case、Domain Guard、Repository；命令存储和读模型分支清楚；没有白名单外框架、文字、箭头、代码或图标；无交叉线、截字和拼写错误。

---

## G-07 FEATURE CONTRACT

### 1. 目的／问题

生成“Feature Contract纵向契约图”，回答一个功能如何把Route、Action、OpenAPI、TS Client、Go Use Case、权限、审计、事件、数据模型、测试和回滚纳入同一注册项。

### 2. 时态

`TARGET`。表示目标契约治理模式，不声明当前全部功能已进入Feature Registry。

### 3. 画布与布局

16:9横版，建议3840×2160。中心放Feature Registry；左侧为UX Contract；中间为API与Runtime；右侧为Data与Delivery。采用三列或四列对齐布局，不使用放射状密集蜘蛛网。Test Case和Rollback位于底部形成关闭门。

### 4. 节点白名单

只允许以下12个节点：

- `01 Feature Registry`
- `02 Route`
- `03 Action ID`
- `04 OpenAPI`
- `05 TS Client`
- `06 Go Use Case`
- `07 Permission`
- `08 Audit Event`
- `09 Proto Event`
- `10 Data Model`
- `11 Test Case`
- `12 Rollback`

### 5. 允许连线

只允许：

- `01 → 02 [DECLARES]`
- `01 → 03 [DECLARES]`
- `01 → 04 [DECLARES]`
- `02 → 05 [BINDS]`
- `04 → 05 [GENERATES]`
- `04 → 06 [BINDS]`
- `03 → 06 [BINDS]`
- `07 → 06 [ENFORCES]`
- `06 → 08 [EMITS]`
- `06 → 09 [EMITS]`
- `06 → 10 [PERSISTS]`
- `11 → 01 [VERIFIES]`
- `12 → 01 [RECOVERS]`

### 6. 禁止关系

禁止Route直接连接Data Model；禁止TS Client手写漂移而绕过OpenAPI；禁止Permission只连接前端；禁止Audit Event由React页面生成；禁止Data Model直接复用为Proto Event；禁止Test Case与Feature Registry断开；禁止Rollback表示物理删除旧能力；禁止添加Swagger UI、数据库名称、Kafka Topic名称或页面截图。

### 7. 统一视觉母版

白灰背景 `#F7F9FC`，深蓝描边 `#16324F`。Feature Registry使用深蓝主卡片；UX节点浅蓝；API与权限浅紫；事件与数据浅绿；测试与回滚浅橙。关系线使用细实线箭头，关系动词放在线中部白色小标签。扁平矢量、无3D、无代码截图、无照片、无Logo。标题左上、TARGET右上，列标题使用小号大写英文。

### 8. 文字白名单

只允许：

`G-07 FEATURE CONTRACT`、`TARGET`、`UX CONTRACT`、`API + RUNTIME`、`DATA + DELIVERY`、12个节点文字、`DECLARES`、`BINDS`、`GENERATES`、`ENFORCES`、`EMITS`、`PERSISTS`、`VERIFIES`、`RECOVERS`。

### 9. 验收标准

恰好12个节点；13条关系全部存在且方向正确；Feature Registry是追溯中心但画面不是蜘蛛网；OpenAPI同时约束TS Client和Go Use Case；Permission进入Go Use Case；Test Case和Rollback回到Feature Registry；无直接UI到DB关系；无额外文字、代码、节点、交叉线或拼写错误。

---

## G-08 DATA FLOW

### 1. 目的／问题

生成“数据总体流转图”，回答原始流量、事件、实时处理、控制面权威状态、分析事实、检索投影、图投影、对象和Web查询如何流动，并明确不同数据语义不能互相反向覆盖。

### 2. 时态

`TARGET`。表示目标数据责任和流向，不证明当前所有Sink、Outbox和对象链均已在生产通过。

### 3. 画布与布局

16:9横版，建议3840×2160。上方主链为Network TAP到Flink；下方左侧为PostgreSQL控制面；下方中部为ClickHouse、OpenSearch、NebulaGraph；右侧为Go API和Web UI；MinIO位于右下。用四种线型区分事件、查询、同步事务和对象。

### 4. 节点白名单

只允许以下12个节点：

- `01 Network TAP`
- `02 Probe Agent`
- `03 Ingest`
- `04 Kafka`
- `05 Flink`
- `06 PostgreSQL`
- `07 ClickHouse`
- `08 OpenSearch`
- `09 NebulaGraph`
- `10 MinIO`
- `11 Go API`
- `12 Web UI`

### 5. 允许连线

只允许：

- `01 → 02 [EVENT]`
- `02 → 03 [EVENT]`
- `03 → 04 [EVENT]`
- `04 → 05 [EVENT]`
- `05 → 07 [EVENT]`
- `05 → 08 [EVENT]`
- `05 → 09 [EVENT]`
- `02 → 10 [OBJECT]`
- `05 ↔ 10 [OBJECT]`
- `11 ↔ 06 [SYNC]`
- `06 → 04 [EVENT, OUTBOX]`
- `11 ↔ 07 [QUERY]`
- `11 ↔ 08 [QUERY]`
- `11 ↔ 09 [QUERY]`
- `11 ↔ 10 [OBJECT]`
- `12 ↔ 11 [QUERY]`

### 6. 禁止关系

禁止Network TAP绕过Probe Agent；禁止Probe Agent直连PostgreSQL、ClickHouse、OpenSearch或NebulaGraph；禁止Web UI直连任何数据组件；禁止OpenSearch或NebulaGraph反向写PostgreSQL；禁止MinIO直连Web UI；禁止Kafka成为页面查询接口；禁止Flink拥有可变控制面业务状态；禁止用Redis替代缺失节点；禁止增加Data Lake、HDFS、Spark等未要求组件。

### 7. 统一视觉母版

白灰背景 `#F7F9FC`，深蓝描边 `#16324F`。采集与流处理节点浅橙 `#FFF0D5`；控制面浅蓝 `#DCEBFF`；数据存储浅绿 `#E0F4EA`；API与UI浅蓝紫。`SYNC`蓝色实线，`EVENT`橙色虚线，`QUERY`青色点线，`OBJECT`绿色双线。扁平矢量、圆角卡片、无数据库厂商Logo、无3D、无照片、无装饰网络地图。标题左上、TARGET右上、图例右下。

### 8. 文字白名单

只允许：

`G-08 DATA FLOW`、`TARGET`、`STREAM PATH`、`CONTROL STATE`、`SERVING DATA`、`OBJECT STORE`、12个节点文字、`SYNC`、`EVENT`、`QUERY`、`OBJECT`、`OUTBOX`。

### 9. 验收标准

恰好12个节点；16条允许连线全部方向正确；原始流量必须经过Probe、Ingest、Kafka、Flink；PostgreSQL通过OUTBOX进入Kafka；Web UI只能连接Go API；CH、OS、Nebula只通过查询返回；MinIO对象线清晰；没有反向覆盖权威状态的箭头；无白名单外文字、组件、性能数字、交叉线、截字或重复节点。
