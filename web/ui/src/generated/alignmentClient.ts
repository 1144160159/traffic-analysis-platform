/* This file is generated by scripts/alignment/generate_ts_client.py. */
/* Do not edit by hand. */

import { api } from '@/services/api';

export type AlignmentFieldError = {
  field: string;
  code: string;
  message: string;
};

export type AlignmentError = {
  code: string;
  message: string;
  trace_id: string;
  retryable: boolean;
  field_errors?: AlignmentFieldError[];
  operation_id?: string;
  current_revision?: number;
  projection_status?: string;
  status_url?: string;
  expected_consistency_window?: string;
};

export type AlignmentMeta = {
  contract_version: number;
  schema_version: number;
  snapshot_id: string;
  as_of: string;
  generated_at: string;
  trace_id: string;
  result_code: string;
  operation_id?: string;
  tenant_id?: string;
  partial: boolean;
  missing_sections: string[];
  source_watermarks: Record<string, string>;
  projection_status?: string;
};

export type AlignmentEnvelope<T = unknown> = {
  data: T;
  meta: AlignmentMeta;
  error: AlignmentError | null;
};

export type AlignmentRequest = {
  path?: Record<string, string>;
  query?: Record<string, string | number | boolean | undefined>;
  body?: unknown;
  idempotencyKey?: string;
};

const resolvePath = (template: string, values: Record<string, string> = {}) =>
  template.replace(/\{([^}]+)\}/g, (_, key: string) => encodeURIComponent(values[key] ?? ''));

export async function acknowledgeProbeOperation(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/probes/{id}/operations/{operation_id}/ack', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function activateDataQualityBaseline(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/data-quality/baseline', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function applyAssetGovernanceAction(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets/governance/work-orders/{work_order_id}/actions', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function archiveWhitelistEntry(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/whitelist/{id}', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'DELETE', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function cancelAlertReport(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/alerts/{id}/reports/{job_id}/cancel', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function cancelAlertResponseAction(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/alerts/{id}/response-actions/{job_id}/cancel', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function cancelAssetDiscoveryJob(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets/discovery/runs/{run_id}/cancel', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function cancelCampaignSOARJob(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/campaigns/{id}/soar-jobs/{job_id}/cancel', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function cancelPlaybookExecution(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/playbooks/executions/{execution_id}/cancel', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function changeAuthenticatedUserPasswordAtomic(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/auth/me/password', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function checkWhitelistValue(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/whitelist/check', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function closeAlertSearchCursorV1(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/alerts/search/cursor', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'DELETE', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function compensateAlertReport(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/alerts/{id}/reports/{job_id}/compensations', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function compensateCampaignSOARJob(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/campaigns/{id}/soar-jobs/{job_id}/compensate', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function compensatePlaybookExecution(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/playbooks/executions/{execution_id}/compensate', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function createAlertReport(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/alerts/{id}/reports/export', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function createAlertResponseAction(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/alerts/{id}/response-actions', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function createAssetDiscoveryJob(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets/discovery/runs', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function createAssetExportJob(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets/exports', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function createAssetGovernanceWorkOrder(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets/{asset_id}/governance/work-orders', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function createDashboardAuditTask(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/dashboard/tasks/audit', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function createDashboardComplianceTask(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/dashboard/tasks/compliance', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function createDashboardEvidenceTask(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/dashboard/tasks/evidence', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function createDashboardFeedbackTask(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/dashboard/tasks/feedback', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function createDashboardSLATask(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/dashboard/tasks/sla', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function createDashboardTask(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/dashboard/tasks', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function createDataQualityContextAction(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/data-quality/actions', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function createDataQualityRepair(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/data-quality/events/{quality_event_id}/repairs', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function createDataQualityRuleDraft(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/data-quality/rules', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function createTopicActionJob(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/topics/{topic}/actions', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function createWhitelistEntry(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/whitelist', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function decideAlertResponseAction(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/alerts/{id}/response-actions/{job_id}/approval', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function decideCampaignSOARJob(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/campaigns/{id}/soar-jobs/{job_id}/approval', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function decidePlaybookExecution(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/playbooks/executions/{execution_id}/approval', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function downloadAlertReport(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/alerts/{id}/reports/{job_id}/download', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function downloadAssetExport(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets/exports/{job_id}/download', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function downloadCampaignReport(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/campaigns/{id}/reports/{report_id}/download', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function exploreBoundedGraph(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/graph/explore', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getAlertEvidence(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/alerts/{id}/evidence', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getAlertReportJob(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/alerts/{id}/reports/{job_id}', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getAlertResponseAction(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/alerts/{id}/response-actions/{job_id}', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getAssetColumnPreference(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets/preferences/columns', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getAssetDetailSnapshot(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets/{asset_id}/snapshot', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getAssetDetailsLegacyCompatible(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets/{id}/details', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getAssetDiscoveryJob(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets/discovery/runs/{run_id}', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getAssetExportJob(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets/exports/{job_id}', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getAssetGovernanceWorkOrder(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets/governance/work-orders/{work_order_id}', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getAssetHistoryLegacyCompatible(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets/{id}/history', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getAssetLegacyCompatible(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets/{id}', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getAssetTopologyLegacyCompatible(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets/{id}/topology', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getCampaign(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/campaigns/{id}', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getCampaignCommandJob(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/campaigns/jobs/{job_id}', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getCampaignReport(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/campaigns/{id}/reports/{report_id}', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getCampaignSOARJob(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/campaigns/{id}/soar-jobs/{job_id}', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getDashboardAlertTrend(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/dashboard/alerts/trend', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getDashboardAttackPhases(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/dashboard/attack-phases', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getDashboardEncryptedTrend(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/dashboard/encrypted/trend', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getDashboardSnapshot(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/dashboard/snapshot', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getDashboardStats(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/dashboard/stats', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getDashboardTask(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/dashboard/tasks/{task_id}', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getDashboardTopIPs(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/dashboard/top-ips/{type}', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getDataQualityControlPlaneSnapshot(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/data-quality', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getPlaybookExecution(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/playbooks/executions/{execution_id}', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getProbeOperation(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/probes/operations/{operation_id}', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getScopeCatalog(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/auth/scopes', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getTopicActionJob(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/topics/{topic}/actions/{job_id}', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function getTopicSnapshot(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/topics/{topic}/snapshot', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function ingestAuditLogBatch(request: AlignmentRequest = {}) {
  const path = resolvePath('/internal/v1/audit/batches', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function linkAlertToCampaign(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/alerts/{id}/campaign-links', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function listAlertCampaignLinks(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/alerts/{id}/campaign-links', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function listAlertViews(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/alerts/views', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function listAssetDiscoveryCandidates(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets/discovery/runs/{run_id}/candidates', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function listAssetDiscoveryCredentials(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets/discovery/credentials', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function listAssetDiscoveryJobHistory(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets/discovery/runs/{run_id}/history', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function listAssetDiscoveryJobs(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets/discovery/runs', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function listAssetGovernanceWorkOrders(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets/{asset_id}/governance/work-orders', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function listAssetsV2(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function listCampaignMembers(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/campaigns/{id}/members', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function listCampaigns(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/campaigns', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function listDataQualityDatasets(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/data-quality/datasets', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function listDataQualityRules(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/data-quality/rules', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function listWhitelistEntries(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/whitelist', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'GET', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function mergeAssetDiscoveryCandidate(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets/discovery/runs/{run_id}/candidates/{candidate_id}/merge', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function pushProbeConfig(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/probes/{id}/config', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function requestAlertResponseCompensation(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/alerts/{id}/response-actions/{job_id}/compensations', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function requestPlaybookExecution(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/playbooks/{name}/execute', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function saveAlertView(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/alerts/views', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function searchAlertsCursorV1(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/alerts/search', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function submitCampaignCommand(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/campaigns/{id}/actions', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function transitionDataQualityRepair(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/data-quality/repairs/{repair_id}/transitions', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function transitionDataQualityRule(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/data-quality/rules/{rule_id}/transitions', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function transitionWhitelistEntry(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/whitelist/{id}', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'PATCH', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function unlinkAlertFromCampaign(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/alerts/{id}/campaign-links/{campaign_id}', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'DELETE', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function updateAssetColumnPreference(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets/preferences/columns', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'PUT', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function updateAuthenticatedUserProfileAtomic(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/auth/me', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'PATCH', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function upsertAssetDiscoveryCredential(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets/discovery/credentials', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function upsertAssetV2(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/assets', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'POST', url: path, params: request.query, data: request.body, headers });
  return response.data;
}

export async function upsertDataQualityDataset(request: AlignmentRequest = {}) {
  const path = resolvePath('/v1/data-quality/datasets/{dataset_id}', request.path);
  const headers = request.idempotencyKey ? { 'Idempotency-Key': request.idempotencyKey } : undefined;
  const response = await api.request({ method: 'PUT', url: path, params: request.query, data: request.body, headers });
  return response.data;
}
